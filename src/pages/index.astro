---
/**
 * Root language-detection redirect (D-04/D-05/D-03, English-first per CHANGE 1).
 *
 * GitHub Pages serves static files, so detection runs client-side:
 *   1. A stored explicit choice (D-05) wins.
 *   2. Otherwise browser language → /es/ for Spanish browsers, else /en/ (default).
 *   3. <noscript> falls back to the English default.
 *
 * Security (T-01-03): redirect targets are the two hard-coded literal paths only.
 * navigator.language merely *selects* between them — it is never interpolated into
 * the URL, so there is no open-redirect surface.
 */
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="theme-color" content="#0a0c14" />
    <meta name="robots" content="noindex" />
    <title>Nocturna Avatars</title>
    <noscript>
      <meta http-equiv="refresh" content="0; url=/en/" />
    </noscript>
    <script is:inline>
      (function () {
        var ES = '/es/';
        var EN = '/en/';
        var target = EN;
        try {
          var stored = localStorage.getItem('nocturna-lang');
          if (stored === 'es') {
            target = ES;
          } else if (stored === 'en') {
            target = EN;
          } else {
            // English-first: only an explicitly Spanish browser goes to /es/.
            var nav = (navigator.language || 'en').toLowerCase();
            target = nav.indexOf('es') === 0 ? ES : EN;
          }
        } catch (e) {
          target = EN;
        }
        location.replace(target);
      })();
    </script>
  </head>
  <body></body>
</html>
