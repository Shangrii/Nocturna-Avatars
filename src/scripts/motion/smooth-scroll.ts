/**
 * smooth-scroll — Lenis wheel-inertia (FX-01). Wired in Task 2.
 * Placeholder no-ops keep the controller type-safe until Task 2 lands the real impl.
 */
export function initSmoothScroll(): void {
  /* Task 2: Lenis on the one-clock recipe. */
}

export function destroySmoothScroll(): void {
  /* Task 2: lenis.destroy() + gsap.ticker.remove(cb). */
}
