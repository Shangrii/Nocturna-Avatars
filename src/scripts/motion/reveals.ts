/**
 * reveals — ScrollTrigger reveals + SplitText (FX-02). Wired in Task 3.
 * Placeholder no-ops keep the controller type-safe until Task 3 lands the real impl.
 */
export function initReveals(): void {
  /* Task 3: ScrollTrigger entrances + font-ready SplitText. */
}

export function destroyReveals(): void {
  /* Task 3: ScrollTrigger.getAll().forEach(t => t.kill()) + split.revert(). */
}
