---
phase: 09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse
plan: 10
type: execute
wave: 3
depends_on: ["09-09"]
gap_closure: true
files_modified:
  - ../nocturna-bot/cogs/jinxxy.py
  - ../nocturna-bot/tests/test_jinxxy_cog.py
autonomous: true
requirements: [STORE-SYNC-02]

must_haves:
  truths:
    - "A non-image or decompression-bomb attachment to /tienda medios yields a clear ephemeral error instead of an unhandled exception that hangs the deferred interaction (closes WR-07)"
    - "A malformed Jinxxy detail record (KeyError/TypeError in map_product) during /tienda sync yields the D-05 ephemeral 'revisa los logs' reply instead of a hung interaction (closes WR-08)"
    - "A Forbidden/HTTPException on the announce channel is logged and skipped — never raised — honoring _announce's own docstring (closes WR-09)"
  artifacts:
    - path: "../nocturna-bot/cogs/jinxxy.py"
      provides: "broadened attachment-optimize guard, broadened /tienda sync guard, wrapped announce channel.send"
      contains: "followup.send"
  key_links:
    - from: "../nocturna-bot/cogs/jinxxy.py::medios"
      to: "interaction.followup.send"
      via: "except Exception around _optimize_attachments"
      pattern: "followup.send"
    - from: "../nocturna-bot/cogs/jinxxy.py::_announce"
      to: "channel.send"
      via: "try/except discord.HTTPException"
      pattern: "channel.send"
---

<objective>
Close the three cog-side command/announce error-handling warnings (WR-07, WR-08, WR-09), all in `cogs/jinxxy.py`, each of which leaves a deferred Discord interaction hanging or breaks the D-05 "one ephemeral signal, never a public error" contract. WR-07: `/tienda medios` only catches `discord.HTTPException`/`GitHubPublishError`, so a non-image or decompression-bomb attachment raises `UnidentifiedImageError`/`DecompressionBombError` out of `_optimize_attachments` uncaught → the invoker is stuck on "thinking…". WR-08: `/tienda sync`'s `except (GitHubPublishError, JinxxyAPIError)` misses a `KeyError`/`TypeError` from `map_product` on a malformed detail → the interaction hangs. WR-09: `_announce`'s `channel.send` is unwrapped, so a `Forbidden`/`HTTPException` propagates — contradicting the method's own "logged and skipped — never raised" docstring.

Purpose: WR-07 guards the STORE-SYNC-02 staff attach flow against malicious/invalid uploads; WR-08 and WR-09 keep every failure path log-only per D-05. Depends on 09-09 (same file) so cog edits land sequentially without conflict.
Output: Three broadened/added guards plus tests exercising each failure path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-CONTEXT.md
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-VERIFICATION.md
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-REVIEW.md

<interfaces>
<!-- Extracted from ../nocturna-bot/cogs/jinxxy.py -->
async def sync(interaction)      # /tienda sync: currently except (GitHubPublishError, JinxxyAPIError)
async def medios(interaction, producto, imagen1..4, descripcion_es, descripcion_en)
  # reads attachments, then: media = await asyncio.to_thread(_optimize_attachments, raws, _slug_from_url(producto))  <-- UNGUARDED
  # then: await github_publish.attach_store_media(producto, media, description)  (already guarded)
async def _announce(result)      # resolves channel, then: await channel.send(embed=...)  <-- UNWRAPPED
def _optimize_attachments(raws, slug)   # calls image_optimize.optimize_to_webp -> PIL.Image.open (raises UnidentifiedImageError / DecompressionBombError on bad input)
# D-05: errors go to logs ONLY; the single user-facing signal is an EPHEMERAL followup to the staff invoker.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard /tienda medios attachment optimization against invalid/bomb images (WR-07)</name>
  <files>../nocturna-bot/cogs/jinxxy.py, ../nocturna-bot/tests/test_jinxxy_cog.py</files>
  <read_first>
    - ../nocturna-bot/cogs/jinxxy.py (read `medios` L284-347, especially the attachment read L314-321 and the UNGUARDED optimize call L322-323)
    - ../nocturna-bot/core/image_optimize.py L30-57 (`optimize_to_webp` calls `PIL.Image.open`; a non-image raises `UnidentifiedImageError`, an oversized image raises `DecompressionBombError` — both propagate out of `asyncio.to_thread`)
    - .planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-REVIEW.md (WR-07 L182-196 — the recommended try/except + ephemeral reply)
    - ../nocturna-bot/tests/test_jinxxy_cog.py (the fake-interaction scaffolding with a followup spy, to assert the ephemeral reply)
  </read_first>
  <behavior>
    - When `_optimize_attachments` raises (e.g. `UnidentifiedImageError` for a PDF or `DecompressionBombError` for an oversized image), `medios` sends ONE ephemeral followup telling staff the image could not be processed, logs the error, and returns — the deferred interaction is resolved, not left hanging.
    - `attach_store_media` is NOT called when optimization failed.
    - A valid image still proceeds to `attach_store_media` (no regression).
    - Nothing is posted to the public announce channel (D-05).
  </behavior>
  <action>
    Wrap the `media = await asyncio.to_thread(_optimize_attachments, raws, _slug_from_url(producto))` call (L322-323) in a `try/except Exception` (broad, because PIL raises several unrelated types — `UnidentifiedImageError`, `DecompressionBombError`, `OSError`). On exception: `log.exception("jinxxy: no pude optimizar los adjuntos de /tienda medios")`, then `await interaction.followup.send("Alguna imagen no se pudo procesar (¿es una imagen válida?).", ephemeral=True)`, then `return` — before `attach_store_media` is reached. Keep the existing download-guard (`except discord.HTTPException`) above it unchanged. Add a comment referencing WR-07 and the decompression-bomb / non-image vectors. Then add a test to test_jinxxy_cog.py: patch `_optimize_attachments` (or `image_optimize.optimize_to_webp`) to raise, drive `medios` with a fake staff interaction, and assert an ephemeral followup was sent and `github_publish.attach_store_media` was never called.
  </action>
  <acceptance_criteria>
    - The `_optimize_attachments` call in `medios` is wrapped in a `try/except Exception` that logs and sends an ephemeral followup, then returns.
    - The test asserts an ephemeral followup on a raising optimizer and that `attach_store_media` was not called.
    - No public channel send occurs on this path (D-05).
    - `cd ../nocturna-bot && python -m pytest tests/test_jinxxy_cog.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd "../nocturna-bot" && python -m pytest tests/test_jinxxy_cog.py -q</automated>
  </verify>
  <done>A bad/bomb attachment gives staff a clear ephemeral error instead of a hung interaction; a test pins it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Broaden /tienda sync error handling to any sync failure (WR-08)</name>
  <files>../nocturna-bot/cogs/jinxxy.py, ../nocturna-bot/tests/test_jinxxy_cog.py</files>
  <read_first>
    - ../nocturna-bot/cogs/jinxxy.py (read `sync` L234-270, especially the `except (github_publish.GitHubPublishError, jinxxy_api.JinxxyAPIError)` at L254)
    - ../nocturna-bot/core/store_sync.py L41-64 (map_product hard-indexes `detail["name"]`/`detail["url"]`/`detail["base_price"]`/`detail["created_at"]` — a malformed detail raises `KeyError`/`TypeError`, neither of the currently-caught types)
    - .planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-REVIEW.md (WR-08 L198-202 — broaden to `except Exception` with the same D-05 ephemeral reply)
  </read_first>
  <behavior>
    - `/tienda sync` where `_run_sync` raises a `KeyError`/`TypeError` (malformed detail) sends the same ephemeral "No pude sincronizar ahora; revisa los logs." reply and logs the error — the interaction never hangs.
    - The existing `GitHubPublishError`/`JinxxyAPIError` behavior is unchanged (same reply).
    - Nothing is posted publicly (D-05).
  </behavior>
  <action>
    In `sync`, broaden the `except (github_publish.GitHubPublishError, jinxxy_api.JinxxyAPIError)` clause (L254) to `except Exception` so ANY sync failure — including a `KeyError`/`TypeError` from `map_product` on a malformed detail — follows the D-05 path: `log.exception("jinxxy: /tienda sync falló")` then the existing ephemeral `interaction.followup.send("No pude sincronizar ahora; revisa los logs.", ephemeral=True)` then `return`. Keep the body identical; only widen the caught type. Add a comment noting the broadening covers `map_product` mapping errors (WR-08). Then add a test to test_jinxxy_cog.py: patch `_run_sync` to raise `KeyError` (or `TypeError`), drive `sync` with a fake staff interaction, and assert the ephemeral "revisa los logs" followup was sent and no public announce occurred.
  </action>
  <acceptance_criteria>
    - The `sync` handler catches `except Exception` (source assertion — it no longer misses `KeyError`/`TypeError`).
    - The test asserts a `KeyError` from `_run_sync` produces the ephemeral "revisa los logs" reply.
    - `cd ../nocturna-bot && python -m pytest tests/test_jinxxy_cog.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd "../nocturna-bot" && python -m pytest tests/test_jinxxy_cog.py -q</automated>
  </verify>
  <done>Any /tienda sync failure (including a malformed-detail mapping error) resolves the interaction with the D-05 ephemeral reply; a test pins it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wrap the announce channel.send so a channel error never propagates (WR-09)</name>
  <files>../nocturna-bot/cogs/jinxxy.py, ../nocturna-bot/tests/test_jinxxy_cog.py</files>
  <read_first>
    - ../nocturna-bot/cogs/jinxxy.py (read `_announce` L381-404 — the channel resolution guard and the UNWRAPPED `await channel.send(embed=...)` at L404, plus the docstring promising "never raised")
    - .planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-REVIEW.md (WR-09 L204-214 — wrap in try/except `discord.HTTPException`)
    - ../nocturna-bot/tests/test_jinxxy_cog.py (fake channel scaffolding to make `send` raise)
  </read_first>
  <behavior>
    - When `channel.send` raises `discord.Forbidden`/`discord.HTTPException`, `_announce` logs it and returns normally — the exception does NOT propagate to the caller (`/tienda sync` still sends its ephemeral summary; the poll does not restart over a cosmetic failure).
    - A successful announce still posts the embed (no regression).
  </behavior>
  <action>
    In `_announce`, wrap the final `await channel.send(embed=self._build_announce_embed(result))` (L404) in `try/except discord.HTTPException` (which covers `discord.Forbidden`, a subclass): on exception `log.exception("jinxxy: no pude publicar el anuncio de la tienda")` and return — never re-raise. This makes the method match its own docstring ("logged and skipped — never raised"). Then add a test to test_jinxxy_cog.py: build a fake channel whose `send` raises `discord.Forbidden` (or `discord.HTTPException`), call `_announce` with a changed result, and assert it does NOT raise and that the error was logged (e.g. via a caplog assertion or a log spy consistent with the repo's test idiom).
  </action>
  <acceptance_criteria>
    - `channel.send` in `_announce` is wrapped in `try/except discord.HTTPException` that logs and returns.
    - The test asserts `_announce` does not raise when `channel.send` raises `discord.Forbidden`/`HTTPException`.
    - `cd ../nocturna-bot && python -m pytest tests/test_jinxxy_cog.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd "../nocturna-bot" && python -m pytest tests/test_jinxxy_cog.py -q</automated>
  </verify>
  <done>A Forbidden/HTTPException on the announce channel is logged and swallowed, honoring the docstring; a test pins it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff Discord attachment → bot (image bytes) | Arbitrary uploaded bytes (non-image, decompression bomb) reach PIL decoding |
| Jinxxy detail record → map_product | A malformed 2xx detail can raise an untyped error |
| bot → announce channel | A missing send permission must not crash the sync/poll |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10-01 | Denial of Service | `optimize_to_webp` on a decompression-bomb / non-image attachment | mitigate | Broad `try/except` around `_optimize_attachments` with an ephemeral error + return (Task 1); PIL's `MAX_IMAGE_PIXELS` bomb guard already raises, now handled |
| T-09-10-02 | Denial of Service | `/tienda sync` hanging on an untyped `map_product` error | mitigate | Broaden the command guard to `except Exception` (Task 2) so any failure resolves the interaction per D-05 |
| T-09-10-03 | Denial of Service | announce `channel.send` propagating a cosmetic Forbidden into the sync/poll | mitigate | Wrap `channel.send` in `try/except discord.HTTPException` (Task 3) |
| T-09-10-SC | Tampering | pip installs | accept | No new dependencies — all edits are to the existing cog and its test; no `pip install` in this plan |
</threat_model>

<verification>
- `cd ../nocturna-bot && python -m pytest tests/test_jinxxy_cog.py -q` passes with the three new tests.
- Full suite unregressed: `cd ../nocturna-bot && python -m pytest -q`.
- Source assertions: the `_optimize_attachments` call is guarded; the `sync` handler catches `Exception`; `_announce`'s `channel.send` is wrapped.
</verification>

<success_criteria>
- A bad attachment, a malformed detail, and an unsendable announce channel each fail gracefully (log-only + ephemeral where applicable), never hanging an interaction or crashing the poll (WR-07/WR-08/WR-09 closed).
</success_criteria>

<output>
Create `.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-10-SUMMARY.md` when done.
</output>
