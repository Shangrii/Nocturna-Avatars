---
phase: 09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse
plan: 13
type: execute
wave: 2
depends_on: ["09-12"]
files_modified:
  - ../nocturna-bot/config.py
  - ../nocturna-bot/cogs/jinxxy.py
  - ../nocturna-bot/.env.example
  - ../nocturna-bot/tests/test_jinxxy_cog.py
autonomous: true
requirements: [STORE-SYNC-01]
gap_closure: true

must_haves:
  truths:
    - "The store announce embed posts in ENGLISH with engaging copy (e.g. 'There's a new product on our webpage! Make sure to check it out'), overriding D-05's Spanish-first for STORE announcements only"
    - "The embed includes a link to the website store page (https://nocturna-avatars.site/en/store)"
    - "Each added/updated product is linked to its checkoutUrl in the embed"
    - "The embed shows a product thumbnail when a product has images (images[0] resolved to an absolute site URL); it is omitted cleanly when no product has images"
    - "Error handling is unchanged: failures stay log-only and the announce channel remains store-news only (D-05); a no-change result is still silent (D-06); the embed still uses brand red 0xC0192C"
  artifacts:
    - path: "../nocturna-bot/cogs/jinxxy.py"
      provides: "_build_announce_embed rewritten: English engaging copy + store page link + per-product checkoutUrl links + best-effort thumbnail"
      contains: "_build_announce_embed"
    - path: "../nocturna-bot/config.py"
      provides: "WEBSITE_BASE_URL + JINXXY_STORE_URL env constants for the store link and image thumbnail base"
      contains: "JINXXY_STORE_URL"
  key_links:
    - from: "../nocturna-bot/cogs/jinxxy.py"
      to: "config.JINXXY_STORE_URL / config.WEBSITE_BASE_URL"
      via: "embed store-page link + images[0] absolute URL"
      pattern: "JINXXY_STORE_URL|WEBSITE_BASE_URL"
    - from: "../nocturna-bot/cogs/jinxxy.py"
      to: "product checkoutUrl"
      via: "per-product markdown link, https-guarded"
      pattern: "checkoutUrl"
---

<objective>
Close GAP-2 (UAT, 2026-07-11): the store announce embed is generic Spanish text (`title="Tienda actualizada"`, ES bucket labels, footer "Nocturna · tienda"). The announce channel is PUBLIC and the audience is now American/English, so the embed must be ENGLISH, engaging, and visual — engaging copy, the website store page link, per-product links (each product's `checkoutUrl`), and a product thumbnail where images exist.

Purpose: the public "new product" announcement is the conversion surface for the store; a plain Spanish status line does not drive American visitors to the storefront. User decision 2026-07-11: this OVERRIDES D-05's Spanish-first for STORE announcements only — error handling stays log-only per D-05, and the no-change-is-silent rule (D-06) is unchanged.

Output: two config constants (`WEBSITE_BASE_URL`, `JINXXY_STORE_URL`) with `.env.example` docs, a rewritten `_build_announce_embed`, and updated announce tests.

Cross-repo, bot-side only. All code lives in `../nocturna-bot`; the website repo gets zero changes. Website store URLs are fixed facts of the deployed site: ES store `https://nocturna-avatars.site/tienda`, EN store `https://nocturna-avatars.site/en/store` (per `src/components/sections/StorePage.astro`); the CNAME domain is `nocturna-avatars.site`. Keep the bot suite green: `cd ../nocturna-bot && python -m pytest -q`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-HUMAN-UAT.md
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Already in the bot repo — do NOT redesign. -->

Announce reconcile result shape (built by core/store_sync.reconcile_store, passed to _announce/_build_announce_embed):
  {"products": [entry, ...],   # merged product dicts, each with checkoutUrl, name{es,en}, images (maybe), ...
   "added": [checkoutUrl, ...], "updated": [checkoutUrl, ...], "removed": [checkoutUrl, ...],
   "changed": bool}
  A removed key is NOT in "products" (already dropped) — the current embed falls back to showing the key for removed items.

Product entry (store.json shape): {"checkoutUrl": "https://jinxxy.com/<store>/<slug>", "name": {"es":..,"en":..}, "images": ["/store/<file>.webp", ...] (may be absent/empty), ...}

Current _build_announce_embed (cogs/jinxxy.py, STATIC method to rewrite):
  Spanish: title "Tienda actualizada"; buckets ("🆕 Nuevos", added), ("✏️ Actualizados", updated), ("🗑️ Quitados", removed); per-key name via by_key.get(k)["name"]["es"]; footer "Nocturna · tienda"; color _BRAND_RED = 0xC0192C; embed.timestamp = utcnow. It builds by_key = {p["checkoutUrl"]: p for p in result["products"]}.

https guard available: core/store_sync.is_https_url(url) — True iff url parses as https with a netloc. Use before rendering any URL as an embed link/thumbnail.

Config env idiom (config.py, "Jinxxy (Fase 9)" block): `NAME = os.getenv("NAME", "default")` with a Spanish comment; existing keys include JINXXY_ANNOUNCE_CHANNEL_ID, WEBSITE_STORE_JSON, WEBSITE_STORE_IMAGE_DIR (default "public/store", served publicly as /store/<file>).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WEBSITE_BASE_URL + JINXXY_STORE_URL config constants (config.py + .env.example)</name>
  <files>../nocturna-bot/config.py, ../nocturna-bot/.env.example</files>
  <read_first>
    - ../nocturna-bot/config.py — read the "Jinxxy (Fase 9: sync de la tienda)" env block (JINXXY_ANNOUNCE_CHANNEL_ID, WEBSITE_STORE_JSON, WEBSITE_STORE_IMAGE_DIR) to match the `os.getenv(NAME, default)` + Spanish-comment idiom
    - ../nocturna-bot/.env.example — read the Jinxxy section to match the documentation style for the two new vars
  </read_first>
  <action>
    In the Jinxxy Fase 9 block of config.py add two constants: `WEBSITE_BASE_URL = os.getenv("WEBSITE_BASE_URL", "https://nocturna-avatars.site")` (the deployed site origin — used to turn a site-relative `/store/<file>.webp` image path into an absolute embed thumbnail URL) and `JINXXY_STORE_URL = os.getenv("JINXXY_STORE_URL", "https://nocturna-avatars.site/en/store")` (the English store page the public announce embed links to — the EN store route per StorePage.astro; the audience is now English so it points at /en/store, not /tienda). Add matching Spanish comment lines in the block's style. Document both in .env.example under the Jinxxy section with their default values and a one-line purpose each.
  </action>
  <verify>
    <automated>cd ../nocturna-bot && python -c "import config; assert config.JINXXY_STORE_URL.startswith('https://') and config.WEBSITE_BASE_URL.startswith('https://'); print('ok', config.JINXXY_STORE_URL, config.WEBSITE_BASE_URL)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "WEBSITE_BASE_URL\|JINXXY_STORE_URL" ../nocturna-bot/config.py` shows both defined via os.getenv with the defaults `https://nocturna-avatars.site` and `https://nocturna-avatars.site/en/store`
    - `grep -n "WEBSITE_BASE_URL\|JINXXY_STORE_URL" ../nocturna-bot/.env.example` shows both documented
    - `python -c "import config"` succeeds and both constants start with `https://`
  </acceptance_criteria>
  <done>The store link and image-base URLs are env-configurable constants with correct defaults, documented in .env.example.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite _build_announce_embed to English, engaging, and visual (cogs/jinxxy.py + tests)</name>
  <files>../nocturna-bot/cogs/jinxxy.py, ../nocturna-bot/tests/test_jinxxy_cog.py</files>
  <read_first>
    - ../nocturna-bot/cogs/jinxxy.py — read `_build_announce_embed` (the method to rewrite), `_announce` (its caller — keep its D-05/D-06 silent-on-no-change + logged-not-raised contract untouched), and `_BRAND_RED`
    - ../nocturna-bot/core/store_sync.py — read `is_https_url` (guard URLs before rendering them as links/thumbnails)
    - ../nocturna-bot/tests/test_jinxxy_cog.py — read `test_announce_sends_branded_embed_on_change`, `test_announce_silent_on_no_change`, `test_announce_forbidden_channel_is_logged_not_raised`, `test_announce_http_error_channel_does_not_raise`, and `_current_entry()`/`KEY` helpers — these announce tests must be updated to the new English/visual contract while keeping the silent/logged-not-raised behavior assertions
  </read_first>
  <action>
    Rewrite the static `_build_announce_embed(result)` to produce an ENGLISH, engaging, visual embed (this OVERRIDES D-05's Spanish-first for STORE announcements only — do NOT change any error path or _announce; the no-change guard in _announce stays, so this method is only reached on a real change). Keep `color=_BRAND_RED` (0xC0192C) and `embed.timestamp = discord.utils.utcnow()`. Set an English title (e.g. "New on the Nocturna store") and an engaging description line (e.g. "There's a new product on our webpage — make sure to check it out!"). Add a top-level store link to `config.JINXXY_STORE_URL` (e.g. an embed field "Store" with the markdown link, or embed.url set to the store URL). For the added/updated/removed buckets, use ENGLISH labels ("🆕 New", "✏️ Updated", "🗑️ Removed"); render each product as a markdown link `[name](checkoutUrl)` when its checkoutUrl passes `store_sync.is_https_url`, otherwise plain name (removed keys are not in products → show the name/key as plain text). SANITIZE each link label by removing markdown-breaking characters (`[`, `]`, `(`, `)`) from the product name so a crafted name cannot break out of the link syntax (threat T-09-25). For the thumbnail: pick the first product (prefer an `added` one, else any in `result["products"]`) whose `images` list is non-empty, and set `embed.set_thumbnail(url=f"{config.WEBSITE_BASE_URL}{images[0]}")` — but only when `images[0]` is a site-relative path starting with `/` (so the composed URL is `https://nocturna-avatars.site/store/<file>.webp`); omit the thumbnail entirely when no product has images (never set an empty/None url). Use the product name from `name["en"]` (falling back to `name["es"]` then the key) since the announce is English-first now. Update the announce tests in test_jinxxy_cog.py: assert the embed title/description are English, that the store link (config.JINXXY_STORE_URL) appears in the embed, that an added product name appears linked to its checkoutUrl, that a product with images sets a thumbnail whose url is `WEBSITE_BASE_URL + images[0]`, that an all-no-images result sets no thumbnail, and KEEP the color==0xC0192C, silent-on-no-change, and forbidden/http-error-logged-not-raised assertions.
  </action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_jinxxy_cog.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The embed title/description are English: `grep -niE "new on the nocturna store|there's a new product|new on" ../nocturna-bot/cogs/jinxxy.py` matches, and the old `Tienda actualizada`/`Nocturna · tienda` strings are gone from _build_announce_embed
    - A test asserts the embed contains the store link value equal to `config.JINXXY_STORE_URL`
    - A test asserts an added product name is rendered as a markdown link containing its checkoutUrl
    - A test asserts a product with `images=["/store/x.webp"]` yields `embed.thumbnail.url == config.WEBSITE_BASE_URL + "/store/x.webp"`, and a separate test asserts no thumbnail is set when no product has images
    - The embed still uses color 0xC0192C, is still silent on no change, and still swallows Forbidden/HTTPException (existing tests updated but their behavior assertions retained)
    - `cd ../nocturna-bot && python -m pytest tests/test_jinxxy_cog.py -q` passes
  </acceptance_criteria>
  <done>The public store announce embed is English, engaging, and visual (store link + per-product checkoutUrl links + best-effort thumbnail) while D-05 error handling and D-06 silence are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store.json product data → public Discord embed | Product name/checkoutUrl/images (from Jinxxy + staff) rendered into a PUBLIC announce channel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-24 | Tampering | checkoutUrl / images[0] used as embed link + thumbnail URL | mitigate | Render a per-product link only when `store_sync.is_https_url(checkoutUrl)` is true; build the thumbnail URL only from a site-relative `/store/...` path prefixed with `config.WEBSITE_BASE_URL` (a trusted constant), never from raw external URL text |
| T-09-25 | Tampering | product name in a markdown link label | mitigate | Strip markdown-breaking characters (`[]()`) from the name before using it as a `[label](url)` link label so a crafted name cannot alter the link target |
| T-09-26 | Information disclosure | public announce channel | accept | The announce channel is intentionally public (D-18) and store-news only; the embed carries product names, links, and thumbnails already published on the site — no secrets. Error paths remain log-only (D-05, unchanged) |
| T-09-SC | Tampering | npm/pip/cargo installs | accept | No new package installs — reuses discord.py already pinned in requirements.txt; no legitimacy checkpoint needed |
</threat_model>

<verification>
- `cd ../nocturna-bot && python -m pytest -q` stays green (baseline 338/338, plus/updated announce tests)
- The announce embed renders English copy + store link + per-product checkoutUrl links + best-effort thumbnail
- `_announce`'s D-05 (errors log-only) and D-06 (silent on no change) behavior is unchanged: `git -C ../nocturna-bot diff` on cogs/jinxxy.py touches `_build_announce_embed` (and imports/config use) but not the `_announce` error/silence branches
</verification>

<success_criteria>
- On a real store change the announce embed is English, engaging, and visual: a store-page link to https://nocturna-avatars.site/en/store, each added/updated product linked to its checkoutUrl, and a product thumbnail when images exist
- The embed keeps brand red, stays silent on no change, and never posts errors (D-05/D-06 intact)
- Full bot suite passes
</success_criteria>

<output>
Create `.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-13-SUMMARY.md` when done
</output>
