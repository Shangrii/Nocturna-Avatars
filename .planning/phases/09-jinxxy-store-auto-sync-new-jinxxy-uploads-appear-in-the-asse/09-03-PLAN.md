---
phase: 09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - ../nocturna-bot/core/store_sync.py
  - ../nocturna-bot/tests/test_store_sync.py
autonomous: true
requirements: [STORE-SYNC-01]

must_haves:
  truths:
    - "A Jinxxy detail record maps to a store.json entry using only API-available fields (name into both locales, price, constructed checkoutUrl, category, nsfw from restrictions, date)"
    - "A three-way (snapshot/live/current) merge writes a sync-owned field only when Jinxxy changed and staff had not edited it; staff-owned fields are never touched"
    - "The whole-store reconcile computes adds, updates, and removals and returns a change summary; no-change produces an empty summary"
    - "checkoutUrl is constructed as https://jinxxy.com/{store_username}/{slug} and validated https-only before it can reach the transport"
  artifacts:
    - path: "../nocturna-bot/core/store_sync.py"
      provides: "map_product, three_way_merge, reconcile_store, https validator — all pure"
      contains: "def three_way_merge"
    - path: "../nocturna-bot/tests/test_store_sync.py"
      provides: "unit tests for mapping, the three merge branches, add/update/remove, https guard"
      contains: "def test_"
  key_links:
    - from: "../nocturna-bot/core/store_sync.py"
      to: "checkoutUrl"
      via: "f-string https://jinxxy.com/{store_username}/{slug}"
      pattern: "jinxxy.com/"
---

<objective>
Build the pure heart of the sync: the Jinxxy-detail → store.json mapper and the three-way field-ownership merge (D-12) that never clobbers staff edits, plus a whole-store reconcile that computes adds/updates/removals and a change summary — all pure functions, fully unit-testable, zero Discord/network/DB.

Purpose: This is the second of the three genuinely-new mechanics (RESEARCH §merge). Getting the three merge branches right (Jinxxy-unchanged / Jinxxy-changed-staff-untouched / both-changed) is what prevents Pitfall 3 "staff edits get clobbered". Isolating it as pure functions makes it the highest-confidence TDD unit in the phase.
Output: `core/store_sync.py` + `tests/test_store_sync.py`. The cog (09-05) orchestrates snapshot(DB)/live(API)/current(store.json) into these functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-CONTEXT.md
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-RESEARCH.md
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-PATTERNS.md

<interfaces>
<!-- store.json product schema (website repo, LOCKED — bot writes only sync-owned fields): -->
<!-- id, name{es,en}, description{es,en}, price (plain number string, NO $), images[] ("/store/<file>" or Jinxxy CDN), checkoutUrl (https:// only), editor, category, nsfw (bool), featured (bool), date (YYYY-MM-DD), + optional license/details/updates{es,en}, storefronts[]. -->
<!-- SYNC-OWNED (merge writes these): name, price, checkoutUrl, category, nsfw, date. -->
<!-- STAFF-OWNED (merge NEVER touches): description, images, license, details, updates, storefronts, featured, id. -->
<!-- name is staff-editable-after-first-write: once current.name != snapshot.name, staff wins (D-10/D-12). price/checkoutUrl/category/nsfw/date: Jinxxy wins on change. -->
<!-- map_product source (live probe D-14..D-17): detail["name"], detail["base_price"], detail["url"] (a SLUG), detail.get("category"), detail.get("restrictions") or [], detail["created_at"][:10]. checkoutUrl = f"https://jinxxy.com/{store_username}/{slug}". nsfw = "CONTENT_MATURE" in restrictions. editor = owner_name from /me. -->
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN — map_product + https validator</name>
  <files>../nocturna-bot/core/store_sync.py, ../nocturna-bot/tests/test_store_sync.py</files>
  <read_first>
    - ../nocturna-bot/cogs/reminders.py (L1-29 module docstring "importing has no side effects" + the pure-helper/validator block L126-171 — replicate the pure-function, import-safe idiom)
    - .planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-PATTERNS.md (core/store_sync.py map_product excerpt lines 52-68)
    - .planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-RESEARCH.md (map_product Code Example lines 374-392)
  </read_first>
  <behavior>
    - Test: `map_product(detail, store_username, owner_name)` with the probe-confirmed Cahuama detail returns `{"checkoutUrl": "https://jinxxy.com/<store_username>/cahuama", "name": {"es": "Cahuama", "en": "Cahuama"}, "price": "0", "category": "avatar-props", "editor": <owner_name>, "nsfw": True, "date": "2026-07-10"}` (nsfw True because restrictions contains "CONTENT_MATURE").
    - Test: a detail with `restrictions: []` or missing → `nsfw: False`.
    - Test: a detail with `category: null` → `category: "assets"` (D-09 default).
    - Test: `map_product` output contains NO `images`, `description`, `featured`, `license`, `details`, `updates`, `storefronts`, or `id` keys (those are staff-owned / assigned elsewhere).
    - Test: `is_https_url("https://jinxxy.com/x/y")` is True; `is_https_url("http://...")` and `is_https_url("javascript:...")` are False.
  </behavior>
  <action>
    Create `core/store_sync.py` with a module docstring stating it has no side effects (imports only stdlib). Implement `map_product(detail: dict, store_username: str, owner_name: str) -> dict` building the entry from the API-available fields only: `name` verbatim into both `es`/`en` (D-10, name only), `price = str(detail["base_price"])`, `checkoutUrl = f"https://jinxxy.com/{store_username}/{detail['url']}"` (D-17 — `url` is a slug), `category = detail.get("category") or "assets"` (D-09), `editor = owner_name`, `nsfw = "CONTENT_MATURE" in (detail.get("restrictions") or [])` (D-16), `date = detail["created_at"][:10]`. Do NOT emit images/description/staff-owned keys. Add `is_https_url(url: str) -> bool` (scheme must be exactly `https`) used to guard `checkoutUrl` before it reaches the transport (V5/security). Write failing tests first, then implement.
  </action>
  <verify>
    <automated>cd "../nocturna-bot" && python -m pytest tests/test_store_sync.py -q -k "map or https or nsfw or category"</automated>
  </verify>
  <acceptance_criteria>
    - `map_product` on the Cahuama detail returns the exact dict above (checkoutUrl constructed, nsfw True, name in both locales, price "0", date "2026-07-10")
    - restrictions empty/missing → `nsfw False`; category null → `"assets"`
    - `map_product` output key set is exactly `{checkoutUrl, name, price, category, editor, nsfw, date}` (no staff-owned keys)
    - `is_https_url` accepts `https://` only and rejects `http://` and `javascript:`
    - `grep -c "import discord\|import requests\|from core import db" ../nocturna-bot/core/store_sync.py` == 0 (pure module)
  </acceptance_criteria>
  <done>The mapper produces exactly the sync-owned field set from probe-confirmed inputs, and the https guard is proven.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED+GREEN — three_way_merge (D-12 branches) + reconcile_store</name>
  <files>../nocturna-bot/core/store_sync.py, ../nocturna-bot/tests/test_store_sync.py</files>
  <read_first>
    - ../nocturna-bot/core/store_sync.py (the map_product + validator from Task 1)
    - ../nocturna-bot/cogs/reminders.py (classify_fire L110-122 — the "small deterministic classifier returning a value + a documented branch decision" shape to mirror)
    - .planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-RESEARCH.md (Pattern 3 three-way merge lines 276-283, Pitfall 3 lines 330-334)
  </read_first>
  <behavior>
    - Test (Jinxxy unchanged): live field == snapshot field → the merged entry keeps `current`'s value (staff edit preserved), field NOT in changed set.
    - Test (Jinxxy changed, staff untouched): live != snapshot AND current == snapshot → merged takes `live`, field IN changed set, snapshot advances to live.
    - Test (both changed, staff-editable field `name`): live != snapshot AND current != snapshot → merged keeps `current` (staff wins for name, D-10); snapshot still advances to live so the edit isn't re-flagged forever.
    - Test (both changed, pure sync-owned field `price`): live != snapshot AND current != snapshot → merged takes `live` (Jinxxy wins for price/checkoutUrl/category/nsfw/date).
    - Test (staff-owned keys): `description`, `images`, `featured`, `license`, `details`, `updates`, `storefronts` present in `current` are carried through untouched in the merged entry regardless of live/snapshot.
    - Test (reconcile add): a live product with no current entry and no snapshot → appears in `added`.
    - Test (reconcile remove): a current+snapshot product absent from live (successful full enumeration) → appears in `removed`.
    - Test (reconcile no-op): live all equal to snapshot and current → `changed`/`added`/`removed` all empty.
  </behavior>
  <action>
    Add `three_way_merge(snapshot: dict|None, live: dict, current: dict|None) -> tuple[dict, list[str]]` returning `(merged_entry, changed_fields)`. Iterate the SYNC-OWNED fields (`name`, `price`, `checkoutUrl`, `category`, `nsfw`, `date`); for each apply the D-12 rule (unchanged→keep current; changed+staff-untouched→take live; both-changed→staff wins for `name` only, else live). Always carry every STAFF-OWNED key (`id`, `description`, `images`, `featured`, `license`, `details`, `updates`, `storefronts`) from `current` unchanged into the merged entry (never from live). New product (snapshot None, current None) → merged is the mapped live entry with a generated stable `id` (e.g. slug of checkoutUrl) and empty staff fields omitted so the site's fallbacks apply. Add `reconcile_store(snapshots: dict, live_by_key: dict, current_by_key: dict) -> {"products": list, "added": list, "updated": list, "removed": list, "changed": bool}` keyed by `checkoutUrl`: iterate the union, run `three_way_merge` per live key, drop products present in current+snapshot but absent from live (removals), and set `changed` True iff any add/update/remove occurred. Write failing tests for all branches first, then implement.
  </action>
  <verify>
    <automated>cd "../nocturna-bot" && python -m pytest tests/test_store_sync.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All five merge-branch tests pass (unchanged, changed-staff-untouched, both-changed-name→staff-wins, both-changed-price→jinxxy-wins, staff-owned-carried-through)
    - reconcile add/remove/no-op tests pass; `changed` is False only when nothing added/updated/removed
    - A merged entry NEVER contains a `description`/`images`/`featured`/etc. value taken from `live` (source + test assertion)
    - `cd ../nocturna-bot && python -m pytest tests/test_store_sync.py -q` reports all passing
  </acceptance_criteria>
  <done>The three-way merge and whole-store reconcile are green across every branch, provably preserving staff edits and staff-owned fields while propagating Jinxxy changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jinxxy response → store.json | mapped fields are untrusted input written into a committed data file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | malformed API field → store.json → site | mitigate | `map_product` normalizes types (str(price), list-guarded restrictions, slice date); `is_https_url` guards `checkoutUrl` before the transport writes it; the site auto-escapes (T-06-01) as defense in depth |
| T-09-08 | Tampering | staff edits clobbered by sync | mitigate | Three-way merge (D-12): a sync-owned field is written only when `live != snapshot AND current == snapshot`; `name` yields to staff on conflict; staff-owned keys never sourced from live — all branch-tested |
| T-09-09 | Denial of Service (of content) | reconcile removes products on partial data | mitigate | `reconcile_store` computes removals only from the passed live enumeration; the cog (09-05) passes live ONLY on a successful full API scan, never on error |
</threat_model>

<verification>
- `cd ../nocturna-bot && python -m pytest tests/test_store_sync.py -q` — all branch tests pass.
- `cd ../nocturna-bot && python -m pytest -q` — full suite green.
- `python -c "from core import store_sync"` imports with no discord/requests/db.
</verification>

<success_criteria>
- `map_product`, `three_way_merge`, `reconcile_store`, `is_https_url` exist and are pure.
- Every D-12 merge branch is test-covered; staff edits and staff-owned fields are provably preserved.
- reconcile computes adds/updates/removals and a correct `changed` flag.
</success_criteria>

<output>
Create `.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-03-SUMMARY.md` when done.
</output>
