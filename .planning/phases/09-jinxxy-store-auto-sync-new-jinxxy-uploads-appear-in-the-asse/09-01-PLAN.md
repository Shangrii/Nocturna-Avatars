---
phase: 09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../nocturna-bot/config.py
  - ../nocturna-bot/core/db.py
  - ../nocturna-bot/.env.example
autonomous: true
requirements: [STORE-SYNC-01]

must_haves:
  truths:
    - "The bot reads JINXXY_API_KEY / JINXXY_ANNOUNCE_CHANNEL_ID / poll interval / store paths / staff roles from env"
    - "A durable SQLite snapshot table records the last-synced Jinxxy value per product so the three-way merge survives restarts"
  artifacts:
    - path: "../nocturna-bot/config.py"
      provides: "Jinxxy env block (key, announce channel, poll hours, store json/image paths, staff roles)"
      contains: "JINXXY_API_KEY"
    - path: "../nocturna-bot/core/db.py"
      provides: "init_store_state() + snapshot getters/setters keyed by checkoutUrl"
      contains: "init_store_state"
    - path: "../nocturna-bot/.env.example"
      provides: "documented Jinxxy env vars for the cinema-host deploy"
      contains: "JINXXY_ANNOUNCE_CHANNEL_ID"
  key_links:
    - from: "../nocturna-bot/config.py"
      to: "os.getenv"
      via: "env-driven constants with defaults"
      pattern: "JINXXY_API_KEY.*os.getenv"
    - from: "../nocturna-bot/core/db.py"
      to: "store_snapshot table"
      via: "CREATE TABLE IF NOT EXISTS + ? placeholders"
      pattern: "store_snapshot"
---

<objective>
Stand up the Phase-9 configuration + durable state surface in the bot repo so every downstream plan (API client, merge, transport, cog, attach flow) has its env constants and the D-12 snapshot table to build against.

Purpose: The three-way ownership merge (D-12) needs a durable per-product snapshot of the last-synced Jinxxy value to distinguish "Jinxxy changed" from "staff edited"; the cog needs env-driven config following the repo's `config.py` idiom. This is the interface-first foundation — contracts before consumers.
Output: `config.py` Jinxxy block, `core/db.py` `init_store_state()` + snapshot read/write helpers, `.env.example` documentation. No cog wiring yet (that lands in 09-05 to keep `bot.py` single-owner).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-CONTEXT.md
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-RESEARCH.md
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-PATTERNS.md

<interfaces>
<!-- Existing config.py Jinxxy-relevant reused constants (bot repo, VERIFIED): -->
<!-- GITHUB_PAT, WEBSITE_REPO="Shangrii/Nocturna-Avatars", WEBSITE_BRANCH="revamp" are reused UNCHANGED. -->
<!-- GALLERY_STAFF_ROLE_IDS = [int(x) for x in os.getenv(...,"").split(",") if x.strip()] is the fallback list. -->
<!-- Existing db.py idiom: _get_conn() with row_factory=sqlite3.Row; per-feature init_*(); ISO via datetime.now(timezone.utc).isoformat(); ALWAYS ? placeholders. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Jinxxy config block + .env.example docs</name>
  <files>../nocturna-bot/config.py, ../nocturna-bot/.env.example</files>
  <read_first>
    - ../nocturna-bot/config.py (see the Galería L51-70, Reseñas L72-84, Recordatorios L86-98 env blocks — copy their commented-Spanish-header + os.getenv-with-default + comma-split-role-list shape exactly)
    - ../nocturna-bot/.env.example (mirror the Galería/Reseñas/Recordatorios sections at L43-61 for placement + comment style)
    - .planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-PATTERNS.md (the config.py env-block excerpt, lines 205-222)
  </read_first>
  <action>
    Append a new "── Jinxxy (Fase 9: sync de la tienda) ──" section to `config.py` after the Recordatorios block defining: `JINXXY_API_KEY = os.getenv("JINXXY_API_KEY", "")` (secret, header-only, never logged — same discipline as `GITHUB_PAT`); `JINXXY_ANNOUNCE_CHANNEL_ID = int(os.getenv("JINXXY_ANNOUNCE_CHANNEL_ID", "1525202600738295818"))` (D-18 default); `JINXXY_POLL_HOURS = int(os.getenv("JINXXY_POLL_HOURS", "6"))` (D-03 band 6–12h); `WEBSITE_STORE_JSON = os.getenv("WEBSITE_STORE_JSON", "src/data/store.json")`; `WEBSITE_STORE_IMAGE_DIR = os.getenv("WEBSITE_STORE_IMAGE_DIR", "public/store")` (the D-15 attach-flow image dir, served as `/store/<file>` by GitHub Pages); and `JINXXY_STAFF_ROLE_IDS = [int(x) for x in os.getenv("JINXXY_STAFF_ROLE_IDS", "").split(",") if x.strip()] or GALLERY_STAFF_ROLE_IDS` (fallback idiom identical to REVIEWS_STAFF_ROLE_IDS). `GITHUB_PAT`/`WEBSITE_REPO`/`WEBSITE_BRANCH` are reused unchanged — do NOT redefine them. Then add a matching commented section to `.env.example` documenting each new var with a Spanish comment, noting `JINXXY_API_KEY=` is left blank (NEVER commit a real key) and `WEBSITE_STORE_IMAGE_DIR=public/store`.
  </action>
  <verify>
    <automated>cd "../nocturna-bot" && python -c "import importlib,config; importlib.reload(config); assert hasattr(config,'JINXXY_API_KEY'); assert config.JINXXY_ANNOUNCE_CHANNEL_ID==1525202600738295818; assert config.JINXXY_POLL_HOURS>=6; assert config.WEBSITE_STORE_JSON=='src/data/store.json'; assert config.WEBSITE_STORE_IMAGE_DIR=='public/store'; assert isinstance(config.JINXXY_STAFF_ROLE_IDS,list); print('config OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import config; print(config.JINXXY_ANNOUNCE_CHANNEL_ID)"` in the bot repo prints `1525202600738295818`
    - `config.WEBSITE_STORE_IMAGE_DIR == "public/store"` and `config.WEBSITE_STORE_JSON == "src/data/store.json"`
    - `config.JINXXY_STAFF_ROLE_IDS` equals `config.GALLERY_STAFF_ROLE_IDS` when `JINXXY_STAFF_ROLE_IDS` is unset (fallback idiom)
    - `grep -n JINXXY_ANNOUNCE_CHANNEL_ID ../nocturna-bot/.env.example` returns a line; `grep -c "pon_aqui\|real" ../nocturna-bot/.env.example` shows no real key on the JINXXY_API_KEY line (left blank)
    - `GITHUB_PAT`, `WEBSITE_REPO`, `WEBSITE_BRANCH` appear exactly once each in `config.py` (not redefined)
  </acceptance_criteria>
  <done>The bot process can read all six Jinxxy config values from env with the documented defaults, and `.env.example` documents them for the manual cinema-host deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Add init_store_state() + snapshot getters/setters to core/db.py</name>
  <files>../nocturna-bot/core/db.py</files>
  <read_first>
    - ../nocturna-bot/core/db.py (see init_gallery_state L38-52, get_cursor/set_cursor L55-78, init_reminders L228-255, and the T-08-03 ?-placeholder + _REMINDER_UPDATABLE whitelist discipline L216-225 — replicate that shape)
    - .planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-PATTERNS.md (the core/db.py excerpt, lines 170-203)
  </read_first>
  <action>
    Add a "── Tienda (Fase 9: snapshot de sync) ──" section to `core/db.py` with `init_store_state()` creating `CREATE TABLE IF NOT EXISTS store_snapshot` keyed `checkout_url TEXT PRIMARY KEY` (the D-13 link key) plus columns `jinxxy_id TEXT, name TEXT, price TEXT, category TEXT, nsfw INTEGER, date TEXT, synced_at TEXT NOT NULL` — one row per synced product holding the last-synced value of every sync-owned field so the three-way compare is durable. Add `get_store_snapshot() -> dict[str, sqlite3.Row]` returning all snapshot rows keyed by `checkout_url` (for a whole-store merge pass), `upsert_store_snapshot(checkout_url, jinxxy_id, name, price, category, nsfw, date)` using `INSERT OR REPLACE` with `synced_at = datetime.now(timezone.utc).isoformat()` and `?` placeholders only, and `delete_store_snapshot(checkout_url)` for delisted products. The cog will call `init_store_state()` in its `__init__` (repo idiom — NOT `init_db()`). Every SQL statement uses `?` placeholders; no column name is ever interpolated from a variable (T-08-03).
  </action>
  <verify>
    <automated>cd "../nocturna-bot" && python -c "import os,tempfile; os.environ['DB_PATH']=tempfile.mktemp(); import config; config.DB_PATH=os.environ['DB_PATH']; from core import db; db.init_store_state(); db.upsert_store_snapshot('https://jinxxy.com/x/cahuama','id1','Cahuama','0','avatar-props',0,'2026-07-10'); s=db.get_store_snapshot(); assert 'https://jinxxy.com/x/cahuama' in s; assert s['https://jinxxy.com/x/cahuama']['name']=='Cahuama'; db.delete_store_snapshot('https://jinxxy.com/x/cahuama'); assert db.get_store_snapshot()=={}; print('db OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Calling `db.init_store_state()` twice does not error (CREATE TABLE IF NOT EXISTS idempotent)
    - `db.upsert_store_snapshot(...)` then `db.get_store_snapshot()` returns a dict keyed by `checkout_url` whose row exposes `name`, `price`, `category`, `nsfw`, `date`, `jinxxy_id`, `synced_at`
    - A second upsert on the same `checkout_url` replaces (not duplicates) the row
    - `db.delete_store_snapshot(url)` removes that row; `get_store_snapshot()` returns `{}` afterward
    - `grep -nE "SET .*\+|f\"|f'|format\(" ../nocturna-bot/core/db.py` shows no f-string/format SQL in the new store functions (only `?` placeholders)
  </acceptance_criteria>
  <done>The bot has a durable, restart-surviving snapshot store keyed by checkoutUrl with idempotent init + upsert + get + delete, matching the repo's SQLite idiom.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | `JINXXY_API_KEY` (secret) enters only via `.env`; must never be logged or committed |
| bot → SQLite | snapshot values (some derived from untrusted API responses) are persisted; column names must never come from variables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | `JINXXY_API_KEY` in config/.env | mitigate | Default `""`; `.env.example` ships blank with a "NEVER commit a real key" comment; the value is only ever read into a request header downstream (09-02), never logged here |
| T-09-02 | Tampering (SQL injection) | `core/db.py` store_snapshot writes | mitigate | Only `?` placeholders; no column name interpolated from a variable (T-08-03 lesson) |
| T-09-SC | Tampering | npm/pip installs | accept | No new packages this plan or phase — RESEARCH Package Legitimacy Audit confirms zero install surface; nothing to verify |
</threat_model>

<verification>
- `cd ../nocturna-bot && python -m pytest -q` still passes (existing 234 bot tests unaffected — this plan only adds new symbols).
- `python -c "import config"` and `python -c "from core import db"` import cleanly in the bot repo.
</verification>

<success_criteria>
- `config.py` exposes the six Jinxxy constants with the documented defaults and the staff-role fallback.
- `core/db.py` exposes `init_store_state`, `get_store_snapshot`, `upsert_store_snapshot`, `delete_store_snapshot`, all using `?` placeholders.
- `.env.example` documents every new var; `JINXXY_API_KEY` is blank.
- No existing bot test regresses.
</success_criteria>

<output>
Create `.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-01-SUMMARY.md` when done.
</output>
