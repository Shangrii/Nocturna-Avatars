---
phase: 09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse
plan: 05
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03", "09-04"]
files_modified:
  - ../nocturna-bot/cogs/jinxxy.py
  - ../nocturna-bot/bot.py
  - ../nocturna-bot/tests/test_jinxxy_cog.py
autonomous: true
requirements: [STORE-SYNC-01]

must_haves:
  truths:
    - "A background loop (6-12h) and a staff-gated /tienda sync both run one full sync: enumerate Jinxxy, map, three-way-merge against snapshot + store.json, commit only on change, update snapshots"
    - "A new Jinxxy product appears in store.json (and thus the web store) after a sync; a price/name change propagates without clobbering staff edits; a delisted product is removed"
    - "Store updates are announced (added/updated/removed) in JINXXY_ANNOUNCE_CHANNEL_ID; no-change syncs are silent; errors go to logs ONLY, never Discord"
    - "A transient Jinxxy API failure aborts the sync with no removals; a bot restart converges via startup reconcile"
  artifacts:
    - path: "../nocturna-bot/cogs/jinxxy.py"
      provides: "JinxxyCog: @tasks.loop poll, /tienda sync command, _run_sync orchestration, announce embed, startup reconcile"
      contains: "class JinxxyCog"
    - path: "../nocturna-bot/bot.py"
      provides: "load_extension('cogs.jinxxy') + JINXXY_API_KEY fail-fast"
      contains: "cogs.jinxxy"
    - path: "../nocturna-bot/tests/test_jinxxy_cog.py"
      provides: "unit tests for _is_staff gate, _run_sync commit-on-change/no-op, removal-safety, announce-on-change"
      contains: "def test_"
  key_links:
    - from: "../nocturna-bot/cogs/jinxxy.py"
      to: "github_publish.sync_store"
      via: "commit only when reconcile.changed"
      pattern: "sync_store"
    - from: "../nocturna-bot/cogs/jinxxy.py"
      to: "jinxxy_api.list_all_products"
      via: "full enumeration before any removal"
      pattern: "list_all_products"
---

<objective>
Wire the three cores (API client, merge, transport) into a `JinxxyCog`: a 6–12h background poll + a staff-gated `/tienda sync` that runs one full sync, commits store.json only on change, updates the snapshot, and announces added/updated/removed products — errors to logs only — plus a startup reconcile so restarts converge and a hard rule that an API failure never mass-removes the storefront.

Purpose: This is the controller that turns "new Jinxxy uploads appear automatically" into reality (STORE-SYNC-01). ~90% is copy-with-cadence-change of the Phase-8 scheduler (loop lifecycle) and the Phase-7 reviews cog (transport calls, staff gate, startup reconcile), with the D-05 divergence that failures NEVER post to Discord.
Output: `cogs/jinxxy.py`, `bot.py` wiring + fail-fast, `tests/test_jinxxy_cog.py`. The D-15 image/description attach flow is a separate plan (09-06) sharing this file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-CONTEXT.md
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-RESEARCH.md
@.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-PATTERNS.md

<interfaces>
<!-- Consumes (all from earlier 09 plans + repo, VERIFIED): -->
<!-- config: JINXXY_API_KEY, JINXXY_ANNOUNCE_CHANNEL_ID, JINXXY_POLL_HOURS, JINXXY_STAFF_ROLE_IDS, WEBSITE_STORE_JSON, WEBSITE_REPO, WEBSITE_BRANCH. -->
<!-- core.jinxxy_api: list_all_products(), get_product(id), get_me() -> {store_username, owner_name...}; raises JinxxyAPIError. -->
<!-- core.store_sync: map_product(detail, store_username, owner_name), reconcile_store(snapshots, live_by_key, current_by_key) -> {products, added, updated, removed, changed}, is_https_url. -->
<!-- core.github_publish: async sync_store(products) -> {committed, commit_sha, ...}; _fetch_store(repo,branch,path) (sync, call via asyncio.to_thread); GitHubPublishError. -->
<!-- core.db: init_store_state(), get_store_snapshot()->{checkout_url: Row}, upsert_store_snapshot(...), delete_store_snapshot(url). -->
<!-- Repo patterns: reminders.py loop lifecycle (__init__ .start / cog_unload .cancel / before_loop wait_until_ready / error restart) L326-334,L720-736; reminders _is_staff L189-197; reminders _deliver channel get_channel->fetch_channel fallback L656-662; brand red 0xC0192C. help.py auto-lists commands from bot.tree (NO edit needed). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JinxxyCog skeleton — loop lifecycle + _run_sync orchestration + bot.py wiring</name>
  <files>../nocturna-bot/cogs/jinxxy.py, ../nocturna-bot/bot.py</files>
  <read_first>
    - ../nocturna-bot/cogs/reminders.py (L313-334 GroupCog + __init__/.start + cog_unload/.cancel; L720-736 @tasks.loop/before_loop wait_until_ready/error restart — copy this lifecycle exactly, changing only cadence to hours=config.JINXXY_POLL_HOURS and the body)
    - ../nocturna-bot/cogs/reviews.py (L570-583 _fetch_entries via asyncio.to_thread(github_publish._fetch_json,...) — mirror with _fetch_store for the current store.json)
    - ../nocturna-bot/bot.py (L42-48 setup_hook load_extension list; L88-99 the gallery GITHUB_PAT/WEBSITE_REPO/GALLERY_STAFF_ROLE_IDS fail-fast block — add the JINXXY_API_KEY equivalent)
    - .planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-PATTERNS.md (cogs/jinxxy.py section lines 247-311)
  </read_first>
  <behavior>
    - Test: `_run_sync` on a live enumeration with one new product calls `github_publish.sync_store` once with the reconciled products and upserts the snapshot for that product.
    - Test: in that committed payload, the newly-added product carries `description == {"es": "", "en": ""}` and a string `id` (shape guaranteed by `store_sync`'s new-product branch — asserted here at the integration boundary so a synced product always passes StorePage.astro's structural filter and renders).
    - Test: `_run_sync` when `reconcile_store` reports `changed=False` calls `sync_store` ZERO times (no-op / silent).
    - Test (removal-safety): when `jinxxy_api.list_all_products` raises `JinxxyAPIError`, `_run_sync` performs NO removals and NO commit (aborts, logs) — the storefront is never mass-removed on an outage.
    - Test: `_is_staff(member)` returns True only for a member holding a `JINXXY_STAFF_ROLE_IDS` role.
  </behavior>
  <action>
    Create `cogs/jinxxy.py` with `import config`, `from core import db, jinxxy_api, store_sync, github_publish`, `log = logging.getLogger(__name__)`. Define `class JinxxyCog(commands.GroupCog, name="Jinxxy", group_name="tienda", group_description="Sincroniza la tienda con Jinxxy (staff)")` whose `__init__` calls `db.init_store_state()` then `self._poll.start()`, and `cog_unload` calls `self._poll.cancel()`. Add `@tasks.loop(hours=config.JINXXY_POLL_HOURS)` `_poll` delegating to `await self._run_sync()`, a `@_poll.before_loop` awaiting `self.bot.wait_until_ready()`, and a `@_poll.error` that does `log.exception(...)` (LOGS ONLY, D-05) then `self._poll.restart()`. Implement `async def _run_sync(self) -> dict`: (1) `me = await asyncio.to_thread(jinxxy_api.get_me)`; extract `store_username` + `owner_name`; (2) `products = await asyncio.to_thread(jinxxy_api.list_all_products)` then per product `detail = await asyncio.to_thread(jinxxy_api.get_product, p["id"])` → `map_product(detail, store_username, owner_name)`, guarding each `checkoutUrl` with `store_sync.is_https_url` (skip + log a non-https entry); build `live_by_key` keyed by checkoutUrl; (3) load `snapshots = db.get_store_snapshot()` and the current store.json via `current = await asyncio.to_thread(github_publish._fetch_store, config.WEBSITE_REPO, config.WEBSITE_BRANCH, config.WEBSITE_STORE_JSON)` → `current_by_key` keyed by checkoutUrl; (4) `result = store_sync.reconcile_store(snapshots, live_by_key, current_by_key)`; (5) if `result["changed"]`: `await github_publish.sync_store(result["products"])`, then upsert snapshots for every live product and `db.delete_store_snapshot` for each removed key; (6) return `result` for the announce step. Wrap the whole body so a `JinxxyAPIError` aborts BEFORE any removal/commit and only logs (never Discord, never a partial removal). Add `_is_staff(member)` copying reminders' shape but using `config.JINXXY_STAFF_ROLE_IDS`. In `bot.py` `setup_hook`, add `await self.load_extension("cogs.jinxxy")`; in `main()` add a fail-fast: if `not config.JINXXY_API_KEY` → `log.error(...)` + `sys.exit(1)` (mirrors the gallery PAT check). Do NOT edit `cogs/help.py` (it auto-lists `/tienda` from the tree). Write the `_run_sync`/`_is_staff` unit tests (SimpleNamespace fakes, monkeypatched cores, `asyncio.run`) alongside.
  </action>
  <verify>
    <automated>cd "../nocturna-bot" && python -m pytest tests/test_jinxxy_cog.py -q -k "run_sync or staff or noop or removal" && python -c "import ast; ast.parse(open('cogs/jinxxy.py').read()); ast.parse(open('bot.py').read()); print('parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `_run_sync` calls `sync_store` exactly once when there is a change and zero times when `changed=False`
    - On `JinxxyAPIError`, `_run_sync` calls neither `sync_store` nor `delete_store_snapshot` (removal-safety test passes)
    - Snapshots are upserted for live products and deleted for removed keys after a committed sync
    - `_is_staff` gates on `JINXXY_STAFF_ROLE_IDS`
    - `grep -c "cogs.jinxxy" ../nocturna-bot/bot.py` ≥ 1 and `grep -c "JINXXY_API_KEY" ../nocturna-bot/bot.py` ≥ 1
    - `grep -c "import discord\|cogs/help" ../nocturna-bot/cogs/help.py` shows help.py untouched (no diff)
  </acceptance_criteria>
  <done>The cog loads, the poll loop lifecycle is in place, and `_run_sync` orchestrates enumerate→map→merge→commit-on-change→snapshot-update with a hard removal-safety abort on API failure.</done>
</task>

<task type="auto">
  <name>Task 2: /tienda sync command + branded announce embed (D-05/D-06)</name>
  <files>../nocturna-bot/cogs/jinxxy.py</files>
  <read_first>
    - ../nocturna-bot/cogs/jinxxy.py (the JinxxyCog + _run_sync from Task 1)
    - ../nocturna-bot/cogs/reminders.py (crear L356-364 staff-gate-FIRST pattern; _deliver embed + channel get_channel->fetch_channel fallback L649-681)
    - ../nocturna-bot/cogs/reviews.py (panel_resenas L232-258 — the staff-gated app_command + ephemeral "Sin permisos." shape)
    - .planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-CONTEXT.md (D-05 errors-never-Discord, D-06 silent-on-no-change + announce channel + embed discretion)
  </read_first>
  <action>
    Add `@app_commands.command(name="sync", description="Fuerza una sincronización de la tienda con Jinxxy (staff)")` on the GroupCog (so it registers as `/tienda sync`). Staff gate FIRST: if `not _is_staff(interaction.user)` → ephemeral "Sin permisos." and return. Then `await interaction.response.defer(ephemeral=True)` (a full sync can exceed 3s), call `result = await self._run_sync()` inside a try/except: on `github_publish.GitHubPublishError` or `jinxxy_api.JinxxyAPIError` → `log.exception(...)` and an EPHEMERAL followup to the STAFF CALLER ONLY ("No pude sincronizar ahora; revisa los logs.") — this is a direct command reply to the invoker, NOT a public error post. USER-CONFIRMED 2026-07-10 during plan-phase: the user explicitly approved the ephemeral failure reply to the invoking staff member ("Sí, respuesta efímera"); D-05's "never announce errors" governs the public announce channel, which stays store-news-only. On success, send an ephemeral summary followup ("Sincronización lista: N nuevos, M actualizados, K quitados" or "Sin cambios."). Add `async def _announce(self, result)`: if `not result["changed"]` return (D-06 silent); resolve `config.JINXXY_ANNOUNCE_CHANNEL_ID` via `get_channel`→`fetch_channel` (log + return if unresolved — never raise); build a Spanish-first branded embed (color `0xC0192C`, footer "Nocturna · tienda") listing added/updated/removed product names, and `await channel.send(embed=...)`. Call `await self._announce(result)` from BOTH the `_poll` body and the `/tienda sync` success path (after the commit). Ensure the announce is only reached after a successful sync — an API/commit failure path logs only and never announces.
  </action>
  <verify>
    <automated>cd "../nocturna-bot" && python -m pytest tests/test_jinxxy_cog.py -q -k "announce or sync_command or gate" && python -c "import ast; ast.parse(open('cogs/jinxxy.py').read()); print('parse OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `_announce` returns without sending when `result["changed"]` is False (silent no-op test)
    - `_announce` builds an embed with color `0xC0192C` and sends to `JINXXY_ANNOUNCE_CHANNEL_ID` when there are changes
    - The `/tienda sync` callback returns "Sin permisos." (ephemeral) for a non-staff caller before any sync work
    - No failure path calls `channel.send` on the announce channel (grep/source assertion: `_announce` is only invoked after a successful `_run_sync`, and error branches only `log.exception`)
    - `grep -c "0xC0192C\|0xc0192c" ../nocturna-bot/cogs/jinxxy.py` ≥ 1
  </acceptance_criteria>
  <done>Staff can force a sync via `/tienda sync`; store changes are announced Spanish-first in the configured channel; no-change syncs are silent and errors never reach the announce channel.</done>
</task>

<task type="auto">
  <name>Task 3: Startup reconcile (on_ready, run-once) + full-suite green</name>
  <files>../nocturna-bot/cogs/jinxxy.py, ../nocturna-bot/tests/test_jinxxy_cog.py</files>
  <read_first>
    - ../nocturna-bot/cogs/jinxxy.py (JinxxyCog from Tasks 1-2)
    - ../nocturna-bot/cogs/reviews.py (on_ready run-once guard L480-492 with self._backfilled; the never-mass-remove philosophy in _reconcile_orphans L530-568)
    - .planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-CONTEXT.md (CONTEXT L108 startup reconvergence: on_ready reconciles snapshot vs live store.json vs Jinxxy so restarts converge)
  </read_first>
  <action>
    Add `@commands.Cog.listener() async def on_ready(self)` with a run-once guard (`self._synced_once`, set in `__init__` to False) — `on_ready` can re-fire on reconnects, so run the reconvergence exactly once. On first ready, `try: result = await self._run_sync(); await self._announce(result) except Exception: log.exception(...)` — a startup sync reconciles snapshot vs live store.json vs Jinxxy (adds missed products, propagates changes, removes delistings) using the SAME `_run_sync` path, so there is no separate reconcile code to drift. The removal-safety in `_run_sync` (API failure → abort, no removals) already protects a cold-start outage. Add the remaining unit tests: startup runs `_run_sync` once even across two `on_ready` fires; announce is called after a changed startup sync. Then run the FULL bot test suite to confirm no regression.
  </action>
  <verify>
    <automated>cd "../nocturna-bot" && python -m pytest tests/test_jinxxy_cog.py -q && python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - Two `on_ready` invocations trigger `_run_sync` exactly once (run-once guard test passes)
    - A startup sync that changes something calls `_announce`; an API failure at startup is caught and logged (no crash, no removal)
    - `cd ../nocturna-bot && python -m pytest tests/test_jinxxy_cog.py -q` all pass
    - `cd ../nocturna-bot && python -m pytest -q` reports the full suite green (≥ 234 prior tests + the new jinxxy tests, zero failures)
  </acceptance_criteria>
  <done>A bot restart converges store.json to the live Jinxxy storefront exactly once, safely, reusing `_run_sync`; the whole bot suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-staff → /tienda sync | a Discord command that triggers cross-repo commits; must be staff-only |
| Jinxxy API → removal logic | an API outage must never be read as "storefront empty" |
| failures → Discord | operational errors must stay in logs, never the announce channel (D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-14 | Elevation of Privilege | `/tienda sync` triggered by non-staff | mitigate | `_is_staff` gate FIRST on the command (ephemeral "Sin permisos."), before defer or any work — the Phase-8 CR-01 lesson |
| T-09-15 | Denial of Service (of content) | API failure mass-removes the storefront | mitigate | `list_all_products`/`get_me` raise `JinxxyAPIError` → `_run_sync` aborts before any `delete_store_snapshot`/`sync_store`; removals only from a successful full enumeration (branch-tested) |
| T-09-16 | Information Disclosure | error details leaked to a public channel | mitigate | D-05: every failure path is `log.exception` only; `_announce` is reached only after a successful sync; the only user-facing error is an EPHEMERAL reply to the staff invoker of `/tienda sync` |
| T-09-17 | Information Disclosure | secrets in logs | mitigate | `JINXXY_API_KEY`/`GITHUB_PAT` stay in their clients' headers (09-02/09-04); the cog logs only ids/labels/counts |
| T-09-SC | Tampering | pip installs | accept | No new packages (RESEARCH audit) |
</threat_model>

<verification>
- `cd ../nocturna-bot && python -m pytest -q` — full suite green including `tests/test_jinxxy_cog.py`.
- `python -c "import ast; ast.parse(open('cogs/jinxxy.py').read())"` in the bot repo parses.
- Manual (documented for the 09-06 deploy notes, NOT run here): with a live key + a real product, `/tienda sync` commits store.json and announces — deferred to the cinema-host deploy.
</verification>

<success_criteria>
- The cog loads via `bot.py`; `JINXXY_API_KEY` missing → fail-fast at startup.
- `/tienda sync` (staff-only) and the poll loop both run `_run_sync`; commit only on change; snapshots updated.
- Store changes are announced Spanish-first; no-change is silent; failures log-only and never mass-remove.
- Startup reconcile converges once; full bot suite green.
</success_criteria>

<output>
Create `.planning/phases/09-jinxxy-store-auto-sync-new-jinxxy-uploads-appear-in-the-asse/09-05-SUMMARY.md` when done.
</output>
