---
phase: 05-photo-publishing-bot-cog
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified: []
autonomous: false
requirements: [BOT-01, BOT-02, BOT-03, BOT-04, BOT-05, BOT-06]
user_setup:
  - service: github
    why: "Cross-repo commits from the bot to the website repo (BOT-04/BOT-05)"
    env_vars:
      - name: GITHUB_PAT
        source: "GitHub -> Settings -> Developer settings -> Fine-grained tokens -> only Shangrii/Nocturna-Avatars, Contents: Read and write"
    dashboard_config:
      - task: "Create a fine-grained PAT scoped to ONLY Shangrii/Nocturna-Avatars with Contents: Read and write; set an expiry + rotation reminder"
        location: "GitHub -> Settings -> Developer settings -> Personal access tokens -> Fine-grained tokens"
  - service: discord
    why: "The staff role gate that authorizes ✅/🌙 (D-01/D-08)"
    env_vars:
      - name: GALLERY_STAFF_ROLE_IDS
        source: "Discord (Developer Mode) -> right-click the staff role(s) -> Copy ID; comma-separate if more than one"
    dashboard_config:
      - task: "Confirm which role(s) gate gallery publishing (reuse ROLE_MODERATOR_ID 1418724526308593834 or a distinct list)"
        location: "Discord server roles"
must_haves:
  truths:
    - "The bot host runs the cog with all Phase-5 config present; startup does not sys.exit"
    - "A staff photo post gets ✅, a staff ✅ publishes it to the live gallery, the caption shows, and the Pages rebuild serves it"
    - "🌙 and message-deletion both remove published photos from the live site"
    - "A restart-then-scan backfills a pending post"
  artifacts: []
  key_links:
    - from: "bot .env (bot host)"
      to: "Shangrii/Nocturna-Avatars @ revamp"
      via: "GITHUB_PAT Contents:write -> Git Data API -> deploy.yml"
      pattern: "n/a (live-config verification)"
---

<objective>
Provision the live secrets and run the end-to-end acceptance of the whole phase: create the fine-grained PAT, confirm the staff role gate, deploy the cog to the running bot, and verify all six success criteria against live Discord + the live gallery.

Purpose: Every prior plan is unit-proven with mocked I/O; only a human with the GitHub/Discord dashboards and the self-hosted bot host can supply the PAT + role IDs, push the cog, and confirm that a real ✅ makes a real photo appear on `/galeria`. This plan is the phase's proof.

Output: A configured, running bot and a completed acceptance checklist. No repo code is authored here.

Note: The website repo GitHub slug was resolved from its git remote to `Shangrii/Nocturna-Avatars` (RESEARCH open-question A1 closed). The two remaining unknowns — the staff role ID(s) (A6) and the PAT itself — are captured in this plan's checkpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-photo-publishing-bot-cog/05-CONTEXT.md
@.planning/phases/05-photo-publishing-bot-cog/05-RESEARCH.md
@.planning/ROADMAP.md

<cross_repo_execution>
CRITICAL:
- The bot code lives in the `nocturna-bot` repo (checkout at `../nocturna-bot`); it runs on the user's self-hosted 24/7 bot host, which THIS session cannot reach.
- The write TARGET is `Shangrii/Nocturna-Avatars` @ `revamp`, paths `src/data/gallery.json` + `public/gallery/<file>`.
- Pushing the cog to `nocturna-bot` needs the user's git credentials (an auth gate, or the user pushes). Installing deps + running the bot happen on the bot host = human.
- SUMMARY is written to THIS website repo at `.planning/phases/05-photo-publishing-bot-cog/05-05-SUMMARY.md`.
</cross_repo_execution>

<reference_patterns>
Live config the bot host needs (from 05-01 config.py / 05-RESEARCH.md):
- `PHOTO_CHANNEL_ID` = `1416329356426481717` (default already correct)
- `GALLERY_STAFF_ROLE_IDS` = comma-separated staff role id(s) — MUST be provided (empty triggers the bot.py `sys.exit(1)` guard)
- `GITHUB_PAT` = fine-grained token, Shangrii/Nocturna-Avatars, Contents: Read and write
- `WEBSITE_REPO` = `Shangrii/Nocturna-Avatars` (default already correct)
- `WEBSITE_BRANCH` = `revamp` (default; flip to the production branch at cutover, D-15)
- `WEBSITE_GALLERY_JSON` = `src/data/gallery.json`, `WEBSITE_IMAGE_DIR` = `public/gallery` (defaults correct)

Deploy trigger (VERIFIED): a PAT push to `revamp` fires `.github/workflows/deploy.yml` (`on: push`), which rebuilds Pages via `withastro/action@v6` + `actions/deploy-pages@v5`; the `pages` concurrency group coalesces rapid builds. A commit made with the Actions `GITHUB_TOKEN` would NOT trigger it — the bot's own PAT is required.

Empty-state tolerance: the site renders a branded EmptyState when `gallery.json` is `[]`, so verification works whether or not the sample data has been reset.
</reference_patterns>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the PAT type and the staff role gate</name>
  <decision>Which PAT type authorizes cross-repo commits, and which Discord role(s) may publish?</decision>
  <context>
    The bot commits to Shangrii/Nocturna-Avatars via a token in its `.env`, and only members holding the configured role(s) may ✅/🌙. RESEARCH recommends a fine-grained PAT scoped to a single repo (least privilege) and a configurable role list. The bot already has `ROLE_MODERATOR_ID = 1418724526308593834`.
  </context>
  <options>
    <option id="fine-grained-single-repo">
      <name>Fine-grained PAT, only Shangrii/Nocturna-Avatars, Contents: Read and write</name>
      <pros>Least privilege; a leak exposes only this one repo's contents; supports expiry/rotation</pros>
      <cons>Requires the newer fine-grained token UI; must remember to renew at expiry</cons>
    </option>
    <option id="classic-pat">
      <name>Classic PAT with repo scope</name>
      <pros>Simplest to create</pros>
      <cons>Broad scope across all the user's repos — larger blast radius on leak (not recommended)</cons>
    </option>
  </options>
  <action>Pause for the developer to choose the PAT type (recommended: fine-grained-single-repo, Contents: Read and write) and to state the concrete GALLERY_STAFF_ROLE_IDS value(s) — reuse 1418724526308593834 or a distinct comma-separated list. Capture both for Task 2; do not proceed until provided.</action>
  <verify><human-check>Developer has stated the PAT type and the concrete GALLERY_STAFF_ROLE_IDS value(s).</human-check></verify>
  <done>PAT type and staff role id(s) are recorded and carried into Task 2.</done>
  <resume-signal>State: (a) PAT type chosen (recommended: fine-grained-single-repo), and (b) the GALLERY_STAFF_ROLE_IDS value(s) — reuse 1418724526308593834 or provide a distinct comma-separated list.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Provision secrets and deploy the cog to the bot host</name>
  <what-built>The cog (`cogs/gallery.py`), transport (`core/github_publish.py`), optimizer (`core/image_optimize.py`), config + loader are complete and unit-tested in the `nocturna-bot` checkout.</what-built>
  <action>Guide the developer through the human-only provisioning + deploy steps below and wait for confirmation. Claude may attempt the `nocturna-bot` push and surface an auth gate if it lacks credentials; the PAT creation, `.env` edit, `pip install`, and bot restart happen on the self-hosted bot host and cannot be done from this session.</action>
  <how-to-verify>
    Perform these human-only steps (no CLI substitute for the dashboard/host actions):
    1. Create the PAT chosen in Task 1: GitHub -> Settings -> Developer settings -> Fine-grained tokens -> repository access = ONLY `Shangrii/Nocturna-Avatars` -> Repository permissions -> Contents: Read and write. Copy the token (shown once).
    2. Push the Phase-5 cog code from the `nocturna-bot` checkout to the `nocturna-bot` remote (main) — or confirm it is already pushed. (Claude may attempt the push and surface an auth gate if it lacks credentials.)
    3. On the bot host: pull the latest `nocturna-bot`, run `pip install -r requirements.txt` (installs Pillow), and set the `.env` values from `<reference_patterns>` — GALLERY_STAFF_ROLE_IDS + GITHUB_PAT are the two you must fill; the rest keep their defaults. Ensure `.env` is git-ignored (it already is).
    4. Restart the bot. Confirm the log shows `cogs.gallery` loaded and NO `sys.exit` (missing config would abort startup).
  </how-to-verify>
  <verify><human-check>Bot host log shows `cogs.gallery` loaded and the process did not `sys.exit` on startup.</human-check></verify>
  <done>The bot runs with the gallery cog loaded and all Phase-5 config present.</done>
  <resume-signal>Reply "deployed" once the bot is running with the gallery cog loaded, or paste the startup error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end acceptance against live Discord + the live gallery</name>
  <what-built>A running bot that watches channel 1416329356426481717, publishes on staff ✅, and unpublishes on 🌙 / message deletion.</what-built>
  <action>Walk the developer through the acceptance checklist below against live Discord and the live site, then record pass/fail per ROADMAP success criterion. Do not mark the phase complete until all six criteria pass.</action>
  <how-to-verify>
    Run each check in the photos channel and on the live site (`/galeria` and the landing featured strip):
    1. BOT-01: As a staff member, post a message with one image. The bot adds a ✅ within a few seconds. Post a message with an image as a NON-staff member (or a text-only staff message) — the bot adds nothing.
    2. BOT-02/03/04/06: React ✅ on your staff image post. Confirm: a 🟢 marker appears + an auto-deleting reply ("Published…"); a single commit `gallery: publish 1 photos (discord msg <id>)` appears in Shangrii/Nocturna-Avatars @ revamp; the file under `public/gallery/` is a `.webp` named `{YYYYMMDD}-{msgid}-1.webp`; after the Pages build (~1-2 min) the photo shows on `/galeria`; if your message had text, it shows as the caption/alt.
    3. Multi-image + idempotency: post a staff message with 2-3 images, ✅ once -> all publish in ONE commit; ✅ again -> nothing happens (no duplicate).
    4. BOT-05 (🌙): React 🌙 on the published message -> a "Removed…" reply, the 🟢 comes off, and after the rebuild the photo(s) are gone from `/galeria`. React 🌙 on a pending (un-✅'d) staff post -> the ✅ prompt is cleared, nothing is committed.
    5. D-10 (delete): Publish a photo, then delete the Discord message -> after the rebuild the photo is gone from the site.
    6. D-20 (backfill): Stop the bot, post a staff image while it is down, start the bot -> it adds the ✅ prompt to the post it missed. (Optionally: leave a ✅ on a post while down -> on restart it publishes.)
    7. D-19 (failure surfacing, optional): temporarily set an invalid PAT, ✅ a post -> after retries a persistent (non-deleting) error reply + ⚠️ appears; restore the PAT and re-✅ to recover.
  </how-to-verify>
  <verify><human-check>All six ROADMAP success criteria (BOT-01..BOT-06, 🗑️ superseded by 🌙) observed passing on live Discord + the live gallery.</human-check></verify>
  <done>Every acceptance check passes; the phase goal is verified live.</done>
  <resume-signal>Reply "approved" if all six criteria pass, or list which checks failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot `.env` -> GitHub | The PAT grants write to the website repo |
| Discord role -> publish authority | Only configured role(s) may publish/unpublish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Information Disclosure / Elevation of Privilege | over-scoped or leaked PAT compromises repos | mitigate | fine-grained PAT scoped to ONLY Shangrii/Nocturna-Avatars, Contents: Read and write; `.env` git-ignored; set expiry + rotation; never paste the token in Discord/commits |
| T-05-14 | Elevation of Privilege | wrong/overbroad staff role gate lets non-staff publish | mitigate | confirm GALLERY_STAFF_ROLE_IDS with the user before enabling; verify a non-staff post gets no ✅ in Task 3 check 1 |
| T-05-18 | Tampering | commits pushed to the wrong branch/repo (D-15 misconfig) | mitigate | WEBSITE_REPO/WEBSITE_BRANCH defaults verified (Shangrii/Nocturna-Avatars @ revamp); Task 3 check 2 confirms the commit lands and Pages rebuilds |
</threat_model>

<verification>
- Bot host log shows `cogs.gallery` loaded with no `sys.exit` (Task 2)
- All six ROADMAP success criteria pass in Task 3 (BOT-01..BOT-06, with 🗑️ superseded by 🌙 per D-06)
- A publish produces exactly one commit + one Pages build; the photo appears on `/galeria` with its caption
- 🌙 and message deletion both remove the photo; restart backfills a missed post
</verification>

<success_criteria>
- The cog runs live with the PAT + staff role gate configured
- A staff ✅ publishes optimized WebP + a gallery.json entry cross-repo in one commit, and the photo goes live with its caption (BOT-01..BOT-04, BOT-06)
- 🌙 / message deletion unpublish; startup backfill recovers missed work (BOT-05, D-10, D-20)
- PAT is fine-grained/least-privilege and the staff role gate is confirmed
</success_criteria>

<output>
Create `.planning/phases/05-photo-publishing-bot-cog/05-05-SUMMARY.md` when done (in THIS website repo). Record the confirmed GALLERY_STAFF_ROLE_IDS, the PAT type chosen, the first live commit SHA, and the acceptance-checklist result per criterion.
</output>
