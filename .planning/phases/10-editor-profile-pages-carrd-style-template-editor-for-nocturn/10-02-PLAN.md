---
phase: 10-editor-profile-pages-carrd-style-template-editor-for-nocturn
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ../nocturna-bot/requirements.txt
  - ../nocturna-bot/config.py
  - ../nocturna-bot/.env.example
  - ../nocturna-bot/core/editors_model.py
  - ../nocturna-bot/tests/test_editors_model.py
autonomous: false
requirements: [EDIT-04, EDIT-06]

must_haves:
  truths:
    - "The admin-app Python dependencies are pinned in requirements.txt at the researched versions"
    - "config.py exposes the new editors/OAuth/session settings from .env"
    - "editors_model.py validates a closed block/link union and rejects raw-HTML and non-https URLs"
    - "A slug normalizer produces [a-z0-9-] and rejects traversal characters"
  artifacts:
    - path: "../nocturna-bot/core/editors_model.py"
      provides: "pydantic EditorPage + closed block union + link/url validation + slug normalization"
      exports: ["EditorPage", "normalize_slug"]
      min_lines: 60
    - path: "../nocturna-bot/config.py"
      provides: "WEBSITE_EDITORS_JSON, WEBSITE_EDITORS_IMAGE_DIR, DISCORD_OAUTH_*, SESSION_SECRET, EDITOR_APP_BASE_URL"
      contains: "WEBSITE_EDITORS_JSON"
    - path: "../nocturna-bot/requirements.txt"
      provides: "pinned fastapi/uvicorn/authlib/python-multipart/jinja2/httpx"
      contains: "fastapi=="
  key_links:
    - from: "../nocturna-bot/core/editors_model.py"
      to: "closed block union"
      via: "pydantic discriminated union on `type`"
      pattern: "type"
---

<objective>
Stand up the nocturna-bot backend foundation for the admin app: pin the researched dependencies (behind a package-legitimacy checkpoint), extend `config.py`/`.env.example` with the editors + OAuth + session settings, and build `core/editors_model.py` — the pydantic schema mirroring the 10-01 `editors.json` contract with a CLOSED block union, `https://`-only URL validation (D-16), and slug normalization (Pitfall 5). This model is the server-side validation gate every save in 10-09 passes through before any commit.

Purpose: Contracts + validation first, so the FastAPI app (10-08) and transport (10-05) build against a typed, tested schema.
Output: Pinned deps, config surface, and a tested pydantic model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-CONTEXT.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-PATTERNS.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-01-PLAN.md

<interfaces>
Mirror the 10-01 canonical `editors.json` entry schema exactly. Existing bot config constants to reuse (config.py): `BOT_TOKEN` (L8), `GUILD_ID` (L12, default 1411899319468167190), `ROLE_MODERATOR_ID` (L15, default 1418724526308593834 — the editor role per D-15), `GITHUB_PAT` (L63), `WEBSITE_REPO` (L65 Shangrii/Nocturna-Avatars), `WEBSITE_BRANCH` (L67 revamp), `WEBSITE_BASE_URL` (L122). New keys mirror the naming of `WEBSITE_STORE_JSON` (L111) / `WEBSITE_STORE_IMAGE_DIR` (L114).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate (blocking) before first install</name>
  <what-built>The RESEARCH.md Package Legitimacy Audit (2026-07-14, slopcheck 0.6.1) scanned all new deps [OK]: fastapi 0.139.0, uvicorn 0.51.0, authlib 1.7.2, python-multipart 0.0.32, jinja2 3.1.6, httpx 0.28.1 (PyPI); alpinejs 3.15.12, sortablejs 1.15.7 (npm, admin frontend only). starlette + itsdangerous arrive transitively — do NOT pin starlette independently.</what-built>
  <how-to-verify>
    1. Confirm each Python package on PyPI: https://pypi.org/project/fastapi/ (repeat for uvicorn, authlib, python-multipart, jinja2, httpx) — maintainer + release date + downloads look legitimate.
    2. Confirm the two npm libs: https://www.npmjs.com/package/alpinejs and https://www.npmjs.com/package/sortablejs.
    3. Confirm you are OK installing into the bot's existing venv on the dev box (and later cinema).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with pinning + install, or name any package to drop.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Pin dependencies + extend config + .env.example</name>
  <files>../nocturna-bot/requirements.txt, ../nocturna-bot/config.py, ../nocturna-bot/.env.example</files>
  <read_first>
    - ../nocturna-bot/requirements.txt (existing exact-pin convention — match it)
    - ../nocturna-bot/config.py (the WEBSITE_STORE_JSON L111 / WEBSITE_STORE_IMAGE_DIR L114 block and the BOT_TOKEN/GUILD_ID/ROLE_MODERATOR_ID header — add the new block alongside)
    - ../nocturna-bot/.env.example (mirror the documented-key style)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md (Standard Stack table — exact versions; "let FastAPI resolve Starlette")
  </read_first>
  <action>Add exact pins to `requirements.txt`: `fastapi==0.139.0`, `uvicorn[standard]==0.51.0`, `authlib==1.7.2`, `python-multipart==0.0.32`, `jinja2==3.1.6`, `httpx==0.28.1`. Do NOT pin `starlette` or `itsdangerous` (FastAPI resolves them). In `config.py` add: `WEBSITE_EDITORS_JSON` (default `src/data/editors.json`), `WEBSITE_EDITORS_IMAGE_DIR` (default `public/editors`), `DISCORD_OAUTH_CLIENT_ID`, `DISCORD_OAUTH_CLIENT_SECRET`, `DISCORD_OAUTH_REDIRECT_URI`, `SESSION_SECRET` (read from env, no default — fail-fast if empty when the app runs), and `EDITOR_APP_BASE_URL` (the admin subdomain origin, e.g. `https://editors.nocturna-avatars.site`). Reuse `ROLE_MODERATOR_ID` as the editor role (D-15) — do NOT create a new role constant. Document every new key in `.env.example` with a one-line comment (mark `DISCORD_OAUTH_CLIENT_SECRET` and `SESSION_SECRET` as secrets — never commit real values). Run `pip install -r requirements.txt` in the venv to confirm the resolver succeeds.</action>
  <verify>
    <automated>cd ../nocturna-bot && python -c "import fastapi,uvicorn,authlib,jinja2,httpx,multipart; import config; assert config.WEBSITE_EDITORS_JSON and config.WEBSITE_EDITORS_IMAGE_DIR" && grep -v '^#' requirements.txt | grep -c 'fastapi==0.139.0'</automated>
  </verify>
  <acceptance_criteria>
    - `pip install -r requirements.txt` resolves with no conflict; `import fastapi, uvicorn, authlib, jinja2, httpx, multipart` all succeed
    - `starlette` is NOT independently pinned in requirements.txt
    - `config.WEBSITE_EDITORS_JSON == 'src/data/editors.json'` and `config.WEBSITE_EDITORS_IMAGE_DIR == 'public/editors'`
    - `config` exposes DISCORD_OAUTH_CLIENT_ID/SECRET/REDIRECT_URI, SESSION_SECRET, EDITOR_APP_BASE_URL
    - `.env.example` documents all new keys; no real secret values committed
  </acceptance_criteria>
  <done>Deps pinned + installed; config + .env.example carry the new editors/OAuth/session settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: editors_model.py — closed block union + validation (TDD)</name>
  <files>../nocturna-bot/core/editors_model.py, ../nocturna-bot/tests/test_editors_model.py</files>
  <read_first>
    - ../nocturna-bot/core/store_sync.py (STAFF_OWNED tuple + is_https_url guard — the closest validation precedent; reuse the https check idiom)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md (Pattern 3 closed-union block shape; Pitfall 5 slug normalization; V5 input validation)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md (Block Inventory — the 8 block types + fields)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-01-PLAN.md (the locked entry schema in its <interfaces> block)
  </read_first>
  <behavior>
    - Test: a valid EditorPage with all 8 block types parses.
    - Test: an unknown block `type` is rejected (closed union).
    - Test: a link/portfolio-extra/image URL that is not `https://` (e.g. `javascript:`, `http://`, `data:`) is rejected (D-16 https-only, reject dangerous schemes).
    - Test: `normalize_slug("Aria X!")` → `aria-x`; `normalize_slug("../etc")` rejects/strips traversal to `[a-z0-9-]` only; empty result raises.
    - Test: a block `text` field is a `{es,en}` dict; a missing locale defaults to empty string (not None), matches render-as-written convention.
    - Test: string length caps enforced (tagline/name/label/text) so a giant payload is rejected before commit.
  </behavior>
  <action>Write `core/editors_model.py` using pydantic v2: a discriminated union of block models keyed on `type` (`bio`/`heading`/`text` with `text:{es,en}`; `links` with `items:[{label,url}]`; `portfolio` with `auto:bool` + `extra:[{image,caption:{es,en}}]`; `quote` with `text:{es,en}` + optional `attribution`; `image` with `src`+`alt`; `divider` with no fields), an `EditorPage` model (`slug`, `discordId`, `published`, `name`, `avatar`, `tagline:{es,en}`, `links:[{label,url}]`, `blocks:[BlockUnion]`), a reusable URL validator that accepts only `https://` (reject `http`, `javascript:`, `data:`, `vbscript:` — D-16 says any https URL, no domain allowlist), and `normalize_slug(raw) -> str` that lowercases, replaces runs of non-`[a-z0-9]` with `-`, strips leading/trailing `-`, and raises `ValueError` on empty (Pitfall 5 path-traversal guard). Enforce max-length caps on all string fields (V5). No raw-HTML field exists anywhere — text is plain strings only (D-02 safety). Write the tests first (RED), then implement (GREEN).</action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_editors_model.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_editors_model.py` passes with the behaviors above covered
    - Parsing a payload with an unknown block `type` raises a validation error
    - A non-`https://` URL in any link/image field raises a validation error
    - `normalize_slug` output matches `^[a-z0-9-]+$` and rejects empty/traversal input
    - No field in any model accepts raw HTML (all text fields are plain `str`)
  </acceptance_criteria>
  <done>editors_model.py validates the closed schema + slug + https-only URLs; full unit suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → bot venv | Third-party supply chain (pip install) |
| editor browser payload → editors_model | Untrusted block/link JSON validated server-side before any commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-SC | Tampering | pip installs (fastapi/uvicorn/authlib/…) | mitigate | RESEARCH Package Legitimacy Audit [OK] + blocking human checkpoint (Task 1) before first install; exact pins only |
| T-10-02-01 | Tampering (XSS) | block text fields | mitigate | Closed pydantic union; all text is plain `str` (no HTML), rendered auto-escaped downstream (10-04) |
| T-10-02-02 | Tampering (injection) | link/image URLs | mitigate | https-only URL validator rejects `javascript:`/`data:`/`http:` (D-16, V5) |
| T-10-02-03 | Tampering (path traversal) | slug | mitigate | `normalize_slug` restricts to `[a-z0-9-]`, rejects `../` before it can enter any commit/file path (Pitfall 5) |
| T-10-02-04 | Info Disclosure | SESSION_SECRET / OAuth secret | mitigate | Read from `.env` only, no defaults, marked secret in `.env.example`, never logged/committed |
</threat_model>

<verification>
- `pip install -r requirements.txt` resolves; imports succeed.
- `pytest tests/test_editors_model.py` green.
- config + .env.example carry the new keys with secrets undocumented-as-values.
</verification>

<success_criteria>
- Pinned deps behind a legitimacy gate; config/.env surface complete; a tested closed-union pydantic model with https-only + slug validation ready for the transport and app.
</success_criteria>

<output>
Create `.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-02-SUMMARY.md` when done.
</output>
