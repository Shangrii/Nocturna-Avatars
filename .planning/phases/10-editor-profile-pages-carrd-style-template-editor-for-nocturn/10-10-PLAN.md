---
phase: 10-editor-profile-pages-carrd-style-template-editor-for-nocturn
plan: 10
type: execute
wave: 4
depends_on: [10-08]
files_modified:
  - ../nocturna-bot/app/main.py
  - ../nocturna-bot/app/templates/editor.html
  - ../nocturna-bot/app/templates/login.html
  - ../nocturna-bot/app/static/editor.css
  - ../nocturna-bot/app/static/alpine.min.js
  - ../nocturna-bot/app/static/Sortable.min.js
  - ../nocturna-bot/tests/test_app_editor.py
autonomous: true
requirements: [EDIT-06, EDIT-07]

must_haves:
  truths:
    - "An editor can add, remove, reorder (drag + keyboard), and edit blocks with a live preview"
    - "An editor uploads an avatar/block image; the server rejects SVG + oversized, re-encodes to WebP, and commits it"
    - "Save publishes immediately (D-13): the page validates and commits editors.json cross-repo"
    - "An editor can self-unpublish their own page (D-16)"
    - "All identity comes from the session; the body never carries slug/discordId"
  artifacts:
    - path: "../nocturna-bot/app/templates/editor.html"
      provides: "two-pane block editor: list/edit + Alpine live preview + SortableJS reorder"
      min_lines: 60
    - path: "../nocturna-bot/app/main.py"
      provides: "GET /editor, POST /editor/image, POST /editor/save, POST /editor/unpublish"
      contains: "require_editor"
  key_links:
    - from: "POST /editor/save"
      to: "editors_model + sync_editors"
      via: "validate then commit for the SESSION's editor"
      pattern: "require_editor"
    - from: "POST /editor/image"
      to: "optimize_to_webp"
      via: "reject-SVG + size-cap + Pillow re-encode"
      pattern: "optimize_to_webp"
---

<objective>
Build the editor authoring surface (Surface B): the two-pane block editor (add/remove/reorder + live preview, D-14) with vendored Alpine.js + SortableJS, the image-upload endpoint (SVG-reject + size-cap + Pillow re-encode, D-17/Pitfall 3), the save/publish endpoint (validate via editors_model → `sync_editors`, publishes immediately per D-13), and self-unpublish (D-16). Every write is scoped to the session's editor via `require_editor` — the body never carries identity (D-08).

Purpose: The actual carrd-style editing experience — the phase's core capability.
Output: `app/templates/*`, `app/static/*`, and the editor endpoints in `app/main.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-08-PLAN.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-02-PLAN.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-05-PLAN.md

<interfaces>
Depends on: app/deps.require_editor (session→{discord_id,slug}, re-checks role); core/editors_model.EditorPage (validation) + normalize_slug; core/github_publish.sync_editors (publish, image blobs in one commit) + unpublish_editor; core/image_optimize.optimize_to_webp(raw)->(webp_bytes,w,h). Front-end libs pinned (10-02 audit): alpinejs 3.15.12, sortablejs 1.15.7 — vendor into app/static (or CDN with SRI). Copy strings from UI-SPEC Surface B (Publicar/Publish, toasts, errors, destructive confirms).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-pane block editor template + live preview + reorder</name>
  <files>../nocturna-bot/app/templates/editor.html, ../nocturna-bot/app/templates/login.html, ../nocturna-bot/app/static/editor.css, ../nocturna-bot/app/static/alpine.min.js, ../nocturna-bot/app/static/Sortable.min.js</files>
  <read_first>
    - src/styles/theme.css (brand palette + fonts to echo — Surface B vendors the SAME hex values, do not fork)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md (Surface B layout: two-pane ≥1024px, single-pane stacked mobile, persistent top bar w/ Publish+Unpublish+sign-out; Block Inventory; Interaction Contracts; 44px targets; a11y/reduced-motion; copy)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md (Alpine live-preview + SortableJS reorder; "no SPA build step"; SRI note)
  </read_first>
  <action>Vendor the pinned `alpine.min.js` (3.15.12) + `Sortable.min.js` (1.15.7) into `app/static/` (or load from a CDN WITH SRI hashes — RESEARCH). Build `app/templates/editor.html`: a two-pane layout (left = block list editor with an "Agregar bloque"/"Add block" typed picker over the CLOSED block set, per-block fields incl. per-locale `{es,en}` inputs, remove-with-confirm, SortableJS drag handles that are ALSO keyboard-operable via move up/down buttons — Interaction Contract; right = an Alpine `x-data`/`x-for` LIVE PREVIEW rendered in the profile look, updating on every field change via `x-model`). Persistent top bar: page title + **Publicar/Publish** (accent), **Despublicar/Unpublish** (red destructive w/ confirm copy), sign-out. Image-upload widget with an "optimizing…" state. Build `login.html` with the "Sign in with Discord"/"Entrar con Discord" CTA + the 403 not-an-editor copy. Surface B uses the SAME brand palette/fonts (chrome-light builder tool). All bilingual strings from the UI-SPEC. 44px min touch targets; AA contrast; `prefers-reduced-motion` respected. Wire the templates via Jinja2 in `app/main.py` (GET `/editor` behind `require_editor`, loads the session editor's current entry).</action>
  <verify>
    <automated>cd ../nocturna-bot && test -f app/static/alpine.min.js && test -f app/static/Sortable.min.js && python -c "from jinja2 import Environment,FileSystemLoader as F; Environment(loader=F('app/templates')).get_template('editor.html')"</automated>
  </verify>
  <acceptance_criteria>
    - alpine.min.js (3.15.12) + Sortable.min.js (1.15.7) vendored (or CDN+SRI documented)
    - `editor.html` renders a two-pane editor with an add-block picker limited to the 8 closed block types, remove-confirm, drag + keyboard reorder, and an Alpine live preview
    - Top bar has Publish (accent) + Unpublish (red, confirm) + sign-out; all strings bilingual per UI-SPEC
    - `GET /editor` is behind `require_editor` and loads the session editor's entry
    - Templates parse under Jinja2
  </acceptance_criteria>
  <done>The block editor renders with live preview + accessible reorder; login/403 template ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Image upload endpoint — SVG-reject + size-cap + Pillow re-encode</name>
  <files>../nocturna-bot/app/main.py, ../nocturna-bot/tests/test_app_editor.py</files>
  <read_first>
    - ../nocturna-bot/core/image_optimize.py (optimize_to_webp L30-57 — reused verbatim; already strips EXIF + has MAX_IMAGE_PIXELS bomb guard)
    - ../nocturna-bot/app/deps.py (require_editor — the upload is scoped to the session editor)
    - ../nocturna-bot/core/github_publish.py (sync_editors image-blob path — the upload commits under public/editors/<slug>/)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md (Pitfall 3 malicious upload; V12 files & resources)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md (image-upload error copy + size note)
  </read_first>
  <behavior>
    - Test: a valid PNG/JPG/WebP upload returns a committed path under public/editors/<session-slug>/ (bytes are the RE-ENCODED WebP, not the original).
    - Test: an SVG (image/svg+xml or .svg) is rejected outright (no Pillow decode attempt trusted from content-type).
    - Test: an upload over the byte-size cap is rejected BEFORE full read.
    - Test: a decompression-bomb / non-image is rejected (Pillow decode fails → error, nothing committed).
    - Test: the target path uses the SESSION slug (normalize_slug), never a body-supplied slug (no path traversal / IDOR).
  </behavior>
  <action>Add `POST /editor/image` to `app/main.py` behind `require_editor`. Enforce a byte-size cap and reject before reading fully; REJECT SVG entirely (raster only); decode + re-encode via `core.image_optimize.optimize_to_webp` (which drops metadata + keeps the bomb guard); commit ONLY the re-encoded WebP bytes under `public/editors/<session_slug>/<generated-name>.webp` via the `sync_editors` image path (or a dedicated helper) — the path is built from the SESSION slug, never client input. Return the committed public path for the live preview. On any failure return the UI-SPEC image-upload error copy; never expose PAT/commit internals. Run the blocking transport off the event loop (`run_in_threadpool`). Tests first (RED) then implement (GREEN).</action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_app_editor.py -x -q -k image</automated>
  </verify>
  <acceptance_criteria>
    - Valid images commit the RE-ENCODED WebP under public/editors/<session-slug>/
    - SVG and over-cap uploads are rejected; nothing committed
    - Non-image / bomb inputs are rejected via Pillow decode failure
    - The commit path uses the session slug only (grep: no body-slug in the path)
    - Errors return the UI-SPEC copy; no PAT/internal leak
  </acceptance_criteria>
  <done>Uploads are validated, re-encoded, and committed safely under the session editor's dir.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Save/publish + self-unpublish endpoints (D-13/D-16)</name>
  <files>../nocturna-bot/app/main.py, ../nocturna-bot/tests/test_app_editor.py</files>
  <read_first>
    - ../nocturna-bot/core/editors_model.py (EditorPage validation — the save gate)
    - ../nocturna-bot/core/github_publish.py (sync_editors publish + unpublish_editor)
    - ../nocturna-bot/app/deps.py (require_editor)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md (Publish success toast; save-failed copy; unpublish confirm copy)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md (Pitfall 1 IDOR; V13 CSRF; Don't-Hand-Roll rate limiting)
  </read_first>
  <behavior>
    - Test: POST /editor/save with a valid body validates via EditorPage, forces discordId+slug from the SESSION (ignoring any body identity), sets published=true, and commits via sync_editors (D-13 immediate publish).
    - Test: POST /editor/save with an invalid block (unknown type / non-https url / oversize) returns a 4xx validation error and does NOT commit.
    - Test: a body that tries to set a different discordId/slug is ignored — the session identity wins (IDOR guard).
    - Test: POST /editor/unpublish sets the session editor's page published=false via unpublish_editor (D-16 self-unpublish).
    - Test: state-changing POSTs require a valid session (SameSite + require_editor); a transient commit failure returns the save-failed copy without leaking internals.
  </behavior>
  <action>Add `POST /editor/save` and `POST /editor/unpublish` to `app/main.py`, both behind `require_editor`. `save`: parse the submitted blocks/links/fields into `EditorPage` (server-side validation — reject before commit), OVERRIDE `discordId`+`slug` from the session (never trust the body — Pitfall 1/D-08), set `published=true`, and commit via `sync_editors` (D-13 publishes immediately). Return the success toast copy + the "couple of minutes" note. `unpublish`: call `unpublish_editor(session discord_id)` (D-16). Add rate limiting on `/login`+callback+state-changing POSTs (slowapi or proxy-level — RESEARCH Don't-Hand-Roll) and confirm SameSite=Lax covers CSRF for same-site form posts. Transient commit failures return the UI-SPEC save-failed copy; never surface PAT/commit internals. Tests first (RED) then implement (GREEN).</action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_app_editor.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Valid save validates via EditorPage, forces session identity, publishes immediately (sync_editors called with published=true)
    - Invalid blocks/URLs return 4xx and do NOT commit
    - Body-supplied discordId/slug is ignored; session identity is authoritative
    - `/editor/unpublish` flips the session editor to published=false
    - Rate limiting present on auth + state-changing POSTs; failure copy leaks no internals
  </acceptance_criteria>
  <done>Save publishes immediately with server-side validation + session-scoped identity; self-unpublish works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor browser → save/upload endpoints | Untrusted block JSON + binary uploads cross into a cross-repo commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-10-01 | Tampering/DoS | image upload (bomb/polyglot/SVG) | mitigate | Size cap before read, reject SVG, Pillow decode + re-encode to WebP (metadata strip + bomb guard), commit re-encoded bytes only (Pitfall 3/V12) |
| T-10-10-02 | Elevation/Tampering | IDOR via body identity on save/upload | mitigate | discordId+slug forced from session; body identity ignored; paths use session slug (D-08/Pitfall 1) |
| T-10-10-03 | Tampering (stored XSS) | block/link save payload | mitigate | EditorPage pydantic validation (closed union, https-only URLs) rejects before commit; render is auto-escaped (10-04) |
| T-10-10-04 | Tampering (CSRF) | state-changing POSTs | mitigate | SameSite=Lax session cookie + require_editor; rate limiting on auth + POSTs (V13) |
| T-10-10-05 | Info Disclosure | PAT/commit internals in errors | mitigate | Transient failures return generic UI-SPEC copy; internals never surfaced |
| T-10-10-06 | Tampering (path traversal) | upload target path | mitigate | Path built from normalized session slug only, under public/editors/<slug>/ (Pitfall 5) |
</threat_model>

<verification>
- `pytest tests/test_app_editor.py` green (upload safety, save validation, IDOR, unpublish, rate limit).
- Templates parse; vendored Alpine+Sortable present.
</verification>

<success_criteria>
- EDIT-06 met + EDIT-07 self-unpublish half: a full block editor with live preview, safe image upload, immediate publish-on-save, and self-unpublish — all session-scoped and validated.
</success_criteria>

<output>
Create `.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-10-SUMMARY.md` when done.
</output>
