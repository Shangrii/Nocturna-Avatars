---
phase: 10-editor-profile-pages-carrd-style-template-editor-for-nocturn
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ../nocturna-bot/deploy/EDITOR_DEPLOY.md
autonomous: false
requirements: [EDIT-04]
user_setup:
  - service: discord-oauth2
    why: "Login for the editor admin app (D-07)"
    env_vars:
      - name: DISCORD_OAUTH_CLIENT_ID
        source: "Discord Developer Portal → your bot application → OAuth2 → Client ID"
      - name: DISCORD_OAUTH_CLIENT_SECRET
        source: "Discord Developer Portal → OAuth2 → Client Secret (reset if needed)"
    dashboard_config:
      - task: "Add redirect URI editors.nocturna-avatars.site/auth/callback"
        location: "Discord Developer Portal → OAuth2 → Redirects"
  - service: dns-and-reverse-proxy
    why: "TLS-terminated public login surface on cinema (Pitfall 8)"
    dashboard_config:
      - task: "Create DNS A/AAAA record for the editor subdomain → cinema host IP"
        location: "Domain registrar / DNS provider for nocturna-avatars.site"

must_haves:
  truths:
    - "A Discord OAuth2 application exists with client id/secret and the exact redirect URI registered"
    - "A DNS subdomain resolves to the cinema host and a reverse proxy with automatic HTTPS is confirmed available"
    - "It is known whether the bot's members privileged gateway intent is enabled (drives D-10 mechanism)"
    - "EDITOR_DEPLOY.md records the confirmed infra facts for the deploy plan (10-11)"
  artifacts:
    - path: "../nocturna-bot/deploy/EDITOR_DEPLOY.md"
      provides: "Infra prerequisites + confirmed values (subdomain, proxy, OAuth redirect URI, intent status) + key-rotation reminder"
      contains: "redirect URI"
  key_links:
    - from: "Discord OAuth2 app redirect URI"
      to: "EDITOR_APP_BASE_URL + /auth/callback"
      via: "exact-match registration"
      pattern: "callback"
---

<objective>
Resolve the two hard, human-only infrastructure prerequisites RESEARCH flags as blocking (A3): (1) a Discord OAuth2 application with credentials + a registered redirect URI, and (2) a DNS subdomain + reverse-proxy/TLS on the cinema host. Also confirm the `members` gateway intent status (A5) so 10-10 picks the right role-loss mechanism. These run in parallel with all Wave-1 code work so nothing waits on them at the end. The confirmed facts land in `EDITOR_DEPLOY.md`, the source of truth for the deploy plan (10-11).

Purpose: De-risk the phase's only non-automatable dependencies early.
Output: A deploy-notes doc capturing confirmed infra values + a key-rotation reminder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold EDITOR_DEPLOY.md with the infra checklist</name>
  <files>../nocturna-bot/deploy/EDITOR_DEPLOY.md</files>
  <read_first>
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md (Environment Availability table + Pitfall 8 + Assumptions A3/A5; the JINXXY_DEPLOY.md precedent from 09-06 is the format model)
    - ../nocturna-bot/config.py (the new DISCORD_OAUTH_* / SESSION_SECRET / EDITOR_APP_BASE_URL keys from 10-02 that must be filled on cinema)
  </read_first>
  <action>Create `../nocturna-bot/deploy/EDITOR_DEPLOY.md` modeled on the existing JINXXY_DEPLOY.md format. Sections: (1) Discord OAuth2 app — steps to create/reuse the app, obtain client id/secret, and register the EXACT redirect URI `${EDITOR_APP_BASE_URL}/auth/callback` (fixed path, no arbitrary post-login redirect — Pitfall 4); (2) DNS subdomain (proposed `editors.nocturna-avatars.site`) → cinema host; (3) reverse proxy with automatic HTTPS (Caddy preferred, nginx+certbot acceptable) fronting uvicorn bound to `127.0.0.1` (Pitfall 8); (4) `.env` keys to fill on cinema (DISCORD_OAUTH_CLIENT_ID/SECRET/REDIRECT_URI, SESSION_SECRET = 32+ random bytes, EDITOR_APP_BASE_URL); (5) `members` gateway intent status field (Yes/No) driving D-10; (6) a prominent KEY ROTATION reminder that any secret pasted during planning must be rotated after ship (mirrors the 09 Jinxxy key note). Leave the confirmed-values fields as TODO placeholders for Tasks 2-4 to fill.</action>
  <verify>
    <automated>test -f ../nocturna-bot/deploy/EDITOR_DEPLOY.md && grep -qi 'redirect URI' ../nocturna-bot/deploy/EDITOR_DEPLOY.md && grep -qi 'rotation' ../nocturna-bot/deploy/EDITOR_DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - `EDITOR_DEPLOY.md` exists under `../nocturna-bot/deploy/` with all six sections
    - The redirect-URI section names the exact fixed callback path
    - A key-rotation reminder is present
  </acceptance_criteria>
  <done>Deploy-notes scaffold created with placeholders for the human-confirmed values.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create/register the Discord OAuth2 application</name>
  <what-built>The admin app needs a Discord OAuth2 client (D-07). This is a Developer-Portal-only action — no CLI/API for redirect-URI registration.</what-built>
  <how-to-verify>
    1. In the Discord Developer Portal, open your existing bot application (reuses the same app/guild — D-15).
    2. OAuth2 → copy the Client ID; reveal/reset the Client Secret.
    3. OAuth2 → Redirects → add exactly `https://editors.nocturna-avatars.site/auth/callback` (or your chosen subdomain + `/auth/callback`).
    4. Paste the Client ID + Secret + final redirect URI into the reply (secrets go into cinema `.env`, NOT the repo).
  </how-to-verify>
  <resume-signal>Reply with the Client ID and the exact redirect URI you registered (keep the secret out of the repo; confirm it is stored for cinema `.env`).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm DNS subdomain + reverse proxy/TLS on cinema + members intent</name>
  <what-built>The public login surface requires a resolvable subdomain, a TLS-terminating reverse proxy, and (for real-time D-10) the members gateway intent. These depend on host/DNS access only the user has.</what-built>
  <how-to-verify>
    1. Confirm you control DNS for nocturna-avatars.site and can add an A/AAAA record for the editor subdomain pointing at cinema.
    2. Confirm cinema runs (or can run) Caddy or nginx for automatic HTTPS in front of a `127.0.0.1` uvicorn.
    3. Confirm whether the bot's `members` privileged gateway intent is enabled (Developer Portal → Bot → Privileged Gateway Intents). If NOT enabled, 10-10 uses a polling sweep as the primary D-10 mechanism.
    4. Report: subdomain chosen, proxy in use, and intent Yes/No.
  </how-to-verify>
  <resume-signal>Reply with: subdomain, proxy (Caddy/nginx/none-yet), and members-intent Yes/No.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record confirmed infra values into EDITOR_DEPLOY.md</name>
  <files>../nocturna-bot/deploy/EDITOR_DEPLOY.md</files>
  <read_first>
    - ../nocturna-bot/deploy/EDITOR_DEPLOY.md (the scaffold from Task 1 with TODO placeholders)
  </read_first>
  <action>Fill the TODO placeholders in `EDITOR_DEPLOY.md` with the values confirmed in Tasks 2-3: the registered redirect URI, the chosen subdomain, the reverse proxy in use, and the `members` intent status (Yes/No) with a one-line note on the resulting D-10 mechanism (real-time `on_member_update` if Yes, periodic polling sweep if No). Do NOT write the client secret or session secret into the file — reference them as cinema `.env` entries only.</action>
  <verify>
    <automated>grep -qi 'members.*intent' ../nocturna-bot/deploy/EDITOR_DEPLOY.md && ! grep -Eq 'CLIENT_SECRET *= *[A-Za-z0-9]' ../nocturna-bot/deploy/EDITOR_DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - The confirmed subdomain, redirect URI, proxy, and members-intent status are recorded
    - The file contains NO literal client/session secret value
    - The D-10 mechanism note matches the intent status
  </acceptance_criteria>
  <done>EDITOR_DEPLOY.md holds the confirmed infra facts; secrets stay out of the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → admin app | Editors log in from anywhere; the surface must be TLS-fronted with a fixed redirect URI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Info Disclosure | session cookie / OAuth code over HTTP | mitigate | Mandatory TLS via Caddy/nginx confirmed here; uvicorn bound to 127.0.0.1 (Pitfall 8) |
| T-10-03-02 | Spoofing | OAuth open redirect / URI mismatch | mitigate | Exact fixed redirect URI registered in the Discord app (Pitfall 4); no arbitrary post-login redirect |
| T-10-03-03 | Info Disclosure | planning-time OAuth/session secrets | mitigate | Secrets stored only in cinema `.env`; key-rotation reminder recorded; never committed |
</threat_model>

<verification>
- EDITOR_DEPLOY.md exists with all infra facts filled and no secret literals.
- Redirect URI registered exactly; subdomain + proxy + intent status confirmed.
</verification>

<success_criteria>
- The two blocking infra prerequisites (OAuth app + DNS/proxy/TLS) are resolved and documented; the D-10 mechanism is decided; 10-08/10-11 can proceed without external blockers.
</success_criteria>

<output>
Create `.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-03-SUMMARY.md` when done.
</output>
