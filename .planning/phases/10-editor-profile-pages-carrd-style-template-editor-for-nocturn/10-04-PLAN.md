---
phase: 10-editor-profile-pages-carrd-style-template-editor-for-nocturn
plan: 04
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - src/components/editor-blocks/BlockRenderer.astro
  - src/components/editor-blocks/BioBlock.astro
  - src/components/editor-blocks/HeadingBlock.astro
  - src/components/editor-blocks/TextBlock.astro
  - src/components/editor-blocks/LinksBlock.astro
  - src/components/editor-blocks/PortfolioBlock.astro
  - src/components/editor-blocks/QuoteBlock.astro
  - src/components/editor-blocks/ImageBlock.astro
  - src/lib/editorTypes.ts
autonomous: true
requirements: [EDIT-01, EDIT-03]

must_haves:
  truths:
    - "D-02: each of the 8 block types renders through a dedicated auto-escaping Astro component from a fixed, closed component library — no raw CSS/HTML anywhere"
    - "BlockRenderer maps block.type → component with no set:html anywhere"
    - "PortfolioBlock auto-pulls credited work by exact slug from store.json + gallery.json, excluding NSFW, and appends hand-added extra items"
    - "An empty portfolio (no credited + no extra) renders nothing"
  artifacts:
    - path: "src/components/editor-blocks/BlockRenderer.astro"
      provides: "closed-union type→component dispatch"
      min_lines: 20
    - path: "src/components/editor-blocks/PortfolioBlock.astro"
      provides: "build-time credited-work join (D-03/D-04/D-12)"
      min_lines: 30
    - path: "src/lib/editorTypes.ts"
      provides: "TS types mirroring the 10-01 editors.json schema"
      exports: ["Editor", "EditorBlock"]
  key_links:
    - from: "src/components/editor-blocks/PortfolioBlock.astro"
      to: "store.json + gallery.json"
      via: "build-time import + filter by editor===slug && !nsfw"
      pattern: "editor === slug"
    - from: "src/components/editor-blocks/BlockRenderer.astro"
      to: "per-block components"
      via: "block.type dispatch"
      pattern: "block.type ==="
---

<objective>
Build the closed-union block component library (D-02) and the portfolio auto-pull (D-03/D-04/D-12) for the public editor page. Eight typed Astro block components + a `BlockRenderer` dispatch, all auto-escaping only (the load-bearing XSS safety guarantee — no `set:html` ever). The portfolio block resolves credited work at build time by exact-slug match against `store.json` + `gallery.json`, drops NSFW, and appends the editor's hand-added items.

Purpose: Give 10-07's page route a ready block renderer; enforce D-02 safety at the component layer.
Output: `src/components/editor-blocks/*` + `src/lib/editorTypes.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-PATTERNS.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-01-PLAN.md
@src/components/ProductCard.astro
@src/components/sections/GalleryPage.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Editor TS types + BlockRenderer dispatch</name>
  <files>src/lib/editorTypes.ts, src/components/editor-blocks/BlockRenderer.astro</files>
  <read_first>
    - src/components/sections/GalleryPage.astro (the inline type→markup decision map idiom, L110-151, to generalize)
    - src/components/ProductCard.astro (typed `interface Props` + destructure + scoped `<style>` structure to copy)
    - src/i18n/ui.ts (type Lang, useTranslations)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-01-PLAN.md (the locked entry/block schema in its <interfaces> block)
  </read_first>
  <action>Create `src/lib/editorTypes.ts` exporting `Editor` and a discriminated `EditorBlock` union mirroring the 10-01 schema exactly (block types: bio, heading, text, links, portfolio, quote, image, divider; `tagline`/`text`/`caption` as `{es:string;en:string}`; `links`/`items` as `{label:string;url:string}[]`). Create `src/components/editor-blocks/BlockRenderer.astro` taking `Props: { block: EditorBlock; lang: Lang; slug: string }` and dispatching on `block.type` to the matching component (`{block.type === 'bio' && <BioBlock .../>}` … `{block.type === 'divider' && <hr class="editor-divider" />}`), passing `slug` only to PortfolioBlock. NO `set:html` anywhere. Unknown types render nothing (defensive — editors.json is externally written).</action>
  <verify>
    <automated>npx astro check 2>&1 | grep -v editor-blocks | tail -5; test -f src/lib/editorTypes.ts && grep -q "block.type ===" src/components/editor-blocks/BlockRenderer.astro</automated>
  </verify>
  <acceptance_criteria>
    - `editorTypes.ts` exports `Editor` and `EditorBlock` covering all 8 block types
    - `BlockRenderer.astro` dispatches on `block.type` and contains no `set:html`
    - An unknown `block.type` produces no output (no throw)
  </acceptance_criteria>
  <done>Types + dispatch exist; renderer is set:html-free.</done>
</task>

<task type="auto">
  <name>Task 2: Text/link/quote/image/divider block components</name>
  <files>src/components/editor-blocks/BioBlock.astro, src/components/editor-blocks/HeadingBlock.astro, src/components/editor-blocks/TextBlock.astro, src/components/editor-blocks/LinksBlock.astro, src/components/editor-blocks/QuoteBlock.astro, src/components/editor-blocks/ImageBlock.astro</files>
  <read_first>
    - src/components/ProductCard.astro (per-block component shape: typed Props, `Astro.props` destructure, scoped `<style>` using brand tokens var(--color-*), var(--font-display), var(--font-mono))
    - src/styles/theme.css (the @theme brand tokens: ink/paper/red-on-ink/red-on-paper/red-wordmark/font-display/font-mono — use these, never raw hex)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md (Typography roles, Color reserved-accent list, Block Inventory fields, Layout Contract ~640px column)
  </read_first>
  <action>Create one component per block type. BioBlock/TextBlock render `block.text[lang]` (fallback `es`→`en`→'') as an escaped paragraph (Body role, Inter 16px/1.7). HeadingBlock renders `block.text[lang]` as a Heading (Space Grotesk 700). LinksBlock renders stacked full-width link-buttons from `block.items` — each an `<a href={item.url}>` with `rel="noopener noreferrer nofollow"` and `target="_blank"`, label auto-escaped, 44px min touch target (guns.lol style). QuoteBlock renders `block.text[lang]` as a pull-quote (Space Grotesk 700, red rule accent via `--color-red-on-ink`) + optional `attribution`. ImageBlock renders a single `<img src={block.src} alt={block.alt}>` full column width, lazy-loaded. All text via `{expr}` auto-escaping ONLY — never `set:html` (D-02 / V5). Scoped `<style>` per component using brand tokens; AA contrast on ink. Do NOT ship a per-block color picker (deferred to v2 per UI-SPEC / A6).</action>
  <verify>
    <automated>! grep -rl "set:html" src/components/editor-blocks/ && npx astro check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All six component files exist, each with a typed `Props` and scoped `<style>` using `var(--...)` brand tokens (no raw hex)
    - `grep -r "set:html" src/components/editor-blocks/` returns nothing
    - LinksBlock anchors carry `rel="noopener noreferrer nofollow"` and render label via auto-escaped `{expr}`
    - `npx astro check` reports no new type errors in editor-blocks
  </acceptance_criteria>
  <done>The non-portfolio blocks render safely with brand styling.</done>
</task>

<task type="auto">
  <name>Task 3: PortfolioBlock build-time credited-work auto-pull</name>
  <files>src/components/editor-blocks/PortfolioBlock.astro</files>
  <read_first>
    - src/components/sections/StorePage.astro (the defensive build-time data read: copy-before-filter, per-field fallback, StoreProduct interface incl. `editor` + `nsfw` + `images`)
    - src/data/store.json (the products[].editor + products[].nsfw + images fields)
    - src/data/gallery.json (flat array; the NEW optional `editor` (slug string) and `nsfw` (boolean) fields are added by 10-06 — treat both as optional; missing `nsfw` means SFW)
    - src/components/ProductCard.astro (image-card markup to model the portfolio grid cell on)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md (Portfolio block empty rule; "From the store"/"From the gallery" credited badge copy)
  </read_first>
  <action>Create `PortfolioBlock.astro` taking `Props: { block, lang, slug }`. At build time import `store.json` + `gallery.json`. Build the credited list: store products where `p.editor === slug && !p.nsfw` (D-04 exclude NSFW), plus gallery entries where `g.editor === slug && g.nsfw !== true` (the new optional `nsfw` boolean from 10-06 — D-04 gallery exclusion; a missing field is SFW), plus the block's hand-added `block.extra ?? []` items (D-03). Match is EXACT slug equality only (D-12) — no substring/fuzzy. Render a grid of image cards (image + optional caption[lang], auto-escaped); auto-pulled items carry a Space Mono credited badge "From the store"/"De la tienda" or "From the gallery"/"De la galería" per source (UI-SPEC). If the combined list is empty, render NOTHING (no empty grid, no placeholder — UI-SPEC). Apply the StorePage defensive pattern: copy before filtering, skip malformed entries, per-field fallback so one bad entry never throws the build. Images are already-optimized WebP paths — render as-is, lazy-loaded.</action>
  <verify>
    <automated>npx astro check 2>&1 | tail -3 && grep -q "editor === slug" src/components/editor-blocks/PortfolioBlock.astro && ! grep -q "set:html" src/components/editor-blocks/PortfolioBlock.astro</automated>
  </verify>
  <acceptance_criteria>
    - Filter uses exact `editor === slug` equality and excludes BOTH `nsfw === true` store products AND `nsfw === true` gallery entries (a gallery entry with `nsfw: true` credited to the slug does NOT render; one with the field missing does)
    - Hand-added `block.extra` items are appended to the credited list
    - When credited + extra are both empty the component renders no markup
    - Malformed store/gallery entries are skipped, not thrown on (defensive read)
    - No `set:html`; captions render via auto-escaped `{expr}`
  </acceptance_criteria>
  <done>PortfolioBlock joins credited SFW work by slug + hand-added items, hides when empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editors.json/store.json/gallery.json → block components | Externally (bot/admin) written content rendered on a public page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Tampering (stored XSS) | all block text/label/caption | mitigate | Astro auto-escaping only; no `set:html`; closed block union — the D-02 safety guarantee enforced at render |
| T-10-04-02 | Tampering (link injection) | LinksBlock hrefs | mitigate | URLs already https-validated upstream (10-02 model); anchors use `rel="noopener noreferrer nofollow"` |
| T-10-04-03 | Info Disclosure | NSFW credited work on a SFW profile | mitigate | Portfolio auto-pull excludes `nsfw` store items + NSFW gallery entries entirely (D-04) |
| T-10-04-04 | Denial of Service | one malformed editors/store/gallery entry | mitigate | Defensive copy-before-filter + per-entry skip so a bad entry can't break the whole-site build |
</threat_model>

<verification>
- `npx astro check` clean for editor-blocks.
- `grep -r set:html src/components/editor-blocks/` returns nothing.
- Portfolio filter is exact-slug + NSFW-excluded; empty → renders nothing.
</verification>

<success_criteria>
- Eight safe, brand-styled block components + a dispatch renderer + build-time portfolio auto-pull, ready for the page route (10-07).
</success_criteria>

<output>
Create `.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-04-SUMMARY.md` when done.
</output>
