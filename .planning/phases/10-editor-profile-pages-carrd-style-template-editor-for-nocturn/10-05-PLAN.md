---
phase: 10-editor-profile-pages-carrd-style-template-editor-for-nocturn
plan: 05
type: execute
wave: 2
depends_on: [10-02]
files_modified:
  - ../nocturna-bot/core/github_publish.py
  - ../nocturna-bot/tests/test_github_publish_editors.py
autonomous: true
requirements: [EDIT-06, EDIT-07]

must_haves:
  truths:
    - "sync_editors() commits editors.json + uploaded image blobs in ONE atomic commit, upserting the editor by discordId"
    - "A concurrent save merges (re-fetch + upsert) instead of clobbering the whole file"
    - "unpublish_editor() flips published=false (or removes the entry) via the same transport"
    - "The existing gallery/store/reviews transports are byte-for-byte unchanged"
  artifacts:
    - path: "../nocturna-bot/core/github_publish.py"
      provides: "sync_editors() + unpublish_editor() paralleling sync_store()"
      exports: ["sync_editors", "unpublish_editor"]
    - path: "../nocturna-bot/tests/test_github_publish_editors.py"
      provides: "HTTP-mocked upsert + concurrent-merge + no-regression tests"
  key_links:
    - from: "sync_editors build_tree"
      to: "freshly fetched editors array"
      via: "upsert-by-discordId inside _commit_with_retry"
      pattern: "discordId"
---

<objective>
Extend `core/github_publish.py` with `sync_editors()` and `unpublish_editor()` — the cross-repo commit path for editor pages (D-06) — reusing the existing generic `_commit_with_retry` atomic blobs→tree→commit→ref core. The `build_tree` upserts THIS editor by `discordId` into the freshly-fetched array each retry (Pitfall 6 concurrent-clobber guard). Image blobs + `editors.json` land in ONE commit (D-17). No change to the gallery/store/reviews paths.

Purpose: Give the FastAPI app (10-08) and the role-loss cog (10-10) a tested, safe write path.
Output: Two new transport functions + HTTP-mocked tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-PATTERNS.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-02-PLAN.md

<interfaces>
Reuse (do not re-port) from github_publish.py:
- `_commit_with_retry(repo, branch, message, build_tree, fetch=None)` (L292) — re-fetches `current` + calls `build_tree(current)` each attempt; 409/422 stale-ref backoff
- `_fetch_json(repo, branch, path)` — generic object/array fetch
- `_serialize_json` / `_serialize_gallery` (L280-288) = `json.dumps(x, ensure_ascii=False, indent=2)`
- The `_sync_store_sync` build_tree (L569-634) upsert-by-checkoutUrl + image-blob-in-same-tree pattern — the exact template
Config: `WEBSITE_EDITORS_JSON` (src/data/editors.json), `WEBSITE_EDITORS_IMAGE_DIR` (public/editors), `WEBSITE_REPO`, `WEBSITE_BRANCH`, `GITHUB_PAT` (from 10-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sync_editors() — atomic upsert-by-discordId + image blobs (TDD)</name>
  <files>../nocturna-bot/core/github_publish.py, ../nocturna-bot/tests/test_github_publish_editors.py</files>
  <read_first>
    - ../nocturna-bot/core/github_publish.py (L280-330 the serialize helpers + `_commit_with_retry`; L569-648 `_sync_store_sync` upsert-by-checkoutUrl + image-blob tree; the async `sync_store` wrapper L762)
    - ../nocturna-bot/tests/test_github_publish.py (the HTTP-mock harness style — mirror it)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md (Pattern 1 + Pitfall 6 concurrent clobber)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-02-PLAN.md (EditorPage schema the entry conforms to)
  </read_first>
  <behavior>
    - Test: sync_editors with a new editor whose discordId is absent appends it; existing entries preserved.
    - Test: sync_editors with an existing discordId REPLACES only that entry (upsert), leaving others byte-identical.
    - Test: on a 409 stale-ref first attempt, the retry re-fetches and the upsert still targets the fresh array (no lost concurrent entry — Pitfall 6).
    - Test: passing image bytes writes an image blob tree entry under public/editors/<slug>/ AND the editors.json blob in the SAME commit (one tree).
    - Test: editors.json is serialized with `ensure_ascii=False, indent=2` (ES/EN text stays readable).
    - Test: GITHUB_PAT appears only in the Authorization header, never in any logged/returned string.
  </behavior>
  <action>Add `sync_editors(entry: dict, images: Sequence[tuple[str, bytes]] = (), *, message: str | None = None)` to `github_publish.py` mirroring `_sync_store_sync`. Its `build_tree(current)` takes the FRESHLY fetched editors ARRAY, computes `others = [e for e in current if e.get("discordId") != entry["discordId"]]`, sets `updated = others + [entry]`, and returns a tree list with the `editors.json` blob (`_serialize_json(updated)`) PLUS one blob entry per uploaded image (path `public/editors/<slug>/<name>`) — all in one tree (D-17). Fetch via `fetch=lambda: _fetch_json(repo, branch, config.WEBSITE_EDITORS_JSON)`. Commit message defaults to `editors: publish <slug>` (fixed template; never interpolate raw editor text — mirrors the 09-12 editor-commit hygiene). Provide an async wrapper like `sync_store` (thread-offloads the blocking `requests`). Write tests first (RED) against the existing HTTP-mock harness, then implement (GREEN).</action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_github_publish_editors.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_github_publish_editors.py` passes all behaviors above
    - `build_tree` operates on the re-fetched array (grep shows it reads `current`, not a captured outer array)
    - Image blobs + editors.json commit in one tree
    - Serialization uses `ensure_ascii=False, indent=2`
  </acceptance_criteria>
  <done>sync_editors atomically upserts by discordId with image blobs; concurrency-safe; tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: unpublish_editor() + no-regression guard (TDD)</name>
  <files>../nocturna-bot/core/github_publish.py, ../nocturna-bot/tests/test_github_publish_editors.py</files>
  <read_first>
    - ../nocturna-bot/core/github_publish.py (the sync_editors build_tree from Task 1 to parallel)
    - ../nocturna-bot/tests/test_github_publish.py (assert existing gallery/store/reviews tests still pass — the no-regression guard)
  </read_first>
  <behavior>
    - Test: unpublish_editor(discord_id) flips that entry's `published` to false and commits, leaving the entry (and images) in place so a later re-publish restores it.
    - Test: unpublish_editor on an unknown discordId is a no-op (no commit / no ref PATCH) — mirrors the store no-op guard.
    - Test: the full existing test_github_publish.py suite (gallery/store/reviews) still passes — no shared-helper regression.
  </behavior>
  <action>Add `unpublish_editor(discord_id: str, *, message: str | None = None)` reusing `_commit_with_retry`: `build_tree` re-fetches the array, finds the entry by `discordId`, sets `published=false`, and re-serializes; if no matching entry OR already unpublished, return without committing (no-op guard, mirrors `_sync_store_sync`'s defensive no-op). Fixed message `editors: unpublish <slug>`. This is the D-16 self-unpublish + D-10 role-loss write path. Confirm the whole existing `test_github_publish.py` suite stays green (shared helpers untouched).</action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_github_publish_editors.py tests/test_github_publish.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `unpublish_editor` flips `published` to false and commits for a known editor
    - `unpublish_editor` on an unknown/already-unpublished editor performs no commit
    - The pre-existing `test_github_publish.py` suite passes unchanged (gallery/store/reviews not regressed)
  </acceptance_criteria>
  <done>unpublish_editor lands with a no-op guard; existing transports non-regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin app → website repo | Cross-repo commit via the GitHub Data API with the bot PAT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Tampering | concurrent editors.json saves | mitigate | Upsert-by-discordId inside `_commit_with_retry`'s re-fetch/rebuild — merge never clobber (Pitfall 6) |
| T-10-05-02 | Info Disclosure | GITHUB_PAT | mitigate | PAT only in the Authorization header; never logged or returned; commit messages use fixed templates (no raw editor text) |
| T-10-05-03 | Tampering (path traversal) | image blob path | mitigate | Paths built from the already-normalized slug (10-02) under public/editors/<slug>/ only |
| T-10-05-04 | Availability | shared-transport regression | mitigate | No-regression test asserts gallery/store/reviews suites stay green |
</threat_model>

<verification>
- `pytest tests/test_github_publish_editors.py tests/test_github_publish.py` green.
- sync_editors upserts by discordId on the re-fetched array; images + json in one commit.
</verification>

<success_criteria>
- Atomic, concurrency-safe editor publish + unpublish transport, reusing the audited commit core, with existing pipelines non-regressed.
</success_criteria>

<output>
Create `.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-05-SUMMARY.md` when done.
</output>
