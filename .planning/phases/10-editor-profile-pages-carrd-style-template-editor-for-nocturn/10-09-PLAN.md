---
phase: 10-editor-profile-pages-carrd-style-template-editor-for-nocturn
plan: 09
type: execute
wave: 3
depends_on: [10-05]
files_modified:
  - ../nocturna-bot/cogs/editors.py
  - ../nocturna-bot/bot.py
  - ../nocturna-bot/tests/test_editors_cog.py
autonomous: true
requirements: [EDIT-07]

must_haves:
  truths:
    - "When an editor loses the editor role, their page is auto-unpublished on the next detection"
    - "Detection works via on_member_update when the members intent is enabled, else a periodic polling sweep"
    - "A transient failure never mass-unpublishes editors"
    - "An optional /mi-pagina slash command DMs the editor their admin-app link"
  artifacts:
    - path: "../nocturna-bot/cogs/editors.py"
      provides: "EditorsCog: role-loss unpublish (on_member_update + polling sweep) + optional /mi-pagina"
      min_lines: 40
    - path: "../nocturna-bot/bot.py"
      provides: "loads cogs.editors"
      contains: "editors"
  key_links:
    - from: "cogs/editors.py on_member_update"
      to: "github_publish.unpublish_editor"
      via: "role removed → unpublish by discordId"
      pattern: "unpublish_editor"
---

<objective>
Close EDIT-07's role-loss half (D-10): a new `EditorsCog` in nocturna-bot auto-unpublishes an editor's page when they lose the editor role, via `on_member_update` (real-time, if the `members` intent is enabled per 10-03) with a periodic polling sweep as the fallback/backstop. Includes the optional `/mi-pagina` slash command that DMs an editor their admin-app link (D-05's remaining bot role). Reuses the 10-05 `unpublish_editor` transport.

Purpose: Offboarded editors don't stay live indefinitely; the bot process (only holder of the gateway) owns role-change detection.
Output: `cogs/editors.py` + bot.py wiring + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-PATTERNS.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-03-PLAN.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-05-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EditorsCog role-loss unpublish (on_member_update + polling sweep)</name>
  <files>../nocturna-bot/cogs/editors.py, ../nocturna-bot/tests/test_editors_cog.py</files>
  <read_first>
    - ../nocturna-bot/cogs/gallery.py (cog class shape, _is_staff L41-47, on_raw_reaction_add listener idiom, setup() L521)
    - ../nocturna-bot/cogs/jinxxy.py (the @tasks.loop periodic-sweep idiom + error containment from Phase 9)
    - ../nocturna-bot/core/github_publish.py (unpublish_editor + the editors.json fetch, from 10-05)
    - ../nocturna-bot/config.py (ROLE_MODERATOR_ID editor role D-15)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-03-PLAN.md (the confirmed members-intent status → which mechanism is primary)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md (A5, Pitfall 2)
  </read_first>
  <behavior>
    - Test: on_member_update where before has ROLE_MODERATOR_ID and after does NOT → calls unpublish_editor(after.id) once.
    - Test: on_member_update with no role change (role still present, or unrelated role change) → no unpublish.
    - Test: the periodic sweep compares published editors.json entries against live guild membership and unpublishes only those whose discordId no longer holds the role.
    - Test: if the guild-membership fetch/API errors, the sweep unpublishes NOTHING (transient-failure mass-unpublish guard — mirror the Phase-9 removal-safety abort).
    - Test: unpublish is idempotent — a second pass on an already-unpublished editor does nothing (unpublish_editor no-op guard from 10-05).
  </behavior>
  <action>Create `cogs/editors.py` with `EditorsCog(commands.Cog)`. Implement `on_member_update(before, after)`: if `ROLE_MODERATOR_ID` was in `before.roles` and not in `after.roles`, await `unpublish_editor(str(after.id))` (D-10). Add a `@tasks.loop` periodic sweep (e.g. hourly) that fetches published editors.json entries and, for each, checks live guild membership for the editor role; unpublish only confirmed losers. CRITICAL removal-safety: if the membership check raises (API/gateway error), abort the sweep with ZERO unpublishes (never mass-remove on a transient failure — mirror JinxxyCog's enumerate-before-remove abort, T-09-15 lineage). Errors go to logs only (D-05). Make the tick delegate to a plain `_process_role_losses(...)` so it is unit-testable (neutralize tasks.Loop.start in the fixture). Write tests first (RED) then implement (GREEN).</action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_editors_cog.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Losing the editor role triggers exactly one unpublish_editor for that discordId
    - No role change → no unpublish
    - A membership-fetch error in the sweep unpublishes nothing (abort-on-error)
    - Repeated passes on an unpublished editor are no-ops
  </acceptance_criteria>
  <done>Role-loss auto-unpublish works via event + sweep with a hard mass-removal guard.</done>
</task>

<task type="auto">
  <name>Task 2: Optional /mi-pagina DM command + bot.py wiring</name>
  <files>../nocturna-bot/cogs/editors.py, ../nocturna-bot/bot.py, ../nocturna-bot/tests/test_editors_cog.py</files>
  <read_first>
    - ../nocturna-bot/bot.py (the cog-load list + fail-fast pattern for required config — mirror how cogs.jinxxy/reminders are loaded)
    - ../nocturna-bot/cogs/help.py (app-command cog registration idiom, if referenced)
    - ../nocturna-bot/config.py (EDITOR_APP_BASE_URL for the DM link)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md (Spanish-first staff-facing copy convention)
  </read_first>
  <action>Add an optional `/mi-pagina` app command to `EditorsCog` (staff-gated via `_is_staff`) that DMs the invoking editor their admin-app link (`EDITOR_APP_BASE_URL`) — the bot's one remaining role per D-05. Copy is Spanish-first (staff-facing). Register the cog in `bot.py`'s load list (mirror `cogs.jinxxy`/`cogs.reminders`). Do NOT add a required fail-fast on OAuth/session config in bot.py (those belong to the admin app process, not the bot) — the cog only needs ROLE_MODERATOR_ID + EDITOR_APP_BASE_URL which already have defaults. Note in the SUMMARY whether the `members` intent is enabled (10-03) so the sweep-vs-realtime posture is recorded.</action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_editors_cog.py -q && grep -q "editors" bot.py</automated>
  </verify>
  <acceptance_criteria>
    - `/mi-pagina` is staff-gated and DMs `EDITOR_APP_BASE_URL` to the invoker
    - `bot.py` loads `cogs.editors`
    - No OAuth/session fail-fast added to bot.py (those are the admin-app process's concern)
    - Full editors-cog test suite passes
  </acceptance_criteria>
  <done>The cog is wired into the bot with an optional admin-link DM command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord gateway/API → unpublish decision | Role state drives whether a page stays public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09-01 | Elevation | offboarded editor stays live | mitigate | on_member_update real-time unpublish + periodic sweep (D-10) |
| T-10-09-02 | Availability | transient API error mass-unpublishes | mitigate | Sweep aborts with zero unpublishes on any membership-check error (Phase-9 removal-safety lineage) |
| T-10-09-03 | Info Disclosure | errors posted to Discord | mitigate | Errors go to logs only (D-05); no channel output on failure |
| T-10-09-04 | Elevation | /mi-pagina used by non-editor | mitigate | Command staff-gated via _is_staff (ROLE_MODERATOR_ID) |
</threat_model>

<verification>
- `pytest tests/test_editors_cog.py` green (role-loss, no-change, sweep abort-on-error, idempotent).
- bot.py loads cogs.editors.
</verification>

<success_criteria>
- EDIT-07 role-loss half met: pages auto-unpublish on role loss with a hard mass-removal guard; optional admin-link DM shipped.
</success_criteria>

<output>
Create `.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-09-SUMMARY.md` when done.
</output>
