---
phase: 10-editor-profile-pages-carrd-style-template-editor-for-nocturn
plan: 07
type: execute
wave: 3
depends_on: [10-01, 10-04]
files_modified:
  - src/pages/[lang]/editores/[slug].astro
  - src/components/sections/EditorsDirectory.astro
  - src/pages/[lang]/[page].astro
  - src/i18n/routes.ts
autonomous: false
requirements: [EDIT-01, EDIT-02]

must_haves:
  truths:
    - "A visitor can open /en/editors/<slug> and /es/editores/<slug> for each published editor"
    - "The per-editor page renders the header (avatar/name/tagline/links) + ordered blocks via BlockRenderer"
    - "A visitor sees /en/editors · /es/editores listing all published editors, EmptyState when none"
    - "The language switcher carries the slug between locales on an editor page"
  artifacts:
    - path: "src/pages/[lang]/editores/[slug].astro"
      provides: "per-editor × per-lang static route via getStaticPaths cartesian product"
      min_lines: 40
    - path: "src/components/sections/EditorsDirectory.astro"
      provides: "published-editor grid + EmptyState (D-20)"
      min_lines: 30
  key_links:
    - from: "src/pages/[lang]/editores/[slug].astro"
      to: "editors.json"
      via: "getStaticPaths over published editors × langs"
      pattern: "getStaticPaths"
    - from: "src/components/sections/EditorsDirectory.astro"
      to: "editorPath"
      via: "per-editor link"
      pattern: "editorPath"
---

<objective>
Ship the public static surface (Surface A): the per-editor page route (the site's first per-item × per-lang dynamic route) rendering the header + ordered blocks via the 10-04 BlockRenderer, and the `/editores` · `/editors` directory (D-20). Extend `routes.ts` `translatePath` so the language switcher carries the `<slug>` between locales. Human-verify the visual result against the UI-SPEC.

Purpose: A visitor can discover and view editor profiles in both locales.
Output: The editor page route + directory section + slug-carrying language switch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-PATTERNS.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-01-PLAN.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-04-PLAN.md
@src/pages/[lang]/[page].astro
@src/components/sections/StorePage.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-editor page route + slug-carrying language switch</name>
  <files>src/pages/[lang]/editores/[slug].astro, src/i18n/routes.ts</files>
  <read_first>
    - src/pages/[lang]/[page].astro (getStaticPaths + lang narrowing + BaseLayout wrapping L32-63 — adapt, do NOT copy verbatim; this is the first per-ITEM route)
    - src/i18n/routes.ts (staticPathsForConcept shape L69-75; editorPath from 10-01; translatePath L55-62 to extend for per-editor slugs)
    - src/components/editor-blocks/BlockRenderer.astro (the block dispatch to loop editor.blocks through)
    - src/lib/editorTypes.ts (Editor type)
    - src/data/editors.json (the array to iterate; use the defensive StorePage read discipline)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md (Layout Contract: ~640px centered ink column, circular avatar 120-160px, name red-accent, tagline dim, links row/stack; NAV-03 chrome via BaseLayout)
  </read_first>
  <action>Create `src/pages/[lang]/editores/[slug].astro`. `getStaticPaths` emits the CARTESIAN product of `editors.json` (filtered `published===true` + valid slug) × `langCodes` — mirror `staticPathsForConcept`'s shape but nest over editors (a new helper `staticPathsForEditors()` in routes.ts is acceptable). Pass the editor entry as a prop. Wrap in `BaseLayout` (inherits Nav/Footer/FloatingCTA/NightBackground so "Abrir Ticket" stays reachable — NAV-03). Render the header: circular avatar, editor name (Display/Heading, red-accent `--color-red-on-ink`), `tagline[lang]` (fallback es→en→''), and the custom links row/stack. Then loop `editor.blocks` in order through `<BlockRenderer block={block} lang={lang} slug={editor.slug} />` with `md` (24px) vertical rhythm, ~640px max-width centered column. Extend `translatePath` in routes.ts so `/en/editors/<slug>` ↔ `/es/editores/<slug>` carries the slug (detect the editors concept + reattach the trailing slug) — the language switcher must not drop to the locale root on an editor page. Apply the defensive read (skip malformed entries).</action>
  <verify>
    <automated>npm run build && ls dist/en/editors/ dist/es/editores/ 2>/dev/null; npx astro check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `getStaticPaths` produces one page per published editor per locale (cartesian product)
    - `npm run build` emits `/en/editors/<slug>` and `/es/editores/<slug>` for each published editor (empty editors.json → no pages, build still green)
    - The page renders header fields + all blocks via BlockRenderer in order, no `set:html`
    - `translatePath('/en/editors/aria','es')` returns `/es/editores/aria` (slug carried)
  </acceptance_criteria>
  <done>Per-editor bilingual pages build and the language switch carries the slug.</done>
</task>

<task type="auto">
  <name>Task 2: EditorsDirectory section + wire the directory concept</name>
  <files>src/components/sections/EditorsDirectory.astro, src/pages/[lang]/[page].astro</files>
  <read_first>
    - src/components/sections/StorePage.astro (near-exact analog: data read → defensive filter/sort → grid of cards → EmptyState fallback; NightBackground + section chrome L127-164)
    - src/pages/[lang]/[page].astro (the concept resolver branch map + staticPathsForConcept list L32-39 — add the editors branch)
    - src/components/EmptyState.astro (heading + body + ctaTicket)
    - src/i18n/routes.ts (editorPath, staticPathsForConcept('editors'))
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-UI-SPEC.md (Directory layout: heading+subhead, 1→2-3 col grid, lg gap, only published; empty-state copy)
  </read_first>
  <action>Create `src/components/sections/EditorsDirectory.astro` modeled on `StorePage.astro`: import `editors.json`, copy-before-sort, filter `published && typeof slug==='string'`, sort by name; render a responsive grid (1 col mobile → 2-3 col desktop, `lg` 32px gap) of directory cards (avatar + name + `tagline[lang]`) each linking via `editorPath(e.slug, lang)`. When the list is empty render the `EmptyState` (heading/body/ctaTicket from the `editores` pages dict — 10-01) so an empty directory never looks broken and the Discord CTA stays reachable. Reuse `<NightBackground fixed />` + `.section section-page-top section--rule` + `.section-header` chrome. In `src/pages/[lang]/[page].astro` add the `editors` branch: include `staticPathsForConcept('editors')` in getStaticPaths and render `<EditorsDirectory lang={lang} />` when `concept === 'editors'`.</action>
  <verify>
    <automated>npm run build && ls dist/en/editors/index.html dist/es/editores/index.html 2>/dev/null; ! grep -rq "set:html" src/components/sections/EditorsDirectory.astro</automated>
  </verify>
  <acceptance_criteria>
    - `/en/editors` and `/es/editores` directory pages build
    - With editors.json empty, the directory shows the EmptyState (heading/body/ctaTicket), no broken grid
    - Directory cards link via `editorPath` to the per-editor pages
    - Only `published` editors are listed; no `set:html`
  </acceptance_criteria>
  <done>The bilingual directory lists published editors with an EmptyState fallback.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification against the UI-SPEC</name>
  <what-built>The public editor page + directory, styled with the Refined Street Editorial system (brand tokens, ink column, red accent).</what-built>
  <how-to-verify>
    1. Add one sample entry to `src/data/editors.json` (published, a couple of blocks incl. portfolio) OR use a temporary fixture, run `npm run dev`.
    2. Visit `/en/editors` and `/es/editores` — confirm the directory grid + card layout + EmptyState-when-empty behavior.
    3. Visit `/en/editors/<slug>` — confirm ~640px centered ink column, circular avatar, red-accent name, tagline, link-buttons, block order/rhythm, AA contrast, responsive at 375/768/1024/1440, and "Abrir Ticket" reachable.
    4. Toggle the language switcher on the editor page — confirm it lands on `/es/editores/<slug>` (slug carried), not the locale root.
    5. Remove the sample entry (or reset editors.json to `[]`) before approving.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/layout issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editors.json → public pages | Externally written data rendered statically to every visitor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Tampering (stored XSS) | editor header + blocks | mitigate | All fields auto-escaped; blocks go through the set:html-free BlockRenderer (10-04) |
| T-10-07-02 | Availability | malformed editors.json entry | mitigate | Defensive copy-before-filter + skip-malformed so one bad entry can't fail the whole-site build |
| T-10-07-03 | Tampering (link) | header custom links | mitigate | Links rendered as escaped anchors with rel=noopener/nofollow; URLs https-validated upstream |
</threat_model>

<verification>
- `npm run build` emits the editor pages + directory in both locales; `npx astro check` clean.
- Language switch carries the slug; EmptyState shows when editors.json is empty.
- Human visual sign-off against the UI-SPEC.
</verification>

<success_criteria>
- EDIT-01/EDIT-02 met: visitors browse a bilingual directory and individual editor profiles, styled to the brand system, CTA reachable.
</success_criteria>

<output>
Create `.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-07-SUMMARY.md` when done.
</output>
