---
phase: 10-editor-profile-pages-carrd-style-template-editor-for-nocturn
plan: 06
type: execute
wave: 3
depends_on: [10-01, 10-05]
files_modified:
  - ../nocturna-bot/cogs/gallery.py
  - ../nocturna-bot/core/github_publish.py
  - ../nocturna-bot/tests/test_gallery_credit.py
  - src/data/store.json
autonomous: false
requirements: [EDIT-08]

must_haves:
  truths:
    - "D-11: After a staff ✅ approves a gallery post, the bot offers a slug-autocomplete to credit an editor, written as gallery.json entry.editor"
    - "The gallery.json editor field value is an editor slug (D-12 exact-slug), not free text"
    - "The extended approve flow can flag a photo NSFW, written as gallery.json entry.nsfw=true (D-04); unflagged publishes omit the field (missing = SFW)"
    - "Existing free-text store.json editor values are re-tagged to editor slugs"
    - "The gallery ✅ publish path still works when no editor is credited (optional field)"
  artifacts:
    - path: "../nocturna-bot/cogs/gallery.py"
      provides: "editor-credit affordance after ✅ (ephemeral slug-autocomplete follow-up)"
      contains: "editor"
    - path: "../nocturna-bot/core/github_publish.py"
      provides: "gallery entry writer includes the optional editor field"
      contains: "editor"
  key_links:
    - from: "gallery ✅ approve flow"
      to: "gallery.json entry.editor"
      via: "slug-autocomplete → publish transport"
      pattern: "editor"
---

<objective>
Close EDIT-08: extend the live gallery ✅ approve flow to capture an editor credit (D-11) written as `gallery.json` `entry.editor` = an editor slug (D-12 exact-slug), and re-tag existing free-text `store.json` `editor` values to slugs. The SAME extended flow also captures an optional NSFW flag written as `entry.nsfw = true` — the gallery-side data source for D-04's SFW-only portfolio exclusion (10-04 skips `nsfw === true` entries). This resolves RESEARCH Open Q1 (a reaction carries no text) by reusing the Phase-9 `/tienda editar` slug-autocomplete pattern as an ephemeral follow-up after ✅. Depends on 10-05 (shares `core/github_publish.py` — serialized per the Phase 5/7/9 shared-file precedent).

Purpose: Supply the `editor` field the portfolio auto-pull (10-04) matches on.
Output: Gallery cog credit affordance + gallery transport editor field + re-tagged store.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-CONTEXT.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-PATTERNS.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-01-PLAN.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Confirm the D-11 gallery-credit affordance</name>
  <decision>How is the editor credit captured on a reaction-only ✅ approve?</decision>
  <context>The current gallery approve is a ✅ reaction (cogs/gallery.py::on_raw_reaction_add) — a reaction carries no text, so there is no place to type the editor slug at approval time (RESEARCH Open Q1 / Pitfall 7). This is the single most likely under-spec point in the phase.</context>
  <options>
    <option id="ephemeral-autocomplete">
      <name>Ephemeral slug-autocomplete follow-up after ✅ (RESEARCH recommendation)</name>
      <pros>Reuses the proven Phase-9 /tienda editar slug-autocomplete; keeps the ✅ flow; slug is validated against editors.json</pros>
      <cons>One extra interaction after approving</cons>
    </option>
    <option id="slash-command">
      <name>Separate /galeria creditar &lt;msg&gt; &lt;slug&gt; command</name>
      <pros>Decoupled from the reaction; can re-credit later</pros>
      <cons>New command surface; extra step disconnected from approval</cons>
    </option>
    <option id="default-approver">
      <name>Default credit to the approving staff member if they are an editor</name>
      <pros>Zero extra interaction</pros>
      <cons>Wrong when the approver isn't the creator; no way to credit someone else</cons>
    </option>
  </options>
  <resume-signal>Select: ephemeral-autocomplete, slash-command, or default-approver.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture editor credit + NSFW flag in the gallery approve flow + transport</name>
  <files>../nocturna-bot/cogs/gallery.py, ../nocturna-bot/core/github_publish.py, ../nocturna-bot/tests/test_gallery_credit.py</files>
  <read_first>
    - ../nocturna-bot/cogs/gallery.py (on_raw_reaction_add ✅ path L124+, _is_staff L41, the publish orchestration)
    - ../nocturna-bot/cogs/jinxxy.py (the /tienda editar slug-autocomplete + validation pattern from 09-12 to mirror)
    - ../nocturna-bot/core/github_publish.py (the gallery entry writer — where the entry dict is built; add the optional editor field)
    - src/data/editors.json (the slug set autocomplete validates against)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-01-PLAN.md (gallery.json editor field is an optional string slug, D-11/D-12)
  </read_first>
  <behavior>
    - Test: publishing a gallery entry WITH a credited slug writes `entry.editor == "<slug>"`.
    - Test: publishing WITHOUT a credit omits `editor` (or null) and still succeeds (optional field, no regression to BOT-01/02).
    - Test: flagging a photo NSFW at approval writes `entry.nsfw == True`; an unflagged publish OMITS the field entirely (missing = SFW, keeps historical entries valid).
    - Test: a credit slug that is not present in editors.json is rejected (must be an exact existing slug, D-12), staff-gated (_is_staff).
    - Test: the credit affordance is staff-gated — a non-staff interaction returns nothing/denied.
  </behavior>
  <action>Implement the affordance chosen in Task 1 (default: ephemeral slug-autocomplete follow-up after ✅). Add the credit capture to `cogs/gallery.py` gated by `_is_staff`; validate the chosen slug against the current `editors.json` slug set (reject unknown slugs — D-12 exact match). Thread the credited slug into the gallery publish so `core/github_publish.py` writes `entry.editor = "<slug>"` as an OPTIONAL field (omit when uncredited — must not break the existing gallery entry shape or BOT-01/02/06). Alongside the credit, the SAME affordance captures an optional NSFW flag (default SFW — D-04): when flagged, the publish writes `entry.nsfw = true`; SFW publishes omit the field entirely so historical entries (no `nsfw` key) read as SFW. This supplies the gallery half of D-04 consumed by 10-04's portfolio filter (`g.nsfw !== true`). The gallery.json entry otherwise keeps its Phase-4 `{file, caption?, width, height, date}` shape plus the new optional `editor` + `nsfw`. Write tests first (RED) then implement (GREEN). Keep the existing gallery test suite green.</action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_gallery_credit.py -x -q && python -m pytest tests/ -q -k gallery</automated>
  </verify>
  <acceptance_criteria>
    - A credited publish writes `entry.editor` = the chosen slug; an uncredited publish omits it
    - An NSFW-flagged publish writes `entry.nsfw` = true; an unflagged publish omits the field
    - An unknown slug is rejected against the editors.json slug set
    - The credit path is staff-gated
    - The existing gallery cog + transport tests still pass
  </acceptance_criteria>
  <done>Gallery ✅ approve optionally credits an editor slug into gallery.json; existing flow intact.</done>
</task>

<task type="auto">
  <name>Task 3: Re-tag existing store.json editor values to slugs (D-12)</name>
  <files>src/data/store.json</files>
  <read_first>
    - src/data/store.json (current products[].editor values — free text today; note products may be [] in this repo checkout)
    - src/data/editors.json (the authoritative slug set to map to)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md (Open Q5 — coordinate with the running JinxxyCog; editor is a staff-owned, sync-preserved field per 09-07/09-12)
  </read_first>
  <action>Re-tag any free-text `products[].editor` values in `src/data/store.json` to the exact editor slug (D-12) so the portfolio auto-pull (10-04) matches. If `products` is `[]` in this checkout (the live store data is on the deployed branch), record in the SUMMARY that the live re-tag must be applied against the deployed store.json and coordinated with the running JinxxyCog (editor is already a sync-preserved staff-owned field, so a Jinxxy sync will not fight the edit). Do NOT alter any sync-owned field (name/price/checkoutUrl/category/nsfw/date) — only `editor`. Preserve the `_comment` and byte shape otherwise.</action>
  <verify>
    <automated>node -e "const s=require('./src/data/store.json'); const bad=(s.products||[]).filter(p=>p.editor&&!/^[a-z0-9-]+$/.test(p.editor)); if(bad.length){console.error(bad);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - Every non-empty `products[].editor` in store.json matches `^[a-z0-9-]+$` (a slug, not free text)
    - No sync-owned field is modified; `_comment` preserved
    - If products is empty, the SUMMARY documents the deferred live re-tag + JinxxyCog coordination
  </acceptance_criteria>
  <done>store.json editor values are slugs (or the deferred live re-tag is documented).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff interaction → gallery.json editor field | Staff-entered credit slug that drives public portfolio grouping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Elevation | credit affordance | mitigate | Credit capture is `_is_staff`-gated (same trust boundary as ✅ approve) |
| T-10-06-02 | Tampering | editor slug value | mitigate | Slug validated against the editors.json slug set (exact match, D-12) before write |
| T-10-06-03 | Tampering | store.json sync-owned fields | mitigate | Re-tag touches only `editor`; sync-owned fields untouched; JinxxyCog coordination noted (09-07) |
| T-10-06-04 | Info Disclosure | NSFW gallery photo on a SFW-only profile | mitigate | NSFW flag captured in the same staff-gated approve affordance as the credit; 10-04 excludes `nsfw === true`; missing-field default is SFW but flagging is presented at every credited approval (D-04) |
</threat_model>

<verification>
- `pytest -k gallery` green including the new credit tests.
- store.json editor values are slugs or the deferred re-tag is documented.
</verification>

<success_criteria>
- EDIT-08 met: gallery ✅ approve can credit an editor slug into gallery.json; store.json editor values normalized to slugs; existing gallery pipeline non-regressed.
</success_criteria>

<output>
Create `.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-06-SUMMARY.md` when done.
</output>
