---
phase: 10-editor-profile-pages-carrd-style-template-editor-for-nocturn
plan: 11
type: execute
wave: 5
depends_on: [10-03, 10-07, 10-09, 10-10]
files_modified:
  - ../nocturna-bot/deploy/nocturna-editor-admin.service
  - ../nocturna-bot/deploy/Caddyfile.snippet
  - ../nocturna-bot/deploy/EDITOR_DEPLOY.md
autonomous: false
requirements: [EDIT-04, EDIT-05, EDIT-06, EDIT-07]

must_haves:
  truths:
    - "A systemd unit runs the admin app under uvicorn on 127.0.0.1 in the bot venv"
    - "A reverse-proxy config TLS-terminates the subdomain and proxies to uvicorn"
    - "The full chain works live: OAuth login → edit blocks → publish → static page rebuild → public render"
    - "A non-editor is denied; role loss auto-unpublishes; self-unpublish works"
  artifacts:
    - path: "../nocturna-bot/deploy/nocturna-editor-admin.service"
      provides: "systemd unit for the admin app"
      contains: "uvicorn"
    - path: "../nocturna-bot/deploy/Caddyfile.snippet"
      provides: "reverse-proxy + auto-HTTPS for the editor subdomain"
      contains: "reverse_proxy"
  key_links:
    - from: "reverse proxy (subdomain)"
      to: "uvicorn 127.0.0.1"
      via: "TLS-terminated proxy_pass"
      pattern: "127.0.0.1"
---

<objective>
Ship and prove the phase live: a systemd unit + reverse-proxy config for the admin app on cinema, then a human end-to-end verification of the whole chain (OAuth login gated by role → block editing with live preview → publish → website repo commit → static rebuild → public `/en/editors/<slug>` render), plus the negative/lifecycle paths (non-editor denied, role-loss auto-unpublish, self-unpublish). Finalizes `EDITOR_DEPLOY.md` and the post-ship key-rotation reminder.

Purpose: Turn built code into a running, verified feature.
Output: Deploy artifacts + a signed-off live verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-03-PLAN.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-08-PLAN.md
@.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-10-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: systemd unit + reverse-proxy config + finalize deploy doc</name>
  <files>../nocturna-bot/deploy/nocturna-editor-admin.service, ../nocturna-bot/deploy/Caddyfile.snippet, ../nocturna-bot/deploy/EDITOR_DEPLOY.md</files>
  <read_first>
    - ../nocturna-bot/deploy/EDITOR_DEPLOY.md (the confirmed infra facts from 10-03: subdomain, proxy, redirect URI, intent)
    - ../nocturna-bot/app/main.py (the ASGI app path `app.main:app` + the 127.0.0.1 bind assumption)
    - .planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-RESEARCH.md (deploy section: separate systemd unit sharing the bot venv/core/.env; Caddy auto-HTTPS; uvicorn on 127.0.0.1)
  </read_first>
  <action>Create `deploy/nocturna-editor-admin.service`: a systemd unit running `uvicorn app.main:app --host 127.0.0.1 --port <PORT>` from the bot's working dir + venv, `EnvironmentFile` = the bot `.env` (shares config/core/.env — RESEARCH), Restart=on-failure, run as the bot user. Create `deploy/Caddyfile.snippet` (or an nginx snippet if 10-03 chose nginx): the editor subdomain block with automatic HTTPS reverse-proxying to `127.0.0.1:<PORT>`, forwarding the standard proxy headers. Update `EDITOR_DEPLOY.md` with the concrete install/enable steps (pip install -r requirements.txt in the venv, systemctl enable/start, proxy reload) and keep the post-ship KEY ROTATION reminder prominent. Do NOT embed secrets in any deploy file — they live in the `.env`.</action>
  <verify>
    <automated>cd ../nocturna-bot && grep -q "uvicorn" deploy/nocturna-editor-admin.service && grep -Eq "reverse_proxy|proxy_pass" deploy/Caddyfile.snippet && grep -q "127.0.0.1" deploy/nocturna-editor-admin.service && ! grep -Eq "SECRET *= *[A-Za-z0-9]{8}" deploy/nocturna-editor-admin.service</automated>
  </verify>
  <acceptance_criteria>
    - The systemd unit launches `app.main:app` under uvicorn bound to 127.0.0.1 with the bot `.env` as EnvironmentFile
    - The proxy snippet TLS-terminates the subdomain and proxies to the uvicorn port
    - No secret literal appears in any deploy file
    - EDITOR_DEPLOY.md has concrete enable/start/reload steps + the rotation reminder
  </acceptance_criteria>
  <done>Deploy artifacts written; the doc has runnable steps and no secrets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live end-to-end verification of the full chain</name>
  <what-built>The admin app + role-loss cog deployed on cinema behind the TLS proxy; the website editor pages/directory built from editors.json.</what-built>
  <how-to-verify>
    1. On cinema: `git pull` the nocturna-bot repo, `pip install -r requirements.txt` in the venv, fill the new `.env` keys (OAuth client id/secret, SESSION_SECRET, EDITOR_APP_BASE_URL, redirect URI), `systemctl enable --now nocturna-editor-admin`, reload the proxy, and restart the bot (to load cogs.editors).
    2. Visit the editor subdomain → "Sign in with Discord" → authorize. As an EDITOR-role account you reach the block editor; first login shows the empty-draft state.
    3. Add a few blocks (bio, links, portfolio, image with an upload), reorder them, confirm the live preview matches; hit Publish.
    4. Wait for the website deploy; confirm `https://nocturna-avatars.site/en/editors/<slug>` renders your page and it appears in `/en/editors`.
    5. Negative path: log in with a NON-editor account → you see the 403 copy, never the editor UI.
    6. Lifecycle: self-unpublish → the page disappears from the site after rebuild. Then have a mod remove your editor role → confirm the page auto-unpublishes (real-time if members intent is on, else after the sweep). Portfolio: confirm an item credited to your slug (store/gallery) shows, and no NSFW item leaks.
    7. Confirm HTTPS is enforced (no http:// login) and the session cookie is Secure.
  </how-to-verify>
  <resume-signal>Type "approved" once all steps pass, or list the failures for gap closure.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record live verification + rotation reminder in STATE/deploy notes</name>
  <files>../nocturna-bot/deploy/EDITOR_DEPLOY.md</files>
  <read_first>
    - ../nocturna-bot/deploy/EDITOR_DEPLOY.md (append the verification outcome)
  </read_first>
  <action>Append a "Live verification" section to `EDITOR_DEPLOY.md` recording the date, the verified chain (login/role-gate/edit/publish/render/unpublish/role-loss), and any accepted deviations. Re-state the KEY ROTATION action: rotate the OAuth client secret + any secret pasted during setup, and confirm SESSION_SECRET is a fresh 32+ byte value not shared with any planning artifact. Do not write secret values.</action>
  <verify>
    <automated>grep -qi "Live verification" ../nocturna-bot/deploy/EDITOR_DEPLOY.md && grep -qi "rotat" ../nocturna-bot/deploy/EDITOR_DEPLOY.md</automated>
  </verify>
  <acceptance_criteria>
    - EDITOR_DEPLOY.md has a Live verification section with the verified chain + date
    - The rotation reminder is restated; no secret values written
  </acceptance_criteria>
  <done>Deploy doc reflects the live-verified state and the rotation follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internet → cinema admin app | The live public login surface behind TLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11-01 | Info Disclosure | cleartext login over HTTP | mitigate | TLS-terminating reverse proxy; uvicorn bound to 127.0.0.1; Secure cookie (Pitfall 8) |
| T-10-11-02 | Info Disclosure | secrets in deploy files | mitigate | Secrets only in `.env` (EnvironmentFile); no literals in unit/proxy/doc; verify gate greps for none |
| T-10-11-03 | Info Disclosure | planning-time OAuth/session secrets reused in prod | mitigate | Post-ship key rotation reminder recorded + acted on |
| T-10-11-04 | Elevation | non-editor / stale-role access live | mitigate | Live negative-path + role-loss verification confirms the gate + auto-unpublish |
</threat_model>

<verification>
- Deploy artifacts present, secret-free; proxy → 127.0.0.1 uvicorn.
- Human sign-off on the full live chain + negative + lifecycle paths.
</verification>

<success_criteria>
- The phase is live and verified end-to-end: editors authenticate, build pages with a live preview, publish to the static site, self-unpublish, and lose access on role removal — the project's first authenticated web surface, shipped safely.
</success_criteria>

<output>
Create `.planning/phases/10-editor-profile-pages-carrd-style-template-editor-for-nocturn/10-11-SUMMARY.md` when done.
</output>
