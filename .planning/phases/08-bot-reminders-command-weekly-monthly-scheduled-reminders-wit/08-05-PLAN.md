---
phase: 08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - ../nocturna-bot/cogs/reminders.py
  - ../nocturna-bot/tests/test_reminders_cog.py
autonomous: true
requirements: [D-02, D-04]
must_haves:
  truths:
    - "A non-staff guild member who fires borrar/editar autocomplete gets zero suggestions (empty list), so they cannot enumerate any reminder name or schedule."
    - "A staff member (holds a REMINDERS_STAFF_ROLE_IDS role) still gets the full filtered reminder Choice list from both autocomplete callbacks."
    - "borrar_autocomplete and editar_autocomplete never touch db.list_reminders() for a non-staff caller."
  artifacts:
    - path: "../nocturna-bot/cogs/reminders.py"
      provides: "borrar_autocomplete + editar_autocomplete gated by _is_staff(interaction.user) as their first statement"
      contains: "if not _is_staff(interaction.user)"
    - path: "../nocturna-bot/tests/test_reminders_cog.py"
      provides: "tests asserting non-staff autocomplete returns [] with no db read, plus staff-happy-path still delegates"
      contains: "assert_not_called"
  key_links:
    - from: "../nocturna-bot/cogs/reminders.py borrar_autocomplete"
      to: "_is_staff"
      via: "staff gate before _reminder_choices"
      pattern: "if not _is_staff\\(interaction.user\\)"
    - from: "../nocturna-bot/cogs/reminders.py editar_autocomplete"
      to: "_is_staff"
      via: "staff gate before _reminder_choices"
      pattern: "if not _is_staff\\(interaction.user\\)"
---

<objective>
Close the single failed verification truth for Phase 8: the D-02 staff trust boundary is
NOT enforced on the `borrar`/`editar` autocomplete channel. `borrar_autocomplete`
(cogs/reminders.py:505-508) and `editar_autocomplete` (633-636) delegate straight to
`_reminder_choices(current)` — which calls `db.list_reminders()` — with no
`_is_staff(interaction.user)` gate. Because no `default_permissions` restricts the
`/recordatorio` group, ANY guild member who can see the command can type into `borrar`/
`editar` and enumerate every reminder's name + full schedule summary. This is the exact
data `listar` refuses to show non-staff, and it directly contradicts CONTEXT.md D-02
("One trust level across the bot") and 08-REVIEW.md CR-01 (Critical/blocker).

Purpose: Restore the D-02 authorization boundary across the entire `/recordatorio` surface
(authorization-bypass / data-enumeration security fix — ASVS L1).
Output: Two-line staff gate added to both autocomplete callbacks, plus tests that prove
non-staff callers get an empty list and never hit the database.

Scope guard: Close ONLY the failed D-02 truth (08-VERIFICATION.md). Do NOT touch the 5
WARNING / 5 INFO findings from 08-REVIEW.md (WR-01..WR-05, IN-01..IN-05) — they were
explicitly classified as NOT gaps. Do NOT modify or re-plan any 08-01..08-04 work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Note: The bot code lives in the SIBLING repo `../nocturna-bot/` (absolute:
`C:\Users\Shangri\Pictures\Nocturna Avatars\Coding\nocturna-bot`). All edits and the
git commit for this plan happen in that repo, NOT the website repo. The website repo
only holds the planning artifacts.
</execution_context>

<context>
@.planning/phases/08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit/08-VERIFICATION.md
@.planning/phases/08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit/08-REVIEW.md

<interfaces>
<!-- Contracts the executor needs — extracted from ../nocturna-bot/cogs/reminders.py. -->
<!-- Use these directly; the current autocomplete bodies are the ONLY thing changing. -->

The staff gate helper (cogs/reminders.py:189-197), module-level, already used as the
first statement of listar/crear/borrar/editar:

    def _is_staff(member) -> bool:
        # True iff member holds a REMINDERS_STAFF_ROLE_IDS role. A None/role-less
        # member is never staff (empty role intersection is falsy).

The shared autocomplete backing (cogs/reminders.py:436-453) — DO NOT change it; it must
keep reading the store for STAFF callers:

    @staticmethod
    def _reminder_choices(current: str) -> list[app_commands.Choice[str]]:
        # Live db.list_reminders() -> labelled Choices, case-insensitive filter, <=25.

The two callbacks to gate, verbatim current state:

    @borrar.autocomplete("recordatorio")
    async def borrar_autocomplete(self, interaction, current):   # lines 505-508
        return self._reminder_choices(current)

    @editar.autocomplete("recordatorio")
    async def editar_autocomplete(self, interaction, current):   # lines 633-636
        return self._reminder_choices(current)

The gating idiom used everywhere else in the file (execution subcommands) returns an
ephemeral "Sin permisos." — but an autocomplete callback CANNOT send a message; it must
return an empty Choice list instead. CR-01's specified fix returns `[]`.
</interfaces>

<test_interfaces>
<!-- Extracted from ../nocturna-bot/tests/test_reminders_cog.py — reuse these helpers. -->

Member fake (line 33) and user fake w/ id (line 241):
    def _member(role_ids, is_bot=False): ...   # SimpleNamespace(roles=[...], bot=...)
    def _user(role_ids, uid=STAFF_UID): ...     # SimpleNamespace(roles=[...], bot=False, id=uid)
    STAFF_ROLE_ID = 111    OTHER_ROLE_ID = 222

The `cog` fixture (line 276) builds a RemindersCog with db.init + scheduler neutralized.
`_row(**over)` (line 446) builds a reminder dict.

The two EXISTING autocomplete tests that currently pass `interaction=None` and MUST be
updated to pass a staff interaction (else they will start returning [] and fail):
    test_borrar_autocomplete_delegates_to_reminder_choices   # line 636, calls cog.borrar_autocomplete(None, "jun")
    test_editar_autocomplete_delegates_to_reminder_choices   # line 833, calls cog.editar_autocomplete(None, "jun")

Autocomplete callbacks only read `interaction.user` (via _is_staff) — they never touch
`interaction.response`. So a minimal stub is enough:
    types.SimpleNamespace(user=_member([STAFF_ROLE_ID]))     # staff
    types.SimpleNamespace(user=_member([OTHER_ROLE_ID]))     # non-staff
</test_interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate borrar_autocomplete and editar_autocomplete on _is_staff</name>
  <files>../nocturna-bot/cogs/reminders.py</files>
  <read_first>
    - ../nocturna-bot/cogs/reminders.py — READ lines 436-453 (_reminder_choices, keep as-is),
      lines 480-508 (borrar + borrar_autocomplete), and lines 606-636 (editar + editar_autocomplete)
      to confirm the current callback bodies and the exact `_is_staff(interaction.user)` idiom
      used as the first statement of listar/crear/borrar/editar.
  </read_first>
  <action>
    In `borrar_autocomplete` (currently `return self._reminder_choices(current)` at ~line 508),
    insert a guard as the FIRST statement of the callback body: if `_is_staff(interaction.user)`
    is falsy, return an empty list (`[]`) immediately; otherwise fall through to the existing
    `return self._reminder_choices(current)`. Apply the identical guard to `editar_autocomplete`
    (~line 636). Call the module-level `_is_staff` (same helper listar/crear/borrar/editar use) —
    do NOT reimplement the role check. Add a short inline comment tying the guard to D-02 / CR-01
    (e.g. "same trust boundary as listar — no store read for a non-staff member (D-02)").
    Do NOT modify `_reminder_choices` itself (staff callers must still get the full list), and do
    NOT add `default_permissions` or change any other subcommand. Autocomplete callbacks cannot
    send an ephemeral reply, so the non-staff response is an empty Choice list, per CR-01.
  </action>
  <verify>
    <automated>cd ../nocturna-bot && grep -c "if not _is_staff(interaction.user)" cogs/reminders.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "if not _is_staff(interaction.user)" ../nocturna-bot/cogs/reminders.py` returns 6
      (was 4 before: listar/crear/borrar/editar; now +2 for the two autocomplete callbacks).
    - Both `borrar_autocomplete` and `editar_autocomplete` return `[]` for a non-staff member and
      call `self._reminder_choices(current)` only when `_is_staff(interaction.user)` is truthy.
    - `_reminder_choices` (lines 436-453) is byte-for-byte unchanged.
    - `cd ../nocturna-bot && python -c "import cogs.reminders, bot"` exits 0 (module still imports).
  </acceptance_criteria>
  <done>Both autocomplete callbacks gate on `_is_staff(interaction.user)` as their first statement, returning `[]` for non-staff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test autocomplete staff-gating (non-staff -> [] + no db read; staff still delegates)</name>
  <files>../nocturna-bot/tests/test_reminders_cog.py</files>
  <read_first>
    - ../nocturna-bot/tests/test_reminders_cog.py — READ lines 33-37 (`_member`), 241-259
      (`_user`, `_crear_interaction`), 276-281 (`cog` fixture), 446-452 (`_row`),
      636-641 (existing `test_borrar_autocomplete_delegates_to_reminder_choices`), and
      833-837 (existing `test_editar_autocomplete_delegates_to_reminder_choices`).
    - ../nocturna-bot/cogs/reminders.py — the Task 1 gated callbacks (so test expectations match).
  </read_first>
  <behavior>
    - Non-staff calling borrar_autocomplete: result is `[]` AND `db.list_reminders` is never called.
    - Non-staff calling editar_autocomplete: result is `[]` AND `db.list_reminders` is never called.
    - Staff calling borrar_autocomplete: still returns the filtered Choice list (value "7" for "jun").
    - Staff calling editar_autocomplete: still returns the filtered Choice list (value "7" for "jun").
  </behavior>
  <action>
    Update the two EXISTING delegate tests so they no longer pass `interaction=None` (which will
    now short-circuit to `[]` and break them): pass a staff interaction stub instead —
    `types.SimpleNamespace(user=_member([STAFF_ROLE_ID]))` — as the first arg to
    `cog.borrar_autocomplete(...)` / `cog.editar_autocomplete(...)`; keep their existing
    `db.list_reminders` monkeypatch and the `assert [c.value for c in choices] == ["7"]` assertion.
    Then ADD two new tests, `test_borrar_autocomplete_non_staff_returns_empty_no_db` and
    `test_editar_autocomplete_non_staff_returns_empty_no_db`: monkeypatch `reminders.db.list_reminders`
    with a `MagicMock`, build a non-staff interaction stub
    `types.SimpleNamespace(user=_member([OTHER_ROLE_ID]))`, `asyncio.run` the callback with a
    non-empty `current` (e.g. `"jun"`), then assert the returned list equals `[]` AND
    `list_reminders.assert_not_called()`. Use `_member` (roles only) — the callbacks read only
    `interaction.user` via `_is_staff`, so no `response`/`followup` stub is needed. Follow the
    repo idiom already in this file: `asyncio.run(...)`, `monkeypatch.setattr`, `MagicMock`,
    SimpleNamespace fakes; no pytest-asyncio.
  </action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_reminders_cog.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd ../nocturna-bot && python -m pytest tests/test_reminders_cog.py -q` reports 70 passed
      (was 68: the 2 delegate tests updated in place + 2 new non-staff tests added).
    - Grep confirms both new test names exist:
      `grep -c "def test_borrar_autocomplete_non_staff_returns_empty_no_db\|def test_editar_autocomplete_non_staff_returns_empty_no_db" tests/test_reminders_cog.py` returns 2.
    - Neither existing delegate test still calls the callback with `None` as the interaction:
      `grep -c "autocomplete(None," tests/test_reminders_cog.py` returns 0.
    - `cd ../nocturna-bot && python -m pytest -q` reports 234 passed (full suite, +2 from 232, no regressions).
  </acceptance_criteria>
  <done>Four autocomplete tests (2 updated staff-happy-path, 2 new non-staff) pass; full suite is 234 passed with zero regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guild member → `/recordatorio` autocomplete | Any member who can invoke application commands can fire the `borrar`/`editar` autocomplete callback; the input (`current`) and the caller identity are untrusted. Only `REMINDERS_STAFF_ROLE_IDS` holders are authorized to read reminder data (D-02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Information Disclosure | `borrar_autocomplete` / `editar_autocomplete` (cogs/reminders.py:505-508, 633-636) | mitigate | THIS PLAN. Add `if not _is_staff(interaction.user): return []` as the first statement of both callbacks so `db.list_reminders()` is never reached for a non-staff caller — closing the enumeration of every reminder name + schedule. Test T-08-06 asserts non-staff → `[]` with `list_reminders.assert_not_called()`. |
| T-08-07 | Elevation of Privilege | `/recordatorio` command group visibility (no `default_permissions`) | accept | Command remains visible to all members, but every execution subcommand (crear/listar/borrar/editar) already gates on `_is_staff` as its first statement (verified truths 11, 16, 17), and after this plan so does the autocomplete channel. Defense is enforced at the data-read boundary, not command visibility; adding `default_permissions` is an out-of-scope UX change not required to close D-02. |
| T-08-08 | Tampering | attacker-chosen autocomplete `value` passed to `borrar`/`editar` | accept | Already mitigated pre-phase: `borrar`/`editar` parse the value with `int(...)` inside a try/except and confirm existence via `db.get_reminder` before acting (verified truth 16; cogs/reminders.py:489-501). No new surface introduced by this plan. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages are installed by this plan (source + test edits only; `_is_staff`, `db`, `app_commands` all already imported). No `[ASSUMED]`/`[SUS]` package checkpoint required. |
</threat_model>

<verification>
Run from the nocturna-bot repo root (`../nocturna-bot`):

1. `grep -c "if not _is_staff(interaction.user)" cogs/reminders.py` → 6 (4 subcommands + 2 autocompletes).
2. `grep -c "autocomplete(None," tests/test_reminders_cog.py` → 0 (no test still passes a None interaction).
3. `python -m pytest tests/test_reminders_cog.py -q` → 70 passed.
4. `python -m pytest -q` → 234 passed (full suite, no regressions vs the prior 232).
5. `python -c "import cogs.reminders, bot"` exits 0.
</verification>

<success_criteria>
- The failed D-02 truth (08-VERIFICATION.md Truth 18) is closed: a non-staff caller of
  `borrar`/`editar` autocomplete receives an empty list and triggers no store read.
- Truths 16 and 17 (partially degraded by the same root cause) are no longer degraded — the
  autocomplete picker that backs `borrar`/`editar` now honors the same trust boundary as `listar`.
- Staff behavior is unchanged: staff still get the full filtered reminder Choice list.
- Full 234-test suite passes with zero regressions.
- No WARNING/INFO findings from 08-REVIEW.md were touched (scope held to the single gap).
</success_criteria>

<output>
Create `.planning/phases/08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit/08-05-SUMMARY.md` when done.
Note: the code + test commit lands in the `../nocturna-bot` repo; the SUMMARY.md lands in this website repo's planning tree.
</output>
