---
phase: 08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ../nocturna-bot/core/db.py
  - ../nocturna-bot/config.py
  - ../nocturna-bot/requirements.txt
  - ../nocturna-bot/.env.example
autonomous: true
requirements: [D-02, D-06, D-07, D-08, D-09, D-12, D-13, D-14, D-16]
must_haves:
  truths:
    - "D-06, D-09, D-16: a reminder row (weekly single-weekday / monthly / one-off, with the next_fire_utc lifecycle cursor) can be created, listed, fetched, updated, deleted, and queried-when-due through core/db.py"
    - "D-08, D-12, D-14: the schema persists day_of_month (the month-clamp input), a per-reminder channel_id, and the seeded-reactions list"
    - "D-02, D-07, D-13: config exposes REMINDERS_TZ (valid IANA zone), REMINDERS_STAFF_ROLE_IDS (fallback to gallery staff), REMINDERS_CATCHUP_GRACE_HOURS (int)"
    - "D-07: tzdata is pinned so zoneinfo resolves REMINDERS_TZ on any OS"
  artifacts:
    - path: "../nocturna-bot/core/db.py"
      provides: "reminders table + CRUD (init_reminders/add/list/get/update/delete/due/set_next_fire)"
      contains: "CREATE TABLE IF NOT EXISTS reminders"
    - path: "../nocturna-bot/config.py"
      provides: "REMINDERS_* env constants"
      contains: "REMINDERS_TZ"
    - path: "../nocturna-bot/requirements.txt"
      provides: "tzdata pin"
      contains: "tzdata"
    - path: "../nocturna-bot/.env.example"
      provides: "documented REMINDERS_* vars"
      contains: "REMINDERS_TZ"
  key_links:
    - from: "config.REMINDERS_STAFF_ROLE_IDS"
      to: "config.GALLERY_STAFF_ROLE_IDS"
      via: "or-fallback when env unset"
      pattern: "or GALLERY_STAFF_ROLE_IDS"
    - from: "db.due_reminders"
      to: "reminders.next_fire_utc"
      via: "WHERE next_fire_utc <= ?"
      pattern: "next_fire_utc <= \\?"
---

<objective>
Build the persistence + configuration foundation for the reminders cog inside the
`nocturna-bot` repo. This plan adds the `reminders` SQLite table and its CRUD helpers to
`core/db.py`, the `REMINDERS_*` env constants to `config.py`, the `tzdata` pin to
`requirements.txt`, and the documented env block to `.env.example`. No cog, no scheduler,
no Discord surface yet — this is the contract layer every later plan builds on.

Purpose: Downstream plans (08-03 scheduler/delivery, 08-04 management commands) consume
these CRUD functions and config constants directly, with no exploration needed.
Output: A restart-safe reminders store and the env surface for timezone, staff gate, and
catch-up grace window.

**REPO NOTE:** All files in this plan live in the separate `nocturna-bot` repo
(`../nocturna-bot`, absolute `C:\Users\Shangri\Pictures\Nocturna Avatars\Coding\nocturna-bot`),
NOT the website repo. Commit these changes in the `nocturna-bot` repo. No website-repo files
are touched. Deployment to the `cinema` systemd host (git pull + restart) is a manual user
step, out of phase scope.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit/08-CONTEXT.md
@.planning/phases/08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit/08-RESEARCH.md
@.planning/phases/08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit/08-PATTERNS.md

<interfaces>
<!-- The CRUD API this plan MUST expose (consumed verbatim by 08-03/08-04). Executor: implement these signatures exactly. -->

core/db.py NEW functions (module already imports `sqlite3`, `from datetime import datetime, timezone`, `import config`; reuses `_get_conn()`):
- init_reminders() -> None                      # CREATE TABLE IF NOT EXISTS reminders
- add_reminder(name, frequency, hour, minute, channel_id, message, created_by,
               weekday=None, day_of_month=None, run_date=None,
               mentions="", reactions="", next_fire_utc="") -> int   # returns new row id (lastrowid)
- list_reminders() -> list[sqlite3.Row]         # ORDER BY next_fire_utc
- get_reminder(reminder_id: int) -> sqlite3.Row | None
- update_reminder(reminder_id: int, **fields) -> None   # UPDATEs only the passed columns
- delete_reminder(reminder_id: int) -> None
- due_reminders(now_utc_iso: str) -> list[sqlite3.Row]  # WHERE next_fire_utc <= ? ORDER BY next_fire_utc
- set_next_fire(reminder_id: int, next_fire_utc_iso: str) -> None

reminders table columns (schema — see RESEARCH "core/db.py additions"):
  id INTEGER PK AUTOINCREMENT, name TEXT NOT NULL, frequency TEXT NOT NULL
  ('weekly'|'monthly'|'oneoff'), weekday INTEGER, day_of_month INTEGER,
  run_date TEXT, hour INTEGER NOT NULL, minute INTEGER NOT NULL,
  channel_id INTEGER NOT NULL, message TEXT NOT NULL, mentions TEXT DEFAULT '',
  reactions TEXT DEFAULT '', next_fire_utc TEXT NOT NULL, created_by INTEGER NOT NULL,
  created_at TEXT NOT NULL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: reminders table + CRUD in core/db.py</name>
  <files>../nocturna-bot/core/db.py</files>
  <read_first>
    - ../nocturna-bot/core/db.py — current idiom: `_get_conn()` (row_factory=sqlite3.Row),
      `init_gallery_state()` (CREATE TABLE IF NOT EXISTS shell), `get_cursor`/`set_cursor`
      (parameterized read/write), `save_post`/`delete_post` (INSERT OR REPLACE / DELETE with
      `?` placeholders + `datetime.now(timezone.utc).isoformat()`). Replicate these exactly.
    - .planning/phases/08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit/08-RESEARCH.md
      — "core/db.py additions" code block (table schema + due_reminders + set_next_fire).
    - .planning/phases/08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit/08-PATTERNS.md
      — "core/db.py (EDIT)" section (hard rules: `?` placeholders only, never f-string SQL;
      timestamps via datetime.now(timezone.utc).isoformat(); `with _get_conn() as conn`).
  </read_first>
  <action>
    Append a "# ── Recordatorios (Fase 8) ──" section to core/db.py implementing the eight
    functions in the interfaces block above. Use the `reminders` table schema exactly as
    listed (id AUTOINCREMENT PK; frequency one of 'weekly'|'monthly'|'oneoff'; nullable
    weekday/day_of_month/run_date for the frequency-specific fields; NOT NULL hour, minute,
    channel_id, message, next_fire_utc, created_by, created_at; mentions/reactions TEXT
    DEFAULT '').
    Rules (mirror the existing file verbatim): every statement uses `?` placeholders — NEVER
    f-string SQL (name/message are user text, T-08-03); wrap each in `with _get_conn() as conn:`
    for auto-commit; `created_at` set via `datetime.now(timezone.utc).isoformat()`.
    - add_reminder INSERTs one row and returns `conn.execute(...).lastrowid` as the new id.
    - list_reminders returns `conn.execute("SELECT * FROM reminders ORDER BY next_fire_utc").fetchall()`.
    - get_reminder returns the single `WHERE id = ?` row or None.
    - update_reminder builds a dynamic `SET col = ?, ...` from the passed **fields dict, with an
      allow-list of column names (name, frequency, weekday, day_of_month, run_date, hour, minute,
      channel_id, message, mentions, reactions, next_fire_utc) so an unexpected key can never
      inject SQL; no-op when fields is empty.
    - due_reminders / set_next_fire exactly as in RESEARCH.
    Do NOT add these calls to init_db(); the cog's __init__ calls db.init_reminders() itself
    (same pattern as init_gallery_state, PATTERNS.md).
  </action>
  <verify>
    <automated>cd ../nocturna-bot && DB_PATH=rem_smoke.db python -c "from core import db; db.init_reminders(); rid=db.add_reminder(name='Junta',frequency='weekly',hour=9,minute=0,channel_id=1,message='hola',created_by=1,weekday=0,next_fire_utc='2026-07-13T15:00:00+00:00'); assert isinstance(rid,int); assert any(r['id']==rid for r in db.list_reminders()); assert db.get_reminder(rid)['name']=='Junta'; db.update_reminder(rid,name='Junta 2',next_fire_utc='2026-07-20T15:00:00+00:00'); assert db.get_reminder(rid)['name']=='Junta 2'; assert any(r['id']==rid for r in db.due_reminders('2027-01-01T00:00:00+00:00')); db.set_next_fire(rid,'2030-01-01T00:00:00+00:00'); assert not db.due_reminders('2026-01-01T00:00:00+00:00'); db.delete_reminder(rid); assert db.get_reminder(rid) is None; print('OK')" && rm -f rem_smoke.db</automated>
  </verify>
  <acceptance_criteria>
    - core/db.py contains `CREATE TABLE IF NOT EXISTS reminders`
    - core/db.py defines `def init_reminders`, `def add_reminder`, `def list_reminders`,
      `def get_reminder`, `def update_reminder`, `def delete_reminder`, `def due_reminders`,
      `def set_next_fire`
    - The Task-1 automated round-trip (init → add → list/get → update → due → set_next_fire →
      delete) prints `OK` and exits 0
    - `grep -n "f\"" core/db.py` shows no f-string used to build a SQL statement in the reminders block (parameterized `?` only)
    - init_db() is NOT modified to call init_reminders()
  </acceptance_criteria>
  <done>All eight reminders CRUD functions exist, follow the parameterized `?` idiom, and the round-trip smoke passes.</done>
</task>

<task type="auto">
  <name>Task 2: REMINDERS_* config + tzdata pin + .env.example docs</name>
  <files>../nocturna-bot/config.py, ../nocturna-bot/requirements.txt, ../nocturna-bot/.env.example</files>
  <read_first>
    - ../nocturna-bot/config.py — the reviews block (REVIEWS_CHANNEL_ID / REVIEWS_STAFF_ROLE_IDS
      with `or GALLERY_STAFF_ROLE_IDS` fallback, comma-split-int idiom). Append after it.
    - ../nocturna-bot/requirements.txt — the "# ── Galería (Fase 5 ...) ──" pinned block style
      (commented, section-grouped). discord.py is already pinned `==2.7.1`.
    - ../nocturna-bot/.env.example — the "# ── Reseñas (Fase 7 ...) ──" section-comment +
      inline-comment style to mirror.
    - .planning/phases/08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit/08-RESEARCH.md
      — "config.py additions" + "Package Legitimacy Audit" (tzdata `[OK]` Approved, pin >=2025.2).
  </read_first>
  <action>
    config.py — append a "# ── Recordatorios (Fase 8: cog de recordatorios programados) ──"
    block with three constants (D-07/D-02/D-13):
    - REMINDERS_TZ = os.getenv("REMINDERS_TZ", "America/Mexico_City")  — IANA zone.
    - REMINDERS_STAFF_ROLE_IDS = [int(x) for x in os.getenv("REMINDERS_STAFF_ROLE_IDS","").split(",") if x.strip()] or GALLERY_STAFF_ROLE_IDS  — comma-split fallback to the existing gallery staff, exactly like REVIEWS_STAFF_ROLE_IDS.
    - REMINDERS_CATCHUP_GRACE_HOURS = int(os.getenv("REMINDERS_CATCHUP_GRACE_HOURS", "6"))  — the D-13 catch-up window (default 6h, inside the 6–12h band).
    requirements.txt — add a "# ── Recordatorios (Fase 8) ──" line pinning `tzdata>=2025.2`
    with a Spanish comment noting it is the IANA tz database fallback for Windows/CI (Linux
    cinema already has a system copy). Do NOT add discord.ext.tasks / sqlite3 / zoneinfo /
    calendar — they ship with discord.py / stdlib.
    .env.example — add a "# ── Recordatorios (Fase 8) ──" block documenting
    REMINDERS_TZ=America/Mexico_City, REMINDERS_STAFF_ROLE_IDS= (empty → GALLERY fallback),
    REMINDERS_CATCHUP_GRACE_HOURS=6, each with a short Spanish inline comment.
  </action>
  <verify>
    <automated>cd ../nocturna-bot && python -c "import importlib,config; importlib.reload(config); assert config.REMINDERS_TZ; assert isinstance(config.REMINDERS_CATCHUP_GRACE_HOURS,int); assert isinstance(config.REMINDERS_STAFF_ROLE_IDS,list); from zoneinfo import ZoneInfo; ZoneInfo(config.REMINDERS_TZ); print('OK')" && grep -q "tzdata" requirements.txt && grep -q "REMINDERS_TZ" .env.example && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - config.py defines REMINDERS_TZ, REMINDERS_STAFF_ROLE_IDS, REMINDERS_CATCHUP_GRACE_HOURS
    - config.py REMINDERS_STAFF_ROLE_IDS line contains `or GALLERY_STAFF_ROLE_IDS`
    - `ZoneInfo(config.REMINDERS_TZ)` resolves without raising (default America/Mexico_City)
    - requirements.txt contains `tzdata`
    - .env.example contains `REMINDERS_TZ`, `REMINDERS_STAFF_ROLE_IDS`, `REMINDERS_CATCHUP_GRACE_HOURS`
    - The Task-2 automated check prints `OK` then `DOCS_OK`
  </acceptance_criteria>
  <done>Config constants exist and resolve, tzdata is pinned, and the env vars are documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff Discord input → SQLite | reminder name/message are arbitrary user text persisted to the DB |
| PyPI → runtime | tzdata is a new third-party dependency pulled at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | core/db.py reminders CRUD | mitigate | Parameterized `?` placeholders only; update_reminder column allow-list — never f-string SQL (name/message are user text) |
| T-08-04a | Tampering | config.REMINDERS_TZ | mitigate | Zone validated by `ZoneInfo(config.REMINDERS_TZ)` (fail-fast wiring lands in 08-03 bot.py) |
| T-08-SC | Tampering | tzdata install (PyPI) | mitigate | RESEARCH Package Legitimacy Audit: tzdata `[OK]` Approved (PyPA project, slopcheck OK); pinned `>=2025.2`. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages → no blocking legitimacy checkpoint required |
</threat_model>

<verification>
- `cd ../nocturna-bot && python -m pytest -q` — the existing suite (164 tests) still passes (no regression from the db.py / config.py edits).
- Both task automated checks print OK.
</verification>

<success_criteria>
- reminders table + 8 CRUD helpers exist in core/db.py and round-trip cleanly
- REMINDERS_TZ / REMINDERS_STAFF_ROLE_IDS / REMINDERS_CATCHUP_GRACE_HOURS resolve from config
- tzdata pinned in requirements.txt; env vars documented in .env.example
- No regression to the existing bot test suite
</success_criteria>

<output>
Create `.planning/phases/08-bot-reminders-command-weekly-monthly-scheduled-reminders-wit/08-01-SUMMARY.md` when done
</output>
