---
phase: 07-reviews-publishing-pipeline-discord-reviews-channel-to-websi
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - nocturna-bot/cogs/reviews.py
  - nocturna-bot/tests/test_reviews_cog.py
autonomous: true
requirements: [REV-02, REV-04, REV-05]
must_haves:
  truths:
    - "A staff command posts a collection embed with two buttons: 'Reseña con nombre' and 'Reseña anónima'"
    - "Clicking a button opens a Discord modal with one review text field capped at 500 characters"
    - "Submitting the named modal posts a bot review message carrying the submitter's display name; submitting the anonymous modal posts a bot review message that carries NO submitter name anywhere"
    - "The bot adds a ✅ pending control to its own posted review message so staff can approve it"
    - "A staff ✅ on a bot-posted review publishes it with author = display name (named) or author = null (anonymous)"
    - "The collection embed's buttons survive a bot restart (persistent view re-registered on startup)"
    - "On a bot restart, `_reconcile` converges bot-posted review embeds too: a staff 🌙 unpublishes, a staff ✅ on an unpublished embed publishes, and an already-published embed is left alone — the cog's own review embeds are recognized by the marker and NO LONGER blanket-skipped (REV-05)"
  artifacts:
    - path: "nocturna-bot/cogs/reviews.py"
      provides: "ReviewModal + ReviewCollectView (persistent) + staff command + extended _review_author_and_text + marker-aware _reconcile"
      contains: "class ReviewModal"
    - path: "nocturna-bot/tests/test_reviews_cog.py"
      provides: "Tests for modal author/anonymity contract + embed parsing + custom_id stability + _reconcile over a bot review embed"
      contains: "anonymous"
  key_links:
    - from: "nocturna-bot/cogs/reviews.py::ReviewModal.on_submit"
      to: "REVIEWS_CHANNEL_ID review embed + ✅"
      via: "bot posts formatted review message and marks it pending"
      pattern: "send_modal|add_reaction"
    - from: "nocturna-bot/cogs/reviews.py::_review_author_and_text"
      to: "bot review embed"
      via: "author sentinel: embed author present → named; absent → anonymous(None)"
      pattern: "embeds"
    - from: "nocturna-bot/cogs/reviews.py::_reconcile"
      to: "_is_own_review_embed (REVIEW_EMBED marker)"
      via: "bot-authored review embeds are reconciled instead of blanket-skipped"
      pattern: "_is_own_review_embed"
---

<objective>
Add the guided collection flow on top of the 07-03 reaction pipeline: a staff command posts a persistent collection embed with two buttons (named / anonymous); each opens a Discord modal with one 500-char review field; on submit the bot posts a formatted review message into the reviews channel and marks it ✅ pending — so it publishes through the SAME staff-gated pipeline. Extend the `_review_author_and_text` seam so the pipeline resolves author/text from the bot's own review embeds, with anonymity guaranteed: the submitter's real name is never written to the embed, the repo, or any log. Finally, lift 07-03's "skip all bot messages" guard in `_reconcile` so a bot restart also converges the cog's OWN posted review embeds (REV-05).

Purpose: This is the lowest-friction collection path from CONTEXT L21 and the hard anonymity requirement from the specifics (L72-73). It reuses everything in 07-03 for publication; it adds the Discord UI (Modal + persistent View — the net-new work with no in-repo analog), the author-resolution extension, and the marker-aware reconcile so the guided flow is not silently skipped on restart.
Output: `ReviewModal`, `ReviewCollectView`, the staff command, persistent-view re-registration, the extended seam, the marker-aware `_reconcile`, and tests.

This plan spans the SIBLING repo `nocturna-bot` (`c:\Users\Shangri\Pictures\Nocturna Avatars\Coding\nocturna-bot`). It shares `cogs/reviews.py` with 07-03, so it runs strictly after it. Commit these changes IN THE nocturna-bot REPO.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reviews-publishing-pipeline-discord-reviews-channel-to-websi/07-CONTEXT.md
@.planning/phases/07-reviews-publishing-pipeline-discord-reviews-channel-to-websi/07-PATTERNS.md

<interfaces>
<!-- From 07-03 (already committed): cogs/reviews.py with ReviewsCog, _review_author_and_text(message)->(author,text), -->
<!-- _is_staff(member), _publish/_unpublish, on_message (skips bot messages), setup(bot). -->
<!-- The bot's own review messages are BOT messages, so on_message does NOT add ✅ to them — the modal on_submit must add ✅ itself. -->
<!-- 07-03 _reconcile(message, entries) currently SKIPS ALL bot messages (its own comment says "07-04 refines"); this plan lifts that for the cog's own review embeds via the marker. -->

<!-- discord.ui analog (the ONLY ui usage in the repo): cogs/forum.py TypoConfirmView L63-87 -->
<!--   @discord.ui.button(label=..., style=discord.ButtonStyle.success, emoji="✅"); async def accept(self, interaction, button) -->
<!-- CRITICAL divergences (PATTERNS L130-134): the collection View must be PERSISTENT (super().__init__(timeout=None), -->
<!--   stable custom_id per button, re-registered via bot.add_view(...)); each button calls interaction.response.send_modal(ReviewModal(...)); NO interaction_check author lock. -->
<!-- Discord Modal has NO in-repo analog — write from discord.py conventions: discord.ui.Modal(title=...), one -->
<!--   discord.ui.TextInput(style=discord.TextStyle.paragraph, max_length=500, required=True), on_submit(interaction). -->

<!-- Button labels (Spanish-first, CONTEXT L21): "Reseña con nombre" (named), "Reseña anónima" (anonymous). -->
<!-- bot.py setup_hook already syncs guild app commands (L55-63); GUILD_ID + ROLE_MODERATOR_ID + DISCORD_USER_ID in config. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReviewModal + ReviewCollectView + the review-embed anonymity contract + seam extension</name>
  <files>nocturna-bot/cogs/reviews.py</files>
  <read_first>
    - nocturna-bot/cogs/reviews.py (the 07-03 file — extend it; DO NOT rewrite. Locate _review_author_and_text and on_message)
    - nocturna-bot/cogs/forum.py (TypoConfirmView L63-87 — the discord.ui.View + @discord.ui.button pattern to adapt; note it is TRANSIENT — you need PERSISTENT)
    - .planning/phases/07-.../07-PATTERNS.md ("collection embed: 2 Buttons + Modal" section — the three critical divergences and the No-Analog Modal guidance)
    - .planning/phases/07-.../07-CONTEXT.md (L21-22 collection flow, L38 500-char cap + no set:html, L40 anonymity, L72-73 anonymity is a hard requirement)
  </read_first>
  <action>
    Extend `cogs/reviews.py`. Define the review-embed contract as module constants: a stable marker to identify the bot's own review embeds — `REVIEW_EMBED_FOOTER = "reseña"` (a fixed footer text; optionally pair with a fixed embed color constant) — and the two button custom_ids `REVIEW_BTN_NAMED = "reviews:collect:named"` and `REVIEW_BTN_ANON = "reviews:collect:anon"`. Add a small pure helper `_is_own_review_embed(message)` that returns True iff `message.author.bot` (the cog's own bot user) AND the message carries an embed whose footer text equals `REVIEW_EMBED_FOOTER` (the marker). This single predicate is reused by both the seam and `_reconcile` (Task 2) so "recognize the cog's own review embed" has one definition.

    Define `class ReviewModal(discord.ui.Modal)`: constructor takes an `anonymous: bool` flag and a `title` (Spanish, e.g. "Escribe tu reseña"); one field `discord.ui.TextInput(label="Tu reseña", style=discord.TextStyle.paragraph, max_length=500, required=True)`. Implement `async def on_submit(self, interaction)`: read the field value (already length-capped by Discord); build a `discord.Embed` whose description is the review text (rendered as plain content — the website escapes it, and Discord does not execute it). For the NAMED case set `embed.set_author(name=interaction.user.display_name)`. For the ANONYMOUS case do NOT set an author and do NOT include the submitter's name, id, or mention anywhere in the embed — put a localized-neutral "Anónimo" label in the embed title/field instead (a fixed string, never the user's name). Set the marker footer (`REVIEW_EMBED_FOOTER`) on BOTH cases so the embed is later recognizable. Post the embed to the reviews channel: resolve the channel via the interaction's client (get_channel/fetch_channel on config.REVIEWS_CHANNEL_ID), `await channel.send(embed=embed)`, then `await sent.add_reaction("✅")` so staff can approve it (on_message skips bot messages, so the modal MUST add ✅ itself). Reply to the interaction ephemerally confirming the review was submitted. Guard the whole on_submit so a send failure is reported to the submitter, never raised silently.

    Define `class ReviewCollectView(discord.ui.View)`: `super().__init__(timeout=None)` (persistent). Two `@discord.ui.button` handlers with `custom_id=REVIEW_BTN_NAMED` / `REVIEW_BTN_ANON`, labels "Reseña con nombre" (style primary) and "Reseña anónima" (style secondary); each handler calls `await interaction.response.send_modal(ReviewModal(anonymous=<bool>, ...))`. Do NOT add an interaction_check author lock (the embed is public).

    Extend `_review_author_and_text(message)`: FIRST branch — if `_is_own_review_embed(message)` (the message is the bot's own review embed), return the parsed pair: text = the embed description; author = the embed's author name if an author is set, else None (anonymous). SECOND branch (existing 07-03 behavior) — non-bot message → (display_name, stripped content). This is the single place that maps "no embed author" → author None, guaranteeing anonymity flows through _publish into a null `author` in reviews.json. Ensure `_publish`'s idempotency/skip logic and the entry build still work unchanged for bot review messages (they carry 🟢 the same way).
  </action>
  <acceptance_criteria>
    - grep confirms cogs/reviews.py defines `class ReviewModal`, `class ReviewCollectView`, `REVIEW_BTN_NAMED`, `REVIEW_BTN_ANON`, `REVIEW_EMBED_FOOTER`, and `_is_own_review_embed`
    - `ReviewModal`'s TextInput uses `max_length=500` and `style=discord.TextStyle.paragraph` (grep confirms)
    - `ReviewCollectView` calls `super().__init__(timeout=None)` and both buttons have a `custom_id=`
    - Both button handlers call `interaction.response.send_modal(` (grep confirms)
    - `_review_author_and_text` for a bot review embed with NO author returns author None; with an author set returns that name — asserted by a unit test in Task 2
    - The anonymous embed-build path contains no reference to `interaction.user.display_name`, `interaction.user.name`, `interaction.user.id`, or `interaction.user.mention` (grep the anonymous branch)
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Shangri/Pictures/Nocturna Avatars/Coding/nocturna-bot" && python -c "from cogs import reviews; assert hasattr(reviews,'ReviewModal') and hasattr(reviews,'ReviewCollectView') and hasattr(reviews,'REVIEW_BTN_ANON') and hasattr(reviews,'REVIEW_EMBED_FOOTER') and hasattr(reviews,'_is_own_review_embed'); v=reviews.ReviewCollectView(); assert v.timeout is None; print('ok')" && python -m pytest tests/ -q</automated>
  </verify>
  <done>ReviewModal (500-char field) + persistent ReviewCollectView (2 buttons) exist; the modal posts a marked review embed and marks it ✅ pending; anonymity is enforced in the embed build; `_is_own_review_embed`/marker are defined; and _review_author_and_text resolves author/text from bot review embeds (absent author → None).</done>
</task>

<task type="auto">
  <name>Task 2: Staff command + persistent-view re-registration + marker-aware _reconcile + anonymity/parsing/reconcile tests</name>
  <files>nocturna-bot/cogs/reviews.py, nocturna-bot/tests/test_reviews_cog.py</files>
  <read_first>
    - nocturna-bot/cogs/reviews.py (the file from Task 1 — add the command, register the view, and refine _reconcile; the on_ready from 07-03 to extend with bot.add_view)
    - nocturna-bot/cogs/forum.py (app_commands.command + staff/admin gate idiom, e.g. the DISCORD_USER_ID checks; and GroupCog/command registration style)
    - nocturna-bot/cogs/gallery.py (setup(bot) + on_ready once-guard — where to add bot.add_view(ReviewCollectView()))
    - nocturna-bot/tests/test_reviews_cog.py (the 07-03 test file — extend it, mirroring its SimpleNamespace + asyncio.run harness)
  </read_first>
  <action>
    Add a staff-gated command to ReviewsCog that posts the collection embed. Follow the repo's app-command conventions (an `@app_commands.command` on the cog, guild-synced by bot.py's setup_hook). Name it in Spanish (e.g. `reseñas_panel` or `panel_resenas` — pick one matching the repo's naming). Gate it: only a member holding a REVIEWS_STAFF_ROLE_IDS role (via `_is_staff`) or `config.DISCORD_USER_ID` may run it; others get an ephemeral "Sin permisos." reply. On success, send a collection embed (Spanish copy inviting clients to leave a review, explaining the two options) with `view=ReviewCollectView()` into the current or reviews channel, and confirm ephemerally.

    Register the persistent view so the buttons survive restarts: in the cog's `on_ready` (the 07-03 once-guard method), call `self.bot.add_view(ReviewCollectView())` BEFORE/around the backfill so Discord routes button interactions to the view after a restart even when the original message is uncached. (add_view is idempotent per custom_id set; the once-guard keeps it single-registered.)

    Refine `_reconcile(message, entries)` (the 07-03 method that currently blanket-SKIPS all bot messages): replace the "skip all bot messages" guard with "skip bot messages that are NOT the cog's own review embed". Concretely — if `message.author.bot and not _is_own_review_embed(message)`: skip (unchanged behavior for foreign bots / the bot's non-review messages). If `_is_own_review_embed(message)`: DO NOT skip — treat it as a reviewable message exactly like a plain client review: resolve author/text via the seam (which already parses bot embeds), a staff 🌙 → `_unpublish`; if `_is_published(message, entries)` return (already-published embed left alone); a staff ✅ on an unpublished embed → `_publish`; else if no ✅ prompt is present → add ✅. This makes the guided (2-button/modal) flow converge on a bot restart, closing REV-05 for bot-posted reviews. Keep the plain-client-message path from 07-03 unchanged.

    Extend `tests/test_reviews_cog.py` with: (1) a test that `_review_author_and_text` on a faked bot review embed with an author returns that author name + the embed description as text; (2) a test that the same on an embed with NO author returns (None, text) — the anonymity contract; (3) a test that a full anonymous flow produces a _publish entry with `author is None`; (4) a test asserting the two button custom_ids are the stable module constants (persistence contract); (5) a test that the anonymous embed built by ReviewModal contains the review text but not a provided fake display name; (6) a `_reconcile` test over a FAKE bot review-embed message: assert a staff ✅ on an unpublished bot review embed calls `_publish` (not skipped), a staff 🌙 calls `_unpublish`, and an already-published bot review embed is left alone — proving bot review embeds are no longer blanket-skipped. Use the existing SimpleNamespace/AsyncMock harness — no pytest-asyncio, no live Discord.
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_reviews_cog.py -q` passes including the new anonymity + parsing + custom_id + _reconcile-over-bot-embed tests
    - `python -m pytest tests/ -q` passes (whole suite)
    - grep confirms cogs/reviews.py contains an `@app_commands.command` handler with an `_is_staff`/DISCORD_USER_ID gate and a `self.bot.add_view(ReviewCollectView())` call in on_ready
    - grep confirms `_reconcile` references `_is_own_review_embed` (the blanket bot-skip is replaced by the marker-aware branch)
    - a test proves an anonymous submission yields `author is None` in the entry that _publish would send to publish_review
    - a test proves the named submission yields `author == <display name>`
    - a test proves `_reconcile` publishes (not skips) a staff-✅'d unpublished bot review embed
  </acceptance_criteria>
  <verify>
    <automated>cd "C:/Users/Shangri/Pictures/Nocturna Avatars/Coding/nocturna-bot" && grep -q "_is_own_review_embed" cogs/reviews.py && python -m pytest tests/test_reviews_cog.py tests/ -q</automated>
  </verify>
  <done>A staff-gated command posts the persistent collection embed, the view is re-registered on startup so buttons survive restarts, `_reconcile` recognizes and converges the cog's own review embeds on restart (REV-05), and the anonymity + embed-parsing + custom_id + reconcile contracts are proven by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Modal submitter → bot review embed | Anyone (public embed) can submit; the submitter's identity must be contained on the named path and ELIMINATED on the anonymous path |
| bot review embed → publish pipeline | The embed is the source the staff-gated pipeline (07-03) publishes; author resolution happens here |
| command invoker → posting the panel | Only staff may post the collection embed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Information disclosure | anonymous modal / embed build | mitigate | The anonymous branch never writes `interaction.user`'s name/id/mention into the embed; `_review_author_and_text` maps "no embed author" → author None → `author: null` in reviews.json; asserted absent by grep + unit tests |
| T-07-03 | Elevation of privilege | collection-panel command / _reconcile | mitigate | The command is gated to `_is_staff` (REVIEWS_STAFF_ROLE_IDS) or DISCORD_USER_ID; a non-staff invoker gets "Sin permisos." Publication itself remains staff-✅-gated (07-03) regardless of who submitted; the marker-aware `_reconcile` still resolves reactors via `_reaction_by_staff` so downtime reactions from non-staff never publish |
| T-07-01 | Injection (XSS) | modal text → embed → reviews.json | mitigate | Review text is treated as data end-to-end: never `set:html` (website 07-01 escapes); the modal caps input at 500 chars; the embed shows it as content, Discord does not execute it |
| T-07-07 | Tampering (stale-view spoof) | persistent view custom_ids | mitigate | Stable custom_ids + `bot.add_view` re-registration; buttons only OPEN a modal (no privileged action), and any resulting review still passes the staff ✅ gate before publishing |
| T-07-SC | Tampering (supply chain) | pip dependencies | accept | No new pip packages — discord.ui.Modal/View ship with the already-present discord.py; no install task, no legitimacy gate |
</threat_model>

<verification>
- `pytest tests/test_reviews_cog.py` + whole `tests/` suite green, including anonymity + parsing + custom_id + _reconcile-over-bot-embed tests.
- grep proves the anonymous branch never references the submitter's identity, and `_reconcile` references `_is_own_review_embed`.
- Live Discord verification (run the command, click both buttons, submit modals, confirm the anonymous post shows no name, staff ✅ publishes with correct author/null, and a restart converges a pending bot-posted review) is a manual user step after deploying to "cinema" — a human-verification note, NOT a task here.
</verification>

<success_criteria>
- Staff post a 2-button collection embed; each button opens a 500-char review modal.
- Named submissions publish with the display name; anonymous submissions publish with author null and never leak identity.
- Buttons survive restarts via a re-registered persistent view; `_reconcile` converges bot-posted reviews on restart (REV-05).
</success_criteria>

<output>
Create `.planning/phases/07-reviews-publishing-pipeline-discord-reviews-channel-to-websi/07-04-SUMMARY.md` when done (in the WEBSITE repo's .planning/).
Commit the code changes IN THE nocturna-bot REPO.
</output>
