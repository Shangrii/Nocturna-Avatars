---
phase: "10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi"
plan: "10"
type: execute
wave: 2
depends_on: ["10.1-09"]
files_modified:
  - ../nocturna-bot/app/templates/editor.html
  - ../nocturna-bot/app/static/editor.css
autonomous: true
requirements: [D-02, D-03, D-04, D-07, D-09, D-11, D-13, D-21, D-23, D-24]
must_haves:
  truths:
    - "The admin app gains a grouped theme panel (Background / Colors / Typography / Buttons / Effects / Audio) with a side-by-side live preview that reflects changes instantly (D-23)"
    - "Eight one-click theme presets apply a full palette+font+buttons+effects combo, and every knob stays tweakable afterward (D-24)"
    - "An editor sets lang, single tagline, badges (from the curated set), and socials in the same surface (D-13/D-07/D-09)"
    - "Reset theme uses an inline confirm and does not commit until save"
  artifacts:
    - path: "../nocturna-bot/app/templates/editor.html"
      provides: "Theme panel controls + preset picker + live preview extension"
      contains: "theme"
    - path: "../nocturna-bot/app/static/editor.css"
      provides: "Theme-panel styling (brand-dark admin surface)"
  key_links:
    - from: "theme control x-model"
      to: "live preview --theme-* binding"
      via: "Alpine reactive style binding"
      pattern: "theme"
---

<objective>
Extend the Phase-10 admin block editor (`app/templates/editor.html` + `app/static/editor.css` on cinema) with the theming surface: a grouped control panel (Background / Colors / Typography / Buttons / Effects / Audio) bound to the same Alpine live-preview pane already proven for blocks (D-23), an 8-preset one-click picker that applies a complete palette+font+buttons+effects combo then leaves every knob tweakable (D-24), plus the new lang / single-tagline / badges (curated set) / socials controls (D-13/D-07/D-09), and a reset-theme inline confirm.

Purpose: This is how editors actually build their premium page — the theme values this panel produces are exactly what the website (plans 02/06) renders. The panel writes into the `page.theme` object the save endpoint (validated by plan 09's ThemeModel) commits.
Output: `editor.html` (theme panel + presets + live preview), `editor.css` (admin styling).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md

<notes>
- Cross-repo: edit the SIBLING `../nocturna-bot` repo. Open `../nocturna-bot/app/templates/editor.html` (existing two-pane Alpine `x-data="editorApp(...)"` block editor + `.pane--edit`/`.pane--preview` + vendored Alpine 3.15.12/SortableJS 1.15.7, NOT CDN) and `../nocturna-bot/app/static/editor.css` (brand-token admin CSS).
- The live preview must apply theme tokens the SAME way the website does: bind the preview root's inline style to `--theme-*` from `page.theme` so the preview mirrors the real page.
- The 8 presets = UI-SPEC Theme Presets table (Midnight Nocturna default, Neon Night, Soft Pastel, Gothic, Y2K, Vaporwave, Minimal Mono, Sakura) — each a full {palette, font, btnStyle, btnShape, effects[]} object.
- No new pip/npm dep; Alpine + vanilla only (matches the 10-10 constraint).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme control panel + lang/tagline/badges/socials + live-preview theme binding</name>
  <read_first>
    - ../nocturna-bot/app/templates/editor.html (WHOLE — the editorApp x-data init, the .pane--edit control groups, the .pane--preview markup L213-272; extend, don't rewrite)
    - ../nocturna-bot/app/static/editor.css (existing admin tokens + pane layout)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Theme Token Contract = the fields each control edits; Effects Catalog = effect toggles; SpecialtyBadges curated set; Copywriting reset-theme + media-cap)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-CONTEXT.md (D-13 single-language, D-07 badges, D-09 socials)
  </read_first>
  <action>
    Extend `editorApp(...)` state to include a `page.theme` object (default Midnight Nocturna, matching plan 09 defaults) and migrate the tagline control to single-language: add a `page.lang` selector (es/en) and change the two tagline inputs to ONE `x-model="page.tagline"` bound field (D-13). Add grouped control sections in `.pane--edit`: (1) Background — bgMedia upload trigger (wired to plan 11's endpoints) + overlay range 0..100 + blur range 0..20 + tint color/none; (2) Colors — free hex pickers for accent/text/textMuted/surface + a palette-preset row; (3) Typography — a `<select>` of the 14 curated font keys bound to `page.theme.font`; (4) Buttons — btnStyle (filled/outline/glass) + btnShape (sharp/rounded/pill) selectors; (5) Effects — a checkbox per Effects-Catalog key toggling membership in `page.theme.effects[]`; (6) Audio — audio upload trigger + clear. Add a badges multi-select limited to the curated `SPECIALTY_BADGES` set (D-07, no free-form) and a socials repeater (label + https url rows, mirroring the existing links block editor). Bind the `.pane--preview` root inline style to emit `--theme-*` from `page.theme` (accent/text/textMuted/surface/font/btn/overlay/blur/tint) so the preview reflects every change instantly (D-23). All preview text stays Alpine `x-text` (never `x-html`) — the D-02 no-raw-HTML rule holds in the admin preview too.
  </action>
  <verify>
    <automated>cd ../nocturna-bot && python -c "from pathlib import Path; t=Path('app/templates/editor.html').read_text(encoding='utf-8'); assert 'page.theme' in t and 'page.lang' in t; print('theme panel present')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Ec "page.theme" ../nocturna-bot/app/templates/editor.html` >= 6 (theme fields bound)
    - Single-language migration: `grep -Ec "page.tagline.es" ../nocturna-bot/app/templates/editor.html` returns 0 AND `grep -Ec "page.lang" ../nocturna-bot/app/templates/editor.html` >= 1
    - All six control groups present: `grep -iE "Background|Colors|Typography|Buttons|Effects|Audio" ../nocturna-bot/app/templates/editor.html` matches each
    - Curated font select + effect toggles + badge set: `grep -Ec "page.theme.font|page.theme.effects|page.badges" ../nocturna-bot/app/templates/editor.html` >= 3
    - Live preview binds theme tokens: `grep -E "--theme-accent|--theme-font" ../nocturna-bot/app/templates/editor.html` matches
    - No raw-HTML in preview: `grep -c "x-html" ../nocturna-bot/app/templates/editor.html` returns 0
    - Bot suite still green: `cd ../nocturna-bot && python -m pytest -q` exits 0
  </acceptance_criteria>
  <done>The admin app has a grouped theme panel + lang/tagline/badges/socials controls with an instant live preview that mirrors the real page tokens.</done>
</task>

<task type="auto">
  <name>Task 2: 8 theme presets (one-click apply-then-tweak) + reset-theme inline confirm</name>
  <read_first>
    - ../nocturna-bot/app/templates/editor.html (this plan's Task 1 additions — the theme state to write into)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Theme Presets table = the 8 full combos; Copywriting reset-theme confirm text ES·EN; Destructive actions §reset)
  </read_first>
  <action>
    Add a preset picker: a row of 8 preset swatches/buttons; clicking one calls an `applyPreset(key)` method that overwrites `page.theme` with that preset's complete `{palette, font, btnStyle, btnShape, effects}` object (the 8 from the UI-SPEC Theme Presets table). After applying, every knob remains editable (apply-then-tweak, D-24) — presets are not locked modes. Add a "Restablecer tema · Reset theme" control using the existing inline-confirm pattern (like the self-unpublish confirm) with the exact UI-SPEC copy ("¿Restablecer el tema a los valores por defecto? Se perderán tus ajustes." · "Reset theme to defaults? Your customizations will be lost." — Confirm/Cancel), resetting `page.theme` to Midnight Nocturna. Neither preset-apply nor reset commits — they only mutate local state; changes persist only on the existing Save (D-13 publish-on-save). Style the preset swatches + panel in `editor.css` on the brand-dark admin surface (ink bg, off-white text, red active, `#e84a5e` destructive).
  </action>
  <verify>
    <automated>cd ../nocturna-bot && python -c "from pathlib import Path; t=Path('app/templates/editor.html').read_text(encoding='utf-8'); assert 'applyPreset' in t; print('presets present')" && python -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "applyPreset" ../nocturna-bot/app/templates/editor.html` >= 1
    - Eight presets defined: the preset object/map contains all 8 keys (`grep -iE "Neon Night|Soft Pastel|Gothic|Y2K|Vaporwave|Minimal Mono|Sakura|Midnight" ../nocturna-bot/app/templates/editor.html` matches multiple)
    - Reset confirm copy present: `grep -E "Restablecer el tema|Reset theme to defaults" ../nocturna-bot/app/templates/editor.html` matches
    - Reset/preset do NOT call the save/commit endpoint directly: `grep -E "applyPreset" ../nocturna-bot/app/templates/editor.html` shows local state mutation only (no `fetch(` inside applyPreset)
    - Bot suite still green: `cd ../nocturna-bot && python -m pytest -q` exits 0
  </acceptance_criteria>
  <done>8 presets apply full theme combos in one click (still tweakable); reset-theme uses an inline confirm and commits nothing until Save.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin panel input → page.theme → save body | Editor-controlled theme values authored in the panel, later validated by plan 09 on save. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-10-01 | XSS | live-preview rendering of editor content | mitigate | Preview binds via Alpine `x-text`/attribute bindings only — never `x-html`; the D-02 no-raw-HTML invariant holds in the admin preview. |
| T-10.1-10-02 | Tampering | theme values authored here | mitigate | This is UI only; the authoritative gate is plan 09's ThemeModel on `/editor/save`. The panel constrains inputs (enum selects, curated font/badge lists, numeric ranges) as a first line, but never as the sole gate. |
| T-10.1-10-SC | Supply chain | admin front-end libs | accept | Reuses vendored Alpine/SortableJS (already vetted in 10-10); no new dependency. |
</threat_model>

<verification>
- `cd ../nocturna-bot && python -m pytest -q` exits 0.
- Theme panel + presets + live preview present; tagline is single-language; no `x-html`.
</verification>

<success_criteria>
The admin app fully authors the new theme/badges/socials/lang model with a live preview and 8 one-click presets, feeding the plan-09-validated save.
</success_criteria>

<output>
Create `.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-10-SUMMARY.md` when done.
</output>
