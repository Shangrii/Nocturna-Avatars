---
phase: "10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi"
plan: "06"
type: execute
wave: 2
depends_on: ["10.1-01", "10.1-02"]
files_modified:
  - src/layouts/EditorLayout.astro
  - src/components/editor-shell/Splash.astro
  - src/scripts/editor/splash.ts
  - src/scripts/editor/effects.ts
autonomous: true
requirements: [D-04, D-05, D-06, D-15, D-20, D-21, D-22, D-26]
must_haves:
  truths:
    - "EditorLayout renders a fully standalone page: no navbar, footer, film grain, or site fonts — only a tiny corner nocturna watermark (D-15)"
    - "EditorLayout emits a FIXED whitelist of --theme-* custom properties from editor.theme with Midnight-Nocturna defaults, clamping numerics (D-26 + theme-injection guard)"
    - "Only the editor's one chosen curated font is preloaded per page (D-03)"
    - "Every page opens with a themed splash (blurred bg peek + name + Enter) that unlocks audio+video autoplay and dispatches editor:enter on dismiss (D-22)"
    - "Opt-in JS effects (typewriter, tilt, particles) run only when enabled and only outside reduced-motion (D-04)"
  artifacts:
    - path: "src/layouts/EditorLayout.astro"
      provides: "Standalone themed layout + token emission + watermark + splash mount"
      contains: "editor-watermark"
    - path: "src/components/editor-shell/Splash.astro"
      provides: "Click-to-enter splash"
    - path: "src/scripts/editor/effects.ts"
      provides: "Opt-in JS effects engine (reduced-motion aware)"
  key_links:
    - from: "Splash dismiss"
      to: "editor:enter document event"
      via: "splash.ts dispatch"
      pattern: "editor:enter"
    - from: "EditorLayout root"
      to: "editor.theme values"
      via: "inline --theme-* style"
      pattern: "--theme-"
---

<objective>
Build the standalone `EditorLayout` (no `BaseLayout` chrome, no site fonts, tiny watermark — D-15), the per-editor theme-token emission (a FIXED whitelist of `--theme-*` inline properties from `editor.theme`, numeric-clamped, defaults for D-26 clean cutover), the per-page single-font preload (D-03), the themed `Splash` click-to-enter overlay (D-22) with its `splash.ts`, and the opt-in JS `effects.ts` engine (typewriter, tilt, particles — reduced-motion aware, D-04).

Purpose: This is the shell every editor page mounts into. The theme-token emission here is the render-side half of the theme-injection defense (bot-side validation is plan 09): the layout emits ONLY known token names and re-clamps numerics, so no editor value can inject an arbitrary declaration. Interface-first: `splash.ts` owns the `editor:enter` event that plan 04's consumers listen for.
Output: `EditorLayout.astro`, `Splash.astro`, `src/scripts/editor/{splash,effects}.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md
@src/layouts/BaseLayout.astro
@src/components/LightboxOverlay.astro
@src/styles/editor-theme.css
@src/lib/editorTypes.ts

<interfaces>
<!-- EVENT CONTRACT (shared with plan 04): on splash dismiss, splash.ts calls
     document.dispatchEvent(new CustomEvent('editor:enter')). Fired exactly once. -->
<!-- EditorLayout props: { editor: Editor; title: string; description: string }.
     It emits the 13 --theme-* tokens inline on the page root from editor.theme, each with a
     Midnight-Nocturna default fallback; overlay clamped 0..100, blur clamped 0..20;
     font resolves editor.theme.font → the --font-editor-<key> stack from editor-fonts.css.
     robots = index,follow (Interaction Contract Discovery/SEO). OG from name+tagline+avatar. -->
<!-- Splash props: { name: string; font-aware via theme }. Shows blurred bg peek + name + Enter CTA
     in the editor language (Copywriting Contract: "Entrar"/"Enter"). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: EditorLayout — standalone shell + theme-token emission + watermark</name>
  <read_first>
    - src/layouts/BaseLayout.astro (SUBTRACTIVE analog — keep the doctype/head/meta skeleton L45-53; DELETE Nav/Footer/FloatingCTA/DiscordModal/ClientRouter, the 6 brand CSS imports, the Google-fonts link, the glitch-static)
    - src/styles/editor-theme.css + src/styles/editor-fonts.css (plan 02 — imported HERE; the --theme-* consumers + --font-editor-<key> map)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md (§EditorLayout.astro — keep/replace/new responsibilities)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Color §watermark; Interaction Contract Discovery/SEO index,follow; Copywriting watermark "nocturna" → /)
  </read_first>
  <action>
    Create EditorLayout.astro, props `{ editor: Editor; title: string; description: string }`. (a) Copy BaseLayout's doctype + `<html lang={editor.lang}>` + charset/viewport/theme-color/description/title/favicon meta; ADD `<meta name="robots" content="index,follow">` and OG tags (og:title=name, og:description=tagline, og:image=avatar). DELETE all site chrome (Nav/Footer/FloatingCTA/DiscordModal), the `ClientRouter`, the 6 brand CSS imports, the Google-fonts `<link>`, and `.glitch-static`. (b) Import ONLY `editor-theme.css` + `editor-fonts.css`. (c) Preload ONLY the editor's chosen font: a `<link rel="preload" as="font" type="font/woff2" crossorigin href="/fonts/editors/<family>/…woff2">` resolved from `editor.theme.font` (map the font key → its primary woff2 file). (d) Emit the theme tokens: build an inline `style` string on the page root (`<div class="editor-page" style={...}>` wrapping the `<slot/>`) that sets EACH of the 13 `--theme-*` properties from `editor.theme.<field> ?? <midnight-default>`, with `overlay` clamped to 0..100 and `blur` to 0..20, and `--theme-font` set to `var(--font-editor-<key>)` (unknown key → Inter). ONLY these known token names are ever emitted — no editor key becomes a CSS property name (theme-injection guard). (e) Render the tiny corner watermark `<a class="editor-watermark" href="/">nocturna</a>` (off-white 50% opacity, D-15). (f) Mount `<Splash name={editor.name} />` and the two editor scripts (`<script>import '../scripts/editor/splash.ts'` + `effects.ts`). NO gallery.ts here (the route imports it). Reduced-motion is centrally handled by editor-theme.css.
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - No site chrome imported: `grep -E "import Nav|import Footer|FloatingCTA|DiscordModal|ClientRouter|styles/theme.css|fonts.googleapis" src/layouts/EditorLayout.astro` returns nothing
    - Emits theme tokens inline: `grep -c "--theme-" src/layouts/EditorLayout.astro` >= 11 AND numeric clamp present (`grep -iE "Math.min|Math.max|clamp|0, ?100|0, ?20" src/layouts/EditorLayout.astro` matches)
    - Single-font preload: `grep -E "rel=\"preload\".*font" src/layouts/EditorLayout.astro` matches AND it is driven by editor.theme.font (`grep -E "theme.font|theme\?\.font" src/layouts/EditorLayout.astro` matches)
    - Watermark + robots: `grep -c "editor-watermark" src/layouts/EditorLayout.astro` >= 1 AND `grep -E "index,follow" src/layouts/EditorLayout.astro` matches
    - Splash mounted: `grep -E "Splash" src/layouts/EditorLayout.astro` matches
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>EditorLayout is a chrome-free standalone shell that emits a fixed clamped theme-token whitelist, preloads one font, shows the watermark, mounts the splash, and sets index,follow + OG.</done>
</task>

<task type="auto">
  <name>Task 2: Splash (click-to-enter) + splash.ts (editor:enter dispatch)</name>
  <read_first>
    - src/components/LightboxOverlay.astro (overlay/dialog scaffold + aria-modal pattern to copy)
    - src/scripts/gallery.ts (event-binding style for the splash script)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Copywriting splash Enter/sub-label; Interaction Contract page-load/splash-dismiss; D-22)
  </read_first>
  <action>
    Splash.astro, props `{ name: string }`: a fixed full-viewport overlay (blurred bg peek via `backdrop-filter` over the themed bg, `aria-modal`, role dialog) showing the editor name (Display role, `var(--theme-font)`) + an Enter CTA (`--theme-accent`) with the bilingual-by-lang copy "Entrar"/"Enter" + the mobile/desktop sub-label; a `data-splash` + `data-splash-enter` hook. `src/scripts/editor/splash.ts`: on click/Enter of the enter control (and on Escape as a skip), fade the splash out, then `document.dispatchEvent(new CustomEvent('editor:enter'))` EXACTLY ONCE (guard a flag), and start any `[data-editor-bg-video]` playback. The single dispatch is the unlock signal plan 04's audio + view-counter consume. Reduced-motion: skip the fade (instant hide) but still dispatch.
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "editor:enter" src/scripts/editor/splash.ts` matches AND a once-guard exists (`grep -iE "entered|once|dispatched" src/scripts/editor/splash.ts` matches)
    - Splash shows the Enter CTA copy: `grep -E "Entrar|Enter" src/components/editor-shell/Splash.astro` matches
    - Splash is a modal overlay: `grep -E "aria-modal|role=\"dialog\"" src/components/editor-shell/Splash.astro` matches
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The splash gates the page, and on Enter dispatches editor:enter once + starts bg video, unlocking audio/video autoplay per D-22.</done>
</task>

<task type="auto">
  <name>Task 3: effects.ts — opt-in JS effects engine (reduced-motion aware)</name>
  <read_first>
    - src/scripts/gallery.ts (event-binding + guard style)
    - src/components/editor-shell/EditorHeaderCard.astro (plan 03 — the data-typewriter hook the engine drives)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Effects Catalog — typewriter/tilt/particles techniques + reduced-motion fallbacks; performance budget: at most ONE canvas effect/page)
  </read_first>
  <action>
    Create `src/scripts/editor/effects.ts`. It reads which effects are active from a page-root data attribute the layout sets (e.g. `data-effects="typewriter tilt particles"` from `editor.theme.effects`), then wires ONLY the JS effects (the CSS effects are handled by editor-theme.css): (1) typewriter — reveal the `[data-typewriter]` tagline character-by-character; (2) tilt-on-hover — pointer-driven `rotate3d` on the glass card, pointer devices only; (3) particles/snow — a single lightweight capped-count canvas overlay (at most one canvas/page, performance budget). Guard the WHOLE engine behind `matchMedia('(prefers-reduced-motion: reduce)')` — if reduced motion is on, do nothing (the static fallbacks are already the default markup/CSS). Each effect is independent and no-ops when its key is absent from `data-effects`. No third-party animation library (no new npm dep).
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Reduced-motion gate present: `grep -E "prefers-reduced-motion" src/scripts/editor/effects.ts` matches
    - Effects gated by data-effects: `grep -E "data-effects|effects.includes|effects.indexOf" src/scripts/editor/effects.ts` matches
    - The three JS effects are addressed: `grep -iE "typewriter" src/scripts/editor/effects.ts` AND `grep -iE "tilt|rotate3d" src/scripts/editor/effects.ts` AND `grep -iE "particle|canvas" src/scripts/editor/effects.ts` all match
    - At most one canvas guard: `grep -iE "querySelector\(.*canvas|createElement\('canvas'\)" src/scripts/editor/effects.ts` shows a single canvas creation path
    - No new npm dependency introduced (`git -C . diff --name-only | grep -c package.json` returns 0 for this plan's changes)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>effects.ts drives typewriter/tilt/particles only when opted-in and only outside reduced-motion, with a single capped canvas.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor.theme values → inline CSS custom properties | Editor-controlled colors/numbers/font key emitted into the page root style. |
| editor.effects → data-effects attribute → JS behavior | Editor-controlled effect keys drive which scripts run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-06-01 | Injection | theme-token emission | mitigate | EditorLayout emits ONLY the 13 known `--theme-*` property NAMES; editor values go into VALUES via Astro attribute auto-escaping (no attribute breakout), numerics are re-clamped (overlay 0..100, blur 0..20), and the font resolves through a fixed key→stack map (unknown → Inter). No editor string ever becomes a property name, selector, or `@import`. Values were already validated bot-side (plan 09). |
| T-10.1-06-02 | XSS | name/tagline in layout + splash | mitigate | Rendered through `{}` auto-escaping only; D-15 removes all site chrome but no `set:html` is introduced. |
| T-10.1-06-03 | DoS (client) | particles canvas | mitigate | At most one canvas/page with a capped particle count; whole effects engine disabled under reduced-motion. |
| T-10.1-06-SC | Supply chain | effects engine | mitigate | Hand-written, no animation library; no new npm dependency. |
</threat_model>

<verification>
- `npm run build` exits 0.
- EditorLayout emits no site chrome and a clamped fixed token whitelist; splash dispatches editor:enter once; effects gated on reduced-motion.
</verification>

<success_criteria>
A standalone themed shell exists with safe theme-token emission, single-font preload, watermark, a splash that unlocks media on Enter, and an opt-in reduced-motion-aware effects engine.
</success_criteria>

<output>
Create `.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-06-SUMMARY.md` when done.
</output>
