---
phase: "10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi"
plan: "11"
type: execute
wave: 2
depends_on: ["10.1-09"]
files_modified:
  - ../nocturna-bot/app/main.py
  - ../nocturna-bot/tests/test_app_editor.py
autonomous: true
requirements: [D-06, D-08, D-18, D-19, D-20]
user_setup:
  - service: ffmpeg
    why: "Server-side background-video/GIF transcode + audio normalize on the cinema host (D-18 optimize-on-upload)"
    dashboard_config:
      - task: "Install ffmpeg on the cinema host (apt install ffmpeg) so the admin app can transcode uploads"
        location: "cinema host shell"
must_haves:
  truths:
    - "An editor uploads a background image/GIF/video and it is validated, optimized, and committed under their own slug directory (D-18)"
    - "An editor uploads a background audio track that is committed under their slug (D-06)"
    - "Uploads over the moderate caps (video/GIF ≤10MB, audio ≤5MB, image ≤2MB) are rejected before the full body is buffered, with the localized too-large copy (D-19)"
    - "The commit path is built from the SESSION slug only — never a client-supplied path (D-08)"
  artifacts:
    - path: "../nocturna-bot/app/main.py"
      provides: "POST /editor/media and /editor/audio endpoints (streamed cap + optimize + commit)"
      contains: "editor/media"
  key_links:
    - from: "upload endpoint"
      to: "github_publish.sync_editors"
      via: "committed media blob under ident.slug"
      pattern: "sync_editors"
---

<objective>
Add the background-media and audio upload endpoints to the admin app (`app/main.py`), cloning the proven `POST /editor/image` discipline: reject-before-decode by type, stream with an early size-cap abort at the D-19 caps (video/GIF ≤10MB, audio ≤5MB, image ≤2MB), optimize server-side (ffmpeg transcode for video/GIF, audio normalize/clip), and commit the optimized bytes under the SESSION slug's directory via `sync_editors` (D-08 path-traversal/IDOR guard). Returns the site-relative path the theme panel writes into `page.theme.bgMedia`/`page.theme.audio`.

Purpose: D-18/D-06 background media + audio are core to the premium feel; this is the only path that puts editor media into the repo. Cross-repo, bot-side.
Output: `app/main.py` (two endpoints + caps), `tests/test_app_editor.py` additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md

<notes>
- Cross-repo: edit the SIBLING `../nocturna-bot` repo. Open `../nocturna-bot/app/main.py` (the existing `POST /editor/image` handler L200-257 is the EXACT analog: reject SVG by content-type+ext, stream chunk-read with `_MAX_UPLOAD_BYTES` early abort, `optimize_to_webp`, commit only re-encoded bytes via `sync_editors`, path from `ident["slug"]` only) and `../nocturna-bot/tests/test_app_editor.py`.
- Video/GIF real compression needs ffmpeg (system binary, NOT a pip package — no legitimacy gate needed; documented in user_setup + the deploy doc). If ffmpeg is absent, the endpoint must FAIL CLOSED (reject with the media-error copy) rather than commit an un-optimized oversized file.
- Caps differ per media kind (D-19) — enforce the specific cap for the endpoint, streamed, before buffering the whole body.
- Reuse `_fetch_current_entry`, `_MAX_UPLOAD_BYTES` idiom, `run_in_threadpool`, `GitHubPublishError` handling, and the bilingual error-copy constants already in main.py.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: POST /editor/media (image + GIF + video, per-kind caps, ffmpeg optimize)</name>
  <read_first>
    - ../nocturna-bot/app/main.py (WHOLE — the `upload_image` handler L200-257 to clone; `_fetch_current_entry`, `require_editor`, `sync_editors`, config.WEBSITE_EDITORS_IMAGE_DIR, error-copy constants)
    - ../nocturna-bot/tests/test_app_editor.py (the existing upload test patterns to extend)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-CONTEXT.md (D-18 image+GIF+video, D-19 caps, D-20 same-on-mobile so muted-loop video is the target)
  </read_first>
  <action>
    Add `@app.post("/editor/media")` `upload_media(request, file, ident=Depends(require_editor))`. (1) Determine the media kind from content-type/extension: raster image (png/jpg/webp) → cap 2MB, re-encode via `optimize_to_webp` (reuse existing); GIF → cap 10MB, transcode to a muted looping mp4/webm via ffmpeg (a GIF background is really a short video, D-18); video (mp4/webm) → cap 10MB, transcode/normalize to a web-optimized muted mp4 (and/or webm) via ffmpeg (strip audio track — bg video is muted, D-20). REJECT svg + any non-media type by content-type AND extension before decode (mirror the existing `_REJECTED_*` guard, extended). (2) Stream-read with an early abort at the KIND-SPECIFIC cap (never buffer past the cap) — on over-cap return 400 with the UI-SPEC too-large copy ("El archivo supera el límite ({cap}). Comprime o elige otro." · "File exceeds the limit ({cap}). …") interpolating the human cap. (3) ffmpeg runs off the event loop (`run_in_threadpool`) writing to a temp file; if ffmpeg is missing or fails, return 400 media-error (fail closed — never commit an un-optimized file). (4) Commit ONLY the optimized bytes under `public/editors/<ident['slug']>/<uuid>.<ext>` via `sync_editors(current, images=[(out_name, out_bytes)])` — path from the SESSION slug only (D-08/T-10-10-06). Return `{ "path": "/editors/<slug>/<file>" }`. On `GitHubPublishError` → 502 with `_SAVE_FAILED_COPY`.
  </action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_app_editor.py -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "@app.post\(\"/editor/media\"\)" ../nocturna-bot/app/main.py` matches
    - Per-kind caps present: `grep -E "2 ?\* ?1024 ?\* ?1024|10 ?\* ?1024" ../nocturna-bot/app/main.py` matches (2MB image + 10MB video/gif caps)
    - Streamed early abort reused: `grep -E "file.read\(" ../nocturna-bot/app/main.py` shows a chunked read with a cap check for the new handler
    - Path from session slug only: `grep -E "ident\[.slug.\]" ../nocturna-bot/app/main.py` present in the media handler; no client path segment used
    - ffmpeg fail-closed: `grep -iE "ffmpeg" ../nocturna-bot/app/main.py` matches AND a failure path returns the media-error copy (no commit)
    - New tests cover over-cap reject + type reject + happy path (image at least): `python -m pytest tests/test_app_editor.py -q` passes
    - Full bot suite green: `cd ../nocturna-bot && python -m pytest -q` exits 0
  </acceptance_criteria>
  <done>/editor/media validates+caps+optimizes image/GIF/video and commits under the session slug; fails closed without ffmpeg.</done>
</task>

<task type="auto">
  <name>Task 2: POST /editor/audio (≤5MB cap, commit under slug)</name>
  <read_first>
    - ../nocturna-bot/app/main.py (this plan's Task 1 additions + the shared upload idiom)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-CONTEXT.md (D-06 audio, D-19 audio ≤5MB)
  </read_first>
  <action>
    Add `@app.post("/editor/audio")` `upload_audio(request, file, ident=Depends(require_editor))`: reject non-audio types (accept mp3/ogg/m4a/webm-audio by content-type+ext), stream-read with an early abort at a 5MB cap (D-19), optionally normalize/clip via ffmpeg (or commit as-is within cap if ffmpeg normalization is not applied — audio does not require transcode, but if over-cap always reject), and commit under `public/editors/<ident['slug']>/<uuid>.<ext>` via `sync_editors` (session slug only, D-08). Return `{ "path": "/editors/<slug>/<file>" }`. Reuse the too-large + save-failed copy. The theme panel (plan 10) writes the returned path into `page.theme.audio`.
  </action>
  <verify>
    <automated>cd ../nocturna-bot && python -m pytest tests/test_app_editor.py -q 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "@app.post\(\"/editor/audio\"\)" ../nocturna-bot/app/main.py` matches
    - 5MB cap present: `grep -E "5 ?\* ?1024 ?\* ?1024" ../nocturna-bot/app/main.py` matches
    - Non-audio rejected before commit: the handler checks content-type/extension against an audio allowlist
    - Path from session slug only (`grep -E "ident\[.slug.\]" ../nocturna-bot/app/main.py` in the audio handler)
    - Tests cover over-cap + type reject: `python -m pytest tests/test_app_editor.py -q` passes; full suite `python -m pytest -q` exits 0
  </acceptance_criteria>
  <done>/editor/audio caps at 5MB, rejects non-audio, and commits under the session slug for the theme panel to reference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| uploaded file bytes → ffmpeg/Pillow → repo commit | Editor-uploaded arbitrary media processed then committed. |
| ident.slug → commit path | Session-derived slug builds the storage path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-11-01 | DoS (decompression/oversize) | media/audio upload | mitigate | Streamed read with an early abort at the per-kind cap BEFORE buffering the full body (Pitfall 3); ffmpeg runs on a temp file off the event loop; Pillow's own bomb guard for raster (reused optimize_to_webp). |
| T-10.1-11-02 | Path traversal / IDOR | commit path | mitigate | Path built from `ident["slug"]` (session) only — never a client-supplied filename/path segment; slug is charset-constrained upstream (T-10-10-06 / D-08). |
| T-10.1-11-03 | Tampering (polyglot/type confusion) | non-media disguised as media | mitigate | Reject by content-type AND extension before decode; only ffmpeg/Pillow RE-ENCODED bytes are committed — the raw upload is never persisted; fail closed if ffmpeg is unavailable. |
| T-10.1-11-SC | Supply chain | ffmpeg system binary | accept | ffmpeg is a distro package on cinema (documented in user_setup + deploy doc), not a pip install — no package-legitimacy gate; no new pip dependency added. |
</threat_model>

<verification>
- `cd ../nocturna-bot && python -m pytest -q` exits 0 with new media/audio upload tests.
- Both endpoints enforce per-kind caps, reject non-media, commit under the session slug, and fail closed without ffmpeg.
</verification>

<success_criteria>
Editors can upload background image/GIF/video + audio; everything is validated, capped, optimized, and committed under their own slug — no path escape, no oversize, no un-optimized commit.
</success_criteria>

<output>
Create `.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-11-SUMMARY.md` when done.
</output>
