---
status: partial
phase: 10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi
source: [10.1-VERIFICATION.md]
started: 2026-07-16T19:22:00Z
updated: 2026-07-16T19:22:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. Live Discord presence dot (D-05)
expected: PresenceDot shows the correct online/idle/dnd/offline colour + activity name for a real published editor with a real Discord ID in the Lanyard guild.
why_human: The only seed entry (aria-test) has a placeholder discordId (000000000000000000); presence.ts is code-verified (correct Lanyard fetch, textContent-only writes, graceful hide-on-failure) but cannot be exercised against real presence data without a real editor in the Lanyard guild.
result: [pending]

### 2. Deployed view-counter endpoint (D-25)
expected: GET https://editors.nocturna-avatars.site/api/views/<slug>?hit=1 returns {count:n} and increments with a 6h dedup window; ViewCounter.astro reveals the count after splash dismiss.
why_human: counter_app.py + systemd/Caddy deploy artifacts are code-verified in nocturna-bot, but the endpoint only resolves once a human enables the unit + reloads Caddy on the cinema host (EDITOR_DEPLOY.md §8).
result: [pending]

### 3. Background media + audio upload endpoints (D-06/D-18/D-19)
expected: POST /editor/media transcodes GIF/video to a capped muted MP4 via ffmpeg (fails closed if ffmpeg is missing) and POST /editor/audio commits a capped audio file, both under the session slug.
why_human: Endpoints and their fail-closed ffmpeg logic are code-verified (12 endpoint tests), but ffmpeg must be installed on the cinema host before GIF/video uploads work in production — a documented human deploy step.
result: [pending]

### 4. Splash-gated background video/audio autoplay in a real browser (D-06/D-20/D-22)
expected: Tapping/clicking the splash 'Enter' CTA reliably starts muted looping background video and unlocks audio playback with volume control, on both desktop and mobile browsers.
why_human: splash.ts / audio.ts are code-verified (once-guarded editor:enter dispatch, shared play() path, muted-first unlock pattern) but actual browser autoplay-policy behavior can only be confirmed by a human in a real browser session.
result: [pending]

### 5. Multi-portfolio-block lightbox click-through (WR-02 regression check)
expected: On an editor page with two portfolio blocks, clicking any tile in the second block opens the lightbox at that tile's own photo, not an earlier block's photo.
why_human: The WR-02 fix (aa3d98f) is code-verified as logically correct (global tile index from querySelectorAll document order), but no seeded editor currently has two portfolio blocks to exercise this end-to-end.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
