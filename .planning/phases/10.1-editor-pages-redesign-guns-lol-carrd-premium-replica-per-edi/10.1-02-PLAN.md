---
phase: "10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/editor-theme.css
  - src/styles/editor-fonts.css
  - public/fonts/editors/
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-11, D-15, D-21, D-26]
user_setup: []
must_haves:
  truths:
    - "An editor page can render in any of the 14 curated fonts, self-hosted, with only the chosen family loaded per page (D-03)"
    - "The theme token system defines all 13 --theme-* custom properties with Midnight-Nocturna defaults so an editor with no theme still renders (D-26)"
    - "Glassmorphism, avatar glow/ring, animated username gradient, link-button hover, button style×shape, and background overlay/blur/tint are available as CSS driven by theme tokens (D-04/D-11/D-21)"
    - "Every motion effect collapses under prefers-reduced-motion while overlay/blur/tint stay on (Effects Catalog global rule)"
  artifacts:
    - path: "src/styles/editor-theme.css"
      provides: "Theme token defaults + CSS effect system + reduced-motion guard"
      contains: "--theme-accent"
    - path: "src/styles/editor-fonts.css"
      provides: "@font-face declarations + font-key → family stack map for the 14 curated fonts"
      contains: "@font-face"
    - path: "public/fonts/editors/"
      provides: "Self-hosted woff2 files for the 14 curated families"
  key_links:
    - from: "editor-theme.css button styles"
      to: "--theme-btn-style / --theme-btn-shape"
      via: "attribute/data-driven CSS"
      pattern: "--theme-btn-(style|shape)"
---

<objective>
Build the standalone visual system that gives editor pages their own identity: the CSS theme-token layer (all 13 `--theme-*` properties with Midnight-Nocturna defaults, D-26), the full premium CSS effect suite driven by those tokens (glassmorphism, avatar glow/ring, animated username gradient, link-button hover, button style×shape matrix, background overlay/blur/tint — D-04/D-11/D-21), the global reduced-motion guard, and the 14 self-hosted curated fonts (D-03) with `@font-face` declarations and a font-key→stack map.

Purpose: Editor pages import NONE of the brand `theme.css` (D-15); this file IS their design system. Shell components (03), blocks (05), and the layout (06) all read these tokens.
Output: `src/styles/editor-theme.css`, `src/styles/editor-fonts.css`, `public/fonts/editors/<family>/*.woff2`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md
@src/styles/theme.css

<notes>
- The existing `theme.css` `@theme` block shows how the project declares spacing (`--space-*`), `--touch-min`, and color tokens. Editor pages ADOPT `--space-*` + `--touch-min` (UI-SPEC Spacing) but define their OWN `--theme-*` color/font/button tokens. Do NOT `@import "tailwindcss"` here — this is a plain CSS partial the standalone layout imports directly.
- Fonts are OFL/Apache (UI-SPEC Curated Font Palette licensing). Fetch woff2 from Google Fonts' woff2 CDN or the upstream repos; subset latin+latin-ext; `font-display: swap`.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Acquire the 14 curated fonts + write @font-face + font-key map</name>
  <read_first>
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Curated Font Palette table — the 14 families + weights loaded; Typography §curated-font weight-fallback rule)
    - src/layouts/BaseLayout.astro (L54-59 — the Google-Fonts `<link>` pattern being REPLACED by self-hosting)
  </read_first>
  <action>
    Download woff2 files for the 14 families in the UI-SPEC Curated Font Palette (Inter, Space Grotesk, Sora, Playfair Display, Fraunces, Bebas Neue, UnifrakturMaguntia, Pirata One, Caveat, Permanent Marker, Press Start 2P, VT323, Orbitron, Rajdhani) at the weights listed (400+700 for variable/two-weight families; 400-only for the single-weight display faces: Bebas Neue, UnifrakturMaguntia, Pirata One, Permanent Marker, Press Start 2P, VT323). Store each under `public/fonts/editors/<family-kebab>/<file>.woff2` (subset latin+latin-ext where the source offers it). Create `src/styles/editor-fonts.css` with one `@font-face` per file (`font-display: swap`, correct `font-weight`, `src: url('/fonts/editors/<family>/<file>.woff2') format('woff2')`) and a CSS custom-property font-key map at `:root` mapping each font key from editorTypes (`Inter`, `Space Grotesk`, …) to a full family stack with a generic fallback (e.g. `--font-editor-inter: 'Inter', system-ui, sans-serif;`). The layout (06) will set `--theme-font` to the chosen family stack. Do NOT preload all 14 — the layout preloads only the editor's chosen family (per-page).
  </action>
  <verify>
    <automated>ls public/fonts/editors/ | wc -l | grep -qE "1[0-4]" && grep -c "@font-face" src/styles/editor-fonts.css</automated>
  </verify>
  <acceptance_criteria>
    - `ls public/fonts/editors/` lists 14 family directories
    - Every family directory contains at least one `.woff2` file: `find public/fonts/editors -name '*.woff2' | wc -l` >= 14
    - `grep -c "@font-face" src/styles/editor-fonts.css` returns >= 14
    - `grep -c "font-display: swap" src/styles/editor-fonts.css` equals the `@font-face` count (every face is swap)
    - `grep -E "Press Start 2P|VT323|UnifrakturMaguntia" src/styles/editor-fonts.css` matches (the display/pixel/blackletter vibes are present)
  </acceptance_criteria>
  <done>14 self-hosted families under public/fonts/editors; editor-fonts.css declares every face (swap) + a font-key→stack map.</done>
</task>

<task type="auto">
  <name>Task 2: Theme token defaults + CSS effect suite + reduced-motion guard</name>
  <read_first>
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Theme Token Contract table = the 13 token defaults; Effects Catalog = each effect's technique + reduced-motion fallback; Color §default theme Midnight Nocturna; Typography roles)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md (Shared Pattern: Theme Token System; Reduced-motion)
    - src/components/editor-blocks/LinksBlock.astro (L76-80 — the reduced-motion pattern to mirror)
  </read_first>
  <action>
    Create `src/styles/editor-theme.css`. (a) A `:root` (or `.editor-page`) block declaring all 13 `--theme-*` tokens at their Midnight-Nocturna DEFAULTS (per the Theme Token Contract table: `--theme-bg:#0a0c14; --theme-overlay:40; --theme-blur:0; --theme-surface:rgba(240,234,228,0.06); --theme-accent:#c0192c; --theme-text:#f0eae4; --theme-text-muted:#9a9198; --theme-btn-style:glass; --theme-btn-shape:rounded;` and `--theme-bg-media`/`--theme-tint` default to none). The layout (06) overrides these inline per editor; defaults here guarantee D-26 clean-cutover rendering. (b) The CSS effect classes, ALL reading theme tokens (never brand `--color-*`): glassmorphism card (`backdrop-filter: blur()` + `--theme-surface`), avatar glow/ring (accent box-shadow + conic ring), animated username gradient (`background-clip:text` keyframes), link-button hover (transform/sheen), the button style×shape matrix keyed off `--theme-btn-style`/`--theme-btn-shape` (filled/outline/glass × sharp 0 / rounded 12px / pill 999px), and the background media treatment layer (overlay scrim from `--theme-overlay`, `--theme-blur`, `--theme-tint`). Adopt `--space-*` and `--touch-min` by NOT redefining them (the layout imports theme.css tokens? NO — editor pages don't; so re-declare the specific `--space-*`/`--touch-min` values the effects need, sourced from theme.css, since brand theme.css is not imported on editor pages). (c) A single `@media (prefers-reduced-motion: reduce)` block that disables every motion effect (gradient anim, glow pulse, hover transforms, sheen) while LEAVING overlay/blur/tint active (they are legibility, not motion). Do NOT include typewriter/tilt/particles here (those are JS, plan 06). Keep every declaration token-driven so free per-editor theming works with zero hard-coded colors.
  </action>
  <verify>
    <automated>grep -c "\-\-theme-" src/styles/editor-theme.css && grep -q "prefers-reduced-motion" src/styles/editor-theme.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 13 tokens declared with defaults: `grep -oE "\-\-theme-[a-z-]+" src/styles/editor-theme.css | sort -u | wc -l` >= 11 (bg-media/tint may be `none`)
    - `grep -E "backdrop-filter" src/styles/editor-theme.css` matches (glassmorphism) AND `grep -E "background-clip: ?text|-webkit-background-clip: ?text" src/styles/editor-theme.css` matches (username gradient)
    - Button matrix present: `grep -E "\-\-theme-btn-(style|shape)" src/styles/editor-theme.css` matches both
    - Background treatment reads all three D-21 knobs: `grep -E "\-\-theme-(overlay|blur|tint)" src/styles/editor-theme.css` matches all three
    - Exactly one reduced-motion block that does NOT disable overlay/blur/tint: `grep -c "prefers-reduced-motion" src/styles/editor-theme.css` returns 1
    - No brand color leakage: `grep -c "var(--color-red)" src/styles/editor-theme.css` returns 0
  </acceptance_criteria>
  <done>editor-theme.css declares all theme tokens at Midnight-Nocturna defaults, the CSS effect suite (all token-driven), and one reduced-motion guard that keeps legibility knobs on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor.theme values → CSS custom properties | Editor-controlled color/font/effect values become CSS at render; a malformed value could break out of a declaration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-02-01 | Tampering/XSS | Token values flowing into CSS (`--theme-bg-media: url(...)`) | mitigate | This plan only defines the token CONSUMERS with defaults; the actual editor values are validated bot-side (plan 09 editors_model: hex-color / enum / numeric / safe-image-ref regex) and emitted through Astro attribute auto-escaping by the layout (plan 06). CSS here reads tokens via `var()` only — no editor string is interpolated into a CSS selector or `@import`. |
| T-10.1-02-SC | Supply chain | self-hosted fonts | mitigate | Fonts are static OFL/Apache woff2 assets committed to the repo (no runtime fetch, no third-party CDN at render). No npm/pip package added. |
</threat_model>

<verification>
- `npm run build` still succeeds (the partials are not yet imported anywhere; they compile as static assets).
- 14 font families present with woff2 files; editor-theme.css declares tokens + effects + reduced-motion.
</verification>

<success_criteria>
The standalone editor design system exists: 14 self-hosted fonts, theme-token defaults, full CSS effect suite driven only by `--theme-*` tokens, and a reduced-motion guard preserving legibility knobs.
</success_criteria>

<output>
Create `.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-02-SUMMARY.md` when done.
</output>
