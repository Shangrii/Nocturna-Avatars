---
phase: "10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi"
plan: "03"
type: execute
wave: 2
depends_on: ["10.1-01", "10.1-02"]
files_modified:
  - src/components/editor-shell/LinkButton.astro
  - src/components/editor-shell/SpecialtyBadges.astro
  - src/components/editor-shell/SocialIconRow.astro
  - src/components/editor-shell/EditorHeaderCard.astro
  - src/icons/social/
  - src/icons/ui/
autonomous: true
requirements: [D-02, D-07, D-09, D-10, D-11]
must_haves:
  truths:
    - "The header renders as a glass card: avatar with accent glow, name, specialty badges, presence slot, tagline, social icon row (D-10)"
    - "Social links auto-detect their platform from the URL and render a recognizable self-hosted icon; unknown hosts fall back to a generic glyph (D-09)"
    - "Link buttons render in the editor's chosen style×shape from theme tokens, with one accent hover, no per-button overrides (D-11)"
    - "Specialty badges render only from the curated team-defined set (D-07)"
    - "Every editor-supplied URL opens in a new tab with rel=noopener noreferrer nofollow and all text renders through auto-escaping (D-02)"
  artifacts:
    - path: "src/components/editor-shell/EditorHeaderCard.astro"
      provides: "Glass header card composing avatar/name/badges/presence/tagline/socials"
      contains: "editor-header-card"
    - path: "src/components/editor-shell/SocialIconRow.astro"
      provides: "URL→platform icon row"
      contains: "hostname"
    - path: "src/components/editor-shell/LinkButton.astro"
      provides: "style×shape link button"
    - path: "src/components/editor-shell/SpecialtyBadges.astro"
      provides: "curated badge pills"
  key_links:
    - from: "SocialIconRow"
      to: "hostname → Simple Icons glyph map"
      via: "URL parse"
      pattern: "hostname"
---

<objective>
Build the four static transform components of the guns.lol header shell, all reading `--theme-*` tokens (never brand colors): `LinkButton` (style×shape matrix, D-11), `SpecialtyBadges` (curated set, D-07), `SocialIconRow` (URL→platform icon, D-09), and `EditorHeaderCard` (the glass card composing them, D-10). Self-host the Simple Icons (social) + Lucide (UI) SVG subset these need.

Purpose: These are the reusable pieces the `/e/<slug>` route (07) assembles into the header. Interface-first: the header card exposes named slots for `PresenceDot` (plan 04) so 03 and 04 stay parallel.
Output: `src/components/editor-shell/{LinkButton,SpecialtyBadges,SocialIconRow,EditorHeaderCard}.astro`, `src/icons/{social,ui}/*.svg`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md
@src/components/editor-blocks/LinksBlock.astro
@src/lib/editorTypes.ts

<interfaces>
<!-- From editorTypes (plan 01): Editor.badges:string[], Editor.socials:EditorLink[], EditorTheme.btnStyle/btnShape.
     EditorLink = { label:string; url:string }. -->
<!-- EditorHeaderCard exposes a NAMED SLOT `presence` that plan 04's PresenceDot fills; leave it empty-safe. -->
<!-- Theme tokens consumed: --theme-accent, --theme-surface, --theme-text, --theme-text-muted, --theme-font,
     --theme-btn-style, --theme-btn-shape (defined in editor-theme.css, plan 02). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LinkButton (style×shape matrix) + SpecialtyBadges (curated set)</name>
  <read_first>
    - src/components/editor-blocks/LinksBlock.astro (the anchor + rel + escaping + 44px + reduced-motion pattern to copy EXACTLY)
    - src/components/editor-blocks/PortfolioBlock.astro (L189-203 — the pill-badge CSS to mirror, recolored to theme tokens)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Theme Token Contract button rules; SpecialtyBadges component note; Copywriting for badge set)
    - src/styles/editor-theme.css (the button style×shape CSS + badge classes from plan 02)
  </read_first>
  <action>
    LinkButton.astro: props `{ label: string; url: string }`. Render an `<a href={url} target="_blank" rel="noopener noreferrer nofollow">` with label via `{}` auto-escaping, `min-height: var(--touch-min)`, class hooks that let editor-theme.css apply the style (filled/outline/glass) × shape (sharp 0 / rounded 12px / pill 999px) from `--theme-btn-style`/`--theme-btn-shape`; colors from `--theme-accent`/`--theme-surface`/`--theme-text` only. No per-button overrides (D-11). Reduced-motion kills the transition (mirror LinksBlock L76-80).
    SpecialtyBadges.astro: props `{ badges: string[] }`. Filter to the curated team-defined set (import the same badge-key→label map used in UI-SPEC — Outfits/Texturas/Blender/Dances/…; render label via `{}` auto-escaping); unknown keys are dropped (no free-form text, D-07). Pill styling from editor-theme.css recolored to `--theme-accent`/`--theme-surface`. Render nothing when the filtered list is empty.
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rel=\"noopener noreferrer nofollow\"" src/components/editor-shell/LinkButton.astro` returns 1
    - `grep -E "var\(--theme-btn-(style|shape)\)|editor-linkbtn" src/components/editor-shell/LinkButton.astro` matches (theme-driven variants)
    - `grep -c "var(--color-red)" src/components/editor-shell/LinkButton.astro src/components/editor-shell/SpecialtyBadges.astro` returns 0 (no brand color)
    - SpecialtyBadges drops unknown keys: the badge-key allowlist is present (`grep -iE "Outfits|Blender|Dances" src/components/editor-shell/SpecialtyBadges.astro` matches)
    - No raw-HTML directive: `grep -c "set:html" src/components/editor-shell/LinkButton.astro src/components/editor-shell/SpecialtyBadges.astro` returns 0
  </acceptance_criteria>
  <done>LinkButton renders theme-driven style×shape with safe outbound rel; SpecialtyBadges renders only curated keys; both token-only, escaped, reduced-motion-safe.</done>
</task>

<task type="auto">
  <name>Task 2: SocialIconRow (URL→platform icon) + self-hosted icon subset</name>
  <read_first>
    - src/components/editor-blocks/PortfolioBlock.astro (L54-59, L66-107 — the source-map/detection pattern to mirror)
    - src/components/LightboxOverlay.astro (inline `<svg>` pattern L42-45 — self-hosted inline SVG, no icon-font/CDN)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Icon Contract — Simple Icons for social, Lucide for UI, URL→platform map, generic fallback)
  </read_first>
  <action>
    Save an SVG subset under `src/icons/social/<platform>.svg` (Simple Icons glyphs: tiktok, youtube, twitch, x, instagram, discord, twitter, github, kofi, bluesky, threads — cover the platforms an editor is likely to use) and `src/icons/ui/<name>.svg` (Lucide: link/globe for the generic fallback; volume/close/eye reserved for plans 04/07). Build SocialIconRow.astro: props `{ socials: EditorLink[] }`. For each social, parse `new URL(url)` defensively (skip malformed), lowercase the hostname, match against a `hostname → platform` map (e.g. `tiktok.com→tiktok`, `youtube.com/youtu.be→youtube`, `x.com/twitter.com→x`, …); unknown host → Lucide `globe`/`link` generic glyph (D-09). Render each as an `<a href={url} target="_blank" rel="noopener noreferrer nofollow" aria-label={platform-or-host}>` wrapping the inline SVG (import the SVG as a raw string via Astro/Vite `?raw` and inline it — the SVG assets are trusted, first-party files, NOT editor content, so inlining them is safe; the editor's URL is never injected into markup except as the escaped href). Icon color = `--theme-text` at rest, `--theme-accent` on hover; 44px touch target. Render nothing when socials is empty.
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5 && ls src/icons/social | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `find src/icons -name '*.svg' | wc -l` >= 8 (self-hosted subset present)
    - `grep -iE "tiktok|youtube|instagram" src/components/editor-shell/SocialIconRow.astro` matches (platform map present)
    - Generic fallback exists: `grep -iE "globe|link|generic|fallback" src/components/editor-shell/SocialIconRow.astro` matches
    - Every social anchor is `rel="noopener noreferrer nofollow"`: `grep -c "noopener noreferrer nofollow" src/components/editor-shell/SocialIconRow.astro` >= 1
    - The editor URL is only ever an href/attribute, never inlined as raw HTML: `grep -c "set:html={.*url" src/components/editor-shell/SocialIconRow.astro` returns 0
  </acceptance_criteria>
  <done>SocialIconRow maps URLs to self-hosted platform glyphs with a generic fallback, safe outbound rels, theme-token colors.</done>
</task>

<task type="auto">
  <name>Task 3: EditorHeaderCard (glass card composition)</name>
  <read_first>
    - src/pages/[lang]/[concept]/[slug].astro (L92-121 header markup + L155-227 CSS — the avatar/name/tagline stack being extracted into a glass card)
    - src/components/editor-shell/LinkButton.astro, SpecialtyBadges.astro, SocialIconRow.astro (this plan's Tasks 1-2)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Component Inventory EditorHeaderCard; Typography Display/Name role; Effects Catalog avatar glow/ring, animated username gradient)
  </read_first>
  <action>
    Build EditorHeaderCard.astro: props `{ name: string; avatar: string; tagline: string; badges: string[]; socials: EditorLink[]; effects: string[] }`. Render a glassmorphism card (class hooks that editor-theme.css styles from `--theme-surface`) containing, in order: circular avatar `clamp(120px,30vw,160px)` with the accent glow/ring applied when `effects` includes the glow key; the name as Display role (`var(--theme-font)`, Name size, with the animated-username-gradient class applied only when `effects` includes the gradient key); `<SpecialtyBadges badges={badges} />`; a NAMED SLOT `presence` (empty-safe — plan 04's PresenceDot fills it); the tagline (`--theme-text-muted`, with a `data-typewriter` hook when `effects` includes typewriter so plan 06's effects.ts can drive it, static otherwise); and `<SocialIconRow socials={socials} />`. All text via `{}` auto-escaping. Do NOT render the header custom `links` here — those are the `LinkButton` list the route places below the card. Reduced-motion handled centrally by editor-theme.css.
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "SpecialtyBadges|SocialIconRow" src/components/editor-shell/EditorHeaderCard.astro` matches both imports (composition)
    - `grep -E "slot=\"presence\"|name=\"presence\"" src/components/editor-shell/EditorHeaderCard.astro` matches (presence slot for plan 04)
    - `grep -E "data-typewriter" src/components/editor-shell/EditorHeaderCard.astro` matches (typewriter hook, effect-gated)
    - `grep -c "var(--color-red)" src/components/editor-shell/EditorHeaderCard.astro` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>EditorHeaderCard composes avatar+glow, gradient-capable name, badges, presence slot, tagline (typewriter-capable), social row — all theme-token driven and escaped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor.socials/badges/name/tagline → header markup | Editor-controlled strings rendered in the header card. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-03-01 | XSS | name/tagline/badge/social label | mitigate | All editor strings render through Astro `{}` auto-escaping; no `set:html` anywhere (D-02 invariant preserved). Icon SVGs are first-party trusted assets inlined via `?raw`; editor URLs appear only as escaped `href`. |
| T-10.1-03-02 | Info disclosure / tabnabbing | social + link anchors | mitigate | Every editor-supplied outbound URL carries `rel="noopener noreferrer nofollow" target="_blank"` (T-10-04-02 pattern). |
| T-10.1-03-SC | Supply chain | icon assets | mitigate | Icons are static first-party SVG files committed to the repo; no icon-font package or CDN. No npm dep added. |
</threat_model>

<verification>
- `npm run build` exits 0 with the four components compiling.
- Icon subset present under src/icons.
</verification>

<success_criteria>
The header shell components exist, compose into a glass card with a presence slot, render only theme tokens + escaped editor content, and carry safe outbound rels.
</success_criteria>

<output>
Create `.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-03-SUMMARY.md` when done.
</output>
