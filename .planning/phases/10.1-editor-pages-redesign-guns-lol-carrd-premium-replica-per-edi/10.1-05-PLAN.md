---
phase: "10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi"
plan: "05"
type: execute
wave: 2
depends_on: ["10.1-01", "10.1-02"]
files_modified:
  - src/components/editor-blocks/BioBlock.astro
  - src/components/editor-blocks/HeadingBlock.astro
  - src/components/editor-blocks/TextBlock.astro
  - src/components/editor-blocks/QuoteBlock.astro
  - src/components/editor-blocks/ImageBlock.astro
  - src/components/editor-blocks/LinksBlock.astro
  - src/components/editor-blocks/BlockRenderer.astro
  - src/components/editor-blocks/PortfolioBlock.astro
autonomous: true
requirements: [D-02, D-10, D-11, D-12]
must_haves:
  truths:
    - "Every block component renders using --theme-* tokens instead of brand tokens, so blocks inherit the editor's theme (D-10)"
    - "The portfolio block renders as a 2-3 column compact thumbnail grid whose cells open the shared gallery lightbox (D-12)"
    - "The build-time SFW-only credited auto-pull is unchanged; NSFW stays fully excluded"
    - "No block introduces a raw-HTML directive — the D-02 XSS guarantee survives the restyle"
  artifacts:
    - path: "src/components/editor-blocks/PortfolioBlock.astro"
      provides: "2-3 col grid + lightbox tile hooks"
      contains: "data-gallery-tile"
    - path: "src/components/editor-blocks/BlockRenderer.astro"
      provides: "theme-threaded block dispatch"
  key_links:
    - from: "PortfolioBlock cells"
      to: "gallery.ts lightbox"
      via: "data-gallery-tile / data-index / data-file hooks"
      pattern: "data-gallery-tile"
---

<objective>
Refactor the seven Phase-10 block components + `BlockRenderer` from brand tokens to `--theme-*` tokens (a token-swap, NOT a rewrite — D-10), and upgrade `PortfolioBlock` to the compact 2–3 column thumbnail grid whose cells open the shared gallery lightbox (D-12) — while keeping the build-time SFW-only credited auto-pull and the D-02 no-`set:html` invariant byte-for-byte intact.

Purpose: Blocks render INSIDE the new themed column, so they must speak the theme-token language. The portfolio grid + lightbox reuse the existing `gallery.ts`/`LightboxOverlay` machinery with zero new JS.
Output: 8 restyled block components with portfolio grid + lightbox hooks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md
@src/components/editor-blocks/PortfolioBlock.astro
@src/components/editor-blocks/BlockRenderer.astro
@src/scripts/gallery.ts

<interfaces>
<!-- gallery.ts::initLightbox (L672-695) binds every [data-lightbox-overlay] + [data-gallery-tile]
     document-wide, deriving entries from each tile's dataset:
       tile.dataset.file, tile.dataset.caption, tile.dataset.index, inner <img alt>.
     Plan 07's route imports gallery.ts + mounts one <LightboxOverlay/>. This plan's PortfolioBlock
     must render each cell with: [data-gallery-tile] data-file=<img src> data-caption=<caption>
     data-index=<sequential> and an inner <img>. A <button> element gives free keyboard semantics. -->
<!-- Theme tokens replace brand tokens: --color-red→--theme-accent, --color-white→--theme-text,
     --color-white-dim→--theme-text-muted, --color-ink-raised→--theme-surface, --font-*→--theme-font.
     --space-* and --touch-min carry over (adopted in editor-theme.css, plan 02). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token-swap the 7 blocks + BlockRenderer (structure unchanged)</name>
  <read_first>
    - src/components/editor-blocks/BlockRenderer.astro (dispatch — thread theme through if needed; keep the divider red rule → --theme-accent)
    - src/components/editor-blocks/LinksBlock.astro (the reference block: anchor/rel/escaping/reduced-motion + the brand tokens to swap)
    - each of BioBlock/HeadingBlock/TextBlock/QuoteBlock/ImageBlock (their `<style>` blocks — the `var(--color-*)`/`var(--font-*)` references to swap)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md (§editor-blocks/* — token-swap refactor, load-bearing no-set:html invariant)
  </read_first>
  <action>
    In each of BioBlock, HeadingBlock, TextBlock, QuoteBlock, ImageBlock, LinksBlock, and BlockRenderer, replace every brand-token reference in the `<style>` blocks with the theme-token equivalent per the `<interfaces>` mapping (`--color-red`/`--color-red-on-ink`→`--theme-accent`; `--color-white`→`--theme-text`; `--color-white-dim`→`--theme-text-muted`; `--color-ink-raised`/`--color-ink`→`--theme-surface`/`--theme-bg`; `--font-display`/`--font-body`/`--font-mono`→`--theme-font`). Keep `--space-*` and `--touch-min` as-is. Do NOT touch any component's markup, defensive reads, prop shapes, or escaping — this is purely a CSS token swap. Preserve LinksBlock's `rel="noopener noreferrer nofollow"` + reduced-motion. Preserve the BlockRenderer divider red rule (now `--theme-accent`). Confirm NO `set:html`/`innerHTML` is introduced anywhere.
  </action>
  <verify>
    <automated>cd "." && grep -rlc "var(--color-red)\|var(--color-white)\|var(--font-display)" src/components/editor-blocks/ | grep -v PortfolioBlock || echo "no brand tokens remain in swapped blocks"; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - No brand color/font tokens remain in the six swapped blocks + BlockRenderer: `grep -rE "var\(--color-(red|white|ink)|var\(--font-(display|body|mono)\)" src/components/editor-blocks/BioBlock.astro src/components/editor-blocks/HeadingBlock.astro src/components/editor-blocks/TextBlock.astro src/components/editor-blocks/QuoteBlock.astro src/components/editor-blocks/ImageBlock.astro src/components/editor-blocks/LinksBlock.astro src/components/editor-blocks/BlockRenderer.astro` returns nothing
    - Theme tokens present: `grep -rc "var(--theme-" src/components/editor-blocks/BlockRenderer.astro` >= 1
    - No raw-HTML directive: `grep -rc "set:html" src/components/editor-blocks/` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 7 blocks + BlockRenderer read theme tokens only; markup/escaping/reads unchanged; build green.</done>
</task>

<task type="auto">
  <name>Task 2: PortfolioBlock → 2-3 col grid + lightbox tile hooks (auto-pull unchanged)</name>
  <read_first>
    - src/components/editor-blocks/PortfolioBlock.astro (WHOLE — keep L61-119 auto-pull logic byte-for-byte; change only the grid CSS + cell markup)
    - src/scripts/gallery.ts (L672-695 initLightbox — the dataset hooks each tile must expose)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Spacing §portfolio breakout max-width 560px; Interaction Contract portfolio thumbnail tap → lightbox)
  </read_first>
  <action>
    Keep the entire build-time credited auto-pull (store + gallery SFW-only join by exact slug, NSFW fully excluded, extra items, defensive reads) UNCHANGED. Change (a) the grid CSS: from `1fr → repeat(2,1fr)` into a compact 2–3 column thumbnail grid (2 col from 480px, 3 col from 640px), allowed to break out to max-width 560px; recolor all `var(--color-*)` to `--theme-*` (frame `--theme-surface`, badge `--theme-accent`, caption `--theme-text-muted`). (b) Make each cell a lightbox trigger: render the cell as a `<button type="button" data-gallery-tile data-file={item.image} data-caption={item.caption} data-index={i}>` wrapping the existing `<img>` (keep the credited badge + figcaption), so `gallery.ts::initLightbox` (imported + `<LightboxOverlay/>` mounted by plan 07) binds click/keyboard with zero new JS. `data-index` is the sequential map index. Captions/badges still render through `{}` auto-escaping. When the combined list is empty, still render NOTHING.
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Lightbox hooks present: `grep -E "data-gallery-tile" src/components/editor-blocks/PortfolioBlock.astro` matches AND `grep -E "data-file=|data-index=|data-caption=" src/components/editor-blocks/PortfolioBlock.astro` matches all three
    - Grid is multi-column with breakout: `grep -E "repeat\(2, ?1fr\)|repeat\(3, ?1fr\)" src/components/editor-blocks/PortfolioBlock.astro` matches AND `grep -E "560px" src/components/editor-blocks/PortfolioBlock.astro` matches
    - Auto-pull invariants intact: `grep -E "p.nsfw !== true" src/components/editor-blocks/PortfolioBlock.astro` matches AND `grep -E "g.editor === slug" src/components/editor-blocks/PortfolioBlock.astro` matches
    - No brand tokens remain: `grep -E "var\(--color-red\)|var\(--color-ink\)" src/components/editor-blocks/PortfolioBlock.astro` returns nothing
    - No `set:html`; `npm run build` exits 0
  </acceptance_criteria>
  <done>PortfolioBlock renders a 2-3 col themed grid whose cells carry the gallery-lightbox dataset hooks; the SFW credited auto-pull is preserved verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor block content + store/gallery data → block markup | Editor + staff/bot-written content rendered in blocks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-05-01 | XSS | block text/caption/portfolio caption | mitigate | Restyle touches CSS only; every content field still renders through Astro `{}` auto-escaping — no `set:html` introduced (D-02 invariant, verified by grep gate). |
| T-10.1-05-02 | Info disclosure | NSFW credited work on a public SFW page | mitigate | The auto-pull's NSFW exclusion (`p.nsfw !== true` / `g.nsfw !== true`) is preserved byte-for-byte (T-10-04-03). |
| T-10.1-05-SC | Supply chain | dependencies | accept | No new package; lightbox reuses existing gallery.ts. |
</threat_model>

<verification>
- `npm run build` exits 0; no brand tokens remain in any editor-block CSS; portfolio cells carry lightbox hooks.
</verification>

<success_criteria>
Blocks are fully theme-token driven, the portfolio is a themed 2-3 col grid wired to the shared lightbox, and the SFW auto-pull + D-02 invariant survive unchanged.
</success_criteria>

<output>
Create `.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-05-SUMMARY.md` when done.
</output>
