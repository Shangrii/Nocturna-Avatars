---
phase: "10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi"
plan: "08"
type: execute
wave: 2
depends_on: ["10.1-01"]
files_modified:
  - src/components/sections/EditorsDirectory.astro
  - src/components/ProductCard.astro
autonomous: true
requirements: [D-16, D-17]
must_haves:
  truths:
    - "The /editores·/editors directory still builds and lists published editors, restyled to tease the new look, marked noindex,follow, and unlisted from the navbar (D-16)"
    - "Every editor credit on a store product card links to /e/<slug> (D-17)"
  artifacts:
    - path: "src/components/sections/EditorsDirectory.astro"
      provides: "Restyled unlisted directory with noindex"
      contains: "noindex"
    - path: "src/components/ProductCard.astro"
      provides: "Editor credit as a link to /e/<slug>"
      contains: "editorPath"
  key_links:
    - from: "ProductCard editor credit"
      to: "/e/<slug>"
      via: "editorPath anchor"
      pattern: "editorPath"
---

<objective>
Complete the discovery half of the phase: restyle the `/editores`·`/editors` directory to tease the new premium look while keeping it unlisted (navbar removal already landed in plan 01) and adding `noindex,follow` (D-16), and turn the store product-card editor credit into a link to the canonical `/e/<slug>` page (D-17) — the organic-discovery path that replaces the removed navbar entry.

Purpose: With no navbar entry, store/gallery credits + the directory are how visitors reach editor pages. The directory stays on the Nocturna brand system (it is site chrome, not a standalone editor page).
Output: restyled `EditorsDirectory.astro`, linked `ProductCard.astro` credit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md
@src/components/sections/EditorsDirectory.astro
@src/components/ProductCard.astro
@src/i18n/routes.ts

<interfaces>
<!-- editorPath (repointed in plan 01) now returns /e/<slug>; the directory already calls
     editorPath(editor.slug, lang) at L79 → it auto-updates to /e/<slug> with no change needed there.
     ProductCard L98 currently renders `{editorCredit} {product.editor}` as PLAIN TEXT — wrap it. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the directory + add noindex,follow (unlisted, teases the new look)</name>
  <read_first>
    - src/components/sections/EditorsDirectory.astro (WHOLE — keep the defensive published-filter L46-63 + editorPath links L79; restyle the cards)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Color §fixed chrome directory = Nocturna brand; Interaction Contract Discovery/SEO directory = noindex,follow; Copywriting directory headings/empty state)
  </read_first>
  <action>
    Keep the defensive published-filter + sort + `editorPath(editor.slug, lang)` links UNCHANGED (they now emit `/e/<slug>` via plan 01). Restyle the `.editor-card` grid to tease the new premium look — glass cards + editor-avatar tiles — while staying on the Nocturna brand tokens (ink bg, red accent, off-white text; this is site chrome, NOT a standalone editor page, so it KEEPS BaseLayout + brand `--color-*`). Add `noindex,follow` robots for this page: since the directory renders through `[lang]/[page].astro` under BaseLayout (which sets no per-page robots), inject a `<meta name="robots" content="noindex,follow">` from within the directory section (Astro hoists head-level meta) OR add it via the page. Keep the EmptyState fallback + "Abrir Ticket" reachability. Copy renders through `{}` auto-escaping (unchanged).
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -8 && grep -rn "editores" dist/en/ 2>/dev/null | head -1 || true</automated>
  </verify>
  <acceptance_criteria>
    - Directory still builds: `npm run build` exits 0 and emits `/es/editores` + `/en/editors` (dist has `dist/es/editores/index.html` and `dist/en/editors/index.html`)
    - `grep -E "noindex" src/components/sections/EditorsDirectory.astro` matches (or the robots meta is present in the built dist directory pages)
    - Links point at the vanity URL: the built directory page contains `href="/e/` (`grep -rc 'href="/e/' dist/en/editors/index.html` >= 1)
    - Defensive filter intact: `grep -E "e.published === true" src/components/sections/EditorsDirectory.astro` matches
    - No `set:html`: `grep -c "set:html" src/components/sections/EditorsDirectory.astro` returns 0
  </acceptance_criteria>
  <done>The directory builds, is restyled + noindex,follow + unlisted, and links to /e/<slug>.</done>
</task>

<task type="auto">
  <name>Task 2: Store product-card editor credit → link to /e/<slug></name>
  <read_first>
    - src/components/ProductCard.astro (L98 credit line `{editorCredit} {product.editor}`; the props incl. `editor: string`; check whether `lang` is available in props — editorPath needs a lang arg even though it now ignores it)
    - src/i18n/routes.ts (editorPath signature — lang param retained)
  </read_first>
  <action>
    Wrap the editor name in ProductCard's credit line in an anchor to `editorPath(product.editor, lang)` (→ `/e/<slug>`), keeping the `{editorCredit}` label prefix as plain text before the link. Import `editorPath` from `../i18n/routes`. If `lang` is not currently a ProductCard prop, pass a literal that satisfies the signature (editorPath ignores lang post-plan-01) or thread `lang` from the parent — prefer threading `lang` if the parent (StorePage/ProductCard usage) already has it; otherwise call `editorPath(product.editor, 'en')` since the slug URL is lang-agnostic now. The credit name still renders through `{}` auto-escaping. Only render the link when `product.editor` is a non-empty string (guard — some products have no editor). Add a subtle hover style consistent with the card.
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "editorPath\(product.editor" src/components/ProductCard.astro` matches (credit is a vanity link)
    - The link is guarded on a non-empty editor: `grep -E "product.editor &&|product.editor\?" src/components/ProductCard.astro` matches
    - A built store page with a credited product contains `href="/e/`: after build, `grep -rc 'product-card__editor' dist/en/store/index.html` >= 1 (credit line still present)
    - Name still escaped (no set:html): `grep -c "set:html" src/components/ProductCard.astro` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Store editor credits link to /e/<slug>; guarded on presence; escaped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editors.json / store.json → directory + card markup | Bot/staff-written data rendered as links + text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-08-01 | XSS | editor name/slug in credit + directory | mitigate | Names render via `{}` auto-escaping; the slug is placed into `editorPath()` which builds `/e/<slug>` — the slug is charset-constrained upstream (editors_model normalize_slug `[a-z0-9-]`), so it cannot break the href. No `set:html`. |
| T-10.1-08-02 | Info disclosure | directory indexing | mitigate | Directory set to `noindex,follow` (unlisted but crawlable via links) per D-16. |
| T-10.1-08-SC | Supply chain | dependencies | accept | No new package. |
</threat_model>

<verification>
- `npm run build` exits 0; directory builds with noindex + /e/ links; store credits link to /e/<slug>.
</verification>

<success_criteria>
The directory is restyled, unlisted, noindex,follow, and links to /e/<slug>; store product credits link to /e/<slug> — organic discovery works without a navbar entry.
</success_criteria>

<output>
Create `.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-08-SUMMARY.md` when done.
</output>
