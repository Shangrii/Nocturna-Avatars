---
phase: "10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi"
plan: "08"
type: execute
wave: 2
depends_on: ["10.1-01"]
files_modified:
  - src/components/sections/EditorsDirectory.astro
  - src/components/ProductCard.astro
  - src/components/sections/GalleryPage.astro
  - src/components/sections/FeaturedGallery.astro
  - src/components/LightboxOverlay.astro
  - src/scripts/gallery.ts
autonomous: true
requirements: [D-16, D-17]
must_haves:
  truths:
    - "The /editores·/editors directory still builds and lists published editors, restyled to tease the new look, marked noindex,follow, and unlisted from the navbar (D-16)"
    - "Every editor credit on a store product card links to /e/<slug> (D-17)"
    - "A gallery photo credited to an editor (entry.editor present) shows a 'por/by <editor>' credit link to /e/<slug> in the lightbox, on both /galeria and the landing featured grid (D-17 gallery half)"
    - "Uncredited gallery photos show no credit element at all"
  artifacts:
    - path: "src/components/sections/EditorsDirectory.astro"
      provides: "Restyled unlisted directory with noindex"
      contains: "noindex"
    - path: "src/components/ProductCard.astro"
      provides: "Editor credit as a link to /e/<slug>"
      contains: "editorPath"
    - path: "src/components/LightboxOverlay.astro"
      provides: "Lightbox editor-credit anchor slot"
      contains: "data-lightbox-credit"
    - path: "src/scripts/gallery.ts"
      provides: "Credit link population from tile data-editor"
      contains: "data-editor"
  key_links:
    - from: "ProductCard editor credit"
      to: "/e/<slug>"
      via: "editorPath anchor"
      pattern: "editorPath"
    - from: "gallery tile data-editor"
      to: "lightbox credit anchor href /e/<slug>"
      via: "gallery.ts entry derivation + textContent/setAttribute"
      pattern: "data-editor"
---

<objective>
Complete the discovery half of the phase: restyle the `/editores`·`/editors` directory to tease the new premium look while keeping it unlisted (navbar removal already landed in plan 01) and adding `noindex,follow` (D-16); turn the store product-card editor credit into a link to the canonical `/e/<slug>` page; and add the MISSING gallery half of D-17 — gallery entries carrying an `editor` slug (the 10-06 `/galeria creditar` field) currently render NO credit anywhere, so this plan adds a credit link in the lightbox (fed by a new `data-editor` tile attribute on both `/galeria` and the landing featured grid).

Purpose: With no navbar entry, store/gallery credits + the directory are how visitors reach editor pages. Gallery tiles are `<button>` elements positioned by the scatter engine — an anchor cannot nest inside them and wrapping them breaks scatter positioning — so the credit renders in the lightbox caption area (the checker-approved placement), which is where a visitor engages an individual photo.
Output: restyled `EditorsDirectory.astro`, linked `ProductCard.astro` credit, `data-editor` on gallery tiles, lightbox credit anchor populated by `gallery.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md
@src/components/sections/EditorsDirectory.astro
@src/components/ProductCard.astro
@src/components/sections/GalleryPage.astro
@src/components/sections/FeaturedGallery.astro
@src/components/LightboxOverlay.astro
@src/scripts/gallery.ts
@src/i18n/routes.ts

<interfaces>
<!-- editorPath (repointed in plan 01) now returns /e/<slug>; the directory already calls
     editorPath(editor.slug, lang) at L79 → it auto-updates to /e/<slug> with no change needed there.
     ProductCard L98 currently renders `{editorCredit} {product.editor}` as PLAIN TEXT — wrap it. -->
<!-- GALLERY CREDIT DATA FLOW (new):
     gallery.json entries may carry an optional `editor` slug (10-06 /galeria creditar field).
     GalleryPage.astro L112-119 + FeaturedGallery.astro L90-93 render tiles as
     <button data-gallery-tile data-file data-caption data-index> — ADD data-editor={entry.editor ?? ''}.
     gallery.ts::initLightbox (L672-684) derives entries from tile datasets — ADD editor: tile.dataset.editor ?? ''.
     LightboxOverlay.astro figcaption.lightbox__meta (L60-63) gains
     <a class="lightbox__credit" data-lightbox-credit hidden></a> — the render path in
     gallery.ts populates its text via textContent and href via setAttribute, unhiding only when editor
     is a non-empty [a-z0-9-]+ slug. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the directory + add noindex,follow (unlisted, teases the new look)</name>
  <read_first>
    - src/components/sections/EditorsDirectory.astro (WHOLE — keep the defensive published-filter L46-63 + editorPath links L79; restyle the cards)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Color §fixed chrome directory = Nocturna brand; Interaction Contract Discovery/SEO directory = noindex,follow; Copywriting directory headings/empty state)
  </read_first>
  <action>
    Keep the defensive published-filter + sort + `editorPath(editor.slug, lang)` links UNCHANGED (they now emit `/e/<slug>` via plan 01). Restyle the `.editor-card` grid to tease the new premium look — glass cards + editor-avatar tiles — while staying on the Nocturna brand tokens (ink bg, red accent, off-white text; this is site chrome, NOT a standalone editor page, so it KEEPS BaseLayout + brand `--color-*`). Add `noindex,follow` robots for this page: since the directory renders through `[lang]/[page].astro` under BaseLayout (which sets no per-page robots), inject a `<meta name="robots" content="noindex,follow">` from within the directory section (Astro hoists head-level meta) OR add it via the page. Keep the EmptyState fallback + "Abrir Ticket" reachability. Copy renders through `{}` auto-escaping (unchanged).
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -8 && grep -rn "editores" dist/en/ 2>/dev/null | head -1 || true</automated>
  </verify>
  <acceptance_criteria>
    - Directory still builds: `npm run build` exits 0 and emits `/es/editores` + `/en/editors` (dist has `dist/es/editores/index.html` and `dist/en/editors/index.html`)
    - `grep -E "noindex" src/components/sections/EditorsDirectory.astro` matches (or the robots meta is present in the built dist directory pages)
    - Links point at the vanity URL: the built directory page contains `href="/e/` (`grep -rc 'href="/e/' dist/en/editors/index.html` >= 1)
    - Defensive filter intact: `grep -E "e.published === true" src/components/sections/EditorsDirectory.astro` matches
    - No `set:html`: `grep -c "set:html" src/components/sections/EditorsDirectory.astro` returns 0
  </acceptance_criteria>
  <done>The directory builds, is restyled + noindex,follow + unlisted, and links to /e/<slug>.</done>
</task>

<task type="auto">
  <name>Task 2: Store product-card editor credit → link to /e/<slug></name>
  <read_first>
    - src/components/ProductCard.astro (L98 credit line `{editorCredit} {product.editor}`; the props incl. `editor: string`; check whether `lang` is available in props — editorPath needs a lang arg even though it now ignores it)
    - src/i18n/routes.ts (editorPath signature — lang param retained)
  </read_first>
  <action>
    Wrap the editor name in ProductCard's credit line in an anchor to `editorPath(product.editor, lang)` (→ `/e/<slug>`), keeping the `{editorCredit}` label prefix as plain text before the link. Import `editorPath` from `../i18n/routes`. If `lang` is not currently a ProductCard prop, pass a literal that satisfies the signature (editorPath ignores lang post-plan-01) or thread `lang` from the parent — prefer threading `lang` if the parent (StorePage/ProductCard usage) already has it; otherwise call `editorPath(product.editor, 'en')` since the slug URL is lang-agnostic now. The credit name still renders through `{}` auto-escaping. Only render the link when `product.editor` is a non-empty string (guard — some products have no editor). Add a subtle hover style consistent with the card.
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "editorPath\(product.editor" src/components/ProductCard.astro` matches (credit is a vanity link)
    - The link is guarded on a non-empty editor: `grep -E "product.editor &&|product.editor\?" src/components/ProductCard.astro` matches
    - A built store page with a credited product contains `href="/e/`: after build, `grep -rc 'product-card__editor' dist/en/store/index.html` >= 1 (credit line still present)
    - Name still escaped (no set:html): `grep -c "set:html" src/components/ProductCard.astro` returns 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Store editor credits link to /e/<slug>; guarded on presence; escaped.</done>
</task>

<task type="auto">
  <name>Task 3: Gallery editor credit → lightbox credit link to /e/<slug> (D-17 gallery half)</name>
  <read_first>
    - src/components/sections/GalleryPage.astro (L110-151 — the tile `<button data-gallery-tile data-file data-caption data-index>` render; add data-editor here; the entries defensive read near the top for the entry type)
    - src/components/sections/FeaturedGallery.astro (L88-122 — the SAME tile pattern on the landing featured grid; add data-editor here too)
    - src/components/LightboxOverlay.astro (L58-63 — the figcaption `.lightbox__meta` with data-lightbox-counter/data-lightbox-caption; the new credit anchor goes here; note the file's textContent-only doctrine comment L12-14)
    - src/scripts/gallery.ts (L672-695 initLightbox entry derivation from tile datasets + the render function that populates data-lightbox-img/-caption/-counter — extend BOTH; L583-607 textContent discipline)
    - src/i18n/routes.ts (editorPath — the /e/<slug> builder; gallery.ts is client TS and COULD import it, but prefer the literal `'/e/' + slug` with a slug regex guard to avoid pulling the i18n module into the gallery bundle — editorPath ignores lang now, so the forms are identical)
  </read_first>
  <action>
    (a) GalleryPage.astro + FeaturedGallery.astro: add `data-editor={entry.editor ?? ''}` to each `<button data-gallery-tile ...>` (extend the local entry interface with `editor?: string` — the optional 10-06 credit slug; defensively typed, missing = uncredited). No visual tile change — tiles are scatter-positioned `<button>`s, so the credit renders in the lightbox, not inline.
    (b) LightboxOverlay.astro: inside `figcaption.lightbox__meta`, add `<a class="lightbox__credit" data-lightbox-credit hidden></a>` styled as a micro credit line (mono label, brand red accent on the ink lightbox surface, adequate tap area). It ships hidden and empty — gallery.ts populates it per photo.
    (c) gallery.ts: in `initLightbox`'s entry derivation, add `editor: tile.dataset.editor ?? ''` to each entry; in the lightbox render/update path (where data-lightbox-caption is populated), populate the credit: if `entry.editor` is non-empty AND matches `/^[a-z0-9-]+$/` (client-side slug guard — defense-in-depth; gallery.json is bot-written but externally sourced), set the anchor's text via `textContent` (localized "por "/"by " prefix from `document.documentElement.lang` + the editor slug), set `href` via `setAttribute('href', '/e/' + entry.editor)`, and remove `hidden`; otherwise re-set `hidden` and clear its text/href (so navigating prev/next between credited and uncredited photos toggles correctly). NEVER use innerHTML. The credit is same-origin navigation — no target=_blank/rel needed.
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5 && grep -c "data-editor" dist/es/galeria/index.html</automated>
  </verify>
  <acceptance_criteria>
    - Tiles carry the editor slug: `grep -E "data-editor=" src/components/sections/GalleryPage.astro src/components/sections/FeaturedGallery.astro` matches BOTH files
    - Lightbox credit anchor exists and ships hidden: `grep -E "data-lightbox-credit" src/components/LightboxOverlay.astro` matches AND the element carries `hidden`
    - gallery.ts derives + populates it: `grep -E "dataset.editor" src/scripts/gallery.ts` matches AND `grep -E "data-lightbox-credit" src/scripts/gallery.ts` matches
    - Slug guard + textContent-only: `grep -E "a-z0-9-" src/scripts/gallery.ts` matches (regex guard) AND no new `innerHTML` (`grep -c "innerHTML" src/scripts/gallery.ts` returns 0)
    - Href is the vanity path: `grep -E "'/e/' ?\+" src/scripts/gallery.ts` matches
    - Uncredited toggle: the populate logic has an else branch that re-hides + clears the anchor when editor is empty/invalid (source assertion)
    - Built /es/galeria page contains `data-editor` attributes; `npm run build` exits 0
  </acceptance_criteria>
  <done>Credited gallery photos show a lightbox credit link to /e/<slug> on /galeria and the landing featured grid; uncredited photos show nothing; prev/next toggles correctly; textContent-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editors.json / store.json / gallery.json → directory + card + lightbox markup | Bot/staff-written data rendered as links + text. |
| gallery entry.editor slug → lightbox credit href | Externally-written slug becomes a same-origin link target at runtime (client JS). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-08-01 | XSS | editor name/slug in credit + directory | mitigate | Names render via `{}` auto-escaping (Astro) or `textContent` (gallery.ts lightbox credit); the slug is charset-constrained upstream (editors_model normalize_slug `[a-z0-9-]`) so it cannot break the href. No `set:html`/`innerHTML`. |
| T-10.1-08-02 | Info disclosure | directory indexing | mitigate | Directory set to `noindex,follow` (unlisted but crawlable via links) per D-16. |
| T-10.1-08-03 | Tampering / open redirect | gallery credit href built client-side from tile dataset | mitigate | gallery.ts validates the slug against `/^[a-z0-9-]+$/` BEFORE constructing `'/e/' + slug` and sets it via `setAttribute` — a crafted `editor` value (e.g. `javascript:`/`//evil`) fails the regex and the credit stays hidden; the path prefix is a fixed literal, so the target is always same-origin `/e/…`. |
| T-10.1-08-SC | Supply chain | dependencies | accept | No new package. |
</threat_model>

<verification>
- `npm run build` exits 0; directory builds with noindex + /e/ links; store credits link to /e/<slug>; gallery tiles carry data-editor and the lightbox credit anchor populates/hides per photo via textContent.
</verification>

<success_criteria>
The directory is restyled, unlisted, noindex,follow, and links to /e/<slug>; store product credits AND credited gallery photos (via the lightbox) link to /e/<slug> — the full D-17 organic-discovery path works without a navbar entry.
</success_criteria>

<output>
Create `.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-08-SUMMARY.md` when done.
</output>
