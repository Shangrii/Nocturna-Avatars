---
phase: "10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi"
plan: "04"
type: execute
wave: 2
depends_on: ["10.1-01", "10.1-02"]
files_modified:
  - src/components/editor-shell/PresenceDot.astro
  - src/components/editor-shell/ViewCounter.astro
  - src/components/editor-shell/AudioPlayer.astro
  - src/scripts/editor/presence.ts
  - src/scripts/editor/viewcounter.ts
  - src/scripts/editor/audio.ts
autonomous: true
requirements: [D-05, D-06, D-08, D-20, D-22, D-25]
must_haves:
  truths:
    - "A live Discord presence dot fetches status client-side via Lanyard and hides itself with no error text when unreachable or the editor isn't in the guild (D-05)"
    - "A view counter pings the cinema endpoint once on splash dismiss and shows the count, hiding entirely with no error text on failure (D-25)"
    - "Background audio stays muted until splash dismiss, then plays with a visible volume control (D-06/D-22)"
    - "All three components write to the DOM via textContent only and degrade gracefully"
  artifacts:
    - path: "src/components/editor-shell/PresenceDot.astro"
      provides: "Lanyard presence dot (graceful-degrade)"
    - path: "src/components/editor-shell/ViewCounter.astro"
      provides: "Cinema view-counter ping + count"
      contains: "textContent"
    - path: "src/components/editor-shell/AudioPlayer.astro"
      provides: "Splash-gated audio + volume"
  key_links:
    - from: "splash dismiss (editor:enter event)"
      to: "viewcounter ping + audio start"
      via: "document event listener"
      pattern: "editor:enter"
---

<objective>
Build the three net-new client-dynamic components — `PresenceDot` (Lanyard live Discord status, D-05), `ViewCounter` (cinema endpoint ping, D-25), and `AudioPlayer` (splash-gated background audio + volume, D-06/D-22) — all following the codebase's graceful-degradation + `textContent`-only discipline. They react to the `editor:enter` DOM event that the splash (plan 06) dispatches on dismiss, so audio/video autoplay and the view ping only fire after the user clicks Enter.

Purpose: These give the page its "live" premium feel while never blocking first paint or leaking error text. Interface-first: the `editor:enter` event contract (dispatched on `document` by plan 06's splash) is the fixed seam between splash and these consumers.
Output: three components + `src/scripts/editor/{presence,viewcounter,audio}.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md
@.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md
@src/scripts/gallery.ts

<interfaces>
<!-- EVENT CONTRACT (shared with plan 06 splash + plan 07 route):
     splash dispatches document.dispatchEvent(new CustomEvent('editor:enter')) on dismiss.
     ViewCounter and AudioPlayer listen for it; PresenceDot may fetch immediately (no gate). -->

<!-- VIEW-COUNTER ENDPOINT CONTRACT (shared with plan 12):
     GET https://editors.nocturna-avatars.site/api/views/<slug>?hit=1
       → 200 {"count": <int>}  (increments once per call; CORS-allowed for the public origin)
     On any non-200 / network error / timeout → hide the element, no error text. -->

<!-- LANYARD CONTRACT (D-05, A4 = public instance first):
     GET https://api.lanyard.rest/v1/users/<discordId>
       → {"data":{"discord_status":"online|idle|dnd|offline", "activities":[...]}}
     404 / non-member / error → hide the dot, no error text. -->

<!-- gallery.ts §L583-607 shows the client-fetch + textContent-only DOM-write pattern to mirror. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PresenceDot (Lanyard) + ViewCounter (cinema ping)</name>
  <read_first>
    - src/scripts/gallery.ts (L583-607 — client fetch + textContent-only write; the graceful-degrade discipline)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Copywriting Contract — presence/view-counter unreachable = hidden, NO error text; Effects Catalog Live Discord presence / Background overlay)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-PATTERNS.md (PresenceDot / ViewCounter sections — no-analog + graceful-degrade rule)
  </read_first>
  <action>
    PresenceDot.astro: props `{ discordId: string }`. Render a hidden-by-default status dot element with a `data-presence` hook + `data-discord-id={discordId}`; `src/scripts/editor/presence.ts` fetches `https://api.lanyard.rest/v1/users/<id>` (public instance per A4), maps `discord_status` to a dot color class (online green / idle amber / dnd red / offline grey), unhides the dot, and OPTIONALLY writes the activity name via `textContent`. Any fetch failure, non-200, 404 (non-member), or missing status → leave the dot hidden, log nothing user-facing, no error text (D-05). Dot color uses `--theme-accent` only for the ring, the status colors are fixed semantic colors.
    ViewCounter.astro: props `{ slug: string }`. Render a hidden micro (12px) element with a `data-viewcount` hook + `data-slug={slug}`; `src/scripts/editor/viewcounter.ts` listens for the `editor:enter` event on `document`, then does ONE `GET https://editors.nocturna-avatars.site/api/views/<slug>?hit=1`, and on `200 {count}` writes the number via `textContent` ONLY and unhides. Any failure → element stays hidden, no error text (D-25). Guard against double-firing (fetch once even if the event repeats). Use the exact endpoint origin from the `<interfaces>` contract (matches plan 12).
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "api.lanyard.rest/v1/users" src/scripts/editor/presence.ts` matches (public Lanyard instance)
    - `grep -E "editors.nocturna-avatars.site/api/views" src/scripts/editor/viewcounter.ts` matches (endpoint contract == plan 12)
    - `grep -c "textContent" src/scripts/editor/viewcounter.ts` >= 1 AND `grep -c "innerHTML" src/scripts/editor/viewcounter.ts` returns 0
    - `grep -E "editor:enter" src/scripts/editor/viewcounter.ts` matches (splash-gated ping)
    - Graceful degrade proven: both scripts have a catch/failure path that hides the element and writes no error string (`grep -iE "catch|\.ok|!res" src/scripts/editor/presence.ts` matches)
  </acceptance_criteria>
  <done>PresenceDot fetches Lanyard and hides on any failure; ViewCounter pings the cinema endpoint once on enter and shows the count via textContent, hiding on failure.</done>
</task>

<task type="auto">
  <name>Task 2: AudioPlayer (splash-gated background audio + volume)</name>
  <read_first>
    - src/components/LightboxOverlay.astro (inline SVG control pattern for the volume/mute glyph)
    - src/icons/ui/ (Lucide volume/mute glyphs — created in plan 03 Task 2 if present; otherwise add a volume.svg here)
    - .planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-UI-SPEC.md (Effects Catalog Background audio; Interaction Contract splash dismiss; D-06/D-22; Mobile D-20 44px)
  </read_first>
  <action>
    AudioPlayer.astro: props `{ src: string }` (the editor's audio ref; render nothing when empty). Emit an `<audio>` element preloaded `none`, muted, looping, with a `data-editor-audio` hook, plus a visible volume/mute toggle control (44px touch target, `--theme-accent` active) using the self-hosted Lucide volume glyph. `src/scripts/editor/audio.ts` listens for `editor:enter` on `document`, then unmutes + plays the track (this satisfies the autoplay-unlock requirement — audio only starts after the user gesture, D-22) and wires the volume slider/toggle. Volume changes are local, no persistence needed. On play() rejection (browser blocks) leave the control in a muted state, no error text. Same experience on mobile (D-20) — the splash gesture unlocks it there too.
  </action>
  <verify>
    <automated>cd "." && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "editor:enter" src/scripts/editor/audio.ts` matches (splash-gated unlock)
    - `<audio>` starts muted+loop: `grep -E "muted|loop" src/components/editor-shell/AudioPlayer.astro` matches both
    - Volume control has a 44px target: `grep -E "var\(--touch-min\)|min-height" src/components/editor-shell/AudioPlayer.astro` matches
    - Renders nothing when src empty: `grep -E "src &&|src\?|Boolean\(src\)" src/components/editor-shell/AudioPlayer.astro` matches
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>AudioPlayer stays muted until the splash enter gesture, then plays with a visible volume control; no error text on autoplay block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Lanyard (third-party) | Read-only cross-origin fetch of a public status API; no secrets sent. |
| client → cinema view-counter | Cross-origin GET to editors.nocturna-avatars.site; no auth, no PII. |
| editor.audio ref → `<audio src>` | Editor-controlled media reference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.1-04-01 | XSS | Lanyard activity text / view count written to DOM | mitigate | Both write via `textContent` only, never `innerHTML` (gallery.ts discipline). No third-party string reaches markup unescaped. |
| T-10.1-04-02 | Tampering | editor.audio `src` scheme | mitigate | The ref is validated bot-side (plan 09 is_safe_image_ref rejects javascript:/data:/http:); the component binds it only as an `<audio src>` attribute (auto-escaped). |
| T-10.1-04-03 | DoS / info leak | cinema endpoint availability | accept | View counter degrades to hidden on any failure; the page never depends on the endpoint. Rate-limiting/dedup is handled at the endpoint (plan 12). |
| T-10.1-04-SC | Supply chain | Lanyard public instance | accept | Public read-only API, no secret, client-side only; self-host fallback documented as a plan-12/deploy option per A4. No npm dep added. |
</threat_model>

<verification>
- `npm run build` exits 0.
- Presence/view-counter/audio all have a failure path that hides the element with no error text.
</verification>

<success_criteria>
The three live components fetch/play only after the splash gesture (view/audio) or on load (presence), write via textContent, and degrade silently — never blocking first paint or leaking errors.
</success_criteria>

<output>
Create `.planning/phases/10.1-editor-pages-redesign-guns-lol-carrd-premium-replica-per-edi/10.1-04-SUMMARY.md` when done.
</output>
