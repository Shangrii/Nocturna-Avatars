---
phase: 03.1-catalog-configurator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/services.json
  - src/components/sections/ServicesPage.astro
autonomous: true
requirements:
  - CAT-02
  - CAT-03

must_haves:
  truths:
    - "services.json has exactly 4 catalog categories: unity, blender, textures, extras (accessories dissolved)"
    - "Each catalog item has an id field (unique slug), bilingual name, price string '99', and optional repeatable field"
    - "All 26 services are present (18 new + 8 redistributed from addons)"
    - "The 4 repeatable items have repeatable:true; all others omit the field"
    - "The typo 'Expressions personalizadas' is corrected to 'Expresiones personalizadas'"
    - "ServicesPage.astro no longer renders the addons block (resolvedAddons loop, addons-title, addon-grid, addonsNote)"
  artifacts:
    - path: "src/data/services.json"
      provides: "Extended catalog schema with 4 categories and 26 items"
      contains: "id.*unity.*blender.*textures.*extras"
    - path: "src/components/sections/ServicesPage.astro"
      provides: "Services page without addons block"
      contains: "enableCart"
  key_links:
    - from: "src/components/sections/ServicesPage.astro"
      to: "src/components/CatalogSection.astro"
      via: "enableCart prop pass-through"
      pattern: "enableCart=\\{true\\}"
    - from: "src/components/CatalogSection.astro"
      to: "src/data/services.json"
      via: "catalog[] items loop"
      pattern: "item\\.id"
---

<objective>
Restructure services.json from 5 empty catalog categories to 4 populated categories, add 26 items with the extended schema (id, repeatable), fix the 'Expressions' typo, and remove the now-superseded addons block from ServicesPage.astro.

Purpose: The configurator's JS (cart.ts) keys cart state by item.id — this field does not exist yet. The 4-category structure is the data contract all later plans depend on.
Output: Extended services.json + cleaned ServicesPage.astro ready to receive cart component imports.
</objective>

<execution_context>
@C:\Users\Shangri\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Shangri\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.1-catalog-configurator/03.1-CONTEXT.md
@.planning/phases/03.1-catalog-configurator/03.1-RESEARCH.md

<interfaces>
<!-- Current services.json catalog structure (5 categories, all items: []) -->
<!-- services.json packages.addons[] — 8 items being redistributed into catalog -->
<!-- CatalogSection.astro current CatalogItem interface: { name: {es, en}, price: string } -->
<!-- ServicesPage.astro: resolvedAddons, addonsNote variables and <div class="addons reveal"> block -->

From src/data/services.json (catalog section, current):
```json
"catalog": [
  { "id": "unity",      "category": {"es":"Unity","en":"Unity"},               "items": [] },
  { "id": "blender",    "category": {"es":"Blender / Malla","en":"Blender / Mesh"}, "items": [] },
  { "id": "textures",   "category": {"es":"Texturas","en":"Textures"},          "items": [] },
  { "id": "accessories","category": {"es":"Accesorios","en":"Accessories"},      "items": [] },
  { "id": "extras",     "category": {"es":"Extras / NSFW","en":"Extras / NSFW"},"items": [] }
]
```

From src/data/services.json (addons currently in packages — 8 items to mirror as catalog items):
- "Outfit adicional" / "Additional outfit" — +$5 USD — repeatable: true
- "Accesorio extra" / "Extra accessory" — +$5 USD — repeatable: true
- "Cabello adicional" / "Additional hair" — +$5 USD — repeatable: true
- "Expressions personalizadas" / "Custom expressions" — +$15 USD — TYPO: fix to "Expresiones personalizadas"
- "Sesión fotográfica" / "Photo session" — +$5 USD — not repeatable
- "Revisión extra" / "Extra revision" — +$5 USD — repeatable: true
- "Entrega urgente" / "Rush delivery" — +$15 USD — not repeatable
- "NSFW" / "NSFW" — +$15 USD — not repeatable

From src/components/sections/ServicesPage.astro (block to remove):
```astro
const resolvedAddons = servicesData.packages.addons.map(...)  // remove
const addonsNote = ...                                          // remove
<div class="addons reveal">...</div>                           // remove entire block
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restructure services.json — 4 categories, 26 items, extended schema</name>
  <files>src/data/services.json</files>
  <read_first>
    - src/data/services.json — read the full file before modifying; keep packages.tiers[], packages.addons[], and packages.addonsNote intact
    - .planning/phases/03.1-catalog-configurator/03.1-CONTEXT.md — D-01 (category names), D-02 (accessories dissolved), D-03 (addons stay in packages AND appear in catalog), D-04 (schema: id, repeatable), D-05 (price: "99")
  </read_first>
  <action>
Replace the "catalog" array in services.json. Keep "packages" exactly as-is (tiers, addons, addonsNote untouched — D-03).

New catalog[] must have exactly 4 entries with these ids and display names:
- id: "unity", category: { es: "Unity & Configuración", en: "Unity & Configuration" }
- id: "blender", category: { es: "Blender & Malla", en: "Blender & Mesh" }
- id: "textures", category: { es: "Texturas & Visual", en: "Textures & Visual" }
- id: "extras", category: { es: "Extras", en: "Extras" }

Each item schema: { "id": string, "name": { "es": string, "en": string }, "price": "99", "repeatable": true } or (without repeatable field for non-repeatable items).

Items per category (26 total):

UNITY (6 items):
- id: "physbones", name: {es:"PhysBones", en:"PhysBones"}, price:"99"
- id: "face-tracking", name: {es:"Face Tracking", en:"Face Tracking"}, price:"99"
- id: "expresiones-personalizadas", name: {es:"Expresiones personalizadas", en:"Custom expressions"}, price:"99"
  (NOTE: "Expresiones" not "Expressions" — fixes the addons typo, D-RESEARCH risk 7)
- id: "parametros-menus", name: {es:"Optimización de parámetros y menús", en:"Parameter & menu optimization"}, price:"99"
- id: "animacion-afk", name: {es:"Animación AFK personalizada", en:"Custom AFK animation"}, price:"99"
- id: "rostro-blendshapes", name: {es:"Rostro personalizado (Face Blendshapes)", en:"Custom face (Face Blendshapes)"}, price:"99"

BLENDER (7 items):
- id: "fitting-rigging", name: {es:"Ajuste de Fitting & Rigging de ropa", en:"Clothing fitting & rigging adjustment"}, price:"99"
- id: "blendshapes-ropa", name: {es:"Adaptación de blendshapes en ropa externa", en:"External clothing blendshape adaptation"}, price:"99"
- id: "fusion-modelos", name: {es:"Fusión de modelos (quimeras)", en:"Model fusion (chimeras)"}, price:"99"
- id: "blendshapes-personalizadas", name: {es:"Creación de blendshapes personalizadas", en:"Custom blendshapes creation"}, price:"99"
- id: "cabello-estilismo", name: {es:"Aplicación y estilismo de cabello", en:"Hair application and styling"}, price:"99"
- id: "outfit-adicional", name: {es:"Outfit adicional", en:"Additional outfit"}, price:"99", repeatable:true
- id: "cabello-adicional", name: {es:"Cabello adicional", en:"Additional hair"}, price:"99", repeatable:true

TEXTURES (5 items):
- id: "texturizado-personalizado", name: {es:"Texturizado personalizado", en:"Custom texturing"}, price:"99"
- id: "tatuajes", name: {es:"Tatuajes personalizados", en:"Custom tattoos"}, price:"99"
- id: "maquillaje", name: {es:"Maquillaje personalizado", en:"Custom makeup"}, price:"99"
- id: "diseno-ojos", name: {es:"Diseño de ojos personalizados", en:"Custom eye design"}, price:"99"
- id: "iluminacion-shaders", name: {es:"Corrección de iluminación y shaders", en:"Lighting and shader correction"}, price:"99"

EXTRAS (8 items):
- id: "integracion-accesorios", name: {es:"Integración de accesorios (props, toys & VFX)", en:"Accessory integration (props, toys & VFX)"}, price:"99"
- id: "configuracion-nsfw", name: {es:"Configuración NSFW", en:"NSFW configuration"}, price:"99"
- id: "fotografia-render", name: {es:"Fotografía y render de presentación", en:"Presentation photography and render"}, price:"99"
- id: "accesorio-extra", name: {es:"Accesorio extra", en:"Extra accessory"}, price:"99", repeatable:true
- id: "revision-extra", name: {es:"Revisión extra", en:"Extra revision"}, price:"99", repeatable:true
- id: "entrega-urgente", name: {es:"Entrega urgente", en:"Rush delivery"}, price:"99"
- id: "sesion-fotografica", name: {es:"Sesión fotográfica", en:"Photo session"}, price:"99"
- id: "nsfw", name: {es:"NSFW", en:"NSFW"}, price:"99"

All ids must be unique kebab-case strings. Repeatable:true on exactly 4 items: outfit-adicional, cabello-adicional, accesorio-extra, revision-extra. All other items omit the repeatable field.

Verify item count: unity=6, blender=7, textures=5, extras=8 = 26 total.
  </action>
  <verify>
    <automated>
      node -e "const d=require('./src/data/services.json'); const c=d.catalog; console.assert(c.length===4,'cat count'); console.assert(c.every(x=>['unity','blender','textures','extras'].includes(x.id)),'ids'); const all=c.flatMap(x=>x.items); console.assert(all.length===26,'item count '+all.length); const rep=all.filter(x=>x.repeatable===true); console.assert(rep.length===4,'repeatable count '+rep.length); const repIds=rep.map(x=>x.id).sort().join(','); console.assert(repIds==='accesorio-extra,cabello-adicional,outfit-adicional,revision-extra','repeatable ids: '+repIds); console.assert(all.every(x=>x.id),'missing ids'); console.assert(!JSON.stringify(d).includes('Expressions personalizadas'),'typo still present'); console.log('PASS')"
    </automated>
  </verify>
  <acceptance_criteria>
    - services.json catalog[] has exactly 4 entries with ids: unity, blender, textures, extras
    - Total items across all categories: 26
    - Exactly 4 items have repeatable:true — ids: outfit-adicional, cabello-adicional, accesorio-extra, revision-extra
    - Every item has an id field (non-empty string)
    - No item has the string "Expressions personalizadas" (typo corrected to "Expresiones")
    - packages.tiers[], packages.addons[], packages.addonsNote are unchanged
  </acceptance_criteria>
  <done>services.json catalog[] has 4 categories with 26 items, extended schema (id + repeatable), typo corrected, packages block untouched</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Clean ServicesPage.astro — remove addons block, pass enableCart</name>
  <files>src/components/sections/ServicesPage.astro</files>
  <read_first>
    - src/components/sections/ServicesPage.astro — read the full file first; identify exact lines for resolvedAddons, addonsNote variables, and the addons div block
    - .planning/phases/03.1-catalog-configurator/03.1-CONTEXT.md — D-03 (remove addons block), D-15 (enableCart prop)
    - .planning/phases/03.1-catalog-configurator/03.1-RESEARCH.md — Section 7 (ServicesPage mount points)
  </read_first>
  <action>
Make these targeted changes to ServicesPage.astro:

1. In the frontmatter (--- block), remove these two variable declarations entirely:
   - The `resolvedAddons` const (the .map() call on servicesData.packages.addons)
   - The `addonsNote` const (the servicesData.packages.addonsNote read)

2. Remove the entire `<div class="addons reveal">...</div>` block from the JSX — this includes the addons-title h3, the addon-grid div (with its resolvedAddons.map() loop), and the addons-note p element.

3. On the existing `<CatalogSection ... />` call, add the prop `enableCart={true}`. The existing props (catalog, lang, emptyHeading, emptyBody, emptyTicketLabel) remain unchanged.

The resulting ServicesPage.astro frontmatter should no longer reference resolvedAddons or addonsNote anywhere. The packages section renders only the PackageCard grid (resolvedTiers loop) — no addons below it.

Do NOT add cart component imports yet (that happens in Plan 03.1-04 when the components exist). Do NOT modify any other part of the file.
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/sections/ServicesPage.astro','utf8'); console.assert(!s.includes('resolvedAddons'),'resolvedAddons still present'); console.assert(!s.includes('addonsNote'),'addonsNote still present'); console.assert(!s.includes('addon-grid'),'addon-grid still present'); console.assert(!s.includes('addons-title'),'addons-title still present'); console.assert(s.includes('enableCart={true}'),'enableCart not added'); console.log('PASS')"
    </automated>
  </verify>
  <acceptance_criteria>
    - ServicesPage.astro no longer contains the string "resolvedAddons"
    - ServicesPage.astro no longer contains the string "addonsNote"
    - ServicesPage.astro no longer contains the string "addon-grid"
    - ServicesPage.astro no longer contains the string "addons-title"
    - CatalogSection component call includes enableCart={true}
    - The packages section still renders (resolvedTiers.map, PackageCard) — only the addons div is removed
  </acceptance_criteria>
  <done>ServicesPage.astro has addons block removed; enableCart={true} passed to CatalogSection; site still builds (no broken references)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON data → Astro template | services.json is staff-edited; all strings are auto-escaped by Astro at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Tampering | src/data/services.json | mitigate | Staff-only repo write access; Astro auto-escapes all JSON strings at build time (existing T-03-04 mitigates XSS) |
| T-03.1-SC | Tampering | npm installs | accept | No new npm packages installed in this plan |
</threat_model>

<verification>
Run `npm run build` — Astro should compile without TypeScript errors. The services page at /es/servicios and /en/services should render 4 catalog categories with items (no EmptyState shown). The addons grid should no longer appear on the services page.
</verification>

<success_criteria>
1. `npm run build` completes with 0 errors
2. services.json catalog[] has 4 categories (unity, blender, textures, extras), 26 items total, each with id and correct repeatable values
3. ServicesPage.astro renders no addons block; CatalogSection receives enableCart={true}
</success_criteria>

<output>
Create `.planning/phases/03.1-catalog-configurator/03.1-01-SUMMARY.md` when done
</output>
