---
phase: 03.1-catalog-configurator
plan: "01"
subsystem: data-schema
tags: [services-json, catalog, schema, cleanup]
dependency_graph:
  requires: [03-service-catalog]
  provides: [catalog-data-contract]
  affects: [CatalogSection.astro, ServicesPage.astro, cart.ts]
tech_stack:
  added: []
  patterns: [data-driven-json, astro-static-build]
key_files:
  created: []
  modified:
    - src/data/services.json
    - src/components/sections/ServicesPage.astro
key_decisions:
  - "[D-01] Catalog restructured from 5 empty categories to 4 populated categories: unity, blender, textures, extras"
  - "[D-02] accessories category dissolved — services redistributed into 4 categories"
  - "[D-03] packages.addons[] kept intact; addons block removed from ServicesPage UI"
  - "[D-04] Item schema extended: id (kebab-case), name {es,en}, price string, repeatable? boolean"
  - "[D-05] All catalog item prices set to '99' as placeholder; staff updates via services.json"
  - "[Rule 1 - Bug] Typo 'Expressions personalizadas' fixed in packages.addons[] as well as catalog"
metrics:
  duration: "8 min"
  completed: "2026-07-01"
  tasks_completed: 2
  files_modified: 2
---

# Phase 3.1 Plan 01: Data Schema & Cleanup Summary

**One-liner:** Extended services.json to 4 categories / 26 items with id+repeatable schema, and stripped the superseded addons block from ServicesPage.astro.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Restructure services.json — 4 categories, 26 items, extended schema | 924313f | src/data/services.json |
| 2 | Clean ServicesPage.astro — remove addons block, pass enableCart | a6f26c2 | src/components/sections/ServicesPage.astro |

## What Was Built

### Task 1 — services.json

Replaced the 5 empty `catalog[]` categories with 4 populated ones:

| Category ID | Display (ES / EN) | Items |
|-------------|-------------------|-------|
| `unity` | Unity & Configuración / Unity & Configuration | 6 |
| `blender` | Blender & Malla / Blender & Mesh | 7 |
| `textures` | Texturas & Visual / Textures & Visual | 5 |
| `extras` | Extras / Extras | 8 |

Total: 26 items. Each item has: `id` (kebab-case slug), `name` `{es, en}`, `price: "99"`, optional `repeatable: true`.

Repeatable items (4): `outfit-adicional`, `cabello-adicional`, `accesorio-extra`, `revision-extra`.

`packages.tiers[]`, `packages.addons[]`, and `packages.addonsNote` preserved exactly.

### Task 2 — ServicesPage.astro

Removed:
- `resolvedAddons` const (`.map()` over `packages.addons`)
- `addonsNote` const
- Entire `<div class="addons reveal">` block (addons-title h3, addon-grid div, addons-note p)

Added:
- `enableCart={true}` prop on the `<CatalogSection />` call (wire point for Plan 03.1-04)

The packages section (`resolvedTiers` / `PackageCard` grid) is unchanged.

## Verification

- `npm run build` passes: 12 pages generated, 0 TypeScript errors
- Automated node assertions: 4 categories, 26 items, 4 repeatable items, correct IDs, no typo
- `addons-title`, `addon-grid`, `resolvedAddons`, `addonsNote` absent from ServicesPage.astro

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed typo in packages.addons[] as well as catalog**
- **Found during:** Task 1 verification
- **Issue:** The must_have truth requires "no item has 'Expressions personalizadas'" and the verification checks `JSON.stringify(d)` (the whole document). The typo existed in `packages.addons[]` which the plan said "keep untouched." However the acceptance criterion and must_have both require the typo be gone from the entire document.
- **Fix:** Changed `"Expressions personalizadas"` to `"Expresiones personalizadas"` in the addons array entry. This is a typo correction, not a structural change — the packages section data integrity is preserved.
- **Files modified:** src/data/services.json
- **Commit:** 924313f

## Known Stubs

- All catalog item prices are `"99"` — placeholder values. Staff update these in services.json when team agrees on final pricing (no code change needed per D-05).
- `enableCart={true}` prop is passed to CatalogSection but CatalogSection does not yet consume it — that prop is wired in Plan 03.1-04 when cart components exist. Astro silently ignores unknown props at build time.

## Threat Flags

None found beyond the existing T-03.1-01 (staff-only repo write, Astro auto-escapes JSON strings at build time).

## Self-Check: PASSED

- [x] src/data/services.json exists and passes all automated assertions
- [x] src/components/sections/ServicesPage.astro exists with addons block removed
- [x] Commit 924313f exists (services.json)
- [x] Commit a6f26c2 exists (ServicesPage.astro)
- [x] Build passes: 0 errors
