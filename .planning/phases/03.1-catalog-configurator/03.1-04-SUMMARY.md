---
phase: 03.1
plan: "04"
subsystem: catalog-configurator
tags:
  - cart-controls
  - catalog-section
  - services-page
  - interactive-rows
dependency_graph:
  requires:
    - 03.1-01  # services.json id/repeatable schema + enableCart prop wired
    - 03.1-02  # CartDrawer shell
    - 03.1-03  # CartSummaryModal shell
  provides:
    - CatalogSection cart controls HTML (Agregar buttons + qty controls)
    - ServicesPage cart component mounts + cart.ts loading
  affects:
    - 03.1-05  # cart.ts full implementation (stub created here)
    - 03.1-06  # E2E verification reads DOM nodes created here
tech_stack:
  added:
    - src/scripts/cart.ts (stub — full implementation in Plan 05)
  patterns:
    - Astro scoped <style> for component-level interactive CSS
    - Conditional Astro template rendering (enableCart prop gate)
    - data-item-id attribute bridge between static HTML and cart.ts DOM queries
    - Scoped <script> import in ServicesPage.astro for page-only JS
key_files:
  created:
    - src/scripts/cart.ts
  modified:
    - src/components/CatalogSection.astro
    - src/components/sections/ServicesPage.astro
decisions:
  - "[03.1-04] cart.ts stub created in Plan 04 (empty initCart) so build succeeds during Wave 2b; full implementation deferred to Plan 05"
  - "[03.1-04] data-item-id attribute set on both the <li> row and individual buttons to give cart.ts multiple query targets"
  - "[03.1-04] Price prefixed with $ only when enableCart=true — preserves existing non-cart display behavior"
metrics:
  duration: "~8min"
  completed: "2026-07-01"
  tasks: 2
  files: 3
requirements:
  - CAT-02
  - CAT-03
---

# Phase 3.1 Plan 04: CatalogSection Cart Controls + ServicesPage Mounts Summary

**One-liner:** Cart controls wired into every catalog row (Agregar toggle / −N+ qty) with CartDrawer, CartPill, CartSummaryModal mounted in ServicesPage and cart.ts stub loading on the services page.

## What Was Built

### Task 1 — CatalogSection.astro: interfaces, cart controls HTML, scoped CSS

Extended `src/components/CatalogSection.astro`:

- **CatalogItem interface:** added `id: string` (required) and `repeatable?: boolean` (optional)
- **Props interface:** added `enableCart?: boolean` (default false)
- **Row render:** `catalog-row--interactive` class and `data-item-id={item.id}` applied conditionally
- **Non-repeatable items** (22 of 26): render `<button class="catalog-row__cart-btn cart-add-trigger">` with Agregar/Add label and `data-item-id`
- **Repeatable items** (4 of 26: outfit-adicional, cabello-adicional, accesorio-extra, revision-extra): render `<div class="catalog-row__qty">` with `qty-btn--dec`, `qty-count` (aria-live="polite"), `qty-btn--inc`
- **Price display:** prefixed with `$` when enableCart=true (non-cotizar items)
- **Scoped CSS added:** `.catalog-row--interactive`, `.catalog-row__cart-btn`, `.catalog-row__cart-btn--added`, `.catalog-row__qty`, `.qty-btn`, `.qty-count`, mobile `@media (max-width: 480px)` price hide rule

**Commit:** c8ee227

### Task 2 — ServicesPage.astro: cart component mounts + cart.ts script

Modified `src/components/sections/ServicesPage.astro`:

- Imported CartDrawer, CartPill, CartSummaryModal
- Mounted all three after the closing `</section>` with `lang={lang}` prop
- Added scoped `<script>` importing `../../scripts/cart.ts` — scopes cart JS to services page only

Created `src/scripts/cart.ts` (stub):

- Module-level `_cartInitialized` guard flag
- Empty `initCart()` function (no-op) auto-invoked on module load
- Build succeeds — Plan 05 fills in the full cart state machine

**Build result:** 0 errors, 12 pages built in 895ms

**Commit:** 33a54a0

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical forward dependency] Created cart.ts stub before ServicesPage import**

- **Found during:** Task 2
- **Issue:** Plan noted that cart.ts does not exist yet and that Astro/Vite will error at build time if the import target is missing. The plan itself specified creating a minimal stub as the correct action.
- **Fix:** Created `src/scripts/cart.ts` with module-level init guard and empty `initCart()`. Build confirms 0 errors.
- **Files modified:** `src/scripts/cart.ts` (created)
- **Commit:** 33a54a0 (included in Task 2 commit)

## Known Stubs

| Stub | File | Line | Reason |
|------|------|------|--------|
| `initCart()` no-op body | `src/scripts/cart.ts` | 14 | Forward stub — full cart state machine implemented in Plan 03.1-05 |

The stub does not prevent this plan's goal (DOM nodes for cart.ts to query are now in the HTML). The stub will be replaced wholesale in Plan 05.

## Threat Surface Scan

No new network endpoints, auth paths, or trust boundaries introduced. `data-item-id` values come from staff-edited `services.json` and are escaped by Astro at build time (T-03.1-04 accepted per threat register).

## Self-Check

- [x] `src/components/CatalogSection.astro` exists and contains all required elements
- [x] `src/components/sections/ServicesPage.astro` imports and mounts all three cart components
- [x] `src/scripts/cart.ts` created (stub)
- [x] Commit c8ee227 exists (Task 1)
- [x] Commit 33a54a0 exists (Task 2)
- [x] `npm run build` passes with 0 errors

## Self-Check: PASSED
