---
phase: 03.1-catalog-configurator
plan: 02
type: execute
wave: 2
depends_on:
  - 03.1-01
files_modified:
  - src/components/CartPill.astro
  - src/components/CartDrawer.astro
autonomous: true
requirements:
  - CAT-02

must_haves:
  truths:
    - "CartPill.astro renders a fixed-position red pill button hidden by default (hidden attribute)"
    - "CartDrawer.astro renders a full right-side drawer with header, empty item list, total, and CTA — hidden by default"
    - "Both components accept a lang prop and render bilingual strings"
    - "All CSS is in each component's scoped <style> block using design tokens from theme.css"
    - "z-index stacking: pill=910, backdrop=950, drawer=960"
  artifacts:
    - path: "src/components/CartPill.astro"
      provides: "Floating cart pill button — static shell"
      contains: "cart-pill"
    - path: "src/components/CartDrawer.astro"
      provides: "Right-side cart drawer panel + backdrop — static shell"
      contains: "cart-drawer"
  key_links:
    - from: "src/components/CartPill.astro"
      to: "src/scripts/cart.ts"
      via: "id='cartPill' data attribute"
      pattern: "id=\"cartPill\""
    - from: "src/components/CartDrawer.astro"
      to: "src/scripts/cart.ts"
      via: "id='cartDrawer' data-cart-close data-cart-ticket attributes"
      pattern: "id=\"cartDrawer\""
---

<objective>
Create the static HTML/CSS shells for CartPill.astro and CartDrawer.astro. No JavaScript behavior — just the rendered markup and styles that cart.ts will wire in Plan 03.1-05.

Purpose: Cart.ts (Wave 3) queries DOM by id and data-* attributes. Those nodes must exist before cart.ts runs. Parallel with Plan 03.1-03 (modal) and Plan 03.1-04 (catalog row controls) since they touch different files.
Output: Two new Astro component files with complete HTML anatomy and scoped CSS matching the UI-SPEC.
</objective>

<execution_context>
@C:\Users\Shangri\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Shangri\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/03.1-catalog-configurator/03.1-CONTEXT.md
@.planning/phases/03.1-catalog-configurator/03.1-UI-SPEC.md

<interfaces>
<!-- Design tokens used — all confirmed in theme.css -->
<!-- --color-red #c0192c, --color-red-dark #7a0f1a, --color-ink #0a0c14 -->
<!-- --color-ink-raised #10131e, --color-border, --color-white #f0eae4 -->
<!-- --color-white-dim #9a9198, --color-paper-on-ink #f0eae4 -->
<!-- --font-body Inter, --font-mono Space Mono -->
<!-- --space-2xs 8px, --space-sm 16px, --space-md 24px, --space-lg 32px -->
<!-- --touch-min 44px -->

From 03.1-UI-SPEC.md — Z-index stack:
- FloatingCTA (Discord): z-index 900, bottom-right position: right: clamp(16px,4vw,32px); bottom: clamp(16px,4vw,32px)
- Cart pill: z-index 910, bottom: calc(clamp(16px,4vw,32px) + 44px + 12px)
- Cart drawer backdrop: z-index 950
- Cart drawer panel: z-index 960

From 03.1-UI-SPEC.md — CartPill HTML anatomy:
```html
<button class="cart-pill" id="cartPill" type="button"
        aria-label="Ver selección: 0 servicios" hidden>
  <span class="cart-pill__badge" aria-hidden="true">0</span>
  <span class="cart-pill__label" data-cart-label>
    <span lang="es">servicios → Abrir Ticket</span>
    <span lang="en" hidden>services → Open Ticket</span>
  </span>
</button>
```

From 03.1-UI-SPEC.md — CartDrawer HTML anatomy:
```html
<div class="cart-backdrop" id="cartBackdrop" aria-hidden="true" hidden></div>
<aside class="cart-drawer" id="cartDrawer"
       role="dialog" aria-modal="true" aria-labelledby="cartDrawerTitle" hidden>
  <div class="cart-drawer__header">...</div>
  <ul class="cart-drawer__items" aria-label="..."><!-- populated by cart.ts --></ul>
  <div class="cart-drawer__footer">
    <div class="cart-drawer__total">...</div>
    <button class="cart-drawer__cta" type="button" data-cart-ticket>...</button>
  </div>
</aside>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create CartPill.astro — floating pill button shell</name>
  <files>src/components/CartPill.astro</files>
  <read_first>
    - .planning/phases/03.1-catalog-configurator/03.1-UI-SPEC.md — "Floating Cart Pill — Visual Contract" section (full HTML anatomy, CSS contract, entrance/exit animation CSS)
    - src/styles/chrome.css — FloatingCTA positioning (.floating-cta) to confirm z-index/position values for offset calculation
  </read_first>
  <action>
Create src/components/CartPill.astro as a new file.

Frontmatter interface:
```
interface Props { lang: 'es' | 'en'; }
const { lang } = Astro.props;
```

HTML template (exact anatomy from UI-SPEC):
- Root element: `<button class="cart-pill" id="cartPill" type="button" aria-label={lang === 'es' ? 'Ver selección: 0 servicios' : 'View selection: 0 services'} hidden>`
- Inside: `<span class="cart-pill__badge" aria-hidden="true">0</span>`
- Inside: `<span class="cart-pill__label" data-cart-label>`
  - `<span lang="es"{lang === 'en' ? ' hidden' : ''}>servicios → Abrir Ticket</span>`
  - `<span lang="en"{lang === 'es' ? ' hidden' : ''}>services → Open Ticket</span>`
- Correct rendering: on ES pages only the es span is visible; on EN pages only the en span is visible

Scoped `<style>` block — include ALL styles from the UI-SPEC "Cart Pill CSS Contract" section:
- `.cart-pill` — position:fixed, right/bottom clamp values, z-index:910, display:flex, align-items:center, gap:var(--space-2xs), padding:0 var(--space-sm), min-height:44px, min-width:44px, background:var(--color-red), color:var(--color-white), border:none, font-family:var(--font-body), font-size:14px, cursor:pointer, transition, white-space:nowrap
- `.cart-pill:hover` — background:var(--color-red-dark), transform:translateY(-2px)
- `.cart-pill:focus-visible` — outline:2px solid var(--color-white), outline-offset:3px
- `.cart-pill[hidden]` — display:none (WR-01 guard)
- `.cart-pill__badge` — inline-flex, min-width:20px, height:20px, padding:0 4px, background:rgba(0,0,0,0.25), font-family:var(--font-mono), font-size:14px, font-weight:700, line-height:1, border-radius:10px
- `.cart-pill--visible` animation: `@keyframes cart-pill-enter { from { opacity:0; transform:translateY(12px) scale(0.92) } to { opacity:1; transform:translateY(0) scale(1) } }` — applied as `animation: cart-pill-enter 0.35s cubic-bezier(0.16,1,0.3,1) forwards`
- `.cart-pill--hiding` animation: `@keyframes cart-pill-exit { from { opacity:1; transform:translateY(0) } to { opacity:0; transform:translateY(8px) } }` — applied as `animation: cart-pill-exit 0.2s ease forwards`
- `@media (prefers-reduced-motion: reduce)` — `.cart-pill--visible, .cart-pill--hiding { animation: none }`

Bottom position calculation (per UI-SPEC): `bottom: calc(clamp(16px, 4vw, 32px) + 44px + 12px)` — positions pill above the FloatingCTA (z-900) with 12px gap.

Do NOT add class="dc-trigger" or class="dc-open-btn" — those open the Discord modal (landmine from RESEARCH.md).
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/CartPill.astro','utf8'); console.assert(s.includes('id=\"cartPill\"'),'missing id'); console.assert(s.includes('hidden'),'missing hidden attr'); console.assert(s.includes('cart-pill__badge'),'missing badge class'); console.assert(s.includes('data-cart-label'),'missing data-cart-label'); console.assert(s.includes('z-index: 910'),'wrong z-index'); console.assert(s.includes('cart-pill--visible'),'missing enter animation'); console.assert(s.includes('cart-pill--hiding'),'missing exit animation'); console.assert(s.includes('prefers-reduced-motion'),'missing reduced-motion'); console.assert(!s.includes('dc-trigger'),'dc-trigger conflict'); console.log('PASS')"
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/CartPill.astro
    - HTML contains id="cartPill", hidden attribute, class="cart-pill"
    - HTML contains span.cart-pill__badge with aria-hidden="true" and initial text "0"
    - HTML contains span[data-cart-label] with two child spans (lang="es" and lang="en")
    - Scoped style block contains z-index: 910
    - Scoped style block contains .cart-pill[hidden] { display: none }
    - Scoped style block contains @keyframes cart-pill-enter and @keyframes cart-pill-exit
    - Scoped style block contains @media (prefers-reduced-motion: reduce) block
    - No class "dc-trigger" or "dc-open-btn" present on the button
  </acceptance_criteria>
  <done>CartPill.astro renders a hidden pill button with correct id, badge, bilingual label, and all CSS including animations and reduced-motion guard</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create CartDrawer.astro — right-side drawer shell + backdrop</name>
  <files>src/components/CartDrawer.astro</files>
  <read_first>
    - .planning/phases/03.1-catalog-configurator/03.1-UI-SPEC.md — "Cart Drawer — Visual Contract" section (full HTML anatomy and CSS contract)
    - .planning/phases/03.1-catalog-configurator/03.1-CONTEXT.md — D-09 (drawer specs), D-10 ("Abrir Ticket" only in drawer), D-12 (drawer CTA opens modal)
  </read_first>
  <action>
Create src/components/CartDrawer.astro as a new file.

Frontmatter interface:
```
interface Props { lang: 'es' | 'en'; }
const { lang } = Astro.props;
```

HTML template — two sibling elements (backdrop + drawer):

1. Backdrop: `<div class="cart-backdrop" id="cartBackdrop" aria-hidden="true" hidden></div>`

2. Drawer aside:
```html
<aside class="cart-drawer" id="cartDrawer"
       role="dialog" aria-modal="true"
       aria-labelledby="cartDrawerTitle" hidden>

  <div class="cart-drawer__header">
    <span class="cart-drawer__title" id="cartDrawerTitle">
      {lang === 'es' ? 'Tu selección' : 'Your selection'}
    </span>
    <button class="cart-drawer__close" type="button"
            data-cart-close
            aria-label={lang === 'es' ? 'Cerrar panel' : 'Close panel'}>
      <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true">
        <path d="M5 5L15 15M15 5L5 15" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
      </svg>
    </button>
  </div>

  <ul class="cart-drawer__items"
      aria-label={lang === 'es' ? 'Servicios seleccionados' : 'Selected services'}>
    <!-- Populated by cart.ts syncCartUI() — do not add static items -->
  </ul>

  <div class="cart-drawer__footer">
    <div class="cart-drawer__total">
      <span class="cart-drawer__total-label">
        {lang === 'es' ? 'Total estimado' : 'Estimated total'}
      </span>
      <span class="cart-drawer__total-value">
        $<span data-cart-total>0</span> USD
      </span>
    </div>
    <button class="cart-drawer__cta" type="button" data-cart-ticket>
      {lang === 'es' ? 'Abrir Ticket' : 'Open Ticket'}
    </button>
  </div>

</aside>
```

Scoped `<style>` block — include ALL CSS from UI-SPEC "Cart Drawer CSS Contract":
- `.cart-backdrop` — position:fixed, inset:0, z-index:950, background:rgba(4,5,10,0.82), opacity:0, transition:opacity 0.3s ease, pointer-events:none
- `.cart-backdrop.active` — opacity:1, pointer-events:all
- `.cart-backdrop[hidden]` — display:none
- `.cart-drawer` — position:fixed, top:0, right:0, bottom:0, z-index:960, width:min(420px,92vw), background:var(--color-ink-raised), border-left:1px solid var(--color-border), display:flex, flex-direction:column, transform:translateX(100%), transition:transform 0.3s ease
- `.cart-drawer.active` — transform:translateX(0)
- `.cart-drawer[hidden]` — display:none
- `.cart-drawer__header` — display:flex, align-items:center, justify-content:space-between, padding:var(--space-sm), background:var(--color-ink), border-bottom:1px solid var(--color-border), flex-shrink:0
- `.cart-drawer__title` — font-family:var(--font-mono), font-size:12px, letter-spacing:0.24em, text-transform:uppercase, color:var(--color-white-dim)
- `.cart-drawer__close` — display:flex, align-items:center, justify-content:center, min-width:32px, min-height:32px, background:transparent, border:none, color:var(--color-white-dim), cursor:pointer, transition:color 0.2s ease
- `.cart-drawer__close:hover, :focus-visible` — color:var(--color-white)
- `.cart-drawer__close:focus-visible` — outline:2px solid var(--color-white), outline-offset:2px
- `.cart-drawer__items` — flex:1, overflow-y:auto, overscroll-behavior:contain, list-style:none, margin:0, padding:0
- `.cart-drawer__item` (for JS-generated items) — display:flex, align-items:center, justify-content:space-between, gap:var(--space-2xs), padding:var(--space-sm), border-bottom:1px solid var(--color-border), transition:background 0.2s ease
- `.cart-drawer__item:hover` — background:color-mix(in srgb, var(--color-paper-on-ink) 4%, transparent)
- `.cart-drawer__item-name` — font-family:var(--font-body), font-size:16px, color:var(--color-paper-on-ink), flex:1
- `.cart-drawer__item-qty` — font-family:var(--font-mono), font-size:14px, color:var(--color-white-dim), flex-shrink:0
- `.cart-drawer__item-price` — font-family:var(--font-mono), font-size:16px, font-variant-numeric:tabular-nums, letter-spacing:0.04em, color:var(--color-paper-on-ink), flex-shrink:0
- `.cart-drawer__item-remove` — background:transparent, border:none, color:var(--color-white-dim), font-size:16px, cursor:pointer, min-width:32px, min-height:32px, display:flex, align-items:center, justify-content:center, transition:color 0.2s ease
- `.cart-drawer__item-remove:hover` — color:var(--color-red-on-paper)
- `.cart-drawer__item-remove:focus-visible` — outline:2px solid var(--color-white), outline-offset:2px
- `.cart-drawer__footer` — flex-shrink:0, padding:var(--space-lg) var(--space-sm), background:var(--color-ink), border-top:1px solid var(--color-border), display:flex, flex-direction:column, gap:var(--space-md)
- `.cart-drawer__total` — display:flex, justify-content:space-between, align-items:baseline
- `.cart-drawer__total-label` — font-family:var(--font-mono), font-size:14px, color:var(--color-white-dim)
- `.cart-drawer__total-value` — font-family:var(--font-mono), font-size:20px, font-weight:700, font-variant-numeric:tabular-nums, color:var(--color-white)
- `.cart-drawer__cta` — display:block, width:100%, min-height:44px, padding:var(--space-sm), background:var(--color-red), color:var(--color-white), border:none, font-family:var(--font-body), font-size:14px, font-weight:400, letter-spacing:0.08em, text-transform:uppercase, cursor:pointer, transition:background 0.2s ease, transform 0.2s ease
- `.cart-drawer__cta:hover` — background:var(--color-red-dark), transform:translateY(-1px)
- `.cart-drawer__cta:focus-visible` — outline:2px solid var(--color-white), outline-offset:3px

Do NOT add "dc-cart-trigger" class to the CTA button — that class does not exist in chrome.ts and would create confusion. Use only data-cart-ticket attribute for JS targeting.
  </action>
  <verify>
    <automated>
      node -e "const fs=require('fs'); const s=fs.readFileSync('src/components/CartDrawer.astro','utf8'); console.assert(s.includes('id=\"cartDrawer\"'),'missing id'); console.assert(s.includes('id=\"cartBackdrop\"'),'missing backdrop id'); console.assert(s.includes('id=\"cartDrawerTitle\"'),'missing title id'); console.assert(s.includes('data-cart-close'),'missing close attr'); console.assert(s.includes('data-cart-ticket'),'missing ticket attr'); console.assert(s.includes('data-cart-total'),'missing total attr'); console.assert(s.includes('z-index: 960'),'wrong drawer z-index'); console.assert(s.includes('z-index: 950'),'wrong backdrop z-index'); console.assert(s.includes('translateX(100%)'),'missing slide transform'); console.assert(s.includes('translateX(0)'),'missing active transform'); console.log('PASS')"
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at src/components/CartDrawer.astro
    - HTML contains id="cartBackdrop" (backdrop div) and id="cartDrawer" (aside)
    - HTML contains id="cartDrawerTitle" on the title span
    - HTML contains data-cart-close on the close button
    - HTML contains data-cart-ticket on the drawer CTA button
    - HTML contains [data-cart-total] span inside .cart-drawer__total-value
    - Both backdrop and drawer have the hidden attribute
    - aside has role="dialog", aria-modal="true", aria-labelledby="cartDrawerTitle"
    - Scoped style block has z-index: 960 (drawer) and z-index: 950 (backdrop)
    - Scoped style block has transform: translateX(100%) on .cart-drawer (closed state)
    - Scoped style block has .cart-drawer[hidden] { display: none } and .cart-backdrop[hidden] { display: none }
  </acceptance_criteria>
  <done>CartDrawer.astro renders hidden backdrop + drawer with all data-* attributes, correct z-indexes, and complete CSS for the slide-in transition</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static HTML → browser | These are static Astro components; no user input rendered here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-02 | Information Disclosure | CartDrawer.astro | accept | Static shell — no sensitive data; cart contents populated at runtime by JS from user interactions |
| T-03.1-SC | Tampering | npm installs | accept | No new npm packages in this plan |
</threat_model>

<verification>
Run `npm run build` — should compile without errors. Verify CartPill.astro and CartDrawer.astro exist in src/components/. The components are not yet mounted in any page (that happens in Plan 03.1-04), so visual verification is not yet possible.
</verification>

<success_criteria>
1. `npm run build` completes with 0 errors
2. src/components/CartPill.astro exists with id="cartPill", cart-pill class, hidden attr, badge, bilingual label
3. src/components/CartDrawer.astro exists with id="cartBackdrop", id="cartDrawer", data-cart-close, data-cart-ticket, data-cart-total, all CSS transitions
</success_criteria>

<output>
Create `.planning/phases/03.1-catalog-configurator/03.1-02-SUMMARY.md` when done
</output>
