---
phase: 02-experimental-motion-layer
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - src/scripts/motion/transitions.ts
  - src/scripts/motion/index.ts
  - src/layouts/BaseLayout.astro
  - src/scripts/chrome.ts
  - src/styles/transitions.css
autonomous: false
requirements: [NAV-04]
user_setup: []

must_haves:
  truths:
    - "Navigating between pages plays a quick glitch-wipe transition (fast wipe + brief chromatic flash) that echoes the hero shader"
    - "On the first visit only, a brief (~1s) skippable glitch-tag intro plays while shader+fonts init; repeat visits skip it instantly; reduced-motion disables it"
    - "The nav hides on scroll-down and shows on scroll-up; the floating Abrir Ticket CTA stays persistent with a subtle pulse"
    - "The CTA stays clickable during the intro, mid-transition, and mid-scroll — it is never blocked or torn down"
    - "Under reduced-motion the transition falls back to a gentle fade/none and the intro does not play"
  artifacts:
    - path: "src/scripts/motion/transitions.ts"
      provides: "Glitch-wipe orchestration + first-visit-only skippable intro (D-17/D-18) + D-19 nav hide/CTA coordination"
      contains: "nocturna-intro-seen"
    - path: "src/layouts/BaseLayout.astro"
      provides: "transition:animate glitch-wipe on the main/slot wrapper"
      contains: "transition:animate"
    - path: "src/styles/transitions.css"
      provides: "glitch-wipe @keyframes (RGB-split red fringe) + reduced-motion fallback + intro styles"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "ClientRouter transition:animate"
      via: "custom glitch-wipe animation on the page wrapper"
      pattern: "transition:animate"
    - from: "src/scripts/motion/transitions.ts"
      to: "localStorage nocturna-intro-seen"
      via: "first-visit-only intro flag (mirrors nocturna-lang idiom)"
      pattern: "nocturna-intro-seen"
    - from: "src/scripts/chrome.ts"
      to: "nav hide-on-scroll (D-19)"
      via: "single shared scroll handler — no double-bind with the existing 60px scrolled toggle"
      pattern: "scroll"
---

<objective>
Finalize NAV-04 and the chrome-in-motion behaviors: the quick glitch-wipe page transition (D-17) that makes the glitch motif read as one language with the hero/cursor, the first-visit-only skippable glitch-tag intro (D-18), and the D-19 chrome behavior — nav hide-on-scroll-down / show-on-scroll-up plus the persistent pulsing Abrir Ticket CTA. This plan completes the through-line so the four glitch surfaces (shader, transition, micro-flicker, intro) feel cohesive, while keeping the conversion path reachable in every motion state.

Purpose: NAV-04's visible payoff (animated transitions) plus the immersion chrome — landed last so it composes the already-shipped scroll/shader/cursor slices rather than blocking them.
Output: `transitions.ts`, the `transition:animate` wiring + intro markup in BaseLayout, `transitions.css`, and the D-19 nav-scroll coordination in `chrome.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-experimental-motion-layer/02-CONTEXT.md
@.planning/phases/02-experimental-motion-layer/02-RESEARCH.md
@.planning/phases/02-experimental-motion-layer/02-PATTERNS.md
@.planning/phases/02-experimental-motion-layer/02-UI-SPEC.md
@.planning/phases/02-experimental-motion-layer/02-01-SUMMARY.md
@.planning/phases/02-experimental-motion-layer/02-02-SUMMARY.md
@.planning/phases/02-experimental-motion-layer/02-03-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 and the codebase. -->

From Plan 01 (src/scripts/motion/index.ts): add initTransitions()/destroyTransitions() to initMotion()/teardownMotion(). Controller already listens on astro:page-load / astro:before-swap.
From Plan 01 (src/scripts/motion/motion-prefs.ts): prefersReduced gates the intro + bold transition.

BaseLayout structure (src/layouts/BaseLayout.astro): page content is the unnamed <slot /> at :47. The glitch-wipe transition:animate goes on the page wrapper (wrap the slot in a <main transition:animate={...}> per RESEARCH §Code Examples). ClientRouter is added by Plan 01.

Existing nav scroll behavior to coordinate with (src/scripts/chrome.ts:24-28) — the 60px `.scrolled` toggle. D-19 hide-on-scroll must SHARE one scroll handler, not bind a second `scroll` listener (Pitfall 8).
FloatingCTA: .floating-cta / .dc-trigger (FloatingCTA.astro:19); the CTA pulse styling was added in Plan 03's choreography.css (D-19) — this plan coordinates the entrance/persistence, do not re-author the pulse keyframes if Plan 03 already shipped them.

Intro persistence idiom (mirror existing nocturna-lang, src/scripts/chrome.ts:52-56 try/catch): localStorage key `nocturna-intro-seen` (RESEARCH Open Q2 — persistent "instant on repeat visits").

Glitch color: red fringe from --red / --red-glow tokens; keep the transition ≤~300ms (D-17 snappy).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Glitch-wipe page transition + first-visit-only intro (D-17/D-18)</name>
  <files>src/scripts/motion/transitions.ts, src/layouts/BaseLayout.astro, src/styles/transitions.css, src/scripts/motion/index.ts</files>
  <read_first>
    - src/layouts/BaseLayout.astro (full — the <slot /> at :47 and the css imports; ClientRouter added by Plan 01)
    - src/pages/[lang]/index.astro:30-44 (localStorage try/catch detection idiom) and src/scripts/chrome.ts:52-56 (try/catch storage idiom)
    - src/styles/tokens.css (--red, --red-glow for the chromatic fringe; --white)
    - .planning/phases/02-experimental-motion-layer/02-RESEARCH.md §Code Examples Glitch-wipe (custom transition:animate, steps() easing, ≤300ms, reduced-motion → fade/none), Open Q2 (intro persistence), Pitfall 9 (intro waits on fonts, must not block readable hero)
    - .planning/phases/02-experimental-motion-layer/02-PATTERNS.md §transitions.ts
    - .planning/phases/02-experimental-motion-layer/02-UI-SPEC.md Page transitions & intro (D-17 quick glitch wipe; D-18 ~1s first-visit-only, skippable, no copy, never blocks CTA/content) + Copywriting (intro uses NO new copy; non-text tap-to-skip preferred)
  </read_first>
  <action>
    Glitch-wipe transition (D-17): in `BaseLayout.astro`, define a custom `transition:animate` object (old/new with `glitch-out`/`glitch-in` named animations, duration ≤~0.3s, `steps()` easing per RESEARCH §Code Examples) and apply it to the page wrapper around the `<slot />` (wrap the slot in `<main transition:animate={...}>` if not already wrapped). Author `@keyframes glitch-out`/`glitch-in` in `transitions.css` with a brief RGB-split / chromatic red fringe from `--red`/`--red-glow` so it echoes the hero shader (one motif). Under `@media (prefers-reduced-motion: reduce)`, the transition collapses to a gentle fade or none (D-21) — use Astro's built-in fade fallback.

    First-visit-only intro (D-18): create `transitions.ts` exporting `initTransitions()` and `destroyTransitions()`. `initTransitions()`: if `prefersReduced`, never play the intro (D-18). Otherwise, on the initial hard load only, check `localStorage.getItem('nocturna-intro-seen')` inside try/catch (mirror the `nocturna-lang` idiom); if unseen, play a brief (~1s) glitch-tag intro overlay while fonts/shader init, set the flag, and make it SKIPPABLE via a non-text click/tap-to-skip (no new copy string — UI-SPEC Copywriting). The intro overlay must be arranged so it NEVER blocks the CTA/content — the FloatingCTA stays clickable; prefer an overlay that auto-dismisses and is click-through-to-skip — and must not delay paint of the readable hero (hero text/CTA already render pre-JS, D-20). On repeat visits / flag set, do nothing (instant). Gate the intro so it plays only on the initial `astro:page-load`, not on every in-site navigation. Add intro overlay styles to `transitions.css`, neutralized under reduced-motion.

    `destroyTransitions()`: remove the intro overlay + its listeners if present (so a swap mid-intro cleans up). Wire `initTransitions()`/`destroyTransitions()` into the Plan 01 controller. Import `transitions.css` from BaseLayout alongside the other motion css.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -q "transition:animate" src/layouts/BaseLayout.astro && grep -q "nocturna-intro-seen" src/scripts/motion/transitions.ts && grep -Eq "glitch-out|glitch-in" src/styles/transitions.css && grep -q "prefers-reduced-motion" src/styles/transitions.css && grep -q "initTransitions" src/scripts/motion/index.ts && npx astro build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `transition:animate` applied on the page wrapper in BaseLayout; `@keyframes glitch-out`/`glitch-in` exist in transitions.css with an RGB-split red fringe (tokens).
    - Transition duration ≤~300ms; under `prefers-reduced-motion: reduce` it collapses to fade/none.
    - `transitions.ts` uses `localStorage` key `nocturna-intro-seen` in try/catch; intro plays only first-visit, only `!prefersReduced`, only on initial load, is skippable, and adds NO new copy string.
    - The intro overlay never blocks the CTA/content and does not delay the readable hero paint; `destroyTransitions()` removes it on swap.
    - Behavior: navigating pages shows the glitch wipe; first ever visit shows the ~1s intro then never again; reduced-motion shows neither; CTA clickable throughout.
    - `npx astro build` succeeds.
  </acceptance_criteria>
  <done>Page transitions play the quick glitch wipe, the first-visit intro plays once and is skippable, reduced-motion disables both, and the CTA stays reachable in every state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: D-19 chrome — nav hide-on-scroll + persistent pulsing CTA (single shared scroll handler)</name>
  <files>src/scripts/chrome.ts, src/scripts/motion/transitions.ts, src/styles/transitions.css</files>
  <read_first>
    - src/scripts/chrome.ts:18-44 (initNav — the existing 60px `.scrolled` passive scroll listener; D-19 must SHARE this, not double-bind — Pitfall 8) and the Plan 01 migration of the bottom gate to astro:page-load
    - src/components/Nav.astro:45 (.nav[data-nav]) and src/components/FloatingCTA.astro:19 (.floating-cta.dc-trigger)
    - src/styles/chrome.css:306-345 (.floating-cta existing box-shadow red-glow values) and :536-544 (reduced-motion block)
    - Plan 03's src/styles/choreography.css (the CTA pulse keyframe, if already shipped — do not duplicate)
    - .planning/phases/02-experimental-motion-layer/02-RESEARCH.md Pitfall 8 (nav scroll listener must coordinate with D-19, do not double-bind)
    - .planning/phases/02-experimental-motion-layer/02-UI-SPEC.md Nav in motion + Floating CTA in motion (D-19) + CTA survivability (hard)
  </read_first>
  <action>
    Extend the existing `initNav` scroll handling in `chrome.ts` (do NOT add a second `scroll` listener — Pitfall 8). In the single passive scroll handler that already toggles `.scrolled` at 60px, also compute scroll direction (compare current `window.scrollY` to a tracked last value) and toggle a `.nav--hidden` class on the nav when scrolling down past a small threshold, removing it when scrolling up (D-19 hide-on-scroll-down / show-on-scroll-up). Keep it in `chrome.ts` (not a new motion module) because the nav listener lives there and Plan 01 already made it swap-safe; this avoids two scroll listeners fighting. Guard the direction logic so it is a no-op increment when scroll positions are equal.

    Add the `.nav--hidden` transform (translateY off-screen) and transition to `transitions.css` (or chrome.css if cleaner) using tokens; under `@media (prefers-reduced-motion: reduce)`, the nav does not hide (always visible — content/chrome fully present, D-21).

    Persistent pulsing CTA (D-19): ensure the `.floating-cta` stays persistent and gets a subtle pulse/entrance. If Plan 03 already shipped the pulse keyframe in choreography.css, just confirm it is applied and reduced-motion-neutralized; otherwise add a subtle pulse using the existing `--red-glow` box-shadow values from chrome.css:306-345. The CTA must remain clickable and NOT be hidden by the nav-hide behavior (it is a separate fixed element). Under reduced-motion the pulse is neutralized but the CTA stays present.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -q "nav--hidden" src/scripts/chrome.ts && grep -Ec "addEventListener\\('scroll'|addEventListener\\(\"scroll\"" src/scripts/chrome.ts | grep -q '^1$' && grep -Eq "nav--hidden|floating-cta" src/styles/transitions.css && npx astro build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `chrome.ts` toggles `.nav--hidden` based on scroll direction within the SINGLE existing scroll listener — `grep -c "addEventListener('scroll'" chrome.ts` returns exactly 1 (no double-bind, Pitfall 8).
    - `.nav--hidden` transform/transition defined (tokens); under `prefers-reduced-motion: reduce` the nav stays visible.
    - The `.floating-cta` pulse is present (Plan 03's or added here) and neutralized under reduced-motion; the CTA is not hidden by the nav-hide behavior.
    - Behavior: scrolling down hides the nav, scrolling up shows it; the Abrir Ticket FloatingCTA stays visible + clickable throughout; reduced-motion keeps the nav always visible.
    - `npx astro build` succeeds.
  </acceptance_criteria>
  <done>Nav hides on scroll-down / shows on scroll-up via one shared listener, the floating CTA stays persistent and pulsing, and reduced-motion keeps the nav visible — the CTA is reachable in every motion state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ClientRouter swap → DOM | Page content swapped + animated client-side |
| localStorage → intro state | `nocturna-intro-seen` flag read/written client-side (UI flag only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-CTA | Availability of conversion path | intro overlay + nav-hide | mitigate | Intro overlay is click-through-to-skip and never blocks the CTA; nav-hide only moves the nav, the FloatingCTA is a separate persistent fixed element; D-19 keeps CTA clickable mid-intro/mid-transition/mid-scroll |
| T-02-DOS | Denial of Service (self-inflicted) | scroll listener + transition | mitigate | Single shared passive scroll handler (no double-bind, Pitfall 8); transition is CSS-keyframe driven (no rAF leak); intro overlay removed on swap |
| T-02-TAMPER | Tampering | nocturna-intro-seen flag | accept | Flag is a non-sensitive UI boolean; tampering only re-plays/skips a ~1s cosmetic intro — no security impact (RESEARCH Runtime State Inventory: only a UI flag) |
| T-02-INJ | Tampering/Injection | transition/intro DOM | mitigate | No `innerHTML`/`eval`; intro overlay built via createElement; glitch keyframes are local CSS, no remote fetch (static CSP posture preserved) |
| T-02-AUTH/INFO/REP | Auth / Info disclosure / Repudiation | n/a | accept | No auth, PII, or transactions (RESEARCH Security Domain) |
</threat_model>

<verification>
- `npx astro build` succeeds and emits static output.
- NAV-04 smoke: navigate landing → /servicios → back → forward → glitch-wipe plays each time; ScrollTrigger/Lenis do not leak (Plan 01 teardown), nav/modal still work (Validation Architecture).
- Intro smoke: clear `nocturna-intro-seen` + first visit → ~1s skippable intro; reload → no intro (instant); reduced-motion → no intro; CTA clickable during the intro.
- D-19 smoke: scroll down → nav hides; scroll up → nav shows; FloatingCTA persistent + pulsing + clickable throughout; reduced-motion → nav stays visible.
- CTA survivability (hard): during intro, mid-transition, and mid-scroll the FloatingCTA opens the Discord modal.
</verification>

<success_criteria>
- NAV-04 complete: animated glitch-wipe page transitions via ClientRouter (D-17), reduced-motion → fade/none.
- First-visit-only skippable intro (D-18) plays once, never blocks the CTA, disabled under reduced-motion.
- D-19 chrome: nav hide-on-scroll-down / show-on-scroll-up (single shared listener), persistent pulsing CTA.
- The Abrir Ticket CTA is reachable in every motion state; the glitch motif reads as one cohesive language across shader/transition/micro-flicker/intro.
- Site builds to static output; no listener/trigger leak across navigation.
</success_criteria>

<output>
Create `.planning/phases/02-experimental-motion-layer/02-04-SUMMARY.md` when done.
</output>
