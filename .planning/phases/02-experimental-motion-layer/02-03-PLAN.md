---
phase: 02-experimental-motion-layer
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/scripts/motion/cursor.ts
  - src/scripts/motion/choreography.ts
  - src/scripts/motion/index.ts
  - src/styles/cursor.css
  - src/styles/choreography.css
autonomous: false
requirements: [FX-04]
user_setup: []

must_haves:
  truths:
    - "A desktop (fine-pointer) visitor sees a blend-mode difference invert-circle custom cursor that lerps after the pointer (D-06: blend-mode difference invert circle)"
    - "Interactive elements (CTA, nav links, cards) magnetically nudge ≤8px toward the cursor and the cursor grows over them, without shifting their click hit target (D-07: magnetic pull on interactive elements + cursor grows over them)"
    - "Buttons/nav/package cards have a tasteful cohesive micro-interaction set (lift / underline-draw / glitch flicker echoing the hero) (D-09: tasteful cohesive micro-interactions — lift/underline-draw/glitch flicker, not noisy)"
    - "The gallery teaser scrolls as a horizontal marquee, package cards reveal with a subtle 3D tilt, and featured work parallaxes against the background (D-14: gallery teaser scroll-driven horizontal marquee; D-15: package cards staggered reveal + subtle 3D tilt/hover; D-16: featured work parallax depth against background on scroll)"
    - "Touch devices keep the native cursor with no magnetism; reduced-motion disables the custom cursor and bold choreography (D-08: desktop/fine-pointer only — gated by pointer:fine and hover:hover; D-21: reduced-motion disables the custom cursor)"
  artifacts:
    - path: "src/scripts/motion/cursor.ts"
      provides: "Custom cursor + gsap.quickTo follow + magnetism, fine-pointer gated"
      contains: "quickTo"
    - path: "src/scripts/motion/choreography.ts"
      provides: "Gallery marquee (D-14), card 3D tilt (D-15), featured parallax (D-16)"
      contains: "ScrollTrigger"
    - path: "src/styles/cursor.css"
      provides: "Custom cursor mix-blend-mode difference + pointer-fine gating + reduced-motion off"
      contains: "mix-blend-mode: difference"
    - path: "src/styles/choreography.css"
      provides: "Micro-interactions (lift/underline/glitch flicker) + reduced-motion overrides"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "src/scripts/motion/cursor.ts"
      to: ".dc-trigger / .nav-link / .hero-cta / .card"
      via: "magnetism targets — transform only, never the hit box"
      pattern: "dc-trigger"
    - from: "src/scripts/motion/index.ts"
      to: "src/scripts/motion/cursor.ts + choreography.ts"
      via: "initCursor()/initChoreography() in initMotion, destroy in teardownMotion"
      pattern: "initCursor"
    - from: "src/scripts/motion/cursor.ts"
      to: "src/scripts/motion/motion-prefs.ts"
      via: "finePointer && !prefersReduced gate"
      pattern: "finePointer"
---

<objective>
Deliver the interaction + landing-choreography slice: the custom cursor (FX-04, D-06) with magnetic pull and grow-on-hover (D-07), the cohesive micro-interaction set (D-09), and the bespoke landing moments — gallery-teaser horizontal marquee (D-14), package-card 3D tilt (D-15), and featured-work parallax (D-16). All of it is desktop/fine-pointer-aware (D-08), reduced-motion-aware (D-21), and — the hard rule — must never shift the Abrir Ticket / Discord-modal click targets (D-19, Pitfall 7).

Purpose: This is the layer that draws the eye toward the conversion CTA and makes the pricing/showcase moments feel premium — the "tasteful, cohesive, not noisy" interaction language.
Output: `cursor.ts`, `choreography.ts`, `cursor.css`, `choreography.css`, wired into the Plan 01 controller.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-experimental-motion-layer/02-CONTEXT.md
@.planning/phases/02-experimental-motion-layer/02-RESEARCH.md
@.planning/phases/02-experimental-motion-layer/02-PATTERNS.md
@.planning/phases/02-experimental-motion-layer/02-UI-SPEC.md
@.planning/phases/02-experimental-motion-layer/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 and the codebase. -->

From Plan 01 (src/scripts/motion/motion-prefs.ts):
  export const prefersReduced: boolean; export const finePointer: boolean; export function webglOK(): boolean;
  // customCursor = finePointer && !prefersReduced

From Plan 01 (src/scripts/motion/index.ts): add initCursor()/initChoreography() to initMotion(); destroyCursor()/destroyChoreography() to teardownMotion().

Magnetism targets (existing wired elements — magnetism must NOT shift their hit box, Pitfall 7):
  .dc-trigger (FloatingCTA.astro:19, Nav.astro:72, PackageCard.astro:65, hero CTAs index.astro:53) — these open the Discord modal
  .nav-link (Nav.astro:64), .hero-cta (index.astro:52-53), .card (PackageCard.astro:40)

Choreography targets (existing markup — animate, do NOT re-author):
  Gallery marquee (D-14): Teaser.astro variant="gallery" → <section class="section section--surface section--rule"> (Teaser:24)
  Card 3D tilt (D-15): PackageCard.astro:40 (.card.reveal) inside .cards.cards--summary (PackagesSummary:28)
  Featured parallax (D-16): Teaser.astro variant="featured"

Brand tokens (src/styles/tokens.css):
  --space-2xs 8px (max magnetism displacement), --space-xl 48px (magnetism catch radius), --red-glow #e8223b (glitch-flicker accent only — NOT a tint on the cursor)
Reduced-motion CSS idiom to extend: src/styles/chrome.css:536-544.
GSAP: import { gsap } from 'gsap'; import { ScrollTrigger } from 'gsap/ScrollTrigger'; gsap.quickTo for the cursor follow.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Custom cursor + magnetism + micro-interactions (FX-04, D-06/07/08/09)</name>
  <files>src/scripts/motion/cursor.ts, src/styles/cursor.css, src/styles/choreography.css, src/scripts/motion/index.ts</files>
  <read_first>
    - src/scripts/chrome.ts:91-123 (initDiscordModal — the .dc-trigger/pointer/listener idiom and the click targets magnetism must not disturb)
    - src/scripts/motion/motion-prefs.ts (Plan 01 — finePointer, prefersReduced)
    - src/components/FloatingCTA.astro:19, src/components/Nav.astro:64,72, src/components/PackageCard.astro:40,65 (magnetism targets)
    - src/styles/tokens.css (--space-2xs 8px displacement, --space-xl 48px radius, --red-glow flicker accent)
    - src/styles/chrome.css:536-544 (reduced-motion @media block to extend) and :306-345 (.floating-cta box-shadow red-glow values to echo)
    - .planning/phases/02-experimental-motion-layer/02-RESEARCH.md Pattern 4 (gsap.quickTo cursor + magnetism) and Pitfall 7 (cursor must never steal/shift CTA/modal hit target; cursor div pointer-events:none)
    - .planning/phases/02-experimental-motion-layer/02-PATTERNS.md §cursor.ts and §motion.css (CTA pulse / micro-interactions)
    - .planning/phases/02-experimental-motion-layer/02-UI-SPEC.md Cursor & micro-interactions (D-06/07/09) + Spacing (≤8px displacement, ~48px radius)
  </read_first>
  <action>
    Create `cursor.ts` exporting `initCursor()` and `destroyCursor()`. `initCursor()`: gate the ENTIRE module behind `finePointer && !prefersReduced` — if false, return and leave the native cursor (D-08/D-21). Otherwise create (once) a fixed-position cursor `<div>` appended to `<body>` with `pointer-events: none` (Pitfall 7). Move it with `gsap.quickTo(cursorEl, 'x'/'y', { duration: 0.25, ease: 'power3' })` driven by a `pointermove` listener (RESEARCH Pattern 4 — frame-synced, no manual loop).

    Magnetism (D-07): for each magnetism target (`.dc-trigger`, `.nav-link`, `.hero-cta`, `.card`), when the pointer is within ~48px (`--space-xl`) of the element, translate the element toward the cursor by ≤8px (`--space-2xs`) using `transform` ONLY (never top/left/width/height, never its hit box — Pitfall 7) and grow the cursor over it. On leave, ease the element + cursor back. The magnetic displacement is purely visual; the click hit-test must stay where the static layout puts it so the Discord modal still opens from `.dc-trigger`.

    `destroyCursor()`: remove the pointermove + per-target listeners, kill the quickTo tweens, and remove the cursor div so a swap re-init starts clean.

    Create `cursor.css`: the cursor div styling (invert circle, `mix-blend-mode: difference` — derives contrast from beneath, introduces NO new hue per D-06), its grow state, all gated behind `@media (pointer: fine) and (hover: hover)`, plus a `@media (prefers-reduced-motion: reduce)` rule that hides the custom cursor entirely (native cursor restored, D-21).

    Create `choreography.css` for the micro-interaction set (D-09) on `.hero-cta`/`.nav-link`/`.card`/buttons: a subtle lift (transform translateY/scale), an underline-draw on links, and a small glitch-flicker accent that echoes the hero (brief, low-coverage, may use `--red-glow` ONLY as the flicker accent — not a tint on every effect, UI-SPEC Color). Add the CTA pulse for `.floating-cta` (D-19) using the existing `--red-glow` box-shadow values from chrome.css:306-345. Extend the reduced-motion `@media` block to neutralize lift/underline/flicker/pulse to a gentle or no-op state (D-21). All values from tokens — no off-scale px.

    Wire `initCursor()`/`destroyCursor()` into the Plan 01 controller.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -q "quickTo" src/scripts/motion/cursor.ts && grep -q "finePointer" src/scripts/motion/cursor.ts && grep -q "pointer-events: none" src/styles/cursor.css && grep -q "mix-blend-mode: difference" src/styles/cursor.css && grep -q "prefers-reduced-motion" src/styles/choreography.css && ! grep -Eq "(top|left|width|height):" src/scripts/motion/cursor.ts && grep -q "initCursor" src/scripts/motion/index.ts && npx astro build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - cursor.ts is fully gated behind `finePointer && !prefersReduced`; cursor div is `pointer-events: none`.
    - Magnetism uses `transform` only — `grep -E "(top|left|width|height):" cursor.ts` finds nothing; displacement caps at `--space-2xs` (8px), catch radius `--space-xl` (48px).
    - cursor.css uses `mix-blend-mode: difference` and gates behind `(pointer: fine) and (hover: hover)`; reduced-motion hides the custom cursor.
    - choreography.css has lift/underline/glitch-flicker micro-interactions + `.floating-cta` pulse, with a reduced-motion neutralizing block; red used only as the flicker/pulse accent.
    - Behavior: desktop → custom cursor follows + magnetism nudges targets ≤8px; the Abrir Ticket FloatingCTA and Discord modal still open on click with the cursor active (Pitfall 7); touch / reduced-motion → native cursor, no magnetism.
    - `npx astro build` succeeds.
  </acceptance_criteria>
  <done>Desktop visitors get the blend-difference cursor with magnetism and a cohesive micro-interaction set; the CTA/modal hit targets are never shifted; touch and reduced-motion fall back to the native cursor.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Landing choreography — gallery marquee, card tilt, featured parallax (D-14/15/16)</name>
  <files>src/scripts/motion/choreography.ts, src/styles/choreography.css, src/scripts/motion/index.ts</files>
  <read_first>
    - src/components/sections/Teaser.astro (variant="gallery" at :24 for the marquee; variant="featured" for parallax)
    - src/components/sections/PackagesSummary.astro:28 (.cards.cards--summary) + src/components/PackageCard.astro:40 (.card.reveal — tilt target)
    - src/scripts/chrome.ts:107-112 (querySelectorAll + bind idiom)
    - src/scripts/motion/motion-prefs.ts (Plan 01 — prefersReduced, finePointer)
    - .planning/phases/02-experimental-motion-layer/02-RESEARCH.md anti-patterns (transform/opacity only; never top/left/width/height) and Pattern 1 (ScrollTrigger already wired by Plan 01's one-clock)
    - .planning/phases/02-experimental-motion-layer/02-PATTERNS.md §choreography.ts
    - .planning/phases/02-experimental-motion-layer/02-UI-SPEC.md Landing choreography (D-14 horizontal marquee, D-15 staggered + 3D tilt, D-16 parallax depth) + D-05 (hero parallax fade is owned by the shader plan; this plan owns sections below the hero)
  </read_first>
  <action>
    Create `choreography.ts` exporting `initChoreography()` and `destroyChoreography()`. All effects gate on `!prefersReduced` (under reduced-motion the sections still reveal via Plan 01's gentle fade — no marquee scrub, no tilt, no parallax, D-21). Only run on the landing (`location.pathname` is `/`, `/es/`, `/en/`) — dedicated pages get the subtler Plan 01 treatment only (D-12).

    - Gallery teaser marquee (D-14): on the `Teaser variant="gallery"` section, build a scroll-driven horizontal track (translateX) using a ScrollTrigger with `scrub` so the preview track moves horizontally as the user scrolls vertically. `transform: translateX` only.
    - Package-card 3D tilt (D-15): on each `.card` inside `.cards--summary`, add a pointer-driven subtle `transform: rotateX/rotateY` tilt (small angles) plus the staggered reveal already handled by Plan 01; gate the pointer tilt behind `finePointer` (touch gets the staggered reveal without tilt). Tilt is `transform` only and must not affect the `.dc-trigger` inside the card's hit box.
    - Featured-work parallax (D-16): on `Teaser variant="featured"`, drive a `transform: translateY` parallax of the foreground vs background with a `scrub` ScrollTrigger (depth against the background). `transform` only — never layout properties (perf budget).

    `destroyChoreography()`: kill the ScrollTriggers and pointer listeners this module created (do NOT kill Plan 01's reveal triggers indiscriminately — track this module's own triggers, e.g. tag them or keep references). This keeps `ScrollTrigger.getAll().length` stable across navigations (Validation Architecture).

    Add any needed choreography styles to `choreography.css` (marquee track overflow, card perspective for the tilt, will-change hints) — token-driven, with the reduced-motion block neutralizing them. Wire `initChoreography()`/`destroyChoreography()` into the Plan 01 controller.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -q "ScrollTrigger" src/scripts/motion/choreography.ts && grep -Eq "translateX|rotateX|rotateY|translateY" src/scripts/motion/choreography.ts && grep -q "prefersReduced" src/scripts/motion/choreography.ts && ! grep -Eq "(^|[^-])(top|left|width|height):" src/scripts/motion/choreography.ts && grep -q "initChoreography" src/scripts/motion/index.ts && npx astro build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - choreography.ts animates only `transform`/`opacity` (translateX marquee, rotateX/Y tilt, translateY parallax) — no top/left/width/height.
    - All three effects are gated behind `!prefersReduced` and run only on the landing path; tilt additionally behind `finePointer`.
    - `destroyChoreography()` kills only this module's ScrollTriggers/listeners; `ScrollTrigger.getAll().length` stays stable across navigations.
    - Behavior: landing → gallery teaser scrolls horizontally on vertical scroll, package cards reveal staggered + tilt under the pointer, featured work parallaxes; reduced-motion → none of these, only Plan 01's gentle fade; the `.dc-trigger` in cards still opens the modal.
    - `npx astro build` succeeds.
  </acceptance_criteria>
  <done>The landing's showcase moments have their bespoke choreography (marquee/tilt/parallax) on capable desktops, degrade to gentle reveals under reduced-motion, run only on the landing, and never leak ScrollTriggers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| end-user pointer → cursor/magnetism/tilt | pointermove drives cursor follow, magnetism, and card tilt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-CTA | Availability of conversion path | cursor magnetism over .dc-trigger | mitigate | Magnetism uses `transform` only, displacement ≤8px, never the hit box; cursor div `pointer-events: none`; acceptance asserts FloatingCTA + Discord modal still click-open with the cursor active (Pitfall 7, D-19) |
| T-02-DOS | Denial of Service (self-inflicted) | pointermove + scrub ScrollTriggers | mitigate | `gsap.quickTo`/ScrollTrigger are frame-synced (no manual loops); choreography triggers tracked + killed on teardown so they never accumulate across navigations |
| T-02-INJ | Tampering/Injection | cursor/choreography DOM | mitigate | No `innerHTML`/`eval`; cursor div created via `createElement`; no remote input. Verify no new sinks |
| T-02-INFO/AUTH/REP | Info disclosure / Auth / Repudiation | n/a | accept | No data, auth, or transactions (RESEARCH Security Domain — pure pointer-driven visuals) |
</threat_model>

<verification>
- `npx astro build` succeeds.
- CTA survivability smoke (hard, Validation Architecture): with the custom cursor active, the FloatingCTA `.dc-trigger` and a card's `.dc-trigger` still open the Discord modal; magnetism never shifts the hit target enough to miss.
- Touch smoke: `(pointer: coarse)` emulation → no custom cursor, no magnetism, no tilt; sections still reveal (Plan 01).
- Reduced-motion smoke: emulate reduce → native cursor, no marquee/tilt/parallax, only gentle fades; CTA reachable.
- No-leak smoke: navigate landing → /servicios → back; `ScrollTrigger.getAll().length` stable, no duplicate cursor div.
</verification>

<success_criteria>
- FX-04: desktop custom cursor (blend-difference) with magnetic pull + grow (D-06/07), fine-pointer gated (D-08), cohesive micro-interactions (D-09).
- Landing choreography: gallery marquee (D-14), card 3D tilt (D-15), featured parallax (D-16) — landing-only, transform/opacity only.
- CTA/modal hit targets never shifted by magnetism (Pitfall 7, D-19).
- Touch + reduced-motion fall back cleanly; no ScrollTrigger/cursor leak across navigation.
</success_criteria>

<output>
Create `.planning/phases/02-experimental-motion-layer/02-03-SUMMARY.md` when done.
</output>
