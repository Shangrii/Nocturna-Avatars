---
phase: 02.1-visual-redesign-refined-street-editorial
plan: 04
type: execute
wave: 4
depends_on: ["02.1-03"]
files_modified:
  - src/styles/chrome.css
  - src/components/Nav.astro
  - src/components/Footer.astro
  - src/components/FloatingCTA.astro
  - src/components/DiscordModal.astro
  - src/components/LanguageSwitcher.astro
autonomous: false

requirements: [PLAT-03, PLAT-04]

must_haves:
  truths:
    - "The nav is a thin editorial bar: Space Mono meta links + graffiti wordmark, active-state indicator, hide-on-scroll behavior from Phase 2 still works (D-13/§5.6)"
    - "Footer, FloatingCTA, DiscordModal, and LanguageSwitcher are restyled to the editorial system (D-13)"
    - "Abrir Ticket is reachable from EVERY page via the nav CTA + persistent floating CTA (NAV-03)"
    - "All chrome controls keep ≥44px touch targets, visible focus rings, keyboard operability; prefers-reduced-motion respected (PLAT-04)"
    - "AA contrast holds for chrome text/icons over both ink and paper backdrops (PLAT-04)"
  artifacts:
    - path: "src/styles/chrome.css"
      provides: "Re-skinned editorial chrome (nav/footer/floating CTA/modal/lang switcher)"
      contains: "font-mono"
  key_links:
    - from: "src/components/Nav.astro"
      to: "Discord modal"
      via: "nav-cta dc-trigger"
      pattern: "dc-trigger"
    - from: "src/components/FloatingCTA.astro"
      to: "Discord modal"
      via: "floating-cta dc-trigger"
      pattern: "dc-trigger"
    - from: "src/scripts/chrome.ts"
      to: "Nav.astro hooks"
      via: "data-nav / data-nav-toggle / data-nav-links selectors"
      pattern: "data-nav"
---

<objective>
Re-skin the global chrome — nav, footer, floating CTA, Discord modal, language switcher — to the
Refined Street Editorial system, and run the final cross-surface a11y/responsive sweep so the whole
site holds the quality bars (D-13 / §5.6). Chrome lives in BaseLayout and renders on every page, so it
is sequenced last to avoid churning against the page slices and to act as the closing verification pass.

After this slice the site is fully re-skinned end-to-end: thin editorial nav with Space Mono meta + the
graffiti wordmark + active indicator + Phase-2 hide-on-scroll, an editorial footer, a restyled
persistent floating Abrir Ticket, a restyled Discord modal, and a restyled language switcher — all
keeping their `chrome.ts` behavior hooks and the conversion path reachable everywhere.

Purpose: ROADMAP success criterion 6 (chrome restyled, Abrir Ticket everywhere, AA, responsive,
reduced-motion); D-13. PLAT-03 (system applied to chrome), PLAT-04 (a11y/responsive — final sweep).
Output: Re-skinned chrome; verified AA/responsive/keyboard/reduced-motion across all surfaces.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md
@docs/superpowers/specs/2026-06-30-website-redesign-design.md
@src/styles/theme.css

<interfaces>
<!-- chrome.ts BEHAVIOR HOOKS THAT MUST SURVIVE (src/scripts/chrome.ts queries these — do NOT rename): -->
  Nav:    [data-nav], [data-nav-toggle], [data-nav-links]; the `.scrolled` class + `.nav--hidden`
          hide-on-scroll class are toggled by chrome.ts (transitions.css neutralizes hide under reduce)
  Lang:   [data-lang-menu], [data-lang-trigger], [data-lang-list], a[data-lang-switch="es|en"]
  Modal:  .dc-trigger (opens), [data-dc-overlay], [data-dc-dismiss] (close); Escape/backdrop close
  FloatingCTA: .floating-cta.dc-trigger
<!-- chrome.css currently owns: .nav/.nav-brand/.nav-link/.nav-cta, .lang-* , .nav-toggle, .footer*,
     .floating-cta, .dc-overlay/.dc-modal/.dc-* , plus the @media(max-width:768px) mobile drawer and
     @media(prefers-reduced-motion) block. Keep every selector name; restyle values. -->
<!-- Tailwind v4 utilities from theme.css: font-mono, font-tag, font-display, bg-ink, text-red, etc.
     The nav backdrop sits over both ink and paper sections — ensure its text/blur reads on both. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin chrome.css (nav, footer, floating CTA, modal, language switcher) to the editorial system</name>
  <files>src/styles/chrome.css</files>
  <read_first>
    - src/styles/chrome.css (every chrome selector + mobile drawer + reduced-motion block)
    - src/components/Nav.astro / Footer.astro / FloatingCTA.astro / DiscordModal.astro / LanguageSwitcher.astro (the class hooks consumed)
    - src/scripts/chrome.ts (the data-* hooks + .scrolled/.nav--hidden classes it toggles — must keep matching)
    - src/styles/theme.css (utilities/tokens)
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§5.6 chrome)
    - .planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md (D-05/D-08/D-09/D-13)
  </read_first>
  <action>
    Rewrite `src/styles/chrome.css` to the editorial system, KEEPING every selector name. Nav: thin bar
    (height `var(--nav-h)`), 0px radii, hairline 1px rules; `.nav-brand` keeps `font-family: var(--font-tag)`
    (graffiti wordmark — the signature accent, D-05); `.nav-link` uses `font-mono` uppercase wide-tracking
    meta with an editorial active indicator (`.nav-link.active` → red underline/marker, the single accent);
    `.nav-cta` is the solid-red Abrir Ticket (the one primary in the bar); keep the `.scrolled` backdrop
    blur and ensure it reads over BOTH ink and paper sections (tune the rgba navy backdrop so light text
    stays AA when the bar floats over a paper section). Footer: editorial — graffiti wordmark accent, mono
    links, hairline top rule, keep the bottom clearance for the floating CTA. FloatingCTA: restyle the
    persistent pill (keep ≥44px, keep the red fill as the primary action; refine the glow to the calmer
    editorial treatment). DiscordModal `.dc-*`: editorial card (0px or minimal radius, hairline border,
    Space Grotesk `.dc-title`, mono buttons), keep the open/close transitions. LanguageSwitcher `.lang-*`:
    editorial mono trigger + dropdown, visible `:focus-visible` ring retained. PRESERVE the
    `@media (max-width:768px)` mobile drawer rules and the `@media (prefers-reduced-motion: reduce)` block
    (chrome animations off under reduce). Every interactive control keeps `min-height: var(--touch-min)`
    (≥44px) and a visible focus ring.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -q "font-mono" src/styles/chrome.css && grep -q "font-tag" src/styles/chrome.css && grep -q "nav-link.active\|nav-link.active::after\|\.active" src/styles/chrome.css && grep -q "prefers-reduced-motion" src/styles/chrome.css && grep -q "max-width: 768px" src/styles/chrome.css && grep -q "touch-min" src/styles/chrome.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `chrome.css` retains all selectors (`.nav`, `.nav-brand`, `.nav-link`, `.nav-cta`, `.lang-*`, `.nav-toggle`, `.footer*`, `.floating-cta`, `.dc-*`) — chrome.ts hooks keep matching.
    - `.nav-brand` uses `font-tag` (graffiti wordmark); `.nav-link` uses `font-mono`; an active-state indicator exists.
    - The `@media (max-width:768px)` mobile drawer block AND the `@media (prefers-reduced-motion: reduce)` block are both present.
    - Interactive chrome controls keep `var(--touch-min)` (≥44px) sizing.
  </acceptance_criteria>
  <done>Chrome re-skinned to editorial system; all chrome.ts hooks + mobile drawer + reduced-motion preserved; touch targets ≥44px.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Chrome component tweaks + full-site build/a11y/responsive sweep</name>
  <files>src/components/Nav.astro, src/components/Footer.astro, src/components/FloatingCTA.astro, src/components/DiscordModal.astro, src/components/LanguageSwitcher.astro</files>
  <read_first>
    - src/components/Nav.astro (links, active-state via isActive, LanguageSwitcher, .nav-cta dc-trigger)
    - src/components/Footer.astro / FloatingCTA.astro / DiscordModal.astro / LanguageSwitcher.astro
    - src/scripts/chrome.ts (behavior — do not change; just confirm hooks intact)
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§6 quality bars: AA, ≥44px, focus rings, keyboard, reduced-motion, 375/768/1024/1440, no horizontal scroll)
    - .planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md (D-13)
  </read_first>
  <action>
    Make only the markup tweaks needed for the re-skin in `Nav.astro`, `Footer.astro`,
    `FloatingCTA.astro`, `DiscordModal.astro`, `LanguageSwitcher.astro` — KEEP every behavior hook
    (`data-nav`, `data-nav-toggle`, `data-nav-links`, `data-lang-*`, `data-dc-*`, `dc-trigger`,
    `aria-*` attributes, `role` attributes) and all copy/i18n keys. Confirm the nav Abrir Ticket
    (`.nav-cta.dc-trigger`) and the persistent `.floating-cta.dc-trigger` both render on every page so
    the conversion path is reachable site-wide (NAV-03). Then run the FULL-SITE sweep: `npx astro build`
    must exit 0 with `dist/CNAME` = `nocturna-avatars.site`; confirm every built page
    (`/`, `/es/`, `/en/`, `/es/servicios`, `/en/services`, `/es/galeria`, `/en/gallery`, `/es/terminos`,
    `/en/terms`, `/404.html`) emits a `dc-trigger` (Abrir Ticket reachable everywhere). Fix any remaining
    hard-coded brand hex left over from the migration in the touched chrome files so all color reads
    flow through theme.css `@theme` (PLAT-03 centralization). Confirm no `tokens.css` or `cursor`
    references remain anywhere in src/.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -q "dc-trigger" src/components/Nav.astro && grep -q "dc-trigger" src/components/FloatingCTA.astro && grep -q "data-nav" src/components/Nav.astro && ! grep -rq "tokens.css" src/ && ! grep -rqi "cursor" src/scripts/motion/index.ts && npx astro build >/tmp/b4.log 2>&1 && grep -q "nocturna-avatars.site" dist/CNAME && for f in dist/index.html dist/es/index.html dist/en/index.html dist/es/servicios/index.html dist/es/galeria/index.html dist/es/terminos/index.html dist/404.html; do grep -q "dc-trigger" "$f" || { echo "MISSING CTA in $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `astro build` exits 0; `dist/CNAME` = `nocturna-avatars.site`.
    - EVERY built page listed in the verify loop contains a `dc-trigger` (Abrir Ticket reachable site-wide — NAV-03).
    - No `tokens.css` reference and no `cursor` reference remain in src/ (D-02/D-10 fully landed).
    - All chrome behavior hooks (`data-nav*`, `data-lang-*`, `data-dc-*`, `dc-trigger`) intact; no copy/i18n key changed.
  </acceptance_criteria>
  <done>Chrome components re-skinned with hooks preserved; full-site build green; Abrir Ticket on every page; no legacy tokens/cursor refs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final human verification — whole-site quality-bar sweep</name>
  <files>See files_modified in frontmatter; this task does not edit source files (verification/gate).</files>
  <read_first>
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§6 quality bars + §5.6 chrome)
    - .planning/ROADMAP.md (Phase 02.1 success criteria 1–6)
    - .planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md (D-01..D-13)
  </read_first>
  <what-built>
    The chrome is re-skinned and the whole site is re-skinned end-to-end. Run `npx astro dev` and do the
    final pass across pages and breakpoints.
  </what-built>
  <action>Pause for human verification only — no source edits. Present the build output / running dev server described below and wait for the resume signal before continuing.</action>
  <how-to-verify>
    1. Nav: thin editorial bar, Space Mono links + graffiti wordmark, active-state indicator on the
       current page, Abrir Ticket CTA; scroll down → nav hides, scroll up → shows (Phase 2 behavior).
    2. Footer / Floating CTA / Discord modal / language switcher all read as editorial; the floating
       Abrir Ticket is visible and opens the modal on every page; the modal closes on Escape/backdrop.
    3. Language switcher: open via keyboard (Tab → Enter), pick the other locale, lands on the
       counterpart page; visible focus rings throughout.
    4. Toggle prefers-reduced-motion ON (devtools): page transitions/marquee/nav-hide calm down.
    5. Across /, /es/, /en/, /servicios, /galeria, /terminos, 404 at 375/768/1024/1440: no horizontal
       scroll, AA contrast on ink AND paper, tap targets ≥44px, red used surgically.
    6. Confirm ROADMAP success criteria 1–6 all hold.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all six ROADMAP success criteria hold and the §6 quality bars pass (AA both surfaces, ≥44px, focus rings, keyboard nav, reduced-motion, responsive 375–1440, no horizontal scroll, Abrir Ticket everywhere).
  </acceptance_criteria>
  <resume-signal>Type "approved" to close the phase, or list issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → conversion path | Nav CTA + floating CTA `.dc-trigger` must remain on every page (chrome is global). |
| keyboard/AT user → chrome | Focus rings, aria-* roles, and ≥44px targets must survive the re-skin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-CTA3 | Denial of Service | site-wide Abrir Ticket | mitigate | Task 2 verify loops over every built page asserting a `.dc-trigger`; Task 3 human check (NAV-03). |
| T-02.1-KBD | Elevation of Privilege (a11y bypass) | nav/lang/modal keyboard + focus | mitigate | Keep aria-* + focus-visible rings + ≥44px; Task 3 keyboard walkthrough. |
| T-02.1-RM | Denial of Service (vestibular) | motion under reduced-motion | mitigate | Preserve `@media (prefers-reduced-motion)` blocks in chrome.css + transitions; Task 3 reduced-motion toggle check. |
</threat_model>

<verification>
- `astro build` exits 0; CNAME intact; every built page emits a `dc-trigger`.
- chrome.css keeps all selectors + mobile drawer + reduced-motion; nav-brand font-tag, nav-link font-mono.
- No tokens.css / cursor references remain in src/.
</verification>

<success_criteria>
- Chrome (nav/footer/floating CTA/lang switcher) restyled to the system; Abrir Ticket reachable from every page (criterion 6; D-13).
- ≥44px targets, focus rings, keyboard nav, reduced-motion respected; AA in ink + paper; responsive 375–1440 (criterion 6; PLAT-04).
- All six ROADMAP success criteria verified (final human pass).
</success_criteria>

<output>
Create `.planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-04-SUMMARY.md` when done.
</output>
