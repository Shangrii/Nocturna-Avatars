---
phase: 02.1-visual-redesign-refined-street-editorial
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - src/pages/[lang]/index.astro
  - src/components/sections/About.astro
  - src/components/sections/Teaser.astro
  - src/components/sections/PackagesSummary.astro
  - src/components/PackageCard.astro
  - src/components/EmptyState.astro
  - src/styles/sections.css
  - src/styles/choreography.css
autonomous: false
requirements: [PLAT-03, PLAT-04]

must_haves:
  truths:
    - "The hero is the clean editorial composition (no diagonal red/navy split, no ink-splatter, no heavy text-shadow); the Space Grotesk tagline is the visual headline and the graffiti 'Nocturna' wordmark is the signature accent (D-04/D-05/D-11)"
    - "The landing order is Hero → Featured gallery → About+Process → Packages summary → Closing CTA (D-12)"
    - "Sections alternate an ink (navy) / paper (off-white) rhythm with red used as one accent per section (D-07/D-08)"
    - "A visitor can reach an Abrir Ticket / Discord CTA on the landing (.dc-trigger preserved — NAV-03)"
    - "Hero text holds AA contrast (≥4.5:1) over its backdrop, and the layout is responsive with no horizontal scroll at 375/768/1024/1440 (PLAT-04)"
    - "The Phase 2 landing choreography (gallery marquee, card tilt, featured parallax) still runs because its hooks survive the re-skin (D-09)"
  artifacts:
    - path: "src/pages/[lang]/index.astro"
      provides: "Re-skinned editorial hero + reordered landing sections"
      contains: "font-display"
    - path: "src/styles/sections.css"
      provides: "Re-skinned section/card/empty-state styles with ink/paper surfaces"
      contains: "paper"
  key_links:
    - from: "src/pages/[lang]/index.astro"
      to: "Discord modal"
      via: "dc-trigger button"
      pattern: "dc-trigger"
    - from: "src/components/sections/Teaser.astro"
      to: "choreography.ts marquee/parallax"
      via: "data-teaser / data-teaser-body hooks"
      pattern: "data-teaser"
    - from: "src/components/PackageCard.astro"
      to: "choreography.ts tilt"
      via: ".cards--summary .card hook"
      pattern: "class=\\{?.*card"
---

<objective>
Re-skin the entire landing to the Refined Street Editorial system and reorder it visual-first
(D-11/D-12), restyling the shared landing building blocks (hero, About/Process, Teaser, packages
summary, PackageCard, EmptyState) to ink/paper rhythm with Space Grotesk display type and surgical
red — while keeping the Phase 2 choreography hooks and the Abrir Ticket conversion path intact.

This is a complete, shippable vertical slice: after it, a visitor lands and is impressed by the new
editorial hero, scrolls into the featured gallery first, then about/process, packages, and the closing
CTA — all in the new look. It owns the shared `sections.css`, `PackageCard.astro`, and
`EmptyState.astro` so Plan 03 (pages) and Plan 04 (chrome) can consume the re-skinned blocks without
file conflict.

Purpose: ROADMAP success criteria 1, 2, 5; D-04/D-05/D-07/D-08/D-11/D-12. PLAT-03 (system applied),
PLAT-04 (a11y/responsive on the landing).
Output: Re-skinned, reordered landing; re-skinned shared section/card/empty-state CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md
@docs/superpowers/specs/2026-06-30-website-redesign-design.md
@src/styles/theme.css

<interfaces>
<!-- Tailwind v4 utilities available from Plan 01's @theme (theme.css): -->
  Colors: bg-ink/text-ink, bg-paper/text-paper, text-ink-on-paper, text-paper-on-ink,
          bg-red/text-red/border-red, text-white-dim
  Fonts:  font-display (Space Grotesk 700 — the editorial headline workhorse),
          font-tag (A Another Tag/Permanent Marker — signature ONLY: wordmark + section tags + 1 hero accent),
          font-body (Inter), font-mono (Space Mono — eyebrows/labels/prices, uppercase wide-tracking)
  Tokens still available as var(--…): --type-display, --type-heading, --section-pad, --page-gutter, --touch-min, etc.

<!-- CHOREOGRAPHY HOOKS THAT MUST SURVIVE (src/scripts/motion/choreography.ts queries these). Do NOT rename: -->
  - gallery marquee:   selector  [data-teaser="gallery"] [data-teaser-body]   (Teaser.astro renders these)
  - featured parallax: selector  [data-teaser="featured"] [data-teaser-body]  (Teaser.astro renders these)
  - card 3D tilt:      selector  .cards--summary .card                        (PackagesSummary + PackageCard)
  Plus micro-interaction selectors used in choreography.css: .hero-cta, .card, .btn-primary, .card-cta, .nav-link, .link-arrow, .dc-trigger

<!-- Discord conversion path: ANY element with class `dc-trigger` opens the shared modal (chrome.ts). Keep it. -->

<!-- Current landing order in src/pages/[lang]/index.astro (TO REORDER per D-12): -->
  hero → <About> → <Teaser variant="featured"> → <PackagesSummary> → <Teaser variant="gallery"> → closing section
<!-- Target order (D-12): hero → <Teaser variant="featured"> (the featured gallery) → <About> → <PackagesSummary> → closing -->
<!-- NOTE: spec §5.2 raises the FEATURED teaser to #2. The second <Teaser variant="gallery"> may be folded into the
     featured teaser or kept; preserve at least the featured-teaser hooks. Keep all data-teaser hooks that remain. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin shared section/card/empty-state CSS to ink/paper editorial system</name>
  <files>src/styles/sections.css, src/styles/choreography.css</files>
  <read_first>
    - src/styles/sections.css (section-header/tag/title, .section--surface, cards, package card, empty-state, closing, 404, responsive)
    - src/styles/choreography.css (micro-interaction selectors that must keep matching: .hero-cta/.card/.btn-primary/.card-cta/.link-arrow)
    - src/components/PackageCard.astro (class hooks: .card/.featured/.card--condensed/.card-name/.card-price/.card-cta)
    - src/components/EmptyState.astro (class hooks: .empty-state/.empty-state-heading/.btn-primary/.btn-outline)
    - src/styles/theme.css (the @theme utilities + tokens to use)
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§3.1 type, §3.2 ink/paper color, §3.3 texture: 0px radii, 1px rules, giant Space Mono section numbers/tags)
    - .planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md (D-04/05/06/07/08/09)
  </read_first>
  <action>
    Rewrite `src/styles/sections.css` to the editorial system while KEEPING every existing class
    selector name (components reference them and choreography matches some). Apply: `.section-title`
    uses `font-family: var(--font-display)` (Space Grotesk), uppercase, `letter-spacing: -0.02em`,
    `line-height: 0.95`, oversized via `var(--type-heading)`/a larger clamp; `.section-tag` uses
    `var(--font-mono)`, uppercase, wide tracking, `color: var(--color-red)` as the single red accent —
    render it as a giant editorial marker (D-09). Introduce surface variants: keep `.section--surface`
    but make it the PAPER surface (`background: var(--color-paper)`; text `var(--color-ink-on-paper)`)
    and ensure default sections read as INK (navy field, off-white text) so the page alternates ink/paper
    (D-07). Set all radii to 0px and borders to hairline 1px rules (D-09). Re-skin `.card`/`.featured`
    (0px radius, 1px border, red only on the featured accent + CTA — D-08), `.card-name` to
    `var(--font-display)`, `.card-price` to `var(--font-mono)` tabular figures, `.card-cta`/`.btn-primary`
    solid red (the one primary action), `.btn-outline` 1px red outline. Re-skin `.empty-state` to a
    paper/ink editorial block (0px radius, 1px dashed→solid hairline). Keep the `.workflow`/`.workflow-step`
    process steps but restyle them as an editorial timeline with Space Mono numbered markers (D-12 process
    as timeline). Verify AA: off-white (#f0eae4) on ink (#0a0c14) and near-black ink-on-paper on paper
    (#f0eae4) both clear 4.5:1; red (#c0192c) text used only on surfaces where it clears AA (use red for
    rules/accents/CTA fills, not small body text on light unless it passes). Keep the `@media (max-width:768px)`
    responsive rules and extend so nothing horizontally scrolls at 375px.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -q "font-display" src/styles/sections.css && grep -Eq "paper|--color-paper" src/styles/sections.css && grep -q "section--surface" src/styles/sections.css && grep -q "card--condensed" src/styles/sections.css && grep -q "cards--summary" src/styles/sections.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `sections.css` `.section-title` declares `font-family: var(--font-display)` and a negative letter-spacing.
    - `.section--surface` renders a PAPER (off-white) field (references `--color-paper` or `#f0eae4`) with dark text — confirming ink/paper alternation exists in CSS.
    - All class hooks still present (`.cards--summary`, `.card`, `.card--condensed`, `.card-cta`, `.empty-state`, `.btn-primary`, `.btn-outline`, `.workflow-step`) so components + choreography keep matching.
    - 0px radii / hairline rules: no `border-radius` value > 4px on cards/sections (editorial, D-09).
  </acceptance_criteria>
  <done>Shared section/card/empty-state CSS re-skinned to ink/paper editorial with Space Grotesk display + surgical red; all class hooks preserved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin + reorder the landing (editorial hero, featured-first order) and restyle landing sections</name>
  <files>src/pages/[lang]/index.astro, src/components/sections/About.astro, src/components/sections/Teaser.astro, src/components/sections/PackagesSummary.astro, src/components/PackageCard.astro, src/components/EmptyState.astro</files>
  <read_first>
    - src/pages/[lang]/index.astro (the hero markup + scoped <style>, and the section render order)
    - src/components/sections/About.astro (features/workflow/story markup + class hooks)
    - src/components/sections/Teaser.astro (renders data-teaser + data-teaser-body — MUST keep these hooks)
    - src/components/sections/PackagesSummary.astro (cards--summary block; section--surface usage)
    - src/components/PackageCard.astro / src/components/EmptyState.astro (class hooks from Task 1)
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§5.1 hero composition, §5.2 landing order)
    - .planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md (D-04/05/08/11/12)
  </read_first>
  <action>
    In `src/pages/[lang]/index.astro`: REORDER the section renders below the hero to D-12:
    `<Teaser variant="featured" />` FIRST (the featured gallery, raised to #2), then `<About />`, then
    `<PackagesSummary />`, then the closing CTA section. Keep the second `<Teaser variant="gallery" />`
    only if its `data-teaser="gallery"` marquee hook is still wanted on the landing; if dropped, ensure
    the featured teaser retains its hooks. Replace the hero's scoped `<style>` and busy markup
    (`.hero-bg::before` diagonal split, `.hero-title` text-shadow + `::before/::after` ink splatters,
    `.hero::before` glow blob) with a clean editorial hero: a Space Mono eyebrow (`.hero-eyebrow`,
    `font-mono`), the graffiti `Nocturna` wordmark as the signature accent ONLY (`.hero-title` keeps
    `font-family: var(--font-tag)` but DROP the heavy text-shadow + splatter pseudo-elements), and the
    tagline (`taglineA`/`taglineB`) promoted to the visual headline in `font-display` (Space Grotesk,
    oversized, uppercase, tight tracking, line-height ~0.9–1.0). Keep the mono services row, keep BOTH
    hero CTAs but ensure the PRIMARY Abrir Ticket CTA is the solid-red `.dc-trigger` button (one primary
    per screen — §UX) and the secondary "Ver Paquetes" is the outline link. KEEP the `.hero-bg`,
    `[data-hero-shader]` canvas, `.hero-fallback`, `.hero-shader-overlay` structure (the Phase 2 shader
    is retained as a decorative backdrop, D-11) but ensure the editorial layout — not the shader — owns
    legibility; keep the contrast overlay so headline+CTA hold AA. Use `min-height: 100dvh` (not 100vh).
    Re-skin the closing section to the editorial system (ink field, one red accent, the `.btn-primary`
    `.dc-trigger`). In `About.astro` restyle the process as the editorial timeline (Task 1 CSS), keep all
    SVG/copy + `reveal` hooks. Do NOT rename any `reveal`, `data-teaser`, `data-teaser-body`, `dc-trigger`,
    `.cards--summary`, or `.card` hook. Do NOT change copy or i18n keys.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -q "dc-trigger" "src/pages/[lang]/index.astro" && grep -q "font-display" "src/pages/[lang]/index.astro" && ! grep -q "ink-splatter\|hero-title::before" "src/pages/[lang]/index.astro" && grep -q "data-teaser=\"featured\"" src/components/sections/Teaser.astro && grep -q "min-height: 100dvh\|100dvh" "src/pages/[lang]/index.astro" && npx astro build >/tmp/b2.log 2>&1 && grep -q "nocturna-avatars.site" dist/CNAME && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `astro build` exits 0 and `dist/CNAME` = `nocturna-avatars.site`.
    - In the built `dist/es/index.html`, the featured teaser (`data-teaser="featured"`) appears BEFORE the about section (`id="about"`) in source order (D-12 reorder).
    - The hero no longer contains the diagonal-split / ink-splatter / heavy text-shadow declarations (grep for the removed `.hero-title::before` splatter / `text-shadow:` 3-layer rule returns nothing in the hero style block).
    - `[lang]/index.astro` uses `font-display` for the tagline headline and retains `font-tag` for the Nocturna wordmark (signature accent).
    - At least one `.dc-trigger` Abrir Ticket control renders on the landing (NAV-03).
    - Hero uses `100dvh` not `100vh`.
  </acceptance_criteria>
  <done>Landing reordered featured-first; editorial hero replaces the busy one; landing sections re-skinned; choreography + CTA hooks preserved; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — landing look, order, contrast, responsiveness, motion</name>
  <files>See files_modified in frontmatter; this task does not edit source files (verification/gate).</files>
  <read_first>
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§5.1, §5.2, §6 quality bars)
    - .planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md (D-07/D-08/D-11/D-12)
  </read_first>
  <what-built>
    The landing is re-skinned to Refined Street Editorial and reordered visual-first. Run `npm run dev`
    (or `npx astro dev`) and open the landing.
  </what-built>
  <action>Pause for human verification only — no source edits. Present the build output / running dev server described below and wait for the resume signal before continuing.</action>
  <how-to-verify>
    1. Visit http://localhost:4321/es/ and http://localhost:4321/en/.
    2. Confirm the hero reads as clean editorial: Space Grotesk tagline is the headline, the graffiti
       "Nocturna" is a signature accent (no diagonal split / splatter / heavy glow), text is legible.
    3. Confirm section order: Hero → Featured gallery → About+Process → Packages → Closing CTA (D-12).
    4. Confirm ink/paper rhythm: sections alternate navy and off-white; red appears as ONE accent each.
    5. Confirm "Abrir Ticket" opens the Discord modal from the hero + closing.
    6. Resize to 375 / 768 / 1024 / 1440 — no horizontal scroll; tap targets ≥44px.
    7. Confirm Phase 2 motion still works: smooth scroll, reveals, gallery marquee, card tilt on the
       packages summary, and (with prefers-reduced-motion ON in devtools) motion is calmed.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms hero, order, ink/paper rhythm, surgical red, CTA reachability, responsiveness, and surviving motion all match the spec.
    - Any contrast or horizontal-scroll issue is reported for fix before approval.
  </acceptance_criteria>
  <resume-signal>Type "approved" or list issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → conversion path | The Abrir Ticket `.dc-trigger` must survive the re-skin so the click still opens the Discord modal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-CTA | Denial of Service | landing Abrir Ticket path | mitigate | Re-skin must preserve every `.dc-trigger` hook; Task 2 verify + Task 3 human check confirm the modal still opens (NAV-03). |
| T-02.1-A11Y | Information Disclosure (legibility) | hero/paper text contrast | mitigate | AA ≥4.5:1 enforced in Task 1 surface tokens; Task 3 human contrast check on ink + paper. |
</threat_model>

<verification>
- `astro build` exits 0; CNAME intact.
- Featured teaser precedes About in built HTML (D-12).
- Hero free of splatter/diagonal/heavy-shadow; uses font-display headline + font-tag wordmark.
- `.dc-trigger` present on landing; choreography hooks (`data-teaser*`, `.cards--summary .card`) intact.
</verification>

<success_criteria>
- New editorial type system on the landing, graffiti rationed to wordmark/tags/one hero accent (criterion 1; D-04/D-05).
- Ink/paper rhythm with surgical red (criterion 2; D-07/D-08).
- Landing order hero → featured gallery → about+process → packages → closing (criterion 5; D-12).
- Phase 2 motion still works re-skinned; Abrir Ticket reachable; AA contrast; responsive 375–1440 (criterion 4/6; PLAT-04).
</success_criteria>

<output>
Create `.planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-02-SUMMARY.md` when done.
</output>
