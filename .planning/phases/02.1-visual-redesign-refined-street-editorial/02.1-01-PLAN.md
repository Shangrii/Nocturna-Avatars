---
phase: 02.1-visual-redesign-refined-street-editorial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - astro.config.mjs
  - package.json
  - src/layouts/BaseLayout.astro
  - src/styles/global.css
  - src/styles/theme.css
  - src/styles/tokens.css
  - src/scripts/motion/index.ts
  - src/scripts/motion/cursor.ts
  - src/styles/cursor.css
autonomous: false
requirements: [PLAT-03, PLAT-04]
user_setup:
  - service: npm package install
    why: "Tailwind v4 Vite plugin (@tailwindcss/vite) must be installed; legitimacy verified via T-02.1-SC checkpoint before install."

must_haves:
  truths:
    - "The site still builds to static output and dist/CNAME is nocturna-avatars.site (PLAT-02 unbroken, D-01)"
    - "Tailwind v4 utilities like bg-ink, bg-paper, text-red, font-display resolve to brand values in the built CSS (D-01/D-02)"
    - "The existing site visually renders unchanged after this slice (incremental, nothing-breaks migration — D-03)"
    - "The custom cursor no longer exists or runs anywhere (D-10)"
  artifacts:
    - path: "astro.config.mjs"
      provides: "Tailwind v4 Vite plugin registration"
      contains: "@tailwindcss/vite"
    - path: "src/styles/theme.css"
      provides: "Tailwind @import + @theme brand token block (ink/paper/red/navy + display/tag/mono fonts + type scale)"
      contains: "@theme"
    - path: "src/styles/global.css"
      provides: "resets, @font-face, film-grain overlay, surface base — tokens.css @import removed, theme.css the new source"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/theme.css"
      via: "import"
      pattern: "import.*theme\\.css"
    - from: "astro.config.mjs"
      to: "@tailwindcss/vite"
      via: "vite plugins array"
      pattern: "tailwindcss\\(\\)"
    - from: "src/scripts/motion/index.ts"
      to: "(cursor removed)"
      via: "no import of ./cursor"
      pattern: "cursor"
---

<objective>
Stand up the Tailwind v4 CSS-first foundation that every later slice re-skins against, and remove
the custom cursor — without changing how the existing site looks or breaking the static GitHub Pages
build (D-01/D-02/D-03/D-10).

This is the enabling vertical slice: after it, the site builds and renders exactly as before, but
Tailwind v4 utilities backed by `@theme` brand tokens (`bg-ink`, `bg-paper`, `text-red`,
`font-display`, `font-tag`, `font-mono`, plus the fluid type scale) are available for Plans 02–04 to
convert components to. `tokens.css` is retired into the `@theme` block; `global.css` keeps resets,
`@font-face`, and the film-grain. The blend-difference cursor (`cursor.ts` + `cursor.css`) is deleted
and unwired from the motion controller.

Purpose: PLAT-03 — a single centralized design system, now expressed as Tailwind v4 `@theme`
utilities. Establishes the contract all subsequent slices implement against (interface-first).
Output: Working Tailwind v4 integration, `theme.css`, slimmed `global.css`, cursor removed, green build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md
@docs/superpowers/specs/2026-06-30-website-redesign-design.md
@CLAUDE.md

<interfaces>
<!-- Current brand tokens to port into @theme. Space Grotesk is NEW (display); A Another Tag /
     Permanent Marker demote to signature accent only (font-tag); Inter (body), Space Mono (mono). -->

Existing tokens (src/styles/tokens.css → port into @theme, then retire the file):
  Colors:  --red #c0192c, --red-dark #7a0f1a, --red-glow #e8223b
           --navy #0a0c14 (the new "ink" field), --navy-mid #10131e, --navy-surface #141828
           --white #f0eae4 (the new "paper" field + ink-on-paper inverse), --white-dim #9a9198
  Fonts:   --font-body 'Inter'; --font-mono 'Space Mono'; --font-tag 'A Another Tag'/'Permanent Marker'
           NEW: --font-display 'Space Grotesk' (700) — the oversized editorial workhorse (D-04)
  Layout:  --nav-h 64px; --section-pad clamp(60px,10vw,120px); --page-gutter clamp(20px,4vw,48px)
  Type:    --type-display, --type-heading, --type-card-title, --type-body 16px, --type-label
  Touch:   --touch-min 44px

NEW semantic surface tokens to ADD (D-07): --ink (#0a0c14), --paper (#f0eae4),
  --ink-on-paper (near-black text on paper, e.g. #0a0c14), --paper-on-ink (#f0eae4 text on ink).

Tailwind v4 maps @theme entries to utilities by namespace:
  --color-* → bg-*/text-*/border-*   (e.g. --color-ink → bg-ink, text-ink)
  --font-*  → font-*                 (e.g. --font-display → font-display)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify @tailwindcss/vite package legitimacy (T-02.1-SC)</name>
  <files>See files_modified in frontmatter; this task does not edit source files (verification/gate).</files>
  <read_first>
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§4 — Tailwind v4 via @tailwindcss/vite)
    - .planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md (D-01)
  </read_first>
  <what-built>
    No install yet — this checkpoint gates the install. RESEARCH.md has no Package Legitimacy Audit
    table for this phase, so per the package legitimacy gate the new dependency `@tailwindcss/vite`
    is treated as [ASSUMED] and must be human-verified before install.
  </what-built>
  <action>Pause for human verification only — no source edits. Present the build output / running dev server described below and wait for the resume signal before continuing.</action>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@tailwindcss/vite
    2. Confirm it is the official Tailwind Labs package (publisher tailwindlabs / tailwindcss org),
       v4.x, high weekly downloads, repository github.com/tailwindlabs/tailwindcss.
    3. Confirm it is the intended replacement for the deprecated @astrojs/tailwind (D-01).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms @tailwindcss/vite is the official Tailwind v4 Vite plugin and approves install.
    - This checkpoint is NOT auto-approvable (legitimacy gate); execution pauses here regardless of auto_advance.
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed with install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install Tailwind v4, register the Vite plugin, author theme.css from the brand tokens</name>
  <files>package.json, astro.config.mjs, src/styles/theme.css</files>
  <read_first>
    - astro.config.mjs (current static-output config — do NOT set `base`; keep `site` + i18n)
    - src/styles/tokens.css (every value to port into @theme)
    - src/styles/global.css (imports tokens.css at top — that import will be removed in Task 3)
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§3 design system, §4 tech approach)
    - .planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md (D-01/D-02/D-04/D-07)
  </read_first>
  <action>
    Install `@tailwindcss/vite` (v4) and `tailwindcss` (v4) as devDependencies via npm; pin exact
    versions (no `^`). In `astro.config.mjs`, import `tailwindcss` from `@tailwindcss/vite` and add it
    to `vite: { plugins: [tailwindcss()] }` — keep `output` static (default), keep `site`
    'https://nocturna-avatars.site', keep the i18n block, and do NOT add a `base`.
    Create `src/styles/theme.css` beginning with `@import "tailwindcss";` followed by a `@theme { … }`
    block. Inside `@theme`, define the brand tokens as Tailwind v4 namespaced custom properties so they
    become utilities: `--color-red: #c0192c;` (+ red-dark #7a0f1a, red-glow #e8223b), `--color-ink:
    #0a0c14;`, `--color-paper: #f0eae4;`, `--color-ink-on-paper: #0a0c14;`, `--color-paper-on-ink:
    #f0eae4;`, plus `--color-navy`, `--color-navy-mid #10131e`, `--color-navy-surface #141828`,
    `--color-white-dim #9a9198`. Fonts: `--font-display: 'Space Grotesk', sans-serif;` (D-04 — the NEW
    display face), `--font-tag: 'A Another Tag', 'Permanent Marker', cursive;` (signature accent only,
    D-05), `--font-body: 'Inter', sans-serif;`, `--font-mono: 'Space Mono', monospace;`. Carry the
    fluid type scale and layout vars as plain custom properties in the same block (`--type-display`,
    `--type-heading`, `--type-card-title`, `--type-body`, `--type-label`, `--nav-h`, `--section-pad`,
    `--page-gutter`, `--touch-min`, the spacing scale, line-heights) so existing component CSS that
    reads `var(--…)` keeps resolving during the incremental migration (D-03 — nothing breaks).
    Add Space Grotesk 700 to the Google Fonts <link> in BaseLayout in Task 3 (font load lives there).
    Do NOT delete tokens.css yet — Task 3 removes its import and retires it once theme.css owns the values.
  </action>
  <verify>
    <automated>cd "$PWD" && node -e "const c=require('fs').readFileSync('astro.config.mjs','utf8'); if(!/@tailwindcss\/vite/.test(c)||!/tailwindcss\(\)/.test(c)) process.exit(1)" && grep -q '@theme' src/styles/theme.css && grep -q 'color-ink' src/styles/theme.css && grep -q 'color-paper' src/styles/theme.css && grep -q "font-display" src/styles/theme.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `astro.config.mjs` imports `tailwindcss` from `@tailwindcss/vite` and lists `tailwindcss()` in `vite.plugins`; no `base` key present; `site` unchanged.
    - `package.json` lists `@tailwindcss/vite` and `tailwindcss` at exact (un-prefixed) versions.
    - `src/styles/theme.css` starts with `@import "tailwindcss";` and contains a `@theme { … }` block defining `--color-ink`, `--color-paper`, `--color-ink-on-paper`, `--color-paper-on-ink`, `--color-red`, and `--font-display` (Space Grotesk).
  </acceptance_criteria>
  <done>Tailwind v4 installed + wired; theme.css authored with brand @theme tokens incl. new ink/paper surface + Space Grotesk display.</done>
</task>

<task type="auto">
  <name>Task 3: Mount theme.css in BaseLayout, retire tokens.css, load Space Grotesk, delete the cursor</name>
  <files>src/layouts/BaseLayout.astro, src/styles/global.css, src/scripts/motion/index.ts (delete: src/styles/tokens.css, src/styles/cursor.css, src/scripts/motion/cursor.ts)</files>
  <read_first>
    - src/layouts/BaseLayout.astro (imports global.css/chrome.css/motion.css/cursor.css/choreography.css/transitions.css; Google Fonts <link>; mounts motion/index.ts)
    - src/styles/global.css (line 7 `@import './tokens.css';` — replace source with theme.css)
    - src/scripts/motion/index.ts (imports initCursor/destroyCursor from ./cursor and calls them in initMotion/teardownMotion)
    - src/scripts/motion/cursor.ts and src/styles/cursor.css (the files to delete — D-10)
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§3.3 cursor removed; §4 migration)
  </read_first>
  <action>
    In `src/layouts/BaseLayout.astro`: add `import '../styles/theme.css';` as the FIRST style import
    (before global.css) so Tailwind's base + the @theme tokens load first; remove the
    `import '../styles/cursor.css';` line. Update the Google Fonts <link> href to also request
    `Space+Grotesk:wght@700` (append `&family=Space+Grotesk:wght@700` to the existing css2 query) and
    add a `<link rel="preload" as="style">`-style preconnect is already present — only add the
    Space Grotesk family; keep `display=swap`. In `src/styles/global.css`, remove the
    `@import './tokens.css';` line (theme.css now owns tokens) and keep everything else (reset,
    @font-face for 'A Another Tag', body base, film-grain `body::after`). Then DELETE
    `src/styles/tokens.css`, `src/styles/cursor.css`, and `src/scripts/motion/cursor.ts`.
    In `src/scripts/motion/index.ts`: remove the `import { initCursor, destroyCursor } from './cursor';`
    line, remove the `initCursor();` call from `initMotion()`, and remove the `destroyCursor();` call
    from `teardownMotion()`. Leave all other motion modules (smooth-scroll, reveals, hero-shader,
    choreography, transitions) intact and wired (D-09/D-11 — motion layer survives, re-skinned later).
    This must leave the site rendering visually unchanged (D-03): component CSS still reads var(--…)
    tokens, now sourced from theme.css instead of tokens.css.
  </action>
  <verify>
    <automated>cd "$PWD" && test ! -f src/styles/tokens.css && test ! -f src/styles/cursor.css && test ! -f src/scripts/motion/cursor.ts && ! grep -q "cursor" src/scripts/motion/index.ts && grep -q "theme.css" src/layouts/BaseLayout.astro && ! grep -q "cursor.css" src/layouts/BaseLayout.astro && grep -q "Space+Grotesk" src/layouts/BaseLayout.astro && npx astro build >/tmp/b.log 2>&1 && grep -q "nocturna-avatars.site" dist/CNAME && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx astro build` exits 0 and `dist/CNAME` equals `nocturna-avatars.site` (PLAT-02 unbroken).
    - `src/styles/tokens.css`, `src/styles/cursor.css`, `src/scripts/motion/cursor.ts` no longer exist.
    - `src/scripts/motion/index.ts` contains no reference to `cursor` (no import, no init/destroy call).
    - `BaseLayout.astro` imports `theme.css`, no longer imports `cursor.css`, and its fonts <link> includes `Space+Grotesk`.
    - The built `/es/index.html` + `/en/index.html` render (grep for `hero-title`) — the existing look is intact (D-03 incremental, nothing broke).
  </acceptance_criteria>
  <done>theme.css mounted first; tokens.css/cursor deleted + unwired; Space Grotesk loaded; build green with CNAME; existing visuals unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | New devDependency `@tailwindcss/vite` enters the build toolchain (supply chain). |
| build → GitHub Pages | dist/ (incl. CNAME) is what ships to the live custom domain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-SC | Tampering | npm install of @tailwindcss/vite + tailwindcss | mitigate | Blocking human legitimacy checkpoint (Task 1) verifying official Tailwind Labs package on npmjs.com before install; exact version pins, no `^`. |
| T-02.1-01 | Denial of Service | static build / GitHub Pages | mitigate | Build must exit 0 and dist/CNAME must stay nocturna-avatars.site (Task 3 verify) — a broken Tailwind integration must not ship a blank or domain-detached site. |
</threat_model>

<verification>
- `npx astro build` exits 0; `dist/CNAME` = `nocturna-avatars.site`.
- Tailwind utilities resolve: a probe class (e.g. `bg-ink`) compiles to `#0a0c14` in the emitted CSS.
- No `cursor` references remain in src/ (`grep -rn cursor src/scripts src/styles src/layouts` returns nothing meaningful).
- The landing still renders the existing hero (visual parity — D-03).
</verification>

<success_criteria>
- Tailwind v4 integrated CSS-first (`@tailwindcss/vite` + `@theme`); brand tokens are utilities; tokens.css retired (D-01/D-02 / success criterion 3).
- Site stays Astro static on GitHub Pages with CNAME intact (D-01 / PLAT-02).
- Custom cursor removed and unwired; rest of the motion layer still builds (D-10 / success criterion 4).
- Existing look unchanged this slice — incremental, shippable (D-03).
</success_criteria>

<output>
Create `.planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-01-SUMMARY.md` when done.
</output>
