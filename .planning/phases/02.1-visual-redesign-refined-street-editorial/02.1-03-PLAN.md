---
phase: 02.1-visual-redesign-refined-street-editorial
plan: 03
type: execute
wave: 3
depends_on: ["02.1-02"]
files_modified:
  - src/components/sections/ServicesPage.astro
  - src/components/sections/GalleryPage.astro
  - src/components/sections/TermsPage.astro
  - src/pages/404.astro
  - src/styles/terms.css
autonomous: false
requirements: [PLAT-03, PLAT-04]

must_haves:
  truths:
    - "/servicios renders the 3 editorial pricing cards (Umbra featured) + add-ons + modular-catalog note in the new system, each card with an Abrir Ticket primary (D-13)"
    - "/galeria renders the masonry/empty-state in the editorial system with a restyled empty state and a category-filter affordance (D-13)"
    - "/terminos renders as a paper reading column (65–75ch measure, line-height 1.8) (D-13)"
    - "404 renders in the new editorial system with the back-home + Abrir Ticket actions (D-13)"
    - "Abrir Ticket / Discord CTA reachable on every one of these pages (NAV-03); AA contrast in ink AND paper; responsive 375/768/1024/1440 with no horizontal scroll (PLAT-04)"
  artifacts:
    - path: "src/styles/terms.css"
      provides: "Paper reading-column legal styling (measure + lh 1.8)"
      contains: "ch"
    - path: "src/pages/404.astro"
      provides: "Re-skinned editorial 404"
      contains: "dc-trigger"
  key_links:
    - from: "src/components/sections/ServicesPage.astro"
      to: "re-skinned PackageCard/sections.css (Plan 02)"
      via: "PackageCard + .cards/.addons classes"
      pattern: "PackageCard"
    - from: "src/components/sections/TermsPage.astro"
      to: "terms.css paper column"
      via: ".terms-wrap measure"
      pattern: "terms-wrap"
---

<objective>
Re-skin the dedicated pages — `/servicios`, `/galeria`, `/terminos`, and the 404 — to the Refined
Street Editorial system, consuming the shared section/card/empty-state CSS re-skinned in Plan 02
(D-13). Each page becomes a complete, shippable editorial surface with the Abrir Ticket path reachable.

This vertical slice owns only the page-body components and `terms.css`; it depends on Plan 02 having
re-skinned `sections.css`, `PackageCard.astro`, and `EmptyState.astro`, so there is no file overlap and
the cards/empty-states arrive already styled. The work here is page-level composition + the legal
reading column + the gallery filter affordance.

Purpose: ROADMAP success criterion 6 (every page restyled, CTA everywhere, AA, responsive); D-13.
PLAT-03 (system applied per page), PLAT-04 (a11y/responsive per surface).
Output: Re-skinned /servicios, /galeria, /terminos, 404; paper legal reading column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md
@docs/superpowers/specs/2026-06-30-website-redesign-design.md
@src/styles/theme.css

<interfaces>
<!-- Shared, already-re-skinned in Plan 02 (consume, do NOT re-style here): -->
  sections.css: .section/.section--surface(paper)/.section--rule, .section-header/.section-tag/.section-title,
                .cards/.cards--summary, .card/.featured, .addons/.addon-grid/.addon-item, .empty-state,
                .btn-primary/.btn-outline, .notfound*
  PackageCard.astro: props { tier, usd, advanceNote, popularBadge, ctaLabel, condensed? }; renders .card + .dc-trigger CTA
  EmptyState.astro:  props { heading, body, ticketLabel, linkLabel?, linkHref? }; renders .empty-state + .dc-trigger + optional .btn-outline link

<!-- Tailwind v4 utilities from theme.css: bg-ink/bg-paper, text-ink-on-paper, font-display, font-mono, text-red, etc. -->
<!-- Discord conversion path: class `dc-trigger` opens the shared modal (chrome.ts). Keep on every page. -->
<!-- The page resolver src/pages/[lang]/[page].astro mounts ServicesPage/GalleryPage/TermsPage and imports sections.css + terms.css. Do NOT change the resolver. -->
<!-- Do NOT change copy, i18n keys, terms text, or the ReviewerBanner draft-flag behavior (out of scope). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Re-skin /servicios + /galeria page bodies to the editorial system</name>
  <files>src/components/sections/ServicesPage.astro, src/components/sections/GalleryPage.astro</files>
  <read_first>
    - src/components/sections/ServicesPage.astro (3 PackageCards full mode + .addons block + catalog EmptyState)
    - src/components/sections/GalleryPage.astro (header + EmptyState)
    - src/components/PackageCard.astro + src/styles/sections.css (the re-skinned classes from Plan 02 — consume them)
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§5.3 /servicios, §5.4 /galeria)
    - .planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md (D-13)
  </read_first>
  <action>
    In `ServicesPage.astro`: keep the data flow (PackageCard tiers from packages.json, add-ons,
    catalog EmptyState) and class hooks (`.cards`, `.addons`, `.addon-grid`, `.section--surface`).
    Restyle the page composition to editorial: ensure the section header uses the re-skinned
    `.section-tag` (Space Mono red marker) + `.section-title` (Space Grotesk), the three tiers render
    as editorial pricing cards with Umbra featured (the single red-accented tier), Space Mono tabular
    prices, and the Abrir Ticket `.dc-trigger` primary on EACH card (already in PackageCard — verify it
    survives). Add a clear modular-catalog note (the catalog EmptyState already says "coming soon" — keep
    it, restyle to editorial). Apply ink/paper alternation across the two sections (one ink, one paper via
    `.section--surface`). In `GalleryPage.astro`: keep the header + EmptyState; restyle to editorial and
    add a CATEGORY-FILTER AFFORDANCE (a visual, non-functional filter chip row using `font-mono` labels
    + hairline 1px borders, marked `aria-disabled`/inert or visually-only since the data-driven gallery +
    real filtering is Phase 4 — this is just the affordance per D-13/§5.4; do NOT wire data). Keep the
    EmptyState `.dc-trigger` so Abrir Ticket stays reachable. Touch targets ≥44px on the filter chips.
    Do NOT change copy/i18n keys.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -q "PackageCard" src/components/sections/ServicesPage.astro && grep -q "addon" src/components/sections/ServicesPage.astro && grep -qi "filter\|filtro\|chip\|categor" src/components/sections/GalleryPage.astro && grep -q "EmptyState" src/components/sections/GalleryPage.astro && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ServicesPage.astro` still renders 3 `PackageCard`s + the `.addons` grid; the catalog EmptyState remains; section header uses re-skinned `.section-tag`/`.section-title`.
    - `GalleryPage.astro` renders a category-filter affordance (a chip/label row, visual-only) AND keeps the EmptyState with its `.dc-trigger` Abrir Ticket.
    - No copy or i18n key changed (data-driven content untouched).
  </acceptance_criteria>
  <done>/servicios editorial pricing cards + add-ons + catalog note re-skinned; /galeria editorial empty state + filter affordance; CTAs intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Re-skin /terminos to a paper reading column and the 404 to the editorial system</name>
  <files>src/styles/terms.css, src/components/sections/TermsPage.astro, src/pages/404.astro</files>
  <read_first>
    - src/components/sections/TermsPage.astro (header, ReviewerBanner EN-only, summary, numbered blocks)
    - src/styles/terms.css (.terms-wrap measure, .terms-block*, .reviewer-banner, .terms-summary)
    - src/pages/404.astro (notfound markup; imports sections.css; back-home + dc-trigger actions)
    - src/styles/sections.css (the re-skinned .notfound* rules from Plan 02 — consume them)
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§5.5 /terminos paper column, §6 quality bars)
    - .planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md (D-07/D-13)
  </read_first>
  <action>
    Rewrite `src/styles/terms.css` so the legal page reads as a PAPER editorial column: set the section
    background to paper (`var(--color-paper)`) with `var(--color-ink-on-paper)` text, constrain
    `.terms-wrap` `max-width` to a 65–75ch measure (use `max-width: 70ch` or `clamp` in ch), set
    `.terms-block-body` `line-height: var(--lh-legal)` (1.8), `.terms-block-title` to `font-family:
    var(--font-display)` with a Space Mono `.terms-block-num` red marker (the single accent), 0px radii,
    1px hairline rules between blocks. Re-skin `.terms-summary` + `.reviewer-banner` to the paper system
    (keep the banner prominent — it's the EN draft flag, D-13 unchanged in behavior; just restyle).
    Verify ink-on-paper text and the red accents clear AA ≥4.5:1 on the paper field. In `TermsPage.astro`,
    keep all markup/hooks (`.terms-wrap`, `.terms-block`, `ReviewerBanner` EN-only) and the `reveal`
    hooks — only structural/class tweaks needed for the column; do NOT touch terms copy or the
    EN-draft logic. In `src/pages/404.astro`: keep the BaseLayout + `.notfound*` markup + the back-home
    `.btn-outline` link and the `.dc-trigger` Abrir Ticket button; restyle the `.notfound-code` to the
    editorial system (graffiti `font-tag` 404 as a signature accent is OK, but drop the heavy 3-layer
    text-shadow to match the refined hero treatment) — the re-skinned `.notfound*` CSS lives in
    sections.css (Plan 02), so 404.astro mainly consumes it; adjust any inline class usage as needed.
    Keep the back-home link and Discord CTA reachable.
  </action>
  <verify>
    <automated>cd "$PWD" && grep -q "ch" src/styles/terms.css && grep -q "lh-legal\|1.8" src/styles/terms.css && grep -Eq "paper|--color-paper|#f0eae4" src/styles/terms.css && grep -q "dc-trigger" src/pages/404.astro && grep -q "terms-wrap" src/components/sections/TermsPage.astro && npx astro build >/tmp/b3.log 2>&1 && grep -q "nocturna-avatars.site" dist/CNAME && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `astro build` exits 0 and `dist/CNAME` = `nocturna-avatars.site`.
    - `terms.css` `.terms-wrap` uses a ch-based measure (65–75ch) and the body line-height is 1.8 (`--lh-legal`); the page is a paper field (references `--color-paper`/#f0eae4).
    - `404.astro` retains both the back-home link and a `.dc-trigger` Abrir Ticket; the `.notfound-code` no longer carries the 3-layer heavy text-shadow.
    - `TermsPage.astro` markup/hooks and EN ReviewerBanner behavior unchanged (only restyled).
    - Built `dist/es/terminos/index.html` and `dist/404.html` render (grep for `terms-block` and `notfound`).
  </acceptance_criteria>
  <done>/terminos is a paper reading column (70ch, lh 1.8); 404 re-skinned editorial with refined code + CTAs; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — dedicated pages look, contrast, CTA, responsiveness</name>
  <files>See files_modified in frontmatter; this task does not edit source files (verification/gate).</files>
  <read_first>
    - docs/superpowers/specs/2026-06-30-website-redesign-design.md (§5.3–§5.5, §6 quality bars)
    - .planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-CONTEXT.md (D-13)
  </read_first>
  <what-built>
    /servicios, /galeria, /terminos, and the 404 are re-skinned to the editorial system. Run
    `npx astro dev` and open each page in ES and EN.
  </what-built>
  <action>Pause for human verification only — no source edits. Present the build output / running dev server described below and wait for the resume signal before continuing.</action>
  <how-to-verify>
    1. /es/servicios + /en/services: 3 editorial pricing cards, Umbra featured (single red accent),
       Space Mono prices, add-ons grid, modular-catalog note, Abrir Ticket on each card.
    2. /es/galeria + /en/gallery: editorial empty state + a category-filter affordance; Abrir Ticket reachable.
    3. /es/terminos + /en/terms: paper reading column ~70ch, comfortable 1.8 line-height; EN shows the
       prominent draft ReviewerBanner; ink-on-paper text is clearly legible (AA).
    4. Visit a bogus URL (e.g. /es/nope) → branded editorial 404 with back-home + Abrir Ticket.
    5. Resize each to 375/768/1024/1440 — no horizontal scroll, tap targets ≥44px, red accents surgical.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all four surfaces match the spec (editorial system, paper legal column, filter affordance, 404), CTA reachable everywhere, AA contrast on ink + paper, responsive with no horizontal scroll.
  </acceptance_criteria>
  <resume-signal>Type "approved" or list issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → conversion path | Abrir Ticket `.dc-trigger` must remain on every restyled page. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-CTA2 | Denial of Service | per-page Abrir Ticket | mitigate | Each page keeps a `.dc-trigger`; Task verifies grep + Task 3 human check (NAV-03). |
| T-02.1-LEGAL | Repudiation | EN Terms draft flag | accept (preserve) | ReviewerBanner draft-flag behavior unchanged — only restyled; EN remains flagged DRAFT (D-13). |
</threat_model>

<verification>
- `astro build` exits 0; CNAME intact.
- terms.css = paper column, ch measure, lh 1.8; 404 + each page keep `.dc-trigger`.
- Gallery filter affordance present; ServicesPage keeps 3 cards + add-ons.
</verification>

<success_criteria>
- /servicios, /galeria, /terminos, 404 restyled to the system; Abrir Ticket reachable from each (criterion 6; D-13).
- /terminos paper reading column (65–75ch, lh 1.8); EN draft flag intact (D-13).
- AA contrast in ink and paper; responsive 375–1440 with no horizontal scroll (PLAT-04).
</success_criteria>

<output>
Create `.planning/phases/02.1-visual-redesign-refined-street-editorial/02.1-03-SUMMARY.md` when done.
</output>
