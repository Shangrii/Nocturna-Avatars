---
phase: quick
plan: 260707-0q0
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/NightBackground.astro
  - src/styles/night-sky.css
  - src/styles/gallery.css
  - src/components/sections/GalleryPage.astro
  - src/components/sections/ServicesPage.astro
  - src/components/sections/TermsPage.astro
  - src/scripts/motion/hero-shader/glitch.frag
  - src/styles/global.css
  - src/pages/[lang]/index.astro
  - src/data/services.json
  - src/i18n/terms.json
  - src/i18n/nav.json
  - .planning/STATE.md
  - .planning/todos/pending/2026-07-07-reuse-gallery-background-on-other-pages.md
  - .planning/todos/pending/2026-07-07-strengthen-home-hero-searchlights-and-static.md
  - .planning/todos/pending/2026-07-07-add-quest-vtuber-vrm-and-unitypackage-to-services-catalog.md
  - .planning/todos/pending/2026-07-07-add-account-ban-disclaimer-to-terms.md
  - .planning/todos/pending/2026-07-07-update-discord-popup-copy-for-store-focus.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "The gallery night-sky background renders behind the Services and Terms pages (and ONLY those two, plus the gallery it came from)"
    - "The Home hero searchlights read as clearly more noticeable; the film-static/grain is slightly more present (subtle, not dramatic)"
    - "The services catalog shows 3 new items with ES+EN copy: Versión de Quest $15, VTuber (VRM) $30, Unitypackage $35"
    - "The Terms page shows a new ES+EN clause disclaiming responsibility for account bans and avatars lost with a banned/lost account"
    - "The Discord popup copy (ES+EN) covers tickets/commissions AND announces the upcoming store as 'próximamente'"
    - "The 5 completed todo files are moved to .planning/todos/completed/ and STATE.md's Pending Todos lists only the 3 bot todos"
  artifacts:
    - path: "src/components/NightBackground.astro"
      provides: "Reusable night-sky + fireflies background component"
    - path: "src/styles/night-sky.css"
      provides: "Shared night-sky/fireflies CSS extracted from gallery.css"
    - path: "src/data/services.json"
      provides: "Catalog with the 3 new export/conversion items"
    - path: "src/i18n/terms.json"
      provides: "Terms block 11 (account-ban disclaimer) in ES + EN"
    - path: "src/i18n/nav.json"
      provides: "Discord popup copy covering tickets + upcoming store"
  key_links:
    - from: "src/components/sections/ServicesPage.astro"
      to: "src/components/NightBackground.astro"
      via: "component import + render"
      pattern: "NightBackground"
    - from: "src/components/sections/TermsPage.astro"
      to: "src/components/NightBackground.astro"
      via: "component import + render"
      pattern: "NightBackground"
---

<objective>
Ship the 5 pre-Phase-6 website todos as 5 atomic commits, then close them out (move todo files + update STATE.md). All user decisions are LOCKED — do not revisit: background goes on Services + Terms ONLY; Unitypackage price is $35; the popup announces the store generically as "próximamente".

Purpose: Clear the pre-Phase-6 backlog captured during the 2026-07-07 testing session so Phase 6 (asset store) starts clean.
Output: Reusable night-sky background on Services/Terms, stronger hero searchlights + slightly stronger static, 3 new catalog items, a Terms account-ban clause, rewritten Discord popup copy, and a closed-out todo backlog.

⚠ Consistency flag for the user (do NOT block — LOCKED decision stands): Terms block 04 (`terms.json`) and the "In short" summary currently state that **no Unity Package is delivered** ("No se entrega Unity Package"). Task 3 adds a paid **Unitypackage $35** catalog item, which contradicts that clause. The $35 item ships as decided; surface this contradiction to the user in the execution summary so they can decide whether Terms block 04 needs a follow-up edit (out of scope for this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<interfaces>
Catalog item shape (src/data/services.json → catalog[].items[]):
```json
{
  "id": "kebab-id",
  "name": { "es": "…", "en": "…" },
  "tooltip": { "es": "…", "en": "…" },
  "price": "15",
  "repeatable": false
}
```

Terms block shape (src/i18n/terms.json → {es|en}.blocks[]):
```json
{
  "num": "11",
  "title": "…",
  "body": [
    { "text": "plain segment " },
    { "text": "bold segment", "strong": true },
    { "text": " plain segment." }
  ]
}
```

Discord popup copy keys (src/i18n/nav.json → {es|en}): modalTitleBefore, modalTitleEm, modalTitleAfter, modalBody, modalConfirm, modalDismiss.

Gallery night background lives in src/components/sections/GalleryPage.astro as two child spans of the `.gallery-night` section:
`<span class="gallery-sky">` + `<span class="gallery-fireflies">…</span>` (FIREFLIES built server-side). Their CSS (`.gallery-night`, `.gallery-sky`, `.gallery-sky::after`, `.gallery-fireflies` + keyframes gallery-star-twinkle / gallery-firefly-drift / gallery-firefly-glow) is in src/styles/gallery.css lines ~397–553.

Hero searchlights = the 4 roaming `lightPoint(...)` calls in src/scripts/motion/hero-shader/glitch.frag (lines ~88–98); each takes (strength, glow, radius). Composite glow multiplier at line ~136. Global film-static overlay = `body::after` in src/styles/global.css (opacity 0.45; SVG rect opacity 0.03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reusable night-sky background on Services + Terms</name>
  <files>src/components/NightBackground.astro, src/styles/night-sky.css, src/styles/gallery.css, src/components/sections/GalleryPage.astro, src/components/sections/ServicesPage.astro, src/components/sections/TermsPage.astro</files>
  <action>
Extract the gallery night background into a reusable component and mount it on Services + Terms ONLY (LOCKED — not on Home, not on the gallery landing grid; the gallery page keeps it because that is its source).

1. Create `src/styles/night-sky.css`: move the night-sky rules out of `src/styles/gallery.css` — `.gallery-sky`, `.gallery-sky::after`, `.gallery-fireflies`, `.gallery-fireflies i`, and keyframes `gallery-star-twinkle`, `gallery-firefly-drift`, `gallery-firefly-glow`, plus the `--bulb`/`--bulb-glow` vars and `position:relative; z-index:0` that `.gallery-night` provides. Rename the public classes to generic names (`.night-bg`, `.night-sky`, `.night-fireflies`) so they are not gallery-coupled; keep the exact background layers, sizes, positions, masks and reduced-motion handling byte-for-byte so the look is identical. Delete the moved rules from gallery.css (leave the wall/tile/thread/torch/lightbox rules untouched).
2. Create `src/components/NightBackground.astro`: renders a `.night-bg` container wrapping the `.night-sky` span and the `.night-fireflies` span (port the deterministic FNV-1a `hashStr` + 26-firefly `FIREFLIES` server-side generation from GalleryPage.astro verbatim). Import `../styles/night-sky.css`. Accept a `fixed?: boolean` prop: when `fixed`, the `.night-bg` is `position:fixed; inset:0; z-index:-1; pointer-events:none` (full-viewport page backdrop); otherwise it fills its positioned parent (gallery's section-scoped use).
3. Refactor `src/components/sections/GalleryPage.astro` to render `<NightBackground />` in place of the two inline `.gallery-sky` + `.gallery-fireflies` spans (keep the `.gallery-night` wrapper class + its `--sky-shift` parallax hook working — gallery.ts drives `--sky-shift`, so the child `.night-sky`/`.night-fireflies` must still read that inherited var; verify the class rename is reflected in gallery.ts if it queries those selectors — it does NOT position them, it only sets the inherited `--sky-shift` on the section, so no JS change is expected, but confirm with a grep).
4. Mount `<NightBackground fixed />` at the top of both `src/components/sections/ServicesPage.astro` and `src/components/sections/TermsPage.astro`. Ensure the night sky is actually VISIBLE: those pages use opaque `.section--surface`/ink sections that would hide a z-index:-1 backdrop, so make the on-page section backgrounds transparent (or semi-transparent) on Services + Terms so the night shows through — scope these overrides to those pages only (both already ship a page-scoped `<style is:global>` / component style block you can extend). Keep text contrast (AA) intact over the darker field. Performance: reuse the SAME single fixed layer per page (do not stack multiples); the film-grain and existing motion budget must not regress.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Visit /es/servicios, /en/services, /es/terminos, /en/terms — the night sky + drifting fireflies show behind the content and text stays readable; /es/galeria still looks identical to before.</human-check>
  </verify>
  <done>Build is green; NightBackground.astro + night-sky.css exist; Services and Terms render the fixed night background; gallery page visually unchanged. Commit: `feat: reuse gallery night background on services and terms`</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen Home hero searchlights + static</name>
  <files>src/scripts/motion/hero-shader/glitch.frag, src/styles/global.css, src/pages/[lang]/index.astro</files>
  <action>
Make the roaming searchlights clearly more noticeable on the landing/hero, and bump the film-static SLIGHTLY (user said "ligeramente" — subtle, not dramatic).

1. `src/scripts/motion/hero-shader/glitch.frag`: increase the `glow` (2nd arg) and modestly the `radius` (4th arg) of the four roamer `lightPoint(...)` calls (lines ~91–98) so the beams read stronger cutting through the dark — roughly +40–70% on glow, small bumps on radius; keep them smaller than the pointer light and keep the field reading dark BETWEEN beams (do not flood it). Optionally nudge the composite roamer contribution at line ~136 (`u_redGlow * glowAccum * (...)`). Preserve the clarity-cycle, aberration, vignette and the CSS `.hero-shader-overlay` scrim so headline + CTA still hold AA.
2. `src/styles/global.css`: bump the global film-grain `body::after` slightly — raise `opacity` (0.45 → ~0.55) and/or the SVG `rect` opacity (0.03 → ~0.04). Small step only.
3. `src/pages/[lang]/index.astro` (optional, if the wordmark searchlight glow still reads weak after step 1): strengthen the `.hero-wordmark::before` red haze (increase the color-mix alpha and/or blur) so the hero TITLE area also gets a stronger light presence. Keep it GPU-cheap (opacity/transform only) and respect the existing `prefers-reduced-motion` disable.
Keep everything within the perf/reduced-motion contracts already in these files. Do not touch the conversion path (CTAs).
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>On /es/ and /en/ the roaming searchlights are visibly stronger and the static is a touch more present, without burying the headline or CTA.</human-check>
  </verify>
  <done>Build green; searchlight glow/radius and film-grain opacity increased subtly; reduced-motion path unchanged. Commit: `feat: strengthen home hero searchlights and static`</done>
</task>

<task type="auto">
  <name>Task 3: Add Quest, VTuber (VRM) and Unitypackage to the services catalog</name>
  <files>src/data/services.json</files>
  <action>
Add three items to the modular catalog (`catalog[]`). Place them in the `extras` category (they are exports/conversions, not per-outfit repeatable add-ons) — or a new dedicated "Exportación / Conversión" category if cleaner; either is fine. All three: `repeatable: false`, ES+EN `name` + `tooltip`, exact prices below.

- id `quest-version`, name ES "Versión de Quest" / EN "Quest version", price "15", tooltip ES ~"Versión compatible con Quest de tu avatar." / EN ~"Quest-compatible version of your avatar."
- id `vtuber-vrm`, name ES "VTuber (avatar en archivo VRM)" / EN "VTuber (avatar as a VRM file)", price "30", tooltip ES ~"Conversión de tu avatar a un archivo VRM para VTubing." / EN ~"Conversion of your avatar to a VRM file for VTubing."
- id `unitypackage`, name ES "Unitypackage" / EN "Unitypackage", price "35", tooltip ES ~"Entrega del proyecto empaquetado como .unitypackage." / EN ~"Delivery of the project packaged as a .unitypackage."

Keep valid JSON (2-space indent, matching the existing style). Prices are strings without the "$" (the UI adds it), matching sibling items. Do NOT alter the packages/tiers block. Remember: the catalog is a cotización (quote) builder, not a cart.

Note in your execution summary the Terms block-04 / summary "no se entrega Unity Package" contradiction flagged in the objective — do not edit Terms here.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>/es/servicios + /en/services list the 3 new items with correct prices ($15/$30/$35) and both-language copy.</human-check>
  </verify>
  <done>Build green; services.json has the 3 new items with ES+EN copy and prices 15/30/35; JSON valid. Commit: `feat: add quest, vtuber vrm and unitypackage to services catalog`</done>
</task>

<task type="auto">
  <name>Task 4: Add account-ban disclaimer to Terms</name>
  <files>src/i18n/terms.json</files>
  <action>
Add a new numbered block `"num": "11"` to BOTH `es.blocks` and `en.blocks` (append after block 10). ES is authoritative/verbatim; EN is the working draft already flagged by the ReviewerBanner (D-13) — keep it consistent, no separate banner needed.

Clause content (write natural legal prose, bolding the key liability sentence via a `strong: true` segment):
- Title ES ~"Baneos y pérdida de cuenta" / EN ~"Account bans and loss of avatars".
- Body: Nocturna Avatars is NOT responsible for bans on the client's VRChat account (or any other platform), whether or not related to the edited/uploaded avatar; and there is NO obligation to recover or re-deliver avatars lost together with a banned or lost account. Bold the core disclaimer sentence.

Follow the exact block `body` segment array shape (see interfaces). Do not renumber existing blocks. Do not touch the summary array. Keep JSON valid (2-space indent).
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>/es/terminos shows clause 11 (ES); /en/terms shows clause 11 (EN) under the draft banner.</human-check>
  </verify>
  <done>Build green; terms.json has block 11 in both es and en with the ban/loss disclaimer; JSON valid. Commit: `feat: add account-ban disclaimer to terms`</done>
</task>

<task type="auto">
  <name>Task 5: Rewrite Discord popup copy for store focus</name>
  <files>src/i18n/nav.json</files>
  <action>
Rewrite the Discord popup body copy (ES+EN) so it covers BOTH opening a ticket for commissions/quotes AND announces the team's own store as coming soon — generic "próximamente" mention (LOCKED; the user will review the final wording). This popup ends the cotización flow.

- Update `modalBody` for both `es` and `en`. ES ~ mention: abre un ticket para tu comisión / cotización personalizada, y menciona que "pronto abriremos nuestra propia tienda" (assets propios) — próximamente. EN equivalent.
- Optionally adjust `modalTitleBefore`/`modalTitleEm`/`modalTitleAfter` if the current "¿Listo para ordenar?" / "Ready to order?" framing no longer fits — keep the `<em>` split shape (three plain-text fields) intact for the XSS-safe markup.
- Do NOT change `modalConfirm` (Discord link label) or `modalDismiss` unless copy consistency demands it. Keep JSON valid; keep both locales in sync in meaning.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Open the Discord popup (hero CTA / floating CTA) on /es/ and /en/ — the copy covers tickets/comisiones AND the upcoming store.</human-check>
  </verify>
  <done>Build green; nav.json modalBody (ES+EN) mentions tickets + upcoming store; JSON valid. Commit: `feat: update discord popup copy for store focus`</done>
</task>

<task type="auto">
  <name>Task 6: Close out the 5 todos (move files + update STATE.md)</name>
  <files>.planning/STATE.md, .planning/todos/pending/2026-07-07-reuse-gallery-background-on-other-pages.md, .planning/todos/pending/2026-07-07-strengthen-home-hero-searchlights-and-static.md, .planning/todos/pending/2026-07-07-add-quest-vtuber-vrm-and-unitypackage-to-services-catalog.md, .planning/todos/pending/2026-07-07-add-account-ban-disclaimer-to-terms.md, .planning/todos/pending/2026-07-07-update-discord-popup-copy-for-store-focus.md</files>
  <action>
Close the backlog:
1. Move these 5 files from `.planning/todos/pending/` to `.planning/todos/completed/` (use `git mv`; create the `completed/` dir if missing):
   - 2026-07-07-reuse-gallery-background-on-other-pages.md
   - 2026-07-07-strengthen-home-hero-searchlights-and-static.md
   - 2026-07-07-add-quest-vtuber-vrm-and-unitypackage-to-services-catalog.md
   - 2026-07-07-add-account-ban-disclaimer-to-terms.md
   - 2026-07-07-update-discord-popup-copy-for-store-focus.md
2. Edit `.planning/STATE.md` `### Pending Todos` section: remove the 5 bullet lines for the todos above, leaving ONLY the 3 bot todos:
   - `2026-07-03-reviews-channel-to-website-publishing-system.md`
   - `2026-07-07-bot-reminders-command-weekly-monthly-custom-message.md`
   - `2026-07-07-carrd-style-template-editor-for-nocturna-editors.md`
Do not alter other STATE.md sections.
  </action>
  <verify>
    <automated>test -d .planning/todos/completed && ls .planning/todos/pending/2026-07-07-*.md 2>/dev/null | grep -v -E 'reviews|reminders|carrd' | grep -c 2026-07-07 | grep -q '^0$' && echo OK</automated>
    <human-check>Pending Todos in STATE.md lists exactly the 3 bot todos.</human-check>
  </verify>
  <done>The 5 files are under .planning/todos/completed/; STATE.md Pending Todos shows only the 3 bot todos. Commit: `docs: complete 5 pre-phase-6 todos (move to completed + update state)`</done>
</task>

</tasks>

<verification>
- `npm run build` is green after each task and at the end.
- Services + Terms show the night background; gallery unchanged.
- Hero searchlights visibly stronger; static slightly stronger.
- 3 catalog items present ES+EN with $15/$30/$35.
- Terms block 11 present ES+EN.
- Discord popup copy mentions tickets + upcoming store, ES+EN.
- 5 todos moved to completed/; STATE.md Pending Todos = 3 bot todos.
</verification>

<success_criteria>
All 6 tasks committed as separate atomic commits (5 feature/doc commits for the todos + 1 closing docs commit), each with `npm run build` green. No new npm dependencies. i18n ES+EN parity preserved on every copy change. The Unitypackage-vs-Terms-block-04 contradiction is surfaced to the user in the execution summary (not silently resolved).
</success_criteria>

<output>
Create `.planning/quick/260707-0q0-ejecutar-5-todos-pendientes-del-sitio-we/260707-0q0-SUMMARY.md` when done.
</output>
