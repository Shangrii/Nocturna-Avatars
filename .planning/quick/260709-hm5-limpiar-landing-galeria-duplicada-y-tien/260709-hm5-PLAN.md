---
phase: quick-260709-hm5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/[lang]/index.astro
  - src/components/sections/Teaser.astro
  - src/data/store.json
autonomous: true
requirements: [CLEANUP-01, STORE-COMINGSOON-01]

must_haves:
  truths:
    - "The landing page renders exactly one gallery section (FeaturedGallery), no duplicate 'Muy pronto'/'Coming soon' teaser after the packages summary"
    - "The Teaser.astro component no longer exists and nothing references it"
    - "The store page (/es/tienda, /en/store) renders the 'Tienda en camino'/'Store coming soon' empty state, no placeholder products"
    - "npm run build succeeds and produces static output for both locales"
  artifacts:
    - path: "src/pages/[lang]/index.astro"
      provides: "Landing composition without the dead Teaser call"
      contains: "FeaturedGallery"
    - path: "src/data/store.json"
      provides: "Empty product catalog with schema _comment preserved"
      contains: "\"products\": []"
  key_links:
    - from: "src/data/store.json"
      to: "src/components/sections/StorePage.astro"
      via: "products.length === 0 -> EmptyState branch"
      pattern: "products\\.length"
---

<objective>
Two-part cleanup: remove the dead duplicate gallery teaser from the landing page, and switch the store to a Coming Soon empty state by clearing the placeholder catalog.

Purpose: The landing currently renders a stale static "Muy pronto"/"Coming soon" Teaser placeholder that was superseded by FeaturedGallery in Phase 4 but never removed — a visible duplicate. The store ships 4 fake demo products with placeholder Jinxxy CDN URLs; at launch it must show the already-built empty state instead.
Output: Landing with a single gallery section, Teaser.astro deleted, store.json with an empty products array (schema _comment intact).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<investigation_ground_truth>
The orchestrator already investigated this codebase. Use these as authoritative facts — do NOT re-derive:

1. DEAD DUPLICATE GALLERY (landing):
   - `src/pages/[lang]/index.astro` line 12 imports Teaser; line 95 renders `<Teaser lang={lang} variant="gallery" />`, positioned between `<PackagesSummary>` (line 94) and `<Reviews>` (line 96).
   - This ALWAYS renders a static "Muy pronto"/"Coming soon" EmptyState placeholder regardless of gallery.json content — it is NOT conditional.
   - `FeaturedGallery.astro` (rendered at line 92) is the real gallery teaser that superseded it (its own file header says so).
   - Grep confirms `Teaser` is imported/used in EXACTLY ONE place across `src/` (index.astro). Removing the index.astro usage makes `src/components/sections/Teaser.astro` fully dead → delete it.
   - The landing-order comment block (lines 83-91) describes "→ gallery teaser →" as a step; that description must be updated to drop the removed step.
   - Do NOT touch `src/components/EmptyState.astro` — it is used by 6 other files (CatalogSection, FeaturedGallery, GalleryPage, Reviews, StorePage) and must remain.

2. STORE COMING SOON:
   - `src/data/store.json` ships 4 placeholder products (Hoodie Neón Callejero, Alas de Medianoche, Set Lencería Nocturne, Pack de Texturas Graffiti) with fake jinxxy-cdn.com URLs. The file's own `_comment` instructs staff to replace them — always seed data.
   - `StorePage.astro` ALREADY has the correct conditional: `{products.length ? (grid) : (<EmptyState ...>)}`.
   - `src/i18n/pages.json` ALREADY has correct copy: ES `emptyHeading: "Tienda en camino"`, EN `emptyHeading: "Store coming soon"`. NO i18n/component changes needed.
   - Fix = set `"products": []` in store.json, KEEPING the top-level `_comment` schema-documentation string. Do NOT touch StorePage.astro, ProductCard.astro, pages.json, store.ts, or store-cart.ts.
</investigation_ground_truth>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead duplicate gallery Teaser from landing and delete the component</name>
  <files>src/pages/[lang]/index.astro, src/components/sections/Teaser.astro</files>
  <action>
In `src/pages/[lang]/index.astro`:
- Remove the import on line 12: `import Teaser from '../../components/sections/Teaser.astro';`
- Remove the render call on line 95: `<Teaser lang={lang} variant="gallery" />` (it sits between the `<PackagesSummary>` and `<Reviews>` lines; leave both of those intact).
- Update the landing-order comment block (lines 83-91). It currently reads the section order as "hero → featured gallery → about+process → packages summary → gallery teaser → reviews → closing" and has a sentence about the "gallery teaser is kept after packages so its data-teaser='gallery' marquee choreography hook survives" plus an ink/paper rhythm line mentioning "gallery(paper)". Rewrite the comment so it no longer references the now-removed gallery teaser step: the order becomes hero → featured gallery → about+process → packages summary → reviews → closing. Drop the "gallery teaser is kept after packages…" sentence, and correct the ink/paper rhythm description so it no longer names the removed gallery(paper) section (the surviving rhythm is featured(ink) → about(paper) → packages(ink) → reviews(paper) → closing(ink)). Keep the comment accurate and concise; do not invent unrelated content.

Then delete the entire file `src/components/sections/Teaser.astro`.

Before deleting, confirm no other reference to Teaser exists anywhere in the repo (the verify step's grep enforces this). Do NOT modify `src/components/EmptyState.astro` or any other component.
  </action>
  <verify>
    <automated>cd "c:/Users/Shangri/Pictures/Nocturna Avatars/Coding/Website" && grep -rn "Teaser" src/ ; test -z "$(grep -rl 'Teaser' src/)" && echo "GREP_CLEAN" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>index.astro no longer imports or renders Teaser and its order comment is updated; Teaser.astro is deleted; repo-wide `grep -rn "Teaser" src/` returns zero matches; `npm run build` succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Empty the store catalog so the Coming Soon empty state renders</name>
  <files>src/data/store.json</files>
  <action>
In `src/data/store.json`, replace the `"products": [ … ]` array (currently 4 placeholder products) with an empty array: `"products": []`.

CRITICAL: Keep the top-level `"_comment"` field exactly as-is — staff still need the schema documentation to add real products later. The resulting file must be valid JSON with just two keys: `_comment` (unchanged) and `products` (now `[]`).

Do NOT modify StorePage.astro, ProductCard.astro, pages.json, store.ts, or store-cart.ts — they already handle the empty case (`products.length` → EmptyState) with the correct ES/EN copy already in place.
  </action>
  <verify>
    <automated>cd "c:/Users/Shangri/Pictures/Nocturna Avatars/Coding/Website" && node -e "const s=require('./src/data/store.json'); if(!Array.isArray(s.products)||s.products.length!==0) throw new Error('products not empty'); if(typeof s._comment!=='string'||!s._comment.length) throw new Error('_comment missing'); console.log('STORE_EMPTY_OK')" && npm run build 2>&1 | tail -5 && grep -q "Tienda en camino" dist/es/tienda/index.html && grep -q "Store coming soon" dist/en/store/index.html && ! grep -qE "Hoodie Neón Callejero|Alas de Medianoche|Set Lencería Nocturne|Pack de Texturas Graffiti" dist/es/tienda/index.html dist/en/store/index.html && echo "STORE_VERIFY_OK"</automated>
  </verify>
  <done>store.json has `"products": []` with `_comment` preserved and valid JSON; build succeeds; built dist/es/tienda/index.html contains "Tienda en camino" and dist/en/store/index.html contains "Store coming soon"; none of the 4 placeholder product names appear in either built store page.</done>
</task>

</tasks>

<verification>
Full-plan verification after both tasks:
1. `npm run build` completes successfully with no errors.
2. `grep -rn "Teaser" src/` returns zero matches (component fully removed, no dangling references).
3. `src/data/store.json` is valid JSON with `products: []` and an intact `_comment`.
4. Built landing pages (dist/es/index.html, dist/en/index.html) render FeaturedGallery once and no second "Muy pronto"/"Coming soon" gallery teaser block after the packages summary.
5. Built store pages show the empty-state copy ("Tienda en camino" / "Store coming soon") and none of the 4 placeholder product names.
</verification>

<success_criteria>
- Landing has a single gallery section (FeaturedGallery); the duplicate Teaser placeholder is gone.
- Teaser.astro is deleted with zero remaining references in src/.
- Store renders the Coming Soon empty state for both locales; catalog is empty but the schema _comment is preserved for staff.
- `npm run build` passes.
- No changes to EmptyState.astro, StorePage.astro, ProductCard.astro, pages.json, store.ts, or store-cart.ts.
</success_criteria>

<output>
Create `.planning/quick/260709-hm5-limpiar-landing-galeria-duplicada-y-tien/260709-hm5-SUMMARY.md` when done.
</output>
